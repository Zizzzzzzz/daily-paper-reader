---
title: Latent-Condensed Transformer for Efficient Long Context Modeling
title_zh: 用于高效长上下文建模的潜空间压缩 Transformer
authors: "Zeng You, Yaofo Chen, Qiuwu Chen, Ying Sun, Shuhai Zhang, Yingjian Li, Yaowei Wang, Mingkui Tan"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12452v2"
tags: ["query:evlm"]
score: 10.0
evidence: Transformer中的KV缓存减少与上下文压缩
tldr: 针对大语言模型长文本处理中KV缓存增长和计算复杂度高的挑战，本文提出Latent-Condensed Attention (LCA)。该方法在多头潜注意力（MLA）的潜空间内直接压缩上下文，通过查询感知池化聚合语义向量并利用锚点选择保留位置信息。LCA在不增加参数的情况下，同时降低了计算成本和存储开销，且具有架构无关性，为高效长文本建模提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12452v2/fig-001.webp\", \"caption\": \"Figure 2: Efficiency comparison of different context lengths. (a) presents the first-token generation latency. (b) shows the per-token (interval) latency during decoding. (c) illustrates the GPU memory footprint of KV cache.\", \"page\": 7, \"index\": 1, \"width\": 820, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2604.12452v2/fig-002.webp\", \"caption\": \"Figure 1: An overview of Latent-Condensed Attention (LCA). The history context is condensed into a compact set of representatives via group-wise conde sation in the latent space: semantic information is aggregated through weighted pooling of CKV , while positional information is preserved via anchor selection from KR. The recent context is retained in full fidelity without condensation.\", \"page\": 4, \"index\": 2, \"width\": 948, \"height\": 478}]"
motivation: 现有的KV缓存压缩技术与稀疏注意力计算优化难以直接结合，导致长文本处理中的存储与计算瓶颈无法被同时有效解决。
method: 在潜空间内将表示解耦为语义潜向量和位置键，分别采用查询感知池化进行聚合以及锚点选择进行保留。
result: "在128K上下文长度下，该方法实现了2.5倍的预填充加速和90%的KV缓存缩减，同时保持了优异的模型性能。"
conclusion: LCA证明了在潜空间进行上下文压缩的有效性，是一种能够显著提升长文本LLM效率且具备理论误差界限的通用机制。
---

## 摘要
大语言模型 (LLM) 在处理长上下文时面临重大挑战，原因在于键值 (KV) 缓存的线性增长以及自注意力机制的平方复杂度。现有方法分别针对这些瓶颈进行处理：多头潜空间注意力 (MLA) 通过将 token 投影到低维潜空间来减少 KV 缓存，而稀疏注意力则减少了计算量。然而，稀疏方法无法原生运行在 MLA 的压缩潜空间结构上，从而错失了联合优化的机会。在本文中，我们提出了潜空间压缩注意力 (LCA)，它直接在 MLA 的潜空间内压缩上下文，其中表示被解耦为语义潜向量和位置键。LCA 通过查询感知池化分别聚合语义向量，并通过锚点选择保留位置键。该方法在不增加参数的情况下，同时降低了计算成本和 KV 缓存。除了 MLA，LCA 的设计与架构无关，可以轻松扩展到其他注意力机制（如 GQA）。在理论上，我们证明了一个与长度无关的误差界限。实验表明，在 128K 上下文下，LCA 在保持竞争性能的同时，实现了高达 2.5 倍的预填充加速和 90% 的 KV 缓存减少。

## Abstract
Large language models (LLMs) face significant challenges in processing long contexts due to the linear growth of the key-value (KV) cache and quadratic complexity of self-attention. Existing approaches address these bottlenecks separately: Multi-head Latent Attention (MLA) reduces the KV cache by projecting tokens into a low-dimensional latent space, while sparse attention reduces computation. However, sparse methods cannot operate natively on MLA's compressed latent structure, missing opportunities for joint optimization. In this paper, we propose Latent-Condensed Attention (LCA), which directly condenses context within MLA's latent space, where the representation is disentangled into semantic latent vectors and positional keys. LCA separately aggregates semantic vectors via query-aware pooling and preserves positional keys via anchor selection. This approach jointly reduces both computational cost and KV cache without adding parameters. Beyond MLA, LCA's design is architecture-agnostic and readily extends to other attention mechanisms such as GQA. Theoretically, we prove a length-independent error bound. Experiments show LCA achieves up to 2.5$\times$ prefilling speedup and 90% KV cache reduction at 128K context while maintaining competitive performance.

---

## 论文详细总结（自动生成）

### 论文总结：用于高效长上下文建模的潜空间压缩 Transformer (LCA)

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在处理长文本时面临两大核心瓶颈：**KV 缓存（KV Cache）的线性增长**导致的内存压力，以及**自注意力机制的平方计算复杂度**导致的推理延迟。
*   **现有技术的局限性**：多头潜空间注意力（MLA）通过低维投影减少了单 token 的缓存大小，但仍需对所有 token 进行计算；稀疏注意力虽减少了计算量，但通常需要在全维度空间操作，无法直接利用 MLA 的压缩结构。
*   **研究目标**：提出一种能够直接在 MLA 潜空间内运行的上下文压缩机制，旨在同时解决存储和计算瓶颈，且不引入额外参数。

#### 2. 方法论：核心思想与技术细节
论文提出了 **Latent-Condensed Attention (LCA)**，其核心思想是在潜空间内对上下文进行结构化压缩。
*   **解耦压缩策略**：针对 MLA 中解耦的语义和位置信息采取不同处理方式：
    *   **语义压缩（加权池化）**：将历史上下文划分为若干组（Group），利用当前查询（Query）的均值计算重要性得分，对组内的语义潜向量（$C_{KV}$）进行查询感知的加权池化，生成代表性向量。
    *   **位置保留（锚点选择）**：由于位置编码（RoPE）具有高度非线性，直接池化会破坏几何关系。LCA 选择组内得分最高的 token 作为“位置锚点”，保留其原始位置键（$K_R$）。
*   **局部窗口保护**：保留最近的 $w$ 个 token 不进行压缩，以确保模型对近期上下文的精细感知。
*   **算法流程**：在预填充阶段，将压缩后的代表键值对与局部全量键值对融合进行计算；在解码阶段，采用在线更新机制，每当新生成的 token 达到组大小时，即触发一次压缩并存入缓存。
*   **理论保障**：论文证明了一个**与序列长度无关的误差上界**，确保了压缩过程的数学严谨性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **长文本评估**：LongBench-E（多任务理解）、RULER（长程检索与推理，最高支持 128K）。
    *   **短文本评估**：MMLU（综合知识）、GSM8K（数学推理）、MBPP（代码生成）。
    *   **泛化性测试**：在 OlympiadBenchMath 上测试其在 GQA 架构下的表现。
*   **对比方法**：
    *   基准模型：DeepSeek-V2-Lite (MLA 架构)。
    *   竞争方法：MInference、FlexPrefill（动态稀疏方法）、KDA（线性注意力变体）。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **8 × NVIDIA H200 GPU** 服务器上进行。
*   **训练细节**：
    *   对预训练模型进行了轻量级微调（Continued Fine-tuning）。
    *   微调步数为 **1,000 步**，涉及约 **2.0B tokens**。
    *   使用了 SlimPajama 数据集，训练序列长度为 64K。
*   **内核优化**：开发了基于 **Triton** 的高性能算子，以实现实际推理中的加速。

#### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **多模型验证**：涵盖了 DeepSeek-V2-Lite (16B)、MiniCPM3-4B 以及基于 GQA 的 Distill-Qwen-7B。
*   **消融实验**：详细探讨了池化策略（均值 vs 加权 vs 最大值）、组大小（$g$）、窗口大小（$w$）以及查询摘要数量对性能的影响。
*   **效率分析**：对比了不同长度下的首字延迟（FTL）、解码速度和内存占用。
*   **客观性**：实验不仅关注长文本，还验证了短文本性能是否退化，并提供了理论误差的实证测量，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **极致效率**：在 128K 上下文下，LCA 实现了 **2.5 倍的预填充加速**和 **90% 的 KV 缓存缩减**。
*   **性能保持**：在 LongBench-E 和 RULER 等长文本任务上，LCA 的表现与全量 MLA 相当，显著优于其他稀疏注意力方法。
*   **架构通用性**：LCA 的设计原则（语义池化+位置锚点）可成功迁移至 GQA 架构，同样获得显著的加速和减存效果。
*   **显存突破**：通过 Triton 优化，LCA 能够支持单机处理高达 **1M（百万级）** 长度的序列。

#### 7. 优点
*   **原生潜空间操作**：避免了将 MLA 还原为全维度 KV 的开销，真正发挥了低秩结构的优势。
*   **无参数开销**：无需增加模型参数，仅需极少量的微调即可适配现有模型。
*   **理论与实践结合**：既有严谨的误差界限证明，又有针对硬件优化的 Triton 内核实现。
*   **解耦设计的合理性**：区分处理语义和位置信息，解决了长文本压缩中常见的信号混淆问题。

#### 8. 不足与局限
*   **任务敏感性**：在极度依赖精确 token 检索的任务（如多文档 QA）中，激进的压缩倍率会导致轻微的性能下降。
*   **工程门槛**：为了达到论文所述的加速效果，必须依赖自定义的 Triton 内核，这增加了在不同推理框架中部署的工程难度。
*   **量化兼容性**：目前实验主要基于 bfloat16/float16，尚未深入探讨在 Int8 或更低精度量化环境下的表现。

（完）
