---
title: Generative Anonymization in Event Streams
title_zh: 事件流中的生成式匿名化
authors: "Adam T. Müller, Mihai Kocsis, Nicolaj C. Stache"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12803v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 针对神经形态视觉传感器事件流的生成式匿名化框架
tldr: 针对神经形态视觉传感器在公共空间部署带来的隐私泄露风险，本文提出了首个事件流生成式匿名化框架。该方法通过将异步事件映射到中间强度表示，利用预训练模型合成虚假身份，再重新编码回事件域，有效解决了隐私保护与数据可用性之间的矛盾。此外，研究还提供了一个同步的真实世界事件与RGB数据集，为隐私保护研究提供了基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12803v1/fig-001.webp\", \"caption\": \"Figure 2. Qualitative examples of source and synthetic identities. Comparison of three subjects (rows). Columns from left-toright: Source event streams, intermediate E2V representations, the anonymized generative output (Anon), V2E projection into a new event stream, and the final downstream E2V validation.\", \"page\": 7, \"index\": 1, \"width\": 496, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2604.12803v1/fig-002.webp\", \"caption\": \"Figure 1. Architectural overview of the generative anonymization pipeline. The framework translates raw asynchronous event data into continuous grayscale frames to detect and swap faces using established generative models. The anonymized identity is subsequently projected back into the event space via a V2E conversion, preserving the underlying spatiotemporal structure.\", \"page\": 4, \"index\": 2, \"width\": 1036, \"height\": 654}]"
motivation: 现有的事件流脱敏方法（如掩码或扰乱）会破坏时空结构，导致下游感知任务的性能大幅下降。
method: 通过将事件流投影为强度图像，利用生成模型替换真实身份，并将其重新编码回神经形态域，实现保持结构完整性的匿名化。
result: 实验证明该方法能有效防止从重建图像中恢复身份，同时保留了下游视觉任务所需的结构数据完整性。
conclusion: 本研究通过生成式匿名化技术成功平衡了事件相机的隐私保护与实用性，并为该领域贡献了高质量的实验基准数据集。
---

## 摘要
神经形态视觉传感器具有低延迟和高动态范围的优势，但其在公共场所的部署引发了严重的数据保护担忧。最近的事件转视频（E2V）模型可以从稀疏的事件流中重建高保真强度图像，从而在无意中暴露了人类身份。目前的混淆方法（如掩码或扰乱）会破坏时空结构，严重降低下游感知任务的数据效用。在本文中，据我们所知，我们提出了首个用于事件流的生成式匿名化框架，以解决这种效用与隐私之间的权衡。通过弥合异步事件与标准空间生成模型之间的模态鸿沟，我们的流水线将事件投影到中间强度表示中，利用预训练模型合成逼真的、不存在的身份，并将特征重新编码回神经形态领域。实验表明，我们的方法能够可靠地防止从 E2V 重建中恢复身份，同时保留下游视觉任务所需的结构数据完整性。最后，为了促进严格的评估，我们引入了一个通过精确机器人轨迹捕获的新型同步真实世界事件和 RGB 数据集，为隐私保护神经形态视觉的未来研究提供了一个稳健的基准。

## Abstract
Neuromorphic vision sensors offer low latency and high dynamic range, but their deployment in public spaces raises severe data protection concerns. Recent Event-to-Video (E2V) models can reconstruct high-fidelity intensity images from sparse event streams, inadvertently exposing human identities. Current obfuscation methods, such as masking or scrambling, corrupt the spatio-temporal structure, severely degrading data utility for downstream perception tasks. In this paper, to the best of our knowledge, we present the first generative anonymization framework for event streams to resolve this utility-privacy trade-off. By bridging the modality gap between asynchronous events and standard spatial generative models, our pipeline projects events into an intermediate intensity representation, leverages pretrained models to synthesize realistic, non-existent identities, and re-encodes the features back into the neuromorphic domain. Experiments demonstrate that our method reliably prevents identity recovery from E2V reconstructions while preserving the structural data integrity required for downstream vision tasks. Finally, to facilitate rigorous evaluation, we introduce a novel, synchronized real-world event and RGB dataset captured via precise robotic trajectories, providing a robust benchmark for future research in privacy-preserving neuromorphic vision.

---

## 论文详细总结（自动生成）

这是一份关于论文《Generative Anonymization in Event Streams》（事件流中的生成式匿名化）的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：神经形态视觉传感器（事件相机）因其低延迟、高动态范围被广泛应用，但其捕获的事件流可通过“事件转视频”（E2V）技术重建出高保真图像，从而泄露生物识别信息（如人脸身份）。
*   **研究动机**：现有的事件流脱敏技术（如空间扰乱、添加对抗噪声或加密）属于“破坏性混淆”，虽然保护了隐私，但严重破坏了数据的时空结构，导致下游视觉任务（如姿态估计、表情识别）的效用大幅下降。
*   **整体含义**：本文旨在提出一种“生成式匿名化”框架，通过将真实身份替换为合成的虚假身份，在彻底消除身份信息的同时，完整保留数据的时空特征和下游任务的实用性。

### 2. 论文提出的方法论
核心思想是**桥接模态鸿沟**，将异步事件流转化为中间强度图像进行处理，再转回事件域。
*   **核心流程**：
    1.  **E2V 投影**：利用 EVREAL 框架将原始异步事件流 $E$ 转换为同步的灰度强度帧。
    2.  **生成式匿名化**：使用 Stable Diffusion 2 (SD2) 生成一个不存在的虚拟身份，并利用 INSwapper 模型将视频中的原始人脸替换为该虚拟身份。
    3.  **时空插值与对齐**：由于事件具有微秒级分辨率，而图像帧是离散的，论文采用**一维分段线性插值**来计算连续的动态边界框 $B(t)$，确保在任何时间点 $t_i$ 都能精确裁剪事件。
    4.  **随机空间羽化（Stochastic Spatial Feathering）**：为了避免匿名区域与背景之间出现生硬的边缘，引入基于半高斯分布的概率采样机制，实现背景事件与匿名事件的平滑融合。
    5.  **V2E 重新编码**：利用视频转事件（v2e）工具将匿名化后的图像序列重新投影回神经形态域，生成最终的合成事件流。

### 3. 实验设计
*   **数据集**：
    *   **自研数据集**：引入了一个新型同步 RGB-Event 数据集。为消除人为运动干扰，传感器安装在**协作机器人（cobot）**上按预设轨迹运动，受试者进行阅读任务以产生自然的微表情。
    *   **对比基准**：使用原始未处理的事件流作为基准（Reference）。
*   **评估指标**：
    *   **身份与特征**：ArcFace 身份相似度、HopeNet 姿态误差、Landmark Distance (LMD) 表情模仿误差。
    *   **下游任务**：YOLOv8 人脸检测（强度域）、基于事件的人脸检测器（事件域）。
    *   **事件域结构指标**：提出**时空倒角距离（STCD）**和基于切片 Wasserstein 距离的**事件移动距离（EMD）**来衡量原始流与匿名流的结构差异。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量及训练/推理时长。
*   **模型依赖**：提到了使用了 FireNet（用于重建）、INSwapper 128（用于换脸）、FSRCNN（用于超分辨率增强）等预训练模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了定性视觉对比（展示了不同受试者的匿名化效果）。
    *   定量分析涵盖了身份相似度、时间稳定性、姿态/表情保留度等多个维度。
    *   针对下游任务，对比了 YOLO 在灰度图和事件表示上的检测精度（IoU、置信度等）。
*   **充分性评价**：实验设计较为充分且客观。通过引入机器人控制变量，排除了相机运动的不确定性；同时，不仅在图像域评估，还创新性地在原始事件域（Raw Event Space）提出了结构性度量指标，验证了匿名化对数据分布的影响。

### 6. 主要结论与发现
*   **隐私保护有效**：身份相似度从基准的 0.713 骤降至 0.118，有效防止了身份恢复。
*   **效用保留极高**：匿名化后的姿态误差仅为 3.304°，表情模仿误差（0.181）甚至低于受试者两次独立录制之间的自然差异（0.239）。
*   **下游任务无损**：YOLO 检测率误差为 0%，检测框 IoU 高达 0.96（图像域）和 0.702（事件域），证明时空结构得到了完美保留。
*   **指标有效性**：STCD 和 EMD 指标显著增加，量化证实了事件流在保持结构的同时完成了身份特征的置换。

### 7. 优点
*   **首创性**：这是首个针对事件流的生成式匿名化框架，打破了以往“破坏性脱敏”的局限。
*   **高质量数据集**：利用机器人轨迹捕获的数据集解决了现有事件数据集运动不受控的问题，并向社区开放。
*   **平滑融合技术**：提出的随机空间羽化算法有效解决了合成事件流中的边界伪影问题。
*   **多维度评估**：不仅关注隐私，还深入评估了微表情、姿态等细粒度语义信息的保留情况。

### 8. 不足与局限
*   **依赖中间模态**：目前仍需先转为图像再转回事件，并非“原生”的事件级生成模型，这增加了计算开销。
*   **离散化伪影**：V2E 转换步骤会导致事件空间出现一定的离散化现象（Discretization），可能影响极高精度的感知任务。
*   **微表情瓶颈**：在处理极细微的嘴部动作时，现有的换脸模型（INSwapper）偶尔会出现失真或模糊。
*   **事件密度问题**：模拟器生成的事件密度有时不足以完全覆盖高频的人脸细节，导致重建时出现抹除感（Smearing）。

（完）
