---
title: Latent-Condensed Transformer for Efficient Long Context Modeling
title_zh: 潜空间压缩 Transformer：实现高效长上下文建模
authors: "Zeng You, Yaofo Chen, Qiuwu Chen, Ying Sun, Shuhai Zhang, Yingjian Li, Yaowei Wang, Mingkui Tan"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12452v1"
tags: ["query:evlm"]
score: 10.0
evidence: 潜空间中的KV缓存减少与上下文压缩
tldr: 针对大语言模型处理长文本时KV缓存增长和计算复杂度高的问题，本文提出Latent-Condensed Attention (LCA)。LCA在MLA的潜空间内直接压缩上下文，通过查询感知池化聚合语义向量并保留位置键，实现了计算开销与KV缓存的协同优化。该方法不增加参数且具有架构通用性，在保持性能的同时显著提升了长文本处理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12452v1/fig-001.webp\", \"caption\": \"Figure 2: Efficiency comparison of different context lengths. (a) presents the first-token generation latency. (b) shows the per-token (interval) latency during decoding. (c) illustrates the GPU memory footprint of KV cache.\", \"page\": 7, \"index\": 1, \"width\": 820, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2604.12452v1/fig-002.webp\", \"caption\": \"Figure 1: An overview of Latent-Condensed Attention (LCA). The history context is condensed into a compact set of representatives via group-wise conde sation in the latent space: semantic information is aggregated through weighted pooling of CKV , while positional information is preserved via anchor selection from KR. The recent context is retained in full fidelity without condensation.\", \"page\": 4, \"index\": 2, \"width\": 948, \"height\": 478}]"
motivation: 现有的KV缓存压缩（如MLA）与计算优化（如稀疏注意力）方法难以原生结合，限制了长文本建模的进一步优化。
method: 提出LCA机制，在潜空间内将表示解耦为语义向量和位置键，分别通过查询感知池化和锚点选择进行压缩。
result: "在128K上下文下实现了2.5倍的预填充加速和90%的KV缓存减少，且具有理论上的长度无关误差界限。"
conclusion: LCA是一种高效且通用的长文本建模方案，能在不增加模型参数的情况下显著降低推理成本并保持模型性能。
---

## 摘要
大语言模型 (LLMs) 在处理长上下文时面临重大挑战，原因在于键值 (KV) 缓存的线性增长以及自注意力机制的二次复杂度。现有方法分别针对这些瓶颈进行处理：多头潜注意力 (MLA) 通过将 token 投影到低维潜空间来减少 KV 缓存，而稀疏注意力则减少了计算量。然而，稀疏方法无法原生运行在 MLA 的压缩潜空间结构上，从而错失了联合优化的机会。在本文中，我们提出了潜空间压缩注意力 (LCA)，它直接在 MLA 的潜空间内压缩上下文，在该空间中，表示被解耦为语义潜向量和位置键。LCA 通过查询感知池化分别聚合语义向量，并通过锚点选择保留位置键。该方法在不增加参数的情况下，同时降低了计算成本和 KV 缓存。除了 MLA，LCA 的设计与架构无关，可以轻松扩展到其他注意力机制（如 GQA）。在理论上，我们证明了一个与长度无关的误差界限。实验表明，在 128K 上下文下，LCA 实现了高达 2.5 倍的预填充加速和 90% 的 KV 缓存减少，同时保持了具有竞争力的性能。

## Abstract
Large language models (LLMs) face significant challenges in processing long contexts due to the linear growth of the key-value (KV) cache and quadratic complexity of self-attention. Existing approaches address these bottlenecks separately: Multi-head Latent Attention (MLA) reduces the KV cache by projecting tokens into a low-dimensional latent space, while sparse attention reduces computation. However, sparse methods cannot operate natively on MLA's compressed latent structure, missing opportunities for joint optimization. In this paper, we propose Latent-Condensed Attention (LCA), which directly condenses context within MLA's latent space, where the representation is disentangled into semantic latent vectors and positional keys. LCA separately aggregates semantic vectors via query-aware pooling and preserves positional keys via anchor selection. This approach jointly reduces both computational cost and KV cache without adding parameters. Beyond MLA, LCA's design is architecture-agnostic and readily extends to other attention mechanisms such as GQA. Theoretically, we prove a length-independent error bound. Experiments show LCA achieves up to 2.5$\times$ prefilling speedup and 90% KV cache reduction at 128K context while maintaining competitive performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Latent-Condensed Transformer for Efficient Long Context Modeling》的深度学术总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在处理长文本时面临两大瓶颈：**KV 缓存（KV Cache）的线性增长**导致显存溢出，以及**自注意力机制的二次计算复杂度**导致推理延迟。
目前已有两类主流优化方案：
*   **KV 缓存压缩（如 MLA）：** 通过低秩投影减少存储，但计算量依然随长度增加。
*   **稀疏/压缩注意力（如 StreamingLLM, H2O）：** 通过丢弃或聚合 token 减少计算，但往往无法直接应用于 MLA 这种已经过投影的潜空间结构。
**研究动机：** 现有的 MLA 架构虽然节省了显存，但其计算开销依然巨大。本文旨在提出一种能直接在 MLA 潜空间内运行的压缩机制，实现显存与计算开销的协同优化。

### 2. 论文提出的方法论：Latent-Condensed Attention (LCA)
LCA 的核心思想是在潜空间内对历史上下文进行“组级压缩（Group-wise Condensation）”，将多个 token 聚合为少量的代表性向量。

*   **解耦压缩策略：** 在 MLA 架构中，KV 被解耦为“语义潜向量（Content Latent Vector）”和“位置键（Positional Key）”。LCA 对两者采取不同处理：
    *   **语义聚合（Semantic Aggregation）：** 利用“查询感知池化（Query-aware Pooling）”，根据当前查询（Query）与历史 token 的相关性，通过加权平均将一组语义向量压缩为一个。
    *   **位置保留（Positional Preservation）：** 为了维持位置信息的精确性，采用“锚点选择（Anchor Selection）”，从每组中挑选一个最具代表性的位置键作为锚点。
*   **混合窗口机制：** 保留最近的上下文（Recent Context）不压缩以维持局部精度，仅对远期历史（History Context）进行压缩。
*   **架构通用性：** 虽然主要针对 MLA 设计，但 LCA 也可以扩展到 GQA（分组查询注意力）等传统架构。
*   **理论保证：** 论文证明了 LCA 具有“长度无关的误差界限（Length-independent Error Bound）”，即压缩带来的近似误差不会随文本长度增加而发散。

### 3. 实验设计
*   **模型与基准：** 主要基于 **DeepSeek-V3**（采用 MLA 架构）和 **Llama-3-8B**（采用 GQA 架构）进行验证。
*   **数据集/场景：**
    *   **长文本理解：** LongBench（包含多任务长文本理解）。
    *   **大海捞针（Needle In A Haystack）：** 测试模型在极长上下文（最高 128K）中的信息检索能力。
    *   **语言建模：** 在 PG19 和 Proof-pile 数据集上测试困惑度（Perplexity）。
*   **对比方法：** 包括原始模型（Full Attention）、StreamingLLM、H2O、Quest 等主流稀疏/压缩注意力方法。

### 4. 资源与算力
*   **硬件环境：** 实验主要在 NVIDIA **H800 GPU** 集群上进行。
*   **训练/微调：** 论文强调 LCA 是 **Training-free（无需训练）** 的，即可以直接应用于现有的预训练模型。但在某些实验中，为了进一步提升性能，进行了少量的上下文微调（Context Fine-tuning）。
*   **具体开销：** 文中未详细列出总的 GPU 小时数，但展示了在 128K 长度下，LCA 相比 Full Attention 能显著降低显存占用并提升预填充（Prefilling）速度。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 8B 到 671B（DeepSeek-V3）不同规模的模型，验证了方法的可扩展性。
*   **消融实验：** 针对压缩率（Condensation Ratio）、窗口大小、池化策略（Mean vs. Max vs. Query-aware）等关键超参数进行了详细讨论。
*   **充分性评价：** 实验设计较为全面，不仅关注准确率（Accuracy），还深入分析了推理延迟（Latency）和显存占用（Memory），对比基准涵盖了当前最先进的稀疏化技术，结果具有较高的客观性。

### 6. 论文的主要结论与发现
*   **高效性：** 在 128K 上下文下，LCA 实现了 **2.5 倍的预填充加速**，并减少了 **90% 的 KV 缓存**。
*   **性能保持：** 在 LongBench 等任务上，LCA 在大幅压缩的情况下，性能损失极小，甚至在某些长文本任务中优于传统的稀疏注意力方法。
*   **理论验证：** 实验结果验证了理论推导的误差界限，证明了在潜空间进行加权聚合比直接丢弃 token（如 H2O）更能保留全局语义。

### 7. 优点
*   **协同优化：** 首次实现了在 MLA 潜空间内的计算与存储双重压缩，填补了该领域的空白。
*   **无需训练：** 具有即插即用的特性，降低了部署成本。
*   **架构无关：** 设计思路具有普适性，不仅限于 DeepSeek 系列模型，对 Llama 等主流模型同样有效。
*   **理论支撑：** 提供了严谨的误差界限证明，增强了算法的可靠性。

### 8. 不足与局限
*   **极端压缩下的性能退化：** 当压缩率极高（如 1/16 以上）时，在极精细的信息检索任务（如复杂的大海捞针）中仍存在一定的精度下降。
*   **硬件算子优化：** 虽然理论复杂度降低，但加权池化等操作在现有深度学习框架下可能需要专门的 Triton 或 CUDA 内核优化才能达到理论上的最高加速比。
*   **动态场景：** 论文主要关注静态长文本，对于多轮对话中 KV 缓存动态更新的复杂交互场景讨论较少。

（完）
