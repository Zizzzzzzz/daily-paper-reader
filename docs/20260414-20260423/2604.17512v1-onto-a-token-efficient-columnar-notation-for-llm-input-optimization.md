---
title: "ONTO: A Token-Efficient Columnar Notation for LLM Input Optimization"
title_zh: ONTO：一种用于大语言模型输入优化的 Token 高效列式表示法
authors: Harshavardhanan Deekeswar
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17512v1"
tags: ["query:evlm"]
score: 9.0
evidence: 用于LLM输入优化的标记高效列式表示法
tldr: "针对大语言模型处理大规模结构化数据时JSON等格式带来的高Token开销问题，本文提出了一种名为ONTO的列式表示法。ONTO通过“一次声明模式，多次排列数据”的设计，利用管道符分隔和缩进层级，消除了重复键名和冗余标点。实验表明，ONTO在保持模型理解准确性的同时，比JSON减少了46-51%的Token消耗，并显著降低了推理延迟，为优化LLM上下文窗口提供了高效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17512v1/fig-001.webp\", \"caption\": \"Figure 1: Token scaling from 100 to 1,000 records. Both formats scale linearly; reduction ratio remains constant (46% for IoT, 51% for Metrics).\", \"page\": 4, \"index\": 1, \"width\": 480, \"height\": 258}]"
motivation: 传统的JSON序列化格式在处理大规模数据时，因重复的字段名和结构化标点产生了巨大的Token冗余，限制了LLM的处理效率。
method: 提出ONTO表示法，采用列式布局，仅声明一次字段名，并使用管道符分隔数值和缩进表示嵌套结构。
result: "与JSON相比，ONTO实现了46-51%的Token减量，使推理延迟降低了5-10%，且在查找、计数和聚合等任务中保持了准确性。"
conclusion: ONTO填补了高效列式存储与层级结构支持之间的空白，是提升LLM处理大规模操作数据效率的有效工具。
---

## 摘要
为文档交换设计的序列化格式会产生结构性开销，当大语言模型（LLM）大规模处理运营数据时，这种开销会变得难以承受。一个包含 1,000 条 IoT 传感器读取值的适度数据集，若以 JSON 序列化，大约需要 80,000 个 token，其中大部分消耗在重复的字段名、嵌套括号和结构性标点符号上，而非语义内容。我们提出了 ONTO（用于 Token 优化的对象表示法），这是一种列式表示法，每个实体仅声明一次字段名，并使用管道符分隔的行和基于缩进的层级结构来排列数值。这种“一次模式，多次数据”的设计消除了每条记录的键名重复，同时保留了人类可读性和对嵌套结构的支持。在三个合成运营数据集上的评估表明，与 JSON 相比，token 减少了 46-51%，且在 100 到 1,000 条记录范围内具有稳定的扩展性。在 Qwen2.5-7B 上的受控推理基准测试显示，延迟相应改善了 5-10%。理解验证确认，在提供格式上下文的情况下，LLM 在查找、计数、提取和聚合操作中的任务准确性没有实质性下降。消融分析显示，键名重复占 JSON 开销的大部分，而嵌套结构中的缩进成本解释了扁平数据与层级数据之间 4 个百分点的差距。ONTO 填补了序列化领域此前的一个空白：兼具列式效率与层级结构，专为 LLM 上下文窗口而非文档交换而优化。代码和规范可在 https://github.com/harsh-aranga/onto 获取。

## Abstract
Serialization formats designed for document interchange impose structural overhead that becomes prohibitive when large language models consume operational data at scale. A modest dataset of 1,000 IoT sensor readings serialized as JSON requires approximately 80,000 tokens - the majority spent on repeated field names, nested braces, and structural punctuation rather than semantic content. We present ONTO (Object Notation for Token Optimization), a columnar notation that declares field names once per entity and arranges values in pipe-delimited rows with indentation-based hierarchy. This schema-once, data-many design eliminates per-record key repetition while preserving human readability and nested structure support. Evaluation across three synthetic operational datasets demonstrates 46-51% token reduction versus JSON, with stable scaling from 100 to 1,000 records. Controlled inference benchmarks on Qwen2.5-7B show corresponding 5-10% latency improvement. Comprehension validation confirms no material degradation in LLM task accuracy across lookup, counting, extraction, and aggregation operations when format context is provided. Ablation analysis reveals that key repetition accounts for the majority of JSON overhead, with indentation costs in nested structures explaining the 4-percentage-point gap between flat and hierarchical data. ONTO occupies a previously unfilled position in the serialization landscape: columnar efficiency with hierarchical structure, optimized for LLM context windows rather than document interchange. Code and specification are available at https://github.com/harsh-aranga/onto.

---

## 论文详细总结（自动生成）

这是一份关于论文《ONTO: A Token-Efficient Columnar Notation for LLM Input Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）处理大规模结构化数据时的 Token 效率问题**。
*   **背景**：随着 LLM 从对话助手转向运营智能系统（如分析 IoT 遥测、日志审计），它们需要消耗大量结构化数据。
*   **核心问题**：传统的序列化格式（如 JSON、YAML）是为文档交换设计的，具有极高的结构开销。例如，1000 条 JSON 格式的传感器记录中，大部分 Token 被重复的字段名（Key）、括号和引号占据，而非实际的语义内容。这导致了高昂的 API 成本、上下文窗口的浪费以及推理延迟的增加。
*   **整体含义**：论文提出了一种专为 LLM 输入优化的列式表示法 **ONTO**，旨在通过消除冗余结构来最大化上下文窗口的利用率。

### 2. 方法论：核心思想与技术细节
ONTO 的核心思想是**“一次声明模式，多次排列数据”（Schema-once, data-many）**。
*   **列式布局**：不再为每条记录重复键名，而是先声明实体和字段，随后以列的形式排列所有值。
*   **关键语法**：
    *   **实体声明**：`EntityName [N]:` 声明实体名及记录总数。
    *   **字段与分隔符**：字段名后跟冒号，数值之间使用管道符 `|` 分隔（如 `temp: 23.5|24.1|22.9`）。
    *   **层级结构**：使用**严格的 4 空格缩进**来表示嵌套对象，取代 JSON 的大括号。
    *   **数组处理**：使用脱字符 `^` 分隔数组元素（如 `tags: a^b|c^d`），避免括号开销。
    *   **类型推断**：自动识别数字、布尔值和 Null，使用反引号 `` ` `` 强制指定字符串。

### 3. 实验设计
*   **数据集/场景**：构建了三个合成数据集，模拟常见的运营模式：
    1.  **IoT Telemetry（嵌套）**：包含地理位置坐标的设备读取值，测试层级支持。
    2.  **System Metrics（扁平）**：服务器监控数据，测试纯列式效率。
    3.  **Log Entries（扁平）**：应用日志，测试混合数据类型。
*   **Benchmark 与对比方法**：
    *   **对比格式**：JSON、YAML。
    *   **评估指标**：Token 计数（使用 `tiktoken` 的 `cl100k_base` 编码）、推理延迟（TTFT 和总时间）、理解准确性（查找、计数、提取、聚合任务）。

### 4. 资源与算力
*   **硬件**：AMD Ryzen 7 3700X (8核), 48GB RAM, **NVIDIA RTX 2070 SUPER 8GB**。
*   **模型**：
    *   本地推理：**Qwen2.5-7B-Instruct**（4-bit 量化），运行于 Ollama 环境。
    *   理解验证：通过 OpenAI API 调用 **GPT-4o-mini**（文中注为 5.4-mini，应为笔误或特定版本）。
*   **训练时长**：该研究不涉及模型训练，仅涉及推理测试。

### 5. 实验数量与充分性
*   **实验规模**：针对每个数据集分别生成了 100、500、1000 条记录的规模。
*   **重复实验**：延迟测量和理解验证均进行了 **20 次独立运行**（使用不同的随机种子生成数据），并报告了平均值和标准差。
*   **消融实验**：详细分析了 Token 节省的构成（键名消除、标点减少、缩进开销等），并对比了扁平与嵌套结构的效率差异。
*   **充分性评价**：实验设计较为客观，涵盖了从 Token 节省量化到模型实际理解能力的闭环验证，且对比了主流格式，具有较强的说服力。

### 6. 主要结论与发现
*   **Token 显著减量**：相比 JSON，ONTO 在 1000 条记录规模下实现了 **46%–51% 的 Token 削减**。
*   **延迟改善**：Token 的减少直接转化为推理性能的提升，首字生成时间（TTFT）改善了 5-7%，总推理时间缩短了 **9-10%**。
*   **理解力保持**：在提供简短的格式说明（Warm Prompt，约 200 Token）后，LLM 在 ONTO 格式上的任务准确率与 JSON 持平，甚至在某些复杂检索任务中表现更好。
*   **开销来源**：消融分析证实，**键名重复**是 JSON 开销的最大来源；而 ONTO 的主要开销在于表示嵌套结构的缩进空格。

### 7. 优点
*   **极致的 Token 效率**：通过列式重构，极大地缓解了长上下文任务中的 Token 压力。
*   **兼顾人类可读性**：虽然是为模型优化，但其结构清晰，便于人类开发者调试和验证。
*   **无损压缩**：与 LLMLingua 等丢弃信息的压缩方法不同，ONTO 是结构性的无损转换。
*   **易于集成**：作为预处理层，不需要修改模型架构，仅需简单的 Prompt 引导。

### 8. 不足与局限
*   **数据集局限性**：主要基于合成数据，尚未在具有复杂边缘情况的真实生产环境日志中广泛验证。
*   **Tokenizer 敏感性**：实验主要基于 OpenAI 的编码器，不同模型的 Tokenizer（如 Llama 的 SentencePiece）可能会导致减量比例有所波动。
*   **结构同质化要求**：ONTO 要求同一实体下的记录必须具有完全相同的 Schema，处理异构数据（Schema 经常变动的记录）时灵活性不如 JSON。
*   **嵌套深度成本**：随着嵌套层级加深，缩进产生的空格 Token 会逐渐抵消列式存储的优势。

（完）
