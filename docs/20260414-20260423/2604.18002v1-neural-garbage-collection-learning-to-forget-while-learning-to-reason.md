---
title: "Neural Garbage Collection: Learning to Forget while Learning to Reason"
title_zh: 神经垃圾回收：在学习推理的同时学习遗忘
authors: "Michael Y. Li, Jubayer Ibn Hamid, Emily B. Fox, Noah D. Goodman"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18002v1"
tags: ["query:evlm"]
score: 9.0
evidence: 决定逐出哪些KV缓存条目
tldr: 针对思维链推理中 KV 缓存随步数增长导致的性能瓶颈，本文提出“神经垃圾回收”（NGC）方法。该方法通过强化学习，使模型在学习推理的同时，学会根据任务奖励自主决定并剔除冗余的 KV 缓存条目。NGC 实现了推理与内存管理的联合优化，在保持高准确率的同时显著降低了内存占用，为构建更高效、可扩展的推理模型提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18002v1/fig-001.webp\", \"caption\": \"Figure 3: Comparing NGC against KV cache eviction baselines. (left) NGC significantly outperforms baseline cache eviction methods. Error bars correspond to 1 SE and we report pass@1 accuracies. All methods evict 50% of entries at each eviction round, corresponding to 2.4x reduction in peak KV cache size. The dashed horizontal line corresponds to a model trained and evaluated with full cache. (right)We also test NGC’s generalization to other eviction rates (smaller cache sizes) that it was not explicitly trained for; the vertical dashed line indicates the minimum cache size during training. NGC pareto-dominates all other methods even at higher compression rates.\", \"page\": 11, \"index\": 1, \"width\": 975, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-002.webp\", \"caption\": \"Figure 1: Neural Garbage Collection: Learning to Forget while Learning to Reason. (top) As the language model (LM) generates its chain-of-thought, it periodically enters an eviction round: (1) the LM scores KV cache entries via softmax, (2) samples cache evictions using those scores via Gumbel top-k, and (3) samples the next token conditioned only on the pruned cache. Since both tokens and cache evictions are discrete actions sampled from the LM, we can jointly train the LM to reason and manage its memory via end-to-end reinforcement learning from outcome-based task reward alone (e.g., binary correctness) — no proxy objectives, no supervised finetuning. (bottom left) NGC substantially outperforms baseline eviction methods at a 50% eviction rate per round, corresponding to 2.4x reduction in peak KV cache size for Countdown and 4.6x for AIME 2025. (bottom right) To give the LM explicit awareness of its resource constraint before it reasons, we include the eviction rate in the prompt during training — a technique we call budget-aware interoception. This technique improves generalization across cache budgets at test time.\", \"page\": 2, \"index\": 2, \"width\": 981, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-003.webp\", \"caption\": \"Figure 10: NGC gives compression meta-tokens for free. Tokens after the summary token g can only attend to g and the KV cache entries the model decides to keep. Since g is just a token, it receives a per-token policy gradient. This gives the model an incentive, via the RL objective, to pack useful information into g’s representation. If all prefix entries before g are evicted, the induced attention mask is exactly that of a gist token [Mu et al., 2024].\", \"page\": 22, \"index\": 3, \"width\": 864, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-004.webp\", \"caption\": \"Figure 8: NGC pass@32 across varying cache size reductions. For moderate reductions in max cache size 2-3x, NGC can still maintain relatively strong accuracy relative to an upper bound that is trained with no eviction and uses no eviction during inference.\", \"page\": 14, \"index\": 4, \"width\": 980, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-005.webp\", \"caption\": \"Figure 7: Pass@k across math reasoning benchmarks. NGC consistently outperforms all inference-time eviction baselines across AMC 2023, AMC 2025, and AIME 2025; all methods evict 50% at each eviction round, corresponding to a 3-5x peak cache size reduction.\", \"page\": 14, \"index\": 5, \"width\": 970, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-006.webp\", \"caption\": \"Figure 5: NGC training dynamics on Countdown. (left) Reward over training: NGC improves steadily but more slowly than training without any eviction (no evict) but eventually reaches a similar level of accuracy. Targeted KV Dropout increases initially at a low eviction rate but collapses after around step 100 due to off-policyness. (center) Targeted Dropout exhibits exploding gradient norms coinciding with its reward collapse, whereas NGC and the no-eviction baseline remain stable throughout training. (right) The staircase eviction rate curriculum, shared across both NGC and targeted KV dropout, which gradually increases the percentage of KV cache evicted to 50% over „ 100 steps. Vertical dashed teal lines in the left and center panels mark the steps where the eviction rate begins to increase.\", \"page\": 13, \"index\": 6, \"width\": 980, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-007.webp\", \"caption\": \"Figure 6: Budget-aware interoception. By training and evaluating the model with the eviction rate ρ in the prompt <eviction_rate>ρ%</eviction_rate>, we see softened performance degradation as peak KV cache size decreases and generalization to stricter budgets. At aggressive budgets, this gives an 8-13% boost in performance. The dashed vertical line represents the minimum cache size used during training.\", \"page\": 13, \"index\": 7, \"width\": 431, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-008.webp\", \"caption\": \"Figure 2: Replay masks enable efficient end-to-end RL training. During reasoning, the language model samples eviction decisions that dynamically change its KV cache. We can perform the policy gradient update efficiently and correctly using replay attention masks. The eviction decisions can be captured by attention masks that reproduce the visibility patterns over previous tokens induced by the eviction decisions. We then use these masks to compute log-probabilities in parallel using a single forward pass over all the tokens. (top) The model evicts cache entries at fixed intervals. (round 1: evicts t1, t3; round 2: evicts t2, t5). For illustrative purposes, we show the schematic for a single layer, but in practice we evict separately across layers. (bottom) The resulting replay mask: row t marks which keys were visible when token t was generated. A single forward pass with this mask reproduces the next-token distributions from generation in parallel. Without the replay mask, token log-probabilities are computed under a richer context than the model actually saw during generation, introducing a systematic off-policyness that causes training collapse.\", \"page\": 8, \"index\": 8, \"width\": 546, \"height\": 541}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-009.webp\", \"caption\": \"Table 1: Countdown training hyperparameters.\", \"page\": 19, \"index\": 9, \"width\": 414, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-010.webp\", \"caption\": \"Figure 9: Replay masks route gradients through eviction decisions. We perform a forward pass over the tokens with πθ using replay attention masks (see Figure 2). Both losses share the same group-normalized advantage Ai (purple) but differ in their log-probabilities: Ltoken uses the per-token next-token log-probs, while Lmem uses the Gumbel-top-k log-prob of the eviction decisions. Crucially, since the inputs that are used to make eviction decisions pk,hqt are collected during this forward pass, they are on the autograd graph. Therefore, gradients from Lmem flow back into θ and, as a consequence, eviction decisions are treated as a native action of the model that is optimized end-to-end just like tokens in the chain-of-thought.\", \"page\": 19, \"index\": 10, \"width\": 734, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.18002v1/fig-011.webp\", \"caption\": \"Table 2: DAPO-17k training hyperparameters.\", \"page\": 20, \"index\": 11, \"width\": 414, \"height\": 392}]"
motivation: 思维链推理导致的 KV 缓存膨胀限制了模型扩展，且现有人工设计的缓存剔除策略缺乏灵活性。
method: 引入 NGC 框架，利用强化学习基于任务结果奖励，训练模型在推理间隙自主选择并清理不再需要的 KV 缓存。
result: 在 Countdown 和 AIME 等数学推理任务中，NGC 在压缩 2-3 倍 KV 缓存的情况下，准确率接近全缓存上限并优于基准算法。
conclusion: 实验证明端到端学习可以使模型自主兼顾推理能力与内存效率，是迈向高效长文本推理的重要一步。
---

## 摘要
思维链（Chain-of-thought）推理推动了语言模型能力的显著进步，但每一个推理步骤都会增加 KV 缓存（KV cache），从而形成了进一步扩展该范式的瓶颈。目前的方法通常使用人工设计的准则来代模型管理这些约束。遵循深度学习中更广泛的模式，一种更具扩展性的方法是让端到端学习完全取代这一设计选择。毕竟，如果模型可以学习推理，为什么它不能学习遗忘呢？我们引入了神经垃圾回收（Neural Garbage Collection, NGC），在这种方法中，语言模型在学习推理的同时学习遗忘，且仅通过基于结果的任务奖励进行端到端训练。当模型进行推理时，它会定期暂停，决定驱逐哪些 KV 缓存条目，并基于剩余的缓存继续推理。通过将思维链中的标记（tokens）和缓存驱逐决策视为从语言模型中采样的离散动作，我们可以利用强化学习来联合优化模型的推理方式及其内存管理方式：模型驱逐的内容决定了它记住的内容，它记住的内容塑造了它的推理，而推理的正确性决定了它的奖励。至关重要的一点是，模型完全从单一的学习信号——基于结果的任务奖励——中学习这种行为，无需监督微调或代理目标。在 Countdown、AMC 和 AIME 任务上，NGC 在峰值 KV 缓存大小压缩 2-3 倍的情况下，相对于全缓存上限保持了强大的准确性，并显著优于驱逐基准。我们的结果是迈向更宏伟愿景的第一步，即端到端优化同时驱动语言模型的能力和效率。

## Abstract
Chain-of-thought reasoning has driven striking advances in language model capability, yet every reasoning step grows the KV cache, creating a bottleneck to scaling this paradigm further. Current approaches manage these constraints on the model's behalf using hand-designed criteria. A more scalable approach would let end-to-end learning subsume this design choice entirely, following a broader pattern in deep learning. After all, if a model can learn to reason, why can't it learn to forget? We introduce Neural Garbage Collection (NGC), in which a language model learns to forget while learning to reason, trained end-to-end from outcome-based task reward alone. As the model reasons, it periodically pauses, decides which KV cache entries to evict, and continues to reason conditioned on the remaining cache. By treating tokens in a chain-of-thought and cache-eviction decisions as discrete actions sampled from the language model, we can use reinforcement learning to jointly optimize how the model reasons and how it manages its own memory: what the model evicts shapes what it remembers, what it remembers shapes its reasoning, and the correctness of that reasoning determines its reward. Crucially, the model learns this behavior entirely from a single learning signal - the outcome-based task reward - without supervised fine-tuning or proxy objectives. On Countdown, AMC, and AIME tasks, NGC maintains strong accuracy relative to the full-cache upper bound at 2-3x peak KV cache size compression and substantially outperforms eviction baselines. Our results are a first step towards a broader vision where end-to-end optimization drives both capability and efficiency in language models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neural Garbage Collection: Learning to Forget while Learning to Reason》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：长链条推理（Chain-of-Thought, CoT）虽然提升了模型能力，但会导致 KV 缓存（KV Cache）随推理步数无限增长，造成严重的内存瓶颈。
*   **研究动机**：现有的缓存管理方法（如启发式剔除、压缩、摘要化）大多依赖人工设计的规则或代理目标（如蒸馏、重建损失）。作者认为，既然模型能通过端到端强化学习（RL）学会推理，也应该能学会自主管理内存。
*   **整体含义**：将“效率”视为一种模型能力，通过端到端优化，让模型在追求任务正确性的同时，学会识别并“遗忘”掉那些对后续推理无用的中间信息。

### 2. 方法论：神经垃圾回收（NGC）
*   **核心思想**：将“缓存驱逐”建模为语言模型的一个离散动作空间，与“生成 Token”共同进行强化学习优化。
*   **关键技术细节**：
    *   **增长-驱逐循环**：模型每生成 $\delta$ 个 Token（驱逐步长），触发一次驱逐轮次。
    *   **动作参数化**：利用 Transformer 原有的注意力机制。通过计算最近 $w$ 个查询（Query）对缓存中各块（Block）的平均注意力得分作为重要性指标。
    *   **Gumbel-top-k 采样**：为了使离散的驱逐决策可微并适用于策略梯度算法，使用 Gumbel-top-k 技巧进行无放回采样，并推导出闭式对数概率。
    *   **回放注意力掩码（Replay Attention Mask）**：这是训练的关键。在 RL 的更新阶段，通过特殊的 2D 注意力掩码重现推理时的可见性模式，确保对数概率计算的准确性，解决“策略偏离”（Off-policyness）问题。
    *   **预算感知内省（Budget-aware Interoception）**：在 Prompt 中加入当前的驱逐率标签，让模型意识到自身的资源约束。
*   **算法流程**：模型生成推理链 $\rightarrow$ 定期评分并采样驱逐块 $\rightarrow$ 基于残余缓存继续推理 $\rightarrow$ 根据最终答案正确性（二元奖励）通过 GRPO 算法同时更新推理参数和驱逐决策参数。

### 3. 实验设计
*   **数据集/场景**：
    *   **Countdown**：组合算术任务，用于控制变量分析和消融实验。
    *   **DAPO-17k**：大规模数学推理数据集，用于扩展性验证。
    *   **测试集**：AMC 2023/2025、AIME 2025 等高难度数学竞赛题目。
*   **基准（Benchmark）**：以“全缓存推理（No Eviction）”作为性能上限。
*   **对比方法**：
    *   **SnapKV**（基于注意力权重）。
    *   **KeyDiff**（基于键相似度/多样性）。
    *   **KNorm**（基于键范数统计）。
    *   **StreamingLLM**（滑动窗口+注意力锚点）。

### 4. 资源与算力
*   **模型规模**：主要基于 DeepSeek-R1-Distill-Qwen-1.5B 进行实验。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了训练参数（如 Countdown 训练 250 步，DAPO-17k 训练 469 步，每步包含 256 个 Prompt 和 8-16 个 Rollouts），这表明实验是在中等规模的计算集群上完成的。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单算术到复杂竞赛数学的跨度，并进行了详细的消融实验（如验证驱逐策略梯度的必要性、回放掩码的有效性）。
*   **充分性与客观性**：
    *   实验对比了多种主流的启发式基准，并展示了 NGC 在不同压缩率下的泛化能力。
    *   通过“预算感知内省”实验展示了模型对资源约束的元认知能力。
    *   **评价**：实验设计较为充分，通过跨任务（Countdown 到 Math）的验证证明了方法的通用性。

### 6. 主要结论与发现
*   **性能卓越**：在 2-3 倍的峰值缓存压缩下，NGC 的准确率显著超过所有启发式基准，且接近全缓存上限。
*   **端到端必要性**：消融实验证明，仅在训练中加入随机丢弃（Targeted KV Dropout）而不进行端到端策略梯度优化会导致训练崩溃或性能低下。
*   **任务自适应**：启发式方法在不同任务间表现不稳定（如 SnapKV 在 Countdown 失败但在数学任务尚可），而 NGC 能自动学习不同任务的重要性分布。
*   **元代币（Meta-Tokens）潜力**：NGC 可以自然地与“摘要 Token”结合，实现信息的压缩存储而非单纯丢弃。

### 7. 优点
*   **哲学领先**：将效率从“系统优化问题”提升为“模型智能问题”，符合深度学习“端到端”演进的大趋势。
*   **无额外开销**：复用了原有的注意力机制，无需引入额外的评分网络或复杂的蒸馏流程。
*   **训练稳定性**：通过回放掩码和课程学习（Curriculum），成功解决了离散内存管理动作带来的训练不稳定问题。

### 8. 不足与局限
*   **硬件加速未实现**：论文重点在于算法和策略，尚未针对 GPU 算子（如 FlashAttention）进行底层的 PagedAttention 优化，目前的实现可能在实际推理速度上尚无直接优势。
*   **模型规模限制**：实验主要集中在 1.5B 小模型上，尚未在大规模模型（如 70B+）上验证其涌现出的内存管理能力。
*   **冷启动依赖**：模型需要先具备一定的推理基础（如使用 R1 蒸馏版），对于完全从零开始学习推理和遗忘的难度尚不明确。

（完）
