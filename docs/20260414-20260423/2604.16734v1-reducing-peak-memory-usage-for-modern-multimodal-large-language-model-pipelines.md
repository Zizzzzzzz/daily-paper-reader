---
title: Reducing Peak Memory Usage for Modern Multimodal Large Language Model Pipelines
title_zh: 降低现代多模态大语言模型流水线的峰值显存占用
authors: "Junwan Kim, Hyunkyung Bae"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16734v1"
tags: ["query:evlm"]
score: 10.0
evidence: 通过识别视觉标记和KV缓存中的冗余来降低峰值内存使用
tldr: 针对多模态大模型（MLLM）在处理高分辨率图像和长视频时面临的显存瓶颈，本文提出了一种降低峰值显存的推理优化方法。不同于以往在处理完所有输入后才进行压缩的策略，该研究利用视觉Token的结构冗余性，在预填充阶段实施顺序输入压缩机制。通过结构感知的KV缓存压缩，该方法在保持生成质量的同时，有效控制了显存增长，显著提升了多模态推理的效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16734v1/fig-001.webp\", \"caption\": \"Table 1: Performance under fixed KV-cache budgets. Best results are in bold. ∆ denotes the difference from the full-cache baseline. Our method maintains stable performance across compression settings, with minimal degradation even at a budget of 1024 (∼ 90% compress)\", \"page\": 4, \"index\": 1, \"width\": 447, \"height\": 333}]"
motivation: 现有的多模态模型在预填充阶段因存储大量视觉Token的KV缓存而面临极高的峰值显存压力。
method: 提出一种顺序输入压缩机制，在预填充过程中利用视觉结构的规律性进行实时的结构感知KV缓存压缩。
result: 该方法在显著降低峰值显存占用的同时，仅导致极小的生成性能下降。
conclusion: 通过在推理过程中动态管理内存预算，该研究为实现更高效、实用的多模态大模型推理提供了可行方案。
---

## 摘要
多模态大语言模型 (MLLM) 最近在理解多样化视觉输入（包括高分辨率图像和长视频序列）并生成响应方面展现出了强大的能力。随着这些模型扩展到更丰富的视觉表示，推理过程越来越依赖于在键值 (KV) 缓存中存储大量的视觉 token，使得显存消耗成为核心瓶颈。现有方法通过识别视觉 token 中的冗余并压缩缓存来解决这一问题，但此类压缩通常仅在处理完所有输入后才进行，导致预填充阶段的峰值显存占用过高。在这项工作中，我们展示了 MLLM 表现出内在的结构规律性和表示冗余，可用于在整个推理过程中控制显存增长。基于这一见解，我们提出了一种顺序输入压缩机制，通过在预填充过程中执行结构感知的键值缓存压缩，来强制执行固定的显存预算。该方法在大幅降低峰值显存占用的同时，仅以极小的性能损失维持了生成性能，从而实现了更具实用性且显存高效的多模态推理。

## Abstract
Multimodal large language models (MLLMs) have recently demonstrated strong capabilities in understanding and generating responses from diverse visual inputs, including high-resolution images and long video sequences. As these models scale to richer visual representations, inference increasingly relies on storing large numbers of vision tokens in the key-value (KV) cache, making memory consumption a central bottleneck. Existing methods address this issue by identifying redundancy in vision tokens and compressing the cache, but such compression is typically applied only after all inputs are processed, resulting in high peak memory usage during the prefill stage. In this work, we show that MLLMs exhibit inherent structural regularities and representational redundancy that can be exploited to control memory growth throughout inference. Based on this insight, we propose a sequential input-compression mechanism that enforces a fixed memory budget by performing structure-aware key-value cache compression during the prefill process. This approach substantially reduces peak memory usage while maintaining generative performance with only minimal degradation, enabling more practical and memory-efficient multimodal inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reducing Peak Memory Usage for Modern Multimodal Large Language Model Pipelines》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
现代多模态大语言模型（MLLM）为了提升视觉推理能力，倾向于采用高分辨率图像分块（Tiling）和高频视频采样，这导致视觉 Token 数量激增（例如一张 4K 图像可产生超过 4.2 万个 Token）。
*   **核心瓶颈**：KV 缓存（Key-Value Cache）随 Token 数量线性增长。
*   **现有缺陷**：现有的 KV 缓存压缩技术通常遵循“先处理、后压缩”的模式，即在完整处理完所有输入后才进行剔除。这意味着在**预填充（Prefill）阶段**，显存会产生一个巨大的峰值（Peak Memory Spike），往往导致 OOM（显存溢出），使得长文本或高分辨率推理在有限硬件上无法实现。
*   **研究目标**：在预填充过程中实时压缩，将显存占用控制在固定预算内。

### 2. 方法论：核心思想与关键技术
论文提出了一种**“边预填充边压缩”（Compress as you prefill）**的框架，核心技术包括：
*   **分块顺序处理（Block-wise Processing）**：将长输入序列划分为连续的数据块（Block）。模型逐块计算 KV 对，每处理完一块就将其加入缓存，并立即触发剔除机制。
*   **结构感知对齐**：针对视觉数据的空间（图像分块）和时间（视频帧）规律，使 Block 的边界与视觉结构对齐（例如 Block 大小匹配图像的原始补丁尺寸），以保留语义完整性。
*   **两种剔除策略**：
    1.  **查询感知剔除（Query-Aware）**：利用文本提示（Prompt）作为代理查询，计算缓存中各 Token 的重要性得分，剔除贡献度低的视觉 Token。
    2.  **查询无关剔除（Query-Agnostic）**：在多轮对话等缺乏明确查询信号的场景下，通过计算 Key 向量与均值向量的差异，保留代表性强的多样化特征。

### 3. 实验设计
*   **模型对象**：主要评估了 **InternVL3.5 (8B/14B)** 和 **Qwen2.5-VL (7B/32B)**，这些是目前最强的开源多模态模型。
*   **数据集/Benchmark**：
    *   **图像定位与推理**：MileBench 中的 ImageNeedleInHaystack、V*。
    *   **视频理解**：MLVU、Video-MME（长视频设置）。
    *   **通用能力**：MMMU、POPE（用于验证压缩是否损害基础性能）。
*   **对比基准**：全缓存（Full Cache）基线，以及 SnapKV 和 KeyDiff 的原始（后压缩）版本。

### 4. 资源与算力
*   **硬件平台**：实验在 **NVIDIA A100 GPU** 上进行。
*   **算力细节**：论文未明确提及具体的训练时长（因为该方法主要针对推理优化，属于无须训练的 Plug-and-play 方法）。实验涉及了对 8B 到 32B 不同参数规模模型的推理测试。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个核心多模态基准测试，并针对不同模型规模（8B, 7B, 14B, 32B）进行了验证。
*   **消融与分析**：
    *   对比了不同 KV 预算（1024 到 4096）下的性能表现。
    *   分析了 Block Size 对性能的影响。
    *   对比了“预填充压缩”与直接“降低输入分辨率”的效果。
*   **充分性评价**：实验设计较为充分，不仅验证了显存降低的有效性，还通过通用任务（MMMU）证明了该方法不会导致模型“变傻”，对比实验考虑了多种主流策略，结果具有客观性。

### 6. 主要结论与发现
*   **显存控制**：该方法能将峰值显存维持在近乎恒定的水平，成功避免了基线模型在处理超过 36 个图像块时的 OOM 问题。
*   **性能保持**：在实现高达 **90% 的压缩率**（预算 1024）时，性能下降极小（如 InternVL3.5-8B 平均仅下降 0.78%）。
*   **视频任务的特殊性**：在某些视频任务中，适度压缩反而提升了性能，这表明预填充阶段的压缩能起到抑制冗余噪声、聚焦关键信息的作用。
*   **结构对齐至关重要**：当 Block 大小与模型原生的视觉 Token 划分（如 28x28）一致时，效果最佳。

### 7. 优点
*   **解决痛点**：直接针对推理中最脆弱的“预填充峰值显存”问题，具有极强的工程实用价值。
*   **无需重训**：作为一种推理侧优化，能够直接应用于现有的预训练 MLLM。
*   **灵活性**：提供了查询感知和查询无关两种模式，适应单轮和多轮对话场景。

### 8. 不足与局限
*   **延迟权衡（Latency Trade-off）**：由于采用了顺序分块处理而非一次性并行处理，首字响应时间（TTFT）会有所增加。
*   **结构依赖**：压缩效果高度依赖于对模型底层视觉结构的理解，如果 Block 划分与模型不匹配，性能会显著下降。
*   **任务局限**：虽然在定位和理解任务上表现良好，但在需要极度精细视觉特征的任务（如超小目标检测）中可能存在风险。

（完）
