---
title: "OneDrive: Unified Multi-Paradigm Driving with Vision-Language-Action Models"
title_zh: OneDrive：基于视觉-语言-动作模型的统一多范式驾驶
authors: "Yiwei Zhang, Xuesong Chen, Jin Gao, Hanshi Wang, Fudong Ge, Weiming Hu, Shaoshuai Shi, Zhipeng Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17915v1"
tags: ["query:evlm"]
score: 10.0
evidence: 基于预训练VLM的统一自动驾驶框架
tldr: OneDrive 提出了一种基于预训练视觉语言模型（VLM）的统一自动驾驶框架，旨在解决现有系统因使用独立解码器处理异构任务而导致的架构碎片化问题。该框架将文本生成、目标检测和轨迹规划等不同范式的任务整合进单一的 Transformer 解码器中，通过共享注意力机制实现了感知与规划的联合优化。实验表明，该方法在 nuScenes 和 NAVSIM 基准测试中均达到 SOTA 性能，并显著提升了推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17915v1/fig-001.webp\", \"caption\": \"Table 3: Closed-loop performance on NAVSIM navtest under supervised fine-tuning. Our unified modeling improves planning stability and interaction awareness over prior end-to-end and VLA methods, while reducing inference cost by approximately 40% through unified causal attention without forwarding the full LLM. † means using Lidar as extra input.\", \"page\": 11, \"index\": 1, \"width\": 716, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.17915v1/fig-002.webp\", \"caption\": \"Table 2: Performance comparison of different methods on the nuScenes dataset for open-loop planning. Methods are categorized into text-based driving models (top) and action-based driving models (bottom). † indicates that the model does not use egovehicle status as input.\", \"page\": 10, \"index\": 2, \"width\": 669, \"height\": 566}, {\"url\": \"assets/figures/arxiv/2604.17915v1/fig-003.webp\", \"caption\": \"Fig. 3: Architecture of OneDrive. Surround-view images are encoded into image tokens by a ViT and concatenated with structured query tokens for detection, lane estimation, and planning, as well as text tokens. The unified token sequence is processed by mixed decoder layers built upon the pretrained LLM causal attention. Perception query tokens are augmented with additional self-attention and task-specific feed-forward networks, while the backbone attention remains unchanged. Task-specific MLP heads decode 3D bounding boxes, lanes, and trajectories in parallel, alongside autoregressive next-token prediction for textual generation, enabling unified multi-task learning within a single transformer decoder.\", \"page\": 5, \"index\": 3, \"width\": 710, \"height\": 442}]"
motivation: 现有自动驾驶系统通常采用分离或级联的解码器来处理结构化输出，导致模型架构碎片化且难以充分复用预训练骨干网络。
method: 将视觉特征和结构化查询（如轨迹和检测）统一放入单个因果解码器中，利用预训练 VLM 的注意力机制协同处理异构任务。
result: "在 nuScenes 开环评估中实现了 0.28 L2 误差和 0.18 碰撞率，在 NAVSIM 闭环评估中表现优异，且推理延迟降低了约 40%。"
conclusion: 该研究验证了预训练 VLM 注意力机制在多模态驾驶任务中的强大迁移能力，为构建统一的视觉-语言-动作模型提供了高效方案。
---

## 摘要
视觉语言模型（VLM）擅长自回归文本生成，但端到端自动驾驶需要具有结构化输出和异构解码行为的多任务学习，例如自回归语言生成、并行目标检测和轨迹回归。为了适应这些差异，现有系统通常引入分离或级联的解码器，导致架构碎片化且骨干网络复用受限。在这项工作中，我们提出了一个基于预训练 VLM 构建的统一自动驾驶框架，其中异构解码行为在单个 Transformer 解码器中得到协调。我们证明了预训练 VLM 的注意力机制在纯语言建模之外表现出强大的可迁移性。通过在单个因果解码器中组织视觉和结构化查询 token，结构化查询可以通过原始注意力机制自然地以视觉上下文为条件。文本和结构化输出共享通用的注意力骨干网络，从而实现跨异构任务的稳定联合优化。通过引入结构化轨迹查询，在同一个因果大语言模型（LLM）解码器中实现了轨迹规划。这种统一的表述使规划能够与图像和感知 token 共享预训练的注意力骨干网络。在端到端自动驾驶基准测试上的广泛实验证明了其最先进的性能，包括在 nuScenes 开环评估中获得 0.28 L2 误差和 0.18 的碰撞率，以及在 NAVSIM 闭环评估中获得具有竞争力的结果（86.8 PDMS）。完整模型保留了多模态生成能力，而高效推理模式则实现了约 40% 的延迟降低。代码和模型可在 https://github.com/Z1zyw/OneDrive 获取。

## Abstract
Vision-Language Models(VLMs) excel at autoregressive text generation, yet end-to-end autonomous driving requires multi-task learning with structured outputs and heterogeneous decoding behaviors, such as autoregressive language generation, parallel object detection and trajectory regression. To accommodate these differences, existing systems typically introduce separate or cascaded decoders, resulting in architectural fragmentation and limited backbone reuse. In this work, we present a unified autonomous driving framework built upon a pretrained VLM, where heterogeneous decoding behaviors are reconciled within a single transformer decoder. We demonstrate that pretrained VLM attention exhibits strong transferability beyond pure language modeling. By organizing visual and structured query tokens within a single causal decoder, structured queries can naturally condition on visual context through the original attention mechanism. Textual and structured outputs share a common attention backbone, enabling stable joint optimization across heterogeneous tasks. Trajectory planning is realized within the same causal LLM decoder by introducing structured trajectory queries. This unified formulation enables planning to share the pretrained attention backbone with images and perception tokens. Extensive experiments on end-to-end autonomous driving benchmarks demonstrate state-of-the-art performance, including 0.28 L2 and 0.18 collision rate on nuScenes open-loop evaluation and competitive results (86.8 PDMS) on NAVSIM closed-loop evaluation. The full model preserves multi-modal generation capability, while an efficient inference mode achieves approximately 40% lower latency. Code and models are available at https://github.com/Z1zyw/OneDrive

---

## 论文详细总结（自动生成）

这是一份关于论文《OneDrive: Unified Multi-Paradigm Driving with Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是：**如何在一个统一的视觉-语言-动作（VLA）模型中，同时处理自动驾驶中截然不同的异构任务？**
*   **背景：** 自动驾驶需要处理多种输出范式，包括自回归的文本生成（用于推理和解释）、并行的结构化预测（用于目标检测和车道线识别）以及连续的轨迹回归（用于规划）。
*   **动机：** 现有方法通常采用“双系统”或“级联”架构，即为不同任务设计独立的解码器。这导致了架构碎片化、预训练权重复用率低以及跨任务信息流受阻。作者希望验证预训练 VLM 的注意力机制是否具备足够的迁移能力，从而在单一解码器内实现所有驾驶任务的统一。

### 2. 方法论：核心思想与关键技术
OneDrive 的核心思想是**将所有驾驶任务转化为统一的 Token 序列，并利用预训练 LLM 的因果注意力机制作为通用的条件接口。**
*   **统一 Token 表示：** 将图像特征（$X_{img}$）、感知查询（$Q_{det}, Q_{lane}$）、规划查询（$Q_{plan}$）和文本 Token（$X_{text}$）拼接成一个长序列。
*   **混合解码器层（Mixed Decoder Layers）：** 
    *   **注意力共享：** 保留预训练 LLM 的因果自注意力（Causal Self-Attention），让查询 Token 通过因果掩码自然地“观察”到图像上下文。
    *   **任务特定适配：** 在浅层解码器中，为感知查询引入额外的自注意力块以支持并行交互，并为不同任务（检测、规划、文本）配置独立的感知前馈网络（FFN），因为实验发现预训练的文本 FFN 难以迁移到回归任务。
*   **三阶段训练策略：** 
    1.  **感知-语言预训练：** 冻结 ViT，训练感知和文本生成能力。
    2.  **规划适配：** 引入规划 Token，优化规划相关的 FFN 和 MLP 头。
    3.  **联合微调：** 全参数（包括 ViT）端到端优化所有任务。

### 3. 实验设计与对比
*   **数据集：** 
    *   **nuScenes：** 用于开环评估（检测、车道线、规划精度及碰撞率）。
    *   **NAVSIM：** 用于闭环评估（PDMS 综合得分、安全性、舒适度等）。
*   **Benchmark 与对比方法：**
    *   **文本驱动模型：** 如 DriveVLM, OmniDrive, SOLVE-VLM 等。
    *   **动作驱动模型：** 如 UniAD, VAD, ColaVLA 等。
    *   **闭环对比：** 对比了 VADv2, TransFuser, Hydra-MDP 等 SOTA 端到端模型。

### 4. 资源与算力
*   **硬件：** 使用了 **64 台 NVIDIA H20 GPU**。
*   **训练细节：** 
    *   nuScenes 实验基于 InternVL3-1B，三个阶段各训练 20 个 Epoch。
    *   NAVSIM 实验基于 InternVL3-2B，初始学习率为 $1 \times 10^{-4}$。
*   **推理效率：** 论文明确提到其推理延迟比同类 VLM 模型降低了约 40%（从 264ms 降至 156ms），因为它避免了多次前向传播或复杂的自回归规划生成。

### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了感知（3D 检测、车道线）、开环规划、闭环规划以及文本推理等多个维度的实验。
*   **消融实验：** 进行了多项关键消融，包括：
    *   预训练权重（Attention vs FFN）的迁移性分析。
    *   文本监督对感知/规划任务的增益分析。
    *   Token 序列顺序的影响。
    *   多阶段训练的必要性。
*   **充分性评价：** 实验设计较为全面，不仅在标准数据集上刷榜，还深入探讨了架构设计的合理性，对比实验涵盖了当前主流的各种技术路线，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **注意力机制的可迁移性：** 预训练 LLM 的注意力模块能够很好地捕捉视觉与查询之间的对应关系，而 FFN 模块则高度绑定于文本分布，难以直接用于结构化预测。
*   **统一架构的优越性：** 单一解码器不仅能达到甚至超过多解码器架构的性能（nuScenes L2 0.28, 碰撞率 0.18%），还能显著提升推理速度。
*   **任务协同：** 保持文本生成任务的梯度有助于稳定感知和规划任务的训练，起到正则化作用。

### 7. 优点与亮点
*   **架构优雅：** 彻底打破了感知和规划需要独立解码器的固有思维，实现了真正的“Token 级统一”。
*   **高效推理：** 通过将规划 Token 化并利用因果注意力，避免了 VLM 常见的自回归规划带来的高延迟。
*   **强迁移性：** 证明了通用大模型权重在自动驾驶垂直领域的深度复用潜力。

### 8. 不足与局限
*   **感知精度瓶颈：** 尽管在统一框架下表现优异，但受限于 VLM 图像下采样和语言导向的预训练目标，其纯视觉 3D 检测精度仍略低于专门针对检测优化的骨干网络（如标准 ViT-L）。
*   **长尾场景覆盖：** 实验主要集中在 nuScenes 和 NAVSIM，对于极端天气、罕见交通参与者等长尾场景的泛化能力尚需更多验证。
*   **算力门槛：** 尽管推理高效，但三阶段的训练过程和对 64 卡 H20 的需求表明其训练成本依然较高。

（完）
