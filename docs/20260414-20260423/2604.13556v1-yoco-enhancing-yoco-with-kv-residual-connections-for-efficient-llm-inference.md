---
title: "YOCO++: Enhancing YOCO with KV Residual Connections for Efficient LLM Inference"
title_zh: YOCO++：通过 KV 残差连接增强 YOCO 以实现高效 LLM 推理
authors: "You Wu, Ziheng Chen, Yizhen Zhang, Haoyi Wu, Chengting Yu, Yuchi Xu, Wenbo Su, Bo Zheng, Kewei Tu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13556v1"
tags: ["query:evlm"]
score: 9.0
evidence: 高效LLM推理中的KV压缩
tldr: "YOCO++ 针对大语言模型推理中的 KV 缓存压力，改进了跨层 KV 压缩方法 YOCO。通过在底层 KV 之间引入加权残差连接，YOCO++ 在保持 50% KV 缓存压缩率和高效推理的同时，显著提升了模型容量。实验证明其性能优于原始 YOCO 及标准 Transformer，实现了推理效率与模型性能的更佳平衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13556v1/fig-001.webp\", \"caption\": \"Figure 4. Training loss curves on a 100B subset of the SlimPajama dataset.\", \"page\": 4, \"index\": 1, \"width\": 492, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2604.13556v1/fig-002.webp\", \"caption\": \"Figure 3. Prefilling latency (left) and decoding throughput (right) on an NVIDIA H20 (96GB) GPU at different sequence lengths.\", \"page\": 4, \"index\": 2, \"width\": 1017, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2604.13556v1/fig-003.webp\", \"caption\": \"Figure 2. Learned residual weights of a 22-layer YOCO++ model without (left) and with (right) the scaling factor.\", \"page\": 3, \"index\": 3, \"width\": 492, \"height\": 592}, {\"url\": \"assets/figures/arxiv/2604.13556v1/fig-004.webp\", \"caption\": \"Figure 1. Illustration of YOCO and YOCO++. KVs that need to be cached are indicated by red boxes. (a) YOCO caches the KVs of the bottom-half layers and shares the KVs of the middle layer with the top-half layers. (b) Based on YOCO, YOCO++ introduces a weighted residual connection between the KVs of each bottomhalf layer and the bottom layer and caches the combined KVs.\", \"page\": 2, \"index\": 4, \"width\": 465, \"height\": 474}]"
motivation: 现有的跨层 KV 压缩方法虽然能减少内存消耗，但往往会导致模型性能出现不可忽视的下降。
method: 在 YOCO 架构基础上，为下半部分各层的 KV 与底层 KV 之间引入了加权残差连接以增强特征传递。
result: "在 50% 的 KV 缓存压缩率下，YOCO++ 达到了跨层压缩方法的领先水平，且性能超越了标准 Transformer。"
conclusion: YOCO++ 证明了通过简单的残差结构改进，可以在不增加推理负担的前提下有效提升 KV 压缩模型的表达能力。
---

## 摘要
跨层键值（KV）压缩已被证明在大语言模型（LLM）的高效推理中非常有效。尽管这些方法减少了 KV 缓存（KV cache）的显存消耗，但通常会引入不可忽视的性能退化。在这项工作中，我们旨在增强 YOCO 的性能。YOCO 是一种跨层 KV 压缩方法，它将中间层的 KV 与上半部分的所有层共享。我们提出了 YOCO++，这是一种增强型的 YOCO，它在每个下半部分层的 KV 与底层 KV 之间引入了加权残差连接。与 YOCO 相比，YOCO++ 在保持相同训练和推理效率的同时提升了模型容量。实验表明，在 50% 的 KV 缓存压缩率下，YOCO++ 在跨层 KV 压缩方法中达到了最先进的性能，且优于标准的 Transformer。

## Abstract
Cross-layer key-value (KV) compression has been found to be effective in efficient inference of large language models (LLMs). Although they reduce the memory consumption of the KV cache, such methods usually introduce non-negligible performance degradation. In this work, we aim to enhance the performance of YOCO, a cross-layer KV compression method that shares the KVs of the middle layer with the top-half layers. We propose YOCO++, an enhanced YOCO that incorporates a weighted residual connection between the KVs of each bottom-half layer and the bottom layer. Compared to YOCO, YOCO++ increases model capacity while maintaining the same training and inference efficiency. Our experiments show that YOCO++ achieves state-of-the-art performance among the cross-layer KV compression methods at a 50% KV cache compression rate, outperforming the standard Transformer.

---

## 论文详细总结（自动生成）

这是一份关于论文《YOCO++: Enhancing YOCO with KV Residual Connections for Efficient LLM Inference》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：大语言模型（LLM）推理时，键值缓存（KV Cache）的显存占用是主要瓶颈。
*   **核心问题**：现有的跨层 KV 压缩方法（如 YOCO）虽然能通过共享 KV 显著减少显存消耗，但往往会导致模型容量下降，进而引发明显的性能退化。
*   **研究动机**：在不增加推理 I/O 开销和计算负担的前提下，如何通过改进架构设计来弥补跨层 KV 共享带来的性能损失。

### 2. 论文提出的方法论：YOCO++
*   **核心思想**：在 YOCO 架构的基础上，为下半部分层（Self-decoder）引入**KV 残差连接**。通过将当前层的 KV 与第一层的 KV 进行加权融合，增强特征的表达能力。
*   **关键技术细节**：
    *   **架构改进**：YOCO 将模型分为下半部的 Self-decoder（生成 KV）和上半部的 Cross-decoder（复用中间层的 KV）。YOCO++ 在 Self-decoder 的每一层 $i$ 中，计算加权后的 $K'_i$ 和 $V'_i$：
        *   $K'_i = \lambda(\alpha_{Ki, 1}K_1 + \alpha_{Ki, 2}K_i)$
        *   $V'_i = \lambda(\alpha_{Vi, 1}V_1 + \alpha_{Vi, 2}V_i)$
    *   **可学习权重与缩放因子**：$\alpha$ 为可学习的残差权重。为了解决训练初期权重难以学习的问题，引入了固定的缩放因子 $\lambda$（实验设定为 35），这有助于模型更有效地利用残差信息。
    *   **推理优化**：在解码阶段，直接缓存组合后的 $K'_i, V'_i$，因此其 I/O 开销与原始 YOCO 完全一致，不会因为引入残差连接而变慢。

### 3. 实验设计
*   **数据集**：使用 **SlimPajama** 数据集的 100B 子集进行从零训练；消融实验使用 10B 子集。
*   **Benchmark**：在常识推理任务上进行零样本（Zero-shot）评估，包括 HellaSwag、OpenBookQA、WinoGrande、ARC-e/c 和 PIQA。
*   **对比方法**：
    *   **Standard Transformer**：基准模型。
    *   **YOCO**：原始跨层共享方法。
    *   **FusedKV / FusedKV-Lite**：其他改进型的跨层融合方法。
*   **模型配置**：基于 TinyLlama 架构（1.1B 参数，22 层，32 个注意力头）。

### 4. 资源与算力
*   **训练算力**：使用了 **32 台 NVIDIA H800 (80G) GPU**。
*   **推理测试**：在 **NVIDIA H20 (96GB) GPU** 上测试了 Prefilling 延迟和 Decoding 吞吐量。
*   **训练细节**：Batch Size 为 512K tokens，采用 AdamW 优化器，最高学习率 $3 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 100B token 的大规模预训练对比，验证了最终性能。
    *   进行了推理效率实验，涵盖了不同序列长度下的延迟和吞吐量。
    *   **消融实验**：针对“缩放因子 $\lambda$ 的必要性”以及“是否需要 Key 残差（仅保留 Value 残差）”进行了对比。
*   **充分性评价**：实验设计较为客观，对比了当前主流的跨层压缩变体，并从训练损失、下游任务准确率、推理速度三个维度进行了全面评估。

### 6. 论文的主要结论与发现
*   **性能超越基准**：在 50% 的 KV 缓存压缩率下，YOCO++ 的平均准确率（48.99%）不仅超过了 YOCO（47.98%），甚至超过了标准的 Transformer（48.37%）。
*   **效率保持一致**：YOCO++ 的 Prefilling 延迟仅为 Transformer 的一半，且 Decoding 吞吐量显著高于 Transformer，同时与 YOCO 保持在同一水平。
*   **残差权重的非对称性**：实验发现 Value 的残差权重学习更活跃，而 Key 的残差权重相对较小，这验证了 KV 在模型中作用的非对称性。

### 7. 优点
*   **高效性**：成功实现了“性能提升”与“推理效率”的解耦，通过缓存组合后的 KV 避免了额外的显存读写。
*   **简单易行**：仅增加了极少量（每层 4 个）的可学习参数，架构改动极小，易于集成到现有框架。
*   **效果显著**：在压缩 50% KV 缓存的前提下实现了 SOTA 性能，打破了“压缩必掉点”的常规认知。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 1.1B 参数的小规模模型上，尚未在百亿或千亿级参数的大模型上验证其扩展性（Scaling Law）。
*   **压缩率单一**：主要针对 50% 的压缩率（即下半部生成、上半部复用）进行了研究，未探讨更高压缩率（如 75% 或更多）下的表现。
*   **超参数敏感性**：缩放因子 $\lambda$ 需要手动设定（实验中为 35），对于不同规模或不同任务的模型，可能需要重新调优。

（完）
