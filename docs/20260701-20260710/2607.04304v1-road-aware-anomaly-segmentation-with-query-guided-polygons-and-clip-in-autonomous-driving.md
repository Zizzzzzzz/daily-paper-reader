---
title: Road-Aware Anomaly Segmentation with Query-Guided Polygons and CLIP in Autonomous Driving
title_zh: 道路感知异常分割：基于查询引导多边形与CLIP的自动驾驶方法
authors: "Zhiran Yan, Gordon Elger"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04304v1"
tags: ["query:mm-trf"]
score: 9.0
evidence: 使用CLIP进行自动驾驶中的道路异常分割
tldr: 自动驾驶中，传统语义分割模型基于封闭集假设，难以识别未知道路异常，存在严重安全隐患。为此，我们提出一种轻量级后处理框架，无需重新训练、无需OOD数据或辅助监督：具体而言，基于mask transformer的查询级掩码置信度与多边形道路先验，检测可能对应异常的区域间隙；再通过CLIP零样本语义过滤模块，使用分布内提示与可选通用OOD提示，有效抑制误报。实验表明，在Fishyscapes、SMIYC和RoadAnomaly三个公开基准上，该方法均取得稳健且优异的性能，尤其在Fishyscapes LostAndFound上达到最高AP，并在大多数指标上超越训练免费基线Maskomaly。该框架结合空间先验与语义验证，产生鲁棒且可解释的异常预测，可解释性强、计算开销小，极具实际部署价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04304v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 912, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04304v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1774, \"height\": 794, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04304v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1863, \"height\": 892, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04304v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1591, \"height\": 677, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04304v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1885, \"height\": 797, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04304v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1446, \"height\": 796, \"label\": \"Table\"}]"
motivation: 传统语义分割模型基于封闭集假设，无法识别自动驾驶道路上的未知异常物体，带来安全隐患。
method: 利用mask transformer的查询级掩码置信度与多边形道路先验检测异常区域间隙，并通过CLIP零样本语义过滤模块抑制误报。
result: 在Fishyscapes、SMIYC和RoadAnomaly三个基准上取得优异性能，其中Fishyscapes LostAndFound达到最高AP，多数指标超越训练免费基线Maskomaly。
conclusion: 提出无需重新训练、无需OOD数据的轻量级异常分割后处理框架，兼具鲁棒性与可解释性，适合实际自动驾驶部署。
---

## 摘要
传统的语义分割模型在封闭集假设下运行，难以识别未知或意外物体——这是自动驾驶的一项关键能力。因此，这类模型常常错误分类或忽略分布外（OOD）道路异常，在开放世界环境中带来安全风险。我们提出一种轻量级、后处理的、道路感知异常分割框架，无需重新训练、无需OOD数据、也无需辅助监督。我们的方法基于掩码变换器分割网络，通过利用查询级掩码置信度并推导多边形道路先验来检测可能对应异常的间隙区域。为进一步抑制假阳性，我们引入基于CLIP的零样本语义过滤模块，使用分布内提示，并可选通用OOD提示。通过联合利用空间先验和语义验证，我们的框架产生了鲁棒且可解释的异常预测。在三个公开基准——Fishyscapes、SMIYC和RoadAnomaly上的评估显示出一致的强性能。特别地，我们的方法在大多数指标上优于无需训练的基线MASKomaly，并在Fishyscapes LostAndFound上取得了最高AP。这些结果证明了我们的方法在实际自动驾驶系统中的实用性和可部署性。

## Abstract
Traditional semantic segmentation models operate under a closed-set assumption and struggle to recognize unknown or unexpected objects-an essential capability for autonomous driving. As a result, such models often misclassify or overlook out-of-distribution (OOD) road anomalies, posing safety risks in open-world environments. We present a lightweight, postprocessing, road-aware anomaly segmentation framework that requires no retraining, no OOD data, and no auxiliary supervision. Our approach builds on a mask transformer-based segmentation network by exploiting query-level mask confidence and deriving a polygonal road prior to detect gap regions that may correspond to anomalies. To further suppress false positives, we introduce a CLIP-based zero-shot semantic filtering module using in-distribution prompts, with optional generalized OOD prompts. By jointly leveraging spatial priors and semantic verification, our framework produces robust and interpretable anomaly predictions. Evaluation on three public benchmarks-Fishyscapes, SMIYC, and RoadAnomaly-shows consistently strong performance. In particular, our method outperforms the training-free baseline Maskomaly on most metrics and achieves the highest AP on Fishyscapes LostAndFound. These results demonstrate the practicality and deployability of our approach for real-world autonomous driving systems.

---

## 论文详细总结（自动生成）

# 论文总结：《Road-Aware Anomaly Segmentation with Query-Guided Polygons and CLIP in Autonomous Driving》

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：传统语义分割模型基于封闭集假设（closed-set assumption），只能识别训练集中出现过的类别，无法检测自动驾驶开放环境中未知的、意外的道路障碍（如遗落物体、碎片、动物等），造成安全隐患。
- **背景**：现有异常分割方法大多需要重新训练分割模型、使用异常数据（Outlier Exposure, OE）或额外网络，缺乏轻量级、即插即用的解决方案。最近的工作Maskomaly虽无需训练，但缺少空间先验和语义验证，容易产生误报。
- **目标**：提出一种**训练免费、后处理式**的道路感知异常分割框架，仅需推理时运行，无需重训练、OE或辅助监督，提升异常定位的精度和可解释性。

## 2. 方法论：核心思想与关键技术细节

### 核心思想
利用Mask2Former（mask transformer）输出的查询级掩码和分类置信度，通过**道路多边形先验**检测路面上缺失的间隙区域作为候选异常，再使用**CLIP零样本分类**对这些候选进行语义验证，抑制符合已知类别的伪正例，最终与软异常图融合。

### 关键技术细节（算法流程）

1. **拒绝/接受软掩码（继承自Maskomaly）**
   - **拒绝掩码 \(S_r\)**：对每个非空类且置信度超过阈值 \(\tau_c=0.7\) 的查询掩码，将像素值乘以 \(1 - m_i \cdot \max(c_i)\)，逐步抑制已知高置信区域。
   - **接受掩码 \(S_a\)**：对预设的4个异常敏感查询（索引49,31,83,32），累加其掩码与置信度的乘积。
   - **初始异常图**：\(S_{\text{init}} = \lambda S_r + (1-\lambda) S_a\)，\(\lambda=0.6\)。

2. **道路先验提取**
   - 经验发现第20个查询掩码对应道路区域；阈值化（0.5）后提取轮廓并填充为二进制多边形掩码 \(P_{\text{road}}\)。
   - **初始道路异常掩码**：\(R_{\text{init}} = P_{\text{road}} \land \neg M_{\text{road}}\)（道路内但未被预测为道路的区域）。

3. **CLIP语义验证**
   - 对 \(R_{\text{init}}\) 中每个连通域提取RGB补丁，用CLIP评估与19个Cityscapes ID类别文本提示（如“a photo of a normal car on the road”）的相似度。
   - 可选添加通用OOD提示（如“something unusual in a driving scene”）。
   - 如果最大ID置信度超过阈值 \(\tau_{\text{ID}}=0.8\)，则判为ID，异常分数设为0.05；否则判为OOD，分数设为0.99。

4. **最终异常图融合**
   - 将CLIP验证为异常的像素直接赋值为0.99，其余保留 \(S_{\text{init}}\) 中的软分数。
   - 公式：\(S_{\text{final}}(x,y) = \begin{cases}0.99 & \text{if } (x,y) \in R_{\text{final}} \\ S(x,y) & \text{otherwise}\end{cases}\)。

## 3. 实验设计

### 数据集与基准
- **Fishyscapes**：包含LostAndFound（FS-LaF，100张验证图）和Static（FS-Static，30张合成验证图）。
- **Segment-Me-If-You-Can (SMIYC)**：包含Anomaly（10验证/100测试）和Obstacle（30验证/327测试）两个子集，涵盖各种真实道路障碍。
- **RoadAnomaly**：60张包含意外物体的真实图像。
- 所有异常定义为不属于Cityscapes 19类别的物体。

### 对比方法
涵盖四大范式：
- **置信度类**：MSP, MaxLogit, SML, Maskomaly, RbA, Mask2Anomaly, Meta-OoD, PEBAL。
- **重建类**：SynBoost, Image Resynthesis。
- **生成类**：DenseHybrid。
- **特征类**：Mahalanobis, RPL+CoroCL。
部分方法需要重训练（Re-T）、OE或额外网络（EN）。本文方法属于“无需重训练、无需OE、无需额外网络”。

### 评价指标
- AUROC（曲线下面积）
- AP（平均精度，即AuPRC）
- FPR 95（假阳性率 @95%真阳性率）

## 4. 资源与算力

- **GPU型号**：NVIDIA GeForce RTX 4080（单卡）。
- **数量与时长**：文中未说明训练时长（因方法为训练免费，仅需推理），也未给出具体推理时间或每秒帧数。

## 5. 实验数量与充分性

- **定量实验**：在3个基准共5个子集上报告了AUROC、AP、FPR 95指标，结果汇总于Table I、II、III。
- **消融**：在每张表中分别报告了“Ours”和“Ours + OOD prompts”两种变体，显示了CLIP OOD提示带来的增益。
- **定性结果**：图3给出多组可视化对比（RPL, RbA-OE, Maskomaly, Ours）。
- **充分性判断**：对比方法全面（12+种），覆盖有监督/无监督、需训练/训练免费多种方法；评价指标标准；数据集覆盖合成和真实场景。但缺少针对不同阈值、道路先验选择、查询索引选择等的详细消融。整体实验设计客观、公平（采用官方实现）。

## 6. 主要结论与发现

1. **方法优势**：在所有5个子集上，本文方法（Ours）在多数指标上优于无训练基线Maskomaly，尤其是在FS-LaF上AP提升超过+50（从17.20%到67.41%），且增加了OOD提示后达到75.01%，为所有方法中最高。
2. **训练免费与监督方法竞争**：无需OE或重训练，性能仍可与许多需OE的方法匹敌，甚至超过部分（如PEBAL、SynBoost等）。
3. **CLIP有效性**：OOD提示进一步提升了真实场景（FS-LaF, RoadAnomaly）上的AP，说明语义验证可有效抑制伪正例。
4. **局限性**：在合成数据集FS-Static上不如OE方法；在SMIYC-Anomaly上AP略低于Maskomaly（-1.30）。

## 7. 优点

- **完全训练免费**：无需重新训练分割模型、无需OOD数据、无需额外网络，即插即用。
- **轻量级后处理**：仅增加少量推理开销（CLIP验证仅对少量候选补丁运行），适合实际部署。
- **结合空间与语义**：道路多边形先验提供空间聚焦，CLIP提供语义验证，互补性强。
- **可解释性**：异常预测可追溯到道路间隙和语义验证结果。
- **代码开源**：GitHub仓库（https://github.com/chrisyan/RAAS）可复现。
- **性能竞争力**：在多个基准上达到甚至超越需OE的方法，尤其在真实场景LostAndFound上取得最佳AP。

## 8. 不足与局限

- **实验覆盖不完整**：未提供实时性指标（如FPS）或推理时间，难以评估实际部署可行性。
- **消融实验单一**：仅对比有/无OOD提示，未消融道路先验、阈值选择、查询索引选择等关键组件的影响。
- **合成数据上的局限性**：在FS-Static（合成粘贴异常）上不如OE方法，说明方法对分布内类边界敏感，可能对逼真合成的异常不鲁棒。
- **依赖特定模型**：仅基于Mask2Former（Swin-L），尚未验证在其他分割架构上的泛化性。
- **单帧处理**：未考虑时间维度（视频序列），可能漏掉动态小物体或产生瞬时伪影。
- **CLIP阈值依赖**：ID置信度阈值0.8为固定值，未进行数据集最优搜索，可能影响泛化。
- **OOD提示可选但仍有依赖**：OOD提示的设计可能引入一定的主观性，且通用提示可能在特定场景下效果不稳定。

（完）
