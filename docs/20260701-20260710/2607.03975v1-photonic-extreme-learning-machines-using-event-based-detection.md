---
title: Photonic Extreme Learning Machines using Event-Based detection
title_zh: 使用基于事件的检测的光子极端学习机
authors: "Vicente Rocha, Tomas Lopes, Joana Teixeira, Tiago Ferreira, Catarina Monteiro, Nuno Silva"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03975v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 在光子极限学习机中使用事件相机进行检测
tldr: "光子极端学习机（PELM）利用随机光传播和检测非线性实现高效光计算，但传统强度检测（CCD/CMOS）限制了隐藏表示维度。本文用事件相机替代强度检测，提供对数强度阈值响应（首次事件时间、二值激活、事件计数）作为像素级隐藏表示。在非线性两螺旋分类任务中达到93±3%的准确率，表现出强泛化能力；回归实验揭示对系统光强漂移敏感，需稳定性改进。事件检测为丰富光子隐藏表示提供了新途径，同时明确了当前局限性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03975v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1898, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03975v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1332, \"height\": 623, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03975v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1306, \"height\": 941, \"label\": \"Figure\"}]"
motivation: 传统强度检测限制了光子极端学习机隐藏表示维度和计算能力。
method: 用事件相机的对数强度阈值响应（首次事件时间、二值激活、事件计数）替代传统强度检测。
result: "两螺旋分类准确率93±3%，回归性能对光强漂移敏感，有效维度受样本限制。"
conclusion: 事件检测能丰富光子隐藏表示，但需解决光强稳定性问题以发挥潜力。
---

## 摘要
光子极端学习机利用随机光传播、检测非线性和训练好的线性读出，实现节能且可扩展的光学计算。然而，使用CCD或CMOS相机的传统强度读出限制了隐藏表示空间的维度。在这里，我们通过实验用基于事件的相机替代强度检测，其阈值化的对数强度响应提供了替代的逐像素隐藏表示：首次事件时间、二元激活和事件计数。在非线性双螺旋分类任务中，我们获得了$93\pm 3\%$的准确率，具有强内在泛化能力，并且脊回归和伪逆性能相当，表明样本限制的有效维度。回归结果揭示了对系统性光强漂移的敏感性，为未来基于事件的PELM确定了稳定性要求。这些结果确立了基于事件的检测作为通往更丰富光子隐藏表示的途径，同时阐明了当前的局限性。

## Abstract
Photonic extreme learning machines use random optical propagation, detection nonlinearity, and a trained linear readout for energy-efficient and scalable optical computing. However, conventional intensity readout with CCD or CMOS cameras constrain the dimensionality of the hidden representation space. Here, we experimentally replace intensity detection with event-based camera, whose thresholded log-intensity response provides alternative pixel-wise hidden representations: first-event time, binary activation, and event count. In nonlinear two spiral classification task we obtain accuracies of $93\pm 3\%$ with strong intrinsic generalization, and comparable ridge and pseudo-inverse performance indicating sample-limited effective dimensionality. Regression results reveal sensitivity to systematic optical-intensity drift, identifying stability requirements for future event-based PELMs. These results establish event-based detection as a route toward richer photonic hidden representations while clarifying current limitations.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：传统光子极端学习机（PELM）使用CCD/CMOS相机的强度读出（平方律检测+饱和响应）生成隐藏层表示，但这种检测非线性的类型和维度受限于硬件，导致隐藏表示空间的维度不足，尤其是在输入特征空间低维时（如二螺旋分类），PELM难以展现强泛化能力。
- **背景**：PELM基于极限学习机（ELM）框架，利用随机光学传播（如多模光纤）产生随机权重，配合检测非线性替代激活函数，最后通过训练线性读出层完成任务。但平方律检测本质上是多项式非线性，限制了表示能力。近年来事件相机（DVS）因其异步、低功耗、高时间分辨率的特点在光学传感中兴起，其阈值化的对数强度响应能提供一种新的非线性映射。
- **整体含义**：本文研究用事件相机替代传统强度检测，探索能否通过不同的事件映射（首次事件时间、二值激活、事件计数）获得更丰富、更非线性的隐藏表示，从而提升PELM在低维非线性任务上的表现，并分析其局限。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将事件相机的异步、阈值化的对数强度响应作为PELM隐藏层的新激活函数，即将每个像素的事件流转换为三种替代的隐藏表示，然后通过传统线性读出层进行训练。
- **关键技术细节**：
  1. **光学前端**：输入特征通过数字微镜器件（DMD）编码到光束振幅，经过多模光纤随机传播产生散斑场。
  2. **事件检测**：动态视觉传感器（DVS）在像素处比较对数强度变化ΔL，超过阈值C时触发事件，记录时间戳和极性。
  3. **三种像素级映射（ϕ）**：
     - **首次事件时间**（first-event time）：每个像素第一次触发事件的时间戳。
     - **二值激活**（binary activation）：指示该像素是否曾触发事件（0/1）。
     - **事件计数**（event count）：每个像素在固定积分窗口内的事件总数。
  4. **读出层训练**：用脊回归（ridge regression，λ≥0）或伪逆（λ=0）计算输出权重β。隐藏层输出矩阵H由Ntrain个样本×Np个像素构成。
  5. **有效维度度量**：通过deff = Σ[σ_i²/(σ_i²+λ)]来量化隐藏表示的灵活性和可利用维度，脊回归通过λ抑制小奇异值方向。
- **数学关系**：像素对数强度L(X)=log(|w̄·E^{(in)}(X)+b|²+B)，事件条件|ΔL|>C。三种映射ϕ(Ε)分别提取时间/二值/计数特征，构成O(X)=ϕ(Ε)。

## 3. 实验设计：使用数据集/场景、基准、对比方法

- **数据集/任务**：
  1. **分类任务**：非线性两螺旋分布（2D，两类）。额外在补充材料中测试了另外两种分布。
  2. **回归任务**：一维sinc函数。
- **基准**：与论文[5]中基于相机饱和响应的PELM进行性能对比（尤其在低维空间的表现）。
- **对比方法**：脊回归不同λ值（包括λ=0即伪逆）；对比三种事件映射方式（首次事件时间、二值激活、事件计数）的性能。
- **实验细节**：
  - 输入特征维度低（2D分类，1D回归）。
  - 每个数据集包含30次重复测量（相同输入特征下的不同噪声采样）。
  - 训练点64个，验证点、测试点各若干（具体数量未明确，但分类决策图显示训练/验证/测试点覆盖不同区域）。
  - 使用5折交叉验证在独立的测试重复上评估泛化。
  - 正则化λ从0扫描到较大值，观察精度/MAE变化。

## 4. 资源与算力

- **文中未明确提及**：论文未描述使用了何种GPU、训练时长、GPU数量等计算资源。所有实验基于光学实验平台和数字线性读出（脊回归计算在普通计算机上即可完成，无大规模深度学习训练）。因此无法总结算力需求。

## 5. 实验数量与充分性

- **实验组数**：
  - 两个主要任务（分类+回归），各在三种事件映射下测试。
  - 分类任务中额外在补充材料报告了另两种分布的结果。
  - 每个任务进行了正则化λ扫描（不同λ值）。
  - 每个配置使用了30次重复测量来评估噪声鲁棒性。
  - 使用5折交叉验证得到平均指标和标准差。
- **充分性评价**：
  - 实验覆盖了不同类型任务（分类与回归），对比了三种映射，并且分析了正则化影响，能够支持主要结论。
  - 但仅使用了一个分类数据集（二螺旋）的详细报告，其他分布仅在补充材料中，且未与更多基线（如传统PELM、数字ELM）做全面对比。回归任务显示漂移问题，但未做更系统的稳定性分析。
  - 总体上实验设计基本充分，但泛化到更复杂任务（如图像分类）未涉及，公平性合理。

## 6. 论文的主要结论与发现

1. **分类性能提升**：在二螺旋分类中，事件计数映射达到93±3%准确率，首次事件时间和二值激活分别约为83%和89%，均优于先前饱和响应PELM在该低维任务上的表现，且表现出强泛化（脊回归与伪逆性能相当）。
2. **有效维度达到样本极限**：ridge与伪逆性能接近，说明隐藏表示矩阵H的秩已达到样本数上限（Nf=64），解决了饱和PELM中有效维度受限的问题。
3. **回归任务揭示漂移问题**：在sinc回归中，训练集内预测准确，但独立测试重复（新一次测量）的误差增大，表明表示存在系统性漂移（非纯加性噪声），推测由激光强度缓慢波动导致事件时序/计数改变。
4. **最佳正则化**：分类和回归任务中，小正则化（λ接近0）获得最佳泛化，进一步验证隐藏表示的高秩和噪声鲁棒性。
5. **事件检测提供更丰富的非线性**：阈值化的对数强度响应比平方律饱和响应具有更高非线性，从而提升了有效维度，但也引入了对光强稳定性的依赖。

## 7. 优点

- **创新性**：首次将事件相机引入PELM隐藏表示生成，提出三种实用映射方式，探索了新型光学非线性探测器在神经形态计算中的应用。
- **实验严谨**：包含重复测量、正则化扫描、有效维度分析，以及分类与回归双任务验证，结果可信。
- **揭示关键限制**：明确指出系统光强漂移对回归性能的影响，为后续工作指明了稳定性改进方向。
- **数学框架清晰**：融合DVS响应模型与ELM理论，定义了事件驱动的隐藏层输出矩阵和有效维度公式。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了低维输入（2D/1D）任务，未在高维特征（如图像识别）中验证；基准对比仅限于文献[5]的饱和PELM，缺少与其他PELM变体（如集成方案）或数字ELM的比较。
- **回归任务性能不佳**：虽然揭示了漂移问题，但未提供缓解方案（如功率稳定、校准帧补偿），实用性受限。
- **资源与可重复性**：未公开数据（声称按需提供），可能影响结果复现；未讨论事件相机的能量消耗与速度优势的具体量化。
- **缺乏更深入的分析**：例如不同事件阈值C对性能的影响未探索；三种映射的优劣原因仅定性解释，缺少理论分析。
- **潜在偏差**：30次重复测量可能不足以覆盖所有噪声模式，且漂移方向可能随时间变化，实验中未控制长时间漂移。

（完）
