---
title: "DepthWeave-KV: Token-Adaptive Cross-Layer Residual Factorization for Long-Context KV Cache Compression"
title_zh: DepthWeave-KV：面向长上下文KV缓存压缩的令牌自适应跨层残差分解
authors: "Anna Cordoba, Adam Puente Tercero, Nerea Angulo Hijo, Mar Linares Tercero, Julia Barrientos, Ainhoa Miranda, Jesus Olivera"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06523v1"
tags: ["query:evlm"]
score: 10.0
evidence: 通过跨层残差分解实现长上下文模型的令牌自适应KV缓存压缩
tldr: 长上下文语言模型推理受限于KV缓存的内存带宽和容量，现有压缩方法统一预算导致检索性能下降。本文提出DepthWeave-KV，通过跨层残差分解为共享低秩基和token特定残差，结合token条件路由器对关键token分配更高重建秩，并利用在线误差追踪自适应压缩。在LongBench等基准上，方法达到8.3倍内存压缩和64K上下文下72.8 token/s，任务质量接近全缓存，显著优于之前方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06523v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 859, \"height\": 429, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06523v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 481, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06523v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 909, \"height\": 545, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06523v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1115, \"height\": 1033, \"label\": \"Table\"}]"
motivation: 长上下文推理中KV缓存内存需求大，现有统一压缩策略难以兼顾检索和语义保留，导致质量下降。
method: 跨层残差分解为共享低秩基和token特定残差，token条件路由器根据重要性分配秩，在线误差追踪自适应调整压缩。
result: 在LongBench等基准上，8.3倍KV内存压缩，64K上下文下72.8 token/s，任务质量接近全缓存，优于现有压缩方法。
conclusion: DepthWeave-KV实现了高效且质量保持的长上下文KV缓存压缩，显著降低内存开销。
---

## 摘要
长上下文语言模型推理日益受到存储键值缓存所需内存带宽和容量的限制，然而现有压缩方法常对各层或各令牌应用统一预算，当词汇线索和语义状态需要不同保留时，会降低检索性能。我们提出DepthWeave-KV，一种令牌自适应缓存压缩方法，该方法使用共享低秩通道基对相邻Transformer层间的键和值状态进行分解，同时在注意力行为敏感的位置保留轻量级的令牌特定残差。DepthWeave-KV结合了跨深度残差分解与令牌条件深度路由器，该路由器为承载指令和检索关键的令牌分配更高的重建秩，并利用来自注意力输出探针的无校准在线误差跟踪，在生成过程中自适应压缩，无需重新训练基础模型。融合CUDA实现联合执行基查找、残差反量化和注意力投影，以减少解码时的内存流量。在LongBench、Needle-in-a-Haystack、L-Eval以及长格式问答和摘要基准测试中，DepthWeave-KV以显著更低的内存使用实现了接近完整缓存的任务质量，相较于先前的压缩缓存提升了平均分数和检索精度，同时在64K上下文下达到8.3倍KV内存缩减和每秒72.8个令牌的吞吐量。

## Abstract
Long-context language model inference is increasingly limited by the memory bandwidth and capacity required to store key-value caches, yet existing compression methods often apply uniform budgets across layers or tokens and degrade retrieval when lexical cues and semantic states require different preservation. We introduce DepthWeave-KV, a token-adaptive cache compression method that factorizes key and value states across neighboring transformer layers using shared low-rank channel bases while retaining lightweight token-specific residuals where attention behavior is sensitive. DepthWeave-KV combines cross-depth residual factorization with a token-conditional depth router that allocates higher reconstruction rank to instruction-bearing and retrieval-critical tokens, and uses calibration-free online error tracking from attention-output probes to adapt compression during generation without retraining the base model. A fused CUDA implementation jointly performs basis lookup, residual dequantization, and attention projection to reduce decode-time memory traffic. Across LongBench, Needle-in-a-Haystack, L-Eval, and long-form QA and summarization benchmarks, DepthWeave-KV achieves near-full-cache task quality with substantially lower memory use, improving average score and retrieval accuracy over prior compressed caches while reaching 8.3x KV memory reduction and 72.8 tokens per second at 64K context.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文大语言模型（LLM）推理时，键值缓存（KV Cache）占主导内存，导致内存带宽和容量瓶颈。现有压缩方法（如令牌驱逐、令牌合并、量化、低秩注意、层共享等）通常对所有层或所有令牌应用统一压缩预算，忽略了不同令牌（如指令词、检索关键词 vs. 流畅续写词）和不同层（浅层词汇锚点 vs. 深层语义抽象）所需的不同保真度，导致在检索、多跳问答等任务中性能下降。
- **研究动机**：观察到相邻Transformer层的KV结构存在冗余，但单纯深度共享不够；需要一种同时考虑深度依赖、令牌自适应性，并能在线感知注意力行为误差的压缩机制。
- **整体含义**：提出DepthWeave-KV，通过跨层残差分解（共享低秩基 + 令牌特定残差）、令牌自适应深度路由器、无校准在线误差追踪，在不重训基模型的情况下实现高质量、高压缩比的KV缓存压缩。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将相邻层的KV状态分解为两部分：①跨层共享的低秩通道基（捕获公共成分）；②轻量级的令牌特定残差（保留层/令牌特有信息）。通过令牌自适应路由器动态分配残差秩，并通过在线注意力输出误差反馈调整压缩强度。
- **关键技术细节**：
  - **跨深度残差分解**：将层划分为重叠深度窗口 \(W_m\)（窗口大小w），每个窗口每注意力头维护共享基 \(B_{m,h}^K, B_{m,h}^V \in \mathbb{R}^{r_b \times d}\)。对窗口内第ℓ层令牌t的键/值状态重建：
    \[
    \hat{X}_{\ell,h,t}^Z = A_{\ell,h,t}^Z B_{m,h}^Z + g_{\ell,h,t}^Z R_{\ell,h,t}^Z, \quad Z\in\{K,V\}
    \]
    其中 \(A\) 是紧凑系数（8-bit量化），\(R\) 是量化残差（4-bit群量化，仅保留路由的通道），\(g\) 是轻量残差门控（0~1）。共享基从首块在线初始化，通过指数移动平均更新。
  - **令牌条件深度路由器**：根据四个在线特征（累积注意力质量、最大注意力尖峰、定界符/指令指示、前探针区间注意力输出重建误差）计算分数 \(\rho_{m,h,t} = \rho_{\min} + (\rho_{\max}-\rho_{\min}) \cdot \mathbf{1}[\sigma(u^\top\phi)>\tau_m]\)。实际使用三级秩：续令牌0或2，结构令牌4，检索关键跨度8。
  - **无校准在线误差追踪**：每 p 个解码步，对部分头最近查询位置，分别用压缩缓存和临时高保真缓存计算注意输出，定义相对误差 \(e_{\ell,h}\)。若误差超过容忍阈值则降低 \(\tau_m\) 增加残差秩，否则提高 \(\tau_m\) 减少存储。该方法直接关注影响下一隐藏状态的数量。
  - **融合CUDA实现**：联合执行基查找、残差反量化、系数乘法和注意力投影，避免中间结果写回全局内存，减少内存流量。

### 3. 实验设计：使用的数据集 / 场景、基准、对比方法

- **数据集/场景**：长上下文检索、问答、摘要任务。包括：LongBench, L-Eval, NarrativeQA, Qasper, HotpotQA, MultiFieldQA-en, GovReport, QMSum, TriviaQA, Needle-in-a-Haystack。
- **基准**：报告平均标准化任务分数（LongBench子集）、Needle精确检索准确率（上下文16K~128K，针深度0%~100%）、KV内存缩减倍数、解码吞吐量（tok/s）、首个令牌延迟（TTFT）、困惑度增量、注意力输出重建误差。
- **对比方法**：Full KV Cache（无压缩）、StreamingLLM、H2O、SnapKV、PyramidKV、MiniCache、KVSharer、ChunkKV、TailorKV、Eigen Attention。所有方法使用相同基模型、分词器、提示格式、解码参数。默认64K预填长度，贪心解码512个生成令牌，压缩方法目标在≥4倍内存缩减下最大化任务分数。

### 4. 资源与算力

- 论文中**未明确说明**使用了何种GPU型号、数量、训练时长。仅提到融合CUDA实现，但未提供硬件训练配置细节。需指出这一点。

### 5. 实验数量与充分性

- **实验数量**：主实验（表1）覆盖10种方法，包含平均分数、Needle准确率、内存缩减、吞吐量、TTFT、PPLΔ、重建误差等7项指标；消融实验（表2）对比了5个组件变体（无跨层基共享、无令牌路由器、无在线误差追踪、无残差门控、仅共享基、无融合内核）。另外有任务族分解分析（检索/问答 vs. 摘要）。
- **充分性**：
  - 覆盖了主流长上下文基准，包括检索、多跳QA、摘要等，场景全面。
  - 公平性：所有方法使用相同基模型、解码参数，压缩方法目标相同内存预算（≥4×）。
  - 消融实验分离了各贡献，证明令牌自适应路由、残差门控、在线追踪、融合内核均有效。
  - 不足之处：仅基于单一基模型（未指明模型名称，但推测为类似Llama的架构），未在不同规模模型上验证。此外，未报告统计显著性检验。

### 6. 论文的主要结论与发现

- DepthWeave-KV在64K上下文下达到**62.9%平均任务分数**（Full KV Cache为63.8%），内存缩减**8.3倍**，Needle准确率**96.1%**（Full KV Cache为98.7%）。所有指标均优于最佳压缩基线TailorKV（61.4%平均分数，92.6% Needle准确率，6.5×缩减）。
- 解码吞吐量**72.8 tok/s**，TTFT**4.89s**，PPL增量仅**0.09**，重建误差**0.027**，均为压缩方法最优。
- 消融显示：令牌自适应路由和残差门控对检索鲁棒性至关重要；跨层基共享和融合内核贡献主要内存和吞吐收益。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：将深度共享从离散复用（如MiniCache）改进为可微的跨层残差分解，既保留共享结构又允许层间差异，更灵活。
- **令牌自适应**：根据令牌重要性动态分配残差秩，避免统一预算导致的关键信息损失，尤其适用于长上下文检索。
- **无校准在线适应**：利用注意力输出探针实时调整压缩比，无需任务特定校准数据或重训，实用性强。
- **系统协同设计**：融合CUDA核将基查找、反量化、注意力计算合并，减少内存流量，实现高吞吐量。
- **实验设计**：不仅报告平均分数，还单独评估检索准确率和重建误差，暴露均匀压缩的隐藏失败模式。消融实验全面，验证了每个组件贡献。

### 8. 不足与局限

- **实验覆盖**：仅基于单一基模型，未在不同模型规模（如7B、13B、70B）或不同架构（如Mamba、Hybrid）上验证通用性。
- **偏差风险**：令牌重要性特征（注意力质量、定界符）可能对某些提示敏感，路由器阈值依赖手工调整（虽然在线追踪可调，但初始设置未分析）。
- **应用限制**：依赖固定深度窗口大小w，假设冗余局部性，可能不适用于层角色异质性大的模型。非连续深度组或动态选择基可进一步改进。
- **计算开销**：在线探针引入额外推理步骤（每p步两次前向），虽比例可控但未报告具体开销。
- **资源与算力细节缺失**：未说明GPU型号、数量、训练/推理环境，影响可复现性评估。
- **未涉及多轮对话或前缀复用场景**，生产级部署中可能需要进一步适配。

（完）
