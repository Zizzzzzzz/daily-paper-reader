---
title: "When Token Compression Breaks: Structural Pruning vs. Token Reduction for Robust ViT Segmentation under High Compression"
title_zh: 当Token压缩失效时：高压缩下鲁棒ViT分割的结构剪枝与Token缩减对比
authors: "Tien-Phat Nguyen, Ngai-Man Cheung"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02237v1"
tags: ["query:evlm"]
score: 9.0
evidence: 对高效ViT分割的令牌压缩方法进行基准测试
tldr: 针对Vision Transformer语义分割在高压缩比下的性能退化问题，本文系统对比了Token压缩和结构剪枝两种方法。在ADE20K和Cityscapes及其带噪变体上等FLOPs评测发现，Token压缩在中低压缩比下有效，高压缩时因信息损失急剧退化；结构剪枝则退化更平滑，高压缩时更稳健。基于此提出“先剪枝再合并”管道：在适度剪枝骨干上应用中等Token压缩，同等FLOPs下实现更好的精度-鲁棒性权衡，为部署提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02237v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1256, \"height\": 418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02237v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1243, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02237v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1253, \"height\": 624, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02237v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 583, \"height\": 425, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02237v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 837, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02237v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1095, \"height\": 826, \"label\": \"Table\"}]"
motivation: 现有Token压缩评测只关注低中压缩，高压缩或输入退化下表现未知；结构剪枝与Token压缩缺乏统一对比。
method: 在等FLOPs下基准测试典型Token压缩与结构剪枝方法，并提出“剪枝后Token合并”组合策略。
result: Token压缩在高压缩时急剧退化，结构剪枝退化更平滑；组合策略在高压缩下精度-鲁棒性更优。
conclusion: 组合策略为部署导向的ViT分割提供了实用方案，代码已开源。
---

## 摘要
视觉Transformer（ViT）是语义分割的强大骨干网络，但其计算成本限制了部署。近期针对高效Transformer分割的Token压缩方法通过减少Token数量来降低计算成本。然而，现有评估主要关注低到中等压缩，其在激进压缩和损坏输入下的行为尚不明确。与此同时，结构剪枝通过去除ViT架构中的冗余组件提供了一种正交的效率提升途径，但在统一协议下很少与Token压缩进行比较。为填补这一空白，我们在ADE20K和Cityscapes及其常见损坏变体ADE20K-C和Cityscapes-C上，在匹配的FLOPs下，对基于ViT的语义分割中的代表性Token压缩和结构剪枝方法进行了基准测试。我们的结果揭示了在干净和损坏输入上的一致趋势：Token压缩在温和缩减下非常有效，但当压缩变得严重时性能急剧下降，这与过度激进的Token缩减导致大量信息损失一致。相比之下，结构剪枝表现出更平滑的退化曲线，在高压缩下更为稳定。基于这些发现，我们研究了一种先剪枝后合并的流程，即在适度剪枝的骨干网络上应用中等程度的Token压缩。在相当的FLOPs下，这种组合策略在高压缩下持续实现了更好的准确率-鲁棒性权衡，为面向部署的ViT分割提供了一种实用的方案。代码可在https://github.com/phatnguyencs/vit-seg-compression获取。

## Abstract
Vision Transformers (ViTs) are strong backbones for semantic segmentation, but their computational cost limits deployment. Recent token compression methods for efficient transformer-based segmentation reduce this cost by decreasing the number of tokens. However, existing evaluations primarily focus on low-to-moderate compression, leaving their behavior under aggressive compression and corrupted inputs unclear. Meanwhile, structural pruning provides an orthogonal route to efficiency by removing redundant components in the ViT architecture, but is rarely compared to token compression under a unified protocol. To bridge this gap, we benchmark representative token compression and structural pruning methods for ViT-based semantic segmentation under matched FLOPs on ADE20K and Cityscapes, together with their common-corruption variants ADE20K-C and Cityscapes-C. Our results reveal a consistent trend on both clean and corrupted inputs: token compression is highly effective at mild reductions but degrades sharply when compression becomes severe, consistent with substantial information loss from overly aggressive token reduction. In contrast, structural pruning exhibits a smoother degradation curve and is more stable at high compression. Motivated by these findings, we study a prune-then-merge pipeline that applies moderate token compression on top of a moderately pruned backbone. At comparable FLOPs, this combined strategy consistently achieves a better accuracy-robustness trade-off at high compression, offering a practical recipe for deployment-oriented ViT segmentation. Code is available at https://github.com/phatnguyencs/vit-seg-compression.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：Vision Transformer（ViT）在语义分割中表现优异，但计算成本高。近期token压缩方法（如ToMe、ALGM、CTS）通过减少token数量来提升效率，但现有评测仅关注低到中等压缩比，其在**高压缩比**和**输入损坏（如噪声、模糊）**条件下的表现尚不清楚。同时，**结构剪枝**（如NViT）通过移除网络冗余组件（注意力头、通道等）提供正交的效率提升，但很少在同一协议下与token压缩进行直接对比。
- **整体含义**：本文系统对比两种压缩策略在匹配FLOPs下的性能与鲁棒性，揭示它们截然不同的退化行为（token压缩在高压缩时急剧下降，结构剪枝退化更平滑），并提出一种“先剪枝再合并”（Prune-then-Merge, PtM）的组合策略，实现更好的精度-鲁棒性权衡。

## 2. 论文提出的方法论

- **核心思想**：在统一分割管道下，公平比较token压缩（数据自适应减少token数量）与结构剪枝（减少模型宽度/容量），并探索两者的组合。
- **关键技术细节**：
  - **分割管道**：采用DeiT-B作为编码器 + Segmenter的轻量Transformer解码器，压缩仅应用于编码器。
  - **Token压缩方法**：
    - ToMe：通用token合并，通过特征空间相似度合并相似token。
    - ALGM、CTS：分割专用token压缩，保留空间结构（如局部语义一致性）。
  - **结构剪枝方法**：NViT，基于Hessian-aware显著性进行全局剪枝，去除注意力头、前馈通道等。
  - **先剪枝再合并（PtM）**：先对NViT进行适度剪枝得到紧凑骨干，再在其上应用ToMe进行中等程度的token合并。
- **训练与压缩协议**：
  - 使用SGD优化器（momentum 0.9），多项式学习率调度（power 0.9），drop-path 0.1，无权重衰减或warmup。
  - ADE20K：64 epochs，batch size 8，学习率1e-3，crop size 512×512。
  - Cityscapes：216 epochs，batch size 8，学习率1e-2，crop size 768×768。
  - 压缩在微调和评估时均启用（非后处理）。
- **效率与评估指标**：
  - FLOPs作为统一计算量度量；额外报告FPS（单卡RTX A6000，batch size 32，混合精度）。
  - 干净输入：mIoU clean；损坏输入：mIoU noise（在ADE20K-C和Cityscapes-C上，含16种损坏类型×5个严重级别）。

## 3. 实验设计

- **数据集**：
  - 干净：ADE20K（验证集）、Cityscapes（验证集）。
  - 损坏变体：ADE20K-C、Cityscapes-C（依据Kamann & Rother 2020的协议，4类共16种损坏，5级严重度）。
- **基准方法**：
  - 基线：未压缩的DeiT-B + Segmenter。
  - Token压缩：ToMe（通用）、ALGM、CTS（分割专用）。
  - 结构剪枝：NViT（不同压缩配置，如NViT-S、NViT-H，或p参数扫点）。
  - 组合：NViT + ToMe（PtM）。
- **实验设置**：在三种压缩水平（轻度~1.4-1.9× FLOPs降低、中度~2.5×、重度~3.4-3.8×）下匹配FLOPs，报告mIoU clean与mIoU noise。

## 4. 资源与算力

- **硬件**：FPS测量和训练使用单张NVIDIA RTX A6000 GPU。论文未明确说明训练时是否使用多卡，但描述中均为单卡设置（batch size 8）。
- **训练时间**：
  - ADE20K：64 epochs。
  - Cityscapes：216 epochs。
- **其他**：未报告精确的GPU耗时或总训练时长。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：图2展示了在ADE20K和Cityscapes上多个FLOPs点的精度-计算量曲线（包括干净和噪声输入）。
  - 表1给出了三个特定压缩水平的详细数值（涵盖5种方法 × 2个数据集 × 3个水平 = 30个配置）。
  - 表2展示了在相似FPS加速比下的对比（约1.4×、2.5×、3.5×加速，包含PtM对比）。
  - 额外分析：图4的有效秩分析（4种方法 × 不同FLOPs）、图3的定性结果样例。
- **充分性**：实验较为充分，覆盖了两个主流数据集、两种任务视角（干净与损坏）、三种压缩水平，同时提供了FPS和FLOPs两种效率度量。消融实验（PtM vs 单独方法）和机理分析（有效秩）增强了说服力。
- **公平性**：所有方法在同一分割管道、同一微调协议下比较，并按FLOPs匹配，降低了不公平因素。但需要注意，每种方法各自的超参数可能未进行完全独立的深度调优（例如ToMe的合并率、NViT的剪枝率），但实验采用了官方推荐或合理扫点，总体公平。

## 6. 论文的主要结论与发现

1. **轻度token压缩可保持性能**：在低到中等压缩（约1.4-2.5× FLOPs降低）下，分割专用方法（CTS、ALGM）接近基线性能，表明ViT中存在大量可安全利用的token冗余。
2. **高压缩下token压缩急剧退化**：当压缩超过3.4×时，token压缩（尤其是通用方法ToMe）性能崩塌（ADE20K上mIoU clean从47.2降至23.9），且鲁棒性mIoU noise同样大幅下降，原因是过度合并导致空间信息丢失。有效秩分析证实特征多样性严重降低。
3. **结构剪枝在高压缩下更稳定**：NViT在重度压缩下（~3.8× FLOPs降低）仍保持较高性能（ADE20K 45.3 mIoU clean），退化曲线更平滑，因为保留了完整的空间token网格。
4. **先剪枝后合并（PtM）实现更好权衡**：在重度压缩下（~3.7× FLOPs），PtM在ADE20K上达到45.71/34.33 mIoU clean/noise，显著优于单独使用token压缩或结构剪枝，证明两种技术互补，组合可避免单一方法的脆弱性。

## 7. 优点

- **统一的比较框架**：首次在同一语义分割管道下，匹配FLOPs，系统比较token压缩与结构剪枝两种正交压缩策略，填补了现有研究的空白。
- **鲁棒性纳入评估**：引入常见损坏变体（ADE20K-C、Cityscapes-C），不仅关注干净准确性，还考虑分布偏移下的鲁棒性，更贴近真实部署场景。
- **有洞察的分析**：通过有效秩（effective rank）定量解释token压缩退化原因，并给出定性可视化，使结论更可信。
- **实用方案**：提出的“先剪枝再合并”组合策略简单有效，可直接应用于现有预训练模型，为高压缩部署提供现成配方。代码已开源。
- **多维效率比较**：同时报告FLOPs和FPS，并指出两者存在差异（剪枝往往FLOPs降低多但FPS提升少），避免单一指标的误导。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅测试了单一ViT骨架（DeiT-B）和单一分割架构（Segmenter），未推广到层次化ViT（如SegFormer、Swin Transformer）或其他密集预测任务（如目标检测、实例分割）。
  - 结构剪枝只考虑NViT一种方法，未比较其他剪枝技术（如DepGraph、SAViT）。token压缩也只包含ToMe、ALGM、CTS，未涵盖DiffRate、DToP等。
  - 组合策略仅验证了ToMe作为合并工具，未测试ALGM或CTS在剪枝骨干上的表现。
- **超参数优化**：虽然采用相同训练配方，但每种压缩方法的最优超参数（如合并率、剪枝率）可能没有进行独立精细化调优，可能影响性能上限。
- **鲁棒性评估范围**：仅使用了合成损坏（噪声、模糊、天气等），未评估自然分布偏移（如新场景、域迁移）或对抗攻击下的鲁棒性。
- **计算资源细节匮乏**：论文未提供训练完整模型所需的GPU时长、功耗等信息，难以评估实际部署成本。
- **仅考虑FLOPs/FPS**：未比较模型参数数量、内存占用或能源效率，这些在高压缩场景下也可能重要。
- **组合策略的通用性**：论文未讨论如何自动确定最佳的剪枝率和合并率组合，目前主要靠手动扫点；未见深度消融研究不同剪枝强度与合并强度的交互。

（完）
