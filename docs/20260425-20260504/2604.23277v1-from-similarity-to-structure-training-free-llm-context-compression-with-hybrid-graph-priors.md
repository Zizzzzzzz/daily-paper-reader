---
title: "From Similarity to Structure: Training-free LLM Context Compression with Hybrid Graph Priors"
title_zh: 从相似性到结构：基于混合图先验的免训练大语言模型上下文压缩
authors: "Yitian Zhou, Chaoning Zhang, Jiaquan Zhang, Zhenzhen Huang, Jinyu Guo, Sung-Ho Bae, Lik-Hang Lee, Caiyan Qin, Yang Yang"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23277v1"
tags: ["query:evlm"]
score: 9.0
evidence: LLM上下文压缩与Token预算管理
tldr: 针对长上下文大模型计算开销大且处理超长输入不可靠的问题，本文提出一种无需训练且模型无关的上下文压缩框架。该方法利用混合图先验构建句子图，结合语义相似度和序列关系提取主题骨架，并通过综合评分机制筛选关键句子。实验证明，该方法在保持任务相关性、主题覆盖度和连贯性方面表现优异，尤其在长文档基准测试中显著优于现有基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23277v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison between our proposed method and representative baselines on CNN/DailyMail, GovReport, arXiv, and PubMed. Best scores are in bold.\", \"page\": 7, \"index\": 1, \"width\": 975, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.23277v1/fig-002.webp\", \"caption\": \"Table 2: Ablation results of different components of our framework on four benchmarks.\", \"page\": 8, \"index\": 2, \"width\": 975, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.23277v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the proposed structure-aware context compression framework.\", \"page\": 4, \"index\": 3, \"width\": 976, \"height\": 517}, {\"url\": \"assets/figures/arxiv/2604.23277v1/fig-004.webp\", \"caption\": \"Table B.2: Joint scan of scoring weights on GovReport (ROUGE-L).\", \"page\": 17, \"index\": 4, \"width\": 525, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2604.23277v1/fig-005.webp\", \"caption\": \"Figure 2: Performance over token budgets on four datasets. We report the most discriminative metric per dataset (QAFactEval for CNN/DailyMail and GovReport, ROUGE-L for arXiv and PubMed).\", \"page\": 9, \"index\": 5, \"width\": 979, \"height\": 654}]"
motivation: 现有的上下文压缩方法在严格的 Token 预算下，难以同时兼顾任务相关性、主题覆盖范围和跨句连贯性。
method: 构建结合语义 k-NN 和短程序列边的稀疏混合句子图，通过聚类提取主题骨架，并利用集成多种结构指标的评分机制进行贪婪筛选。
result: 在四个数据集上的实验结果显示，该方法在长文档任务中表现出色，性能可与强力的抽取式和生成式基线方法相媲美。
conclusion: 引入结构化图先验可以在无需额外训练的情况下，有效提升长文本压缩的质量和可解释性。
---

## 摘要
长上下文大语言模型（LLM）的运行计算成本依然高昂，且往往无法可靠地处理极长输入，这使得上下文压缩成为许多系统的重要组成部分。现有的压缩方法通常依赖于经过训练的压缩器、稠密检索式选择或启发式裁剪，在严格的 Token 预算下，它们往往难以同时兼顾任务相关性、主题覆盖范围和跨句连贯性。为解决这一问题，我们提出了一种免训练且与模型无关的压缩框架，该框架在结构化图先验的指导下选择紧凑的句子集。我们的方法构建了一个稀疏混合句子图，结合了互 k-NN 语义边与短程序列边，通过聚类提取主题骨架，并使用整合了任务相关性、聚类代表性、桥接中心性和环覆盖线索的可解释评分对句子进行排序。随后，采用带有冗余抑制的有预算贪婪选择，按原始顺序生成可读的压缩上下文。在四个数据集上的实验结果表明，我们的方法与强大的抽取式和生成式基准模型相比具有竞争力，在长文档基准测试中表现出更大的增益。

## Abstract
Long-context large language models remain computationally expensive to run and often fail to reliably process very long inputs, which makes context compression an important component of many systems. Existing compression approaches typically rely on trained compressors, dense retrieval-style selection, or heuristic trimming, and they often struggle to jointly preserve task relevance, topic coverage, and cross-sentence coherence under a strict token budget. To address this, we propose a training-free and model-agnostic compression framework that selects a compact set of sentences guided by structural graph priors. Our method constructs a sparse hybrid sentence graph that combines mutual k-NN semantic edges with short-range sequential edges, extracts a topic skeleton via clustering, and ranks sentences using an interpretable score that integrates task relevance, cluster representativeness, bridge centrality, and a cycle coverage cue. A budgeted greedy selection with redundancy suppression then produces a readable compressed context in original order. Experimental results on four datasets show that our approach is competitive with strong extractive and abstractive baselines, demonstrating larger gains on long-document benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Similarity to Structure: Training-free LLM Context Compression with Hybrid Graph Priors》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）支持的上下文窗口不断扩大（如 GPT-5 达到 400K），处理长文本的计算成本（Transformer 注意力的平方级复杂度）和可靠性问题（如“迷失在中间”现象）日益凸显。现有的上下文压缩技术主要分为三类：启发式裁剪（如只保留开头）、基于检索的选择（如 RAG 风格）和训练专用压缩模型（如 LLMLingua）。
然而，这些方法往往面临以下挑战：
*   **缺乏结构感知**：容易破坏文档的论证链条和叙述连贯性。
*   **部署成本高**：训练专用压缩器增加了系统复杂性。
*   **不可解释性**：难以审计为何某些关键证据被删除。
**核心目标**：开发一种**免训练（Training-free）**、**模型无关（Model-agnostic）**且**可解释**的压缩框架，通过引入图结构先验来保留长文档的主题覆盖度和逻辑骨架。

### 2. 方法论：核心思想与关键技术
该方法将文档视为一个由句子组成的图结构，通过四个阶段实现压缩：

*   **混合句子图构建（Hybrid Sentence Graph）**：
    *   **语义边**：利用句子嵌入（Embedding）构建互 k-最近邻（k-NN）图，捕捉“哪些句子在谈论同一件事”。
    *   **序列边**：在相邻句子间建立边，模拟叙述流和局部连贯性。
    *   **融合**：通过加权系数 $\alpha$ 和 $\beta$ 结合两种边，形成既有全局语义关联又有局部顺序约束的稀疏图。
*   **主题骨架提取**：
    *   使用轻量级的 MiniBatch K-Means 对句子进行聚类，确定文档的潜在主题分布。
*   **结构感知评分机制**：
    *   **任务相关性**：句子与查询（Query）或文档质心的余弦相似度。
    *   **主题代表性**：句子在其所属聚类中的中心程度，确保主题覆盖。
    *   **桥接中心性（Bridge Centrality）**：计算介数中心性（Betweenness Centrality），识别连接不同主题或章节的“桥梁”句子。
    *   **环覆盖线索（Cycle Coverage）**：识别图中的小环结构（如论点-证据闭环），优先保留参与环结构的句子。
*   **带预算的贪婪选择**：
    *   在 Token 预算约束下，按评分从高到低选择句子。
    *   引入**冗余抑制（NMS）**：若候选句子与已选句子相似度过高则剔除。
    *   最后按原始物理顺序输出，保证可读性。

### 3. 实验设计
*   **数据集**：涵盖短中篇新闻到长篇学术报告。
    *   CNN/DailyMail（新闻摘要）
    *   GovReport（政府报告，长文档）
    *   arXiv & PubMed（学术论文，极长文档）
*   **对比方法（Baselines）**：
    *   **无监督/启发式**：TextRank, LEAD-3, RankSum。
    *   **有监督抽取式**：BERTSum, MatchSum, MemSum。
    *   **生成式/长文本模型**：BART, PEGASUS, BigBird, DANCER。
    *   **LLM 导向压缩**：HiGen, SigExt。
*   **评估指标**：ROUGE-1/2/L（词汇重叠）、BERTScore（语义相似度）、QAFactEval（事实一致性）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总计算时长。
*   **效率分析**：由于该方法是免训练的，其主要开销在于句子嵌入计算（调用 OpenAI API 或本地模型）和图算法（介数中心性采用了采样近似算法以提升速度）。相比需要微调的神经网络方法，其算力需求极低。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集上进行了全面测试。
*   **消融实验**：非常充分。分别验证了序列边、主题代表性、桥接中心性、环覆盖和 NMS 冗余抑制的有效性。
*   **参数敏感性分析**：对混合图权重 $\beta$、邻居数 $k$、NMS 阈值 $\tau$ 以及 Token 预算比例进行了详细的曲线分析。
*   **客观性**：实验涵盖了从 10% 到 50% 的不同压缩率，展示了方法在不同预算下的鲁棒性，对比基准包含了最新的 2024/2025 年方法，具有较高的参考价值。

### 6. 主要结论与发现
*   **长文档优势明显**：在 GovReport 和 arXiv 等长文档任务上，该方法显著优于所有基线，证明了结构先验在处理复杂长文本时的必要性。
*   **结构先验的价值**：消融实验显示，**序列边**对保持连贯性至关重要，而**桥接中心性**能有效防止关键转折信息的丢失。
*   **预算效率**：在极低 Token 预算下，该方法比传统的 RAG 检索式选择更能保持文档的整体语义完整性。

### 7. 优点
*   **无需训练**：即插即用，适合快速部署到现有的 LLM 工作流中。
*   **结构感知力强**：通过图论指标（介数、环）捕捉了传统向量检索容易忽略的逻辑连接点。
*   **高可解释性**：每个被选中的句子都有明确的得分组成（相关性、代表性、桥接性等），便于人工审计。
*   **通用性**：不依赖特定的 LLM 架构，对任何 Tokenizer 都友好。

### 8. 不足与局限
*   **句子边界依赖**：依赖于预处理的分句质量，对于格式混乱或缺乏标点的文本可能效果受限。
*   **计算复杂度**：虽然使用了近似算法，但在处理超大规模图（如数万个句子）时，计算介数中心性仍可能存在延迟。
*   **语义理解深度**：作为一种抽取式方法，它无法像生成式压缩那样对信息进行重组或高度概括，可能在极高压缩比下显得信息密度不足。
*   **多模态缺失**：目前仅针对纯文本，未考虑文档中的图表、公式等结构化信息的压缩。

（完）
