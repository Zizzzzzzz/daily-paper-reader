---
title: "DAK: Direct-Access-Enabled GPU Memory Offloading with Optimal Efficiency for LLM Inference"
title_zh: DAK：面向大语言模型推理的高效直接访问 GPU 内存卸载框架
authors: "Shouxu Lin, Zhiyuan Guo, Jiaxin Lin"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26074v1"
tags: ["query:evlm"]
score: 9.0
evidence: LLM推理中的KV缓存卸载
tldr: 提出DAK框架，通过直接访问远程内存优化LLM推理及KV缓存管理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26074v1/fig-001.webp\", \"caption\": \"Figure 14: Figure shows the performance for GH200 under different configurations. The table shows the memory footprint for different configurations and the corresponding global memory offload ratio.\", \"page\": 15, \"index\": 1, \"width\": 486, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2604.26074v1/fig-002.webp\", \"caption\": \"Figure 10: Figure shows the performance for GH200 under different configurations. The table shows the memory footprint for different configurations and the corresponding global memory offload ratio.\", \"page\": 10, \"index\": 2, \"width\": 486, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.26074v1/fig-003.webp\", \"caption\": \"Figure 8: Performance w/ batch size = 8 under vary offloading ratios.\", \"page\": 9, \"index\": 3, \"width\": 1004, \"height\": 496}]"
motivation: LLM推理中的KV缓存卸载。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
大语言模型（LLM）推理受限于 GPU 内存容量和带宽。分层内存架构通过允许 GPU 将内存卸载到远程层来缓解这一问题。然而，现有的内存卸载框架依赖于将数据预取到本地 GPU HBM 中。这种方法会引入 HBM 争用、浪费内存容量并产生流水线气泡，导致系统资源利用不足。我们证明，启用 GPU 对远程内存的直接访问显著优于预取，能够实现最优的系统聚合带宽。我们提出了 DAK，这是一个端到端的直接访问内存卸载框架，它重新利用张量内存加速器（TMA）将卸载的权重和 KV 缓存异步地从远程内存直接取回到 GPU 共享内存（SMEM）中。为了最大化远程访问性能，DAK 引入了一种贪心算法来确定每个操作的最优卸载比例，并结合主动拥塞控制和 TMA 组播来消除互连瓶颈和读取放大。在多种架构上的评估表明，与最先进的内存卸载基准相比，DAK 实现了接近最优的带宽聚合，在 NVLink-C2C 上获得了高达 3 倍的性能提升，在 PCIe 系统上获得了 1.8 倍的性能提升。

## Abstract
LLM inference is constrained by GPU memory capacity and bandwidth. Tiered memory architectures mitigate this by allowing the GPU to offload memory to the remote tier. However, existing memory offloading frameworks rely on prefetching data into local GPU HBM. This approach underutilizes system resources by introducing HBM contention, squandering memory capacity, and creating pipeline bubbles. We show that enabling direct GPU access to remote memory significantly outperforms prefetching, achieving optimal aggregate system bandwidth. We propose DAK, an end-to-end direct-access memory offloading framework that repurposes the Tensor Memory Accelerator (TMA) to asynchronously fetch offloaded weights and KV caches directly from remote memory into GPU shared memory (SMEM). To maximize remote access performance, DAK introduces a greedy algorithm to determine optimal per-operation offloading ratios, alongside active congestion control and TMA multicast to eliminate interconnect bottlenecks and read amplification. Evaluations across diverse architectures show that DAK achieves near-optimal bandwidth aggregation, with up to 3$\times$ performance gains on NVLink-C2C and 1.8$\times$ on PCIe systems compared to state-of-the-art memory offloading baselines.

---

## 论文详细总结（自动生成）

### 论文总结：DAK —— 面向大语言模型推理的高效直接访问 GPU 内存卸载框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）参数量和上下文长度的激增，GPU 的本地高带宽内存（HBM）容量和带宽已成为推理性能的瓶颈（即“内存墙”问题）。现有的分层内存卸载方案（如将权重或 KV 缓存存放在 CPU 内存中）主要依赖**预取（Prefetching）**机制。
*   **预取的局限性**：预取需要将数据先拷贝到 HBM 暂存，这会产生 HBM 带宽争用、占用额外的显存空间作为缓冲区，并因计算与通信无法完美重叠而产生流水线气泡。
*   **核心目标**：DAK 旨在打破预取模式，通过**直接访问（Direct Access）**远程内存，让 GPU 流处理器（SM）直接将数据读取到内部共享内存（SMEM），从而实现本地 HBM 带宽与远程互连带宽的真正聚合。

#### 2. 方法论：核心思想与关键技术
DAK 的核心思想是利用硬件特性绕过 HBM 暂存，直接在远程内存上执行计算。
*   **TMA 异步直接访问**：首次重新利用 NVIDIA Hopper 架构中的**张量内存加速器（TMA）**，将其从原本的“HBM 到 SMEM”传输扩展为“远程内存（Host）到 SMEM”的异步传输，实现了计算与远程访存的深度重叠。
*   **贪心卸载算法（Greedy Offload）**：针对推理流水线中不同算子（计算密集型 vs. 访存密集型）对带宽的敏感度，建立数学模型。通过贪心策略优先为访存密集型算子分配卸载配额，以最大化整体有效带宽（EB），并给出了最优性证明。
*   **主动拥塞控制**：针对大量并发远程请求可能阻塞 GPU 内部互连并干扰本地 HBM 访问的问题，DAK 引入了静态拥塞窗口，限制在途（in-flight）TMA 请求的数量。
*   **TMA 组播与主机感知调度**：针对 CPU 内存不可被 GPU L2 缓存缓存导致的“读取放大”问题，利用 TMA Multicast 技术将一份远程数据同时广播给多个 SM，减少互连流量。

#### 3. 实验设计
*   **硬件环境**：
    *   **GH200 (Grace Hopper)**：代表高带宽互连（NVLink-C2C, 900GB/s）。
    *   **RTX 6000 Blackwell**：代表主流 PCIe 互连（PCIe Gen5, 64GB/s）。
*   **测试模型**：OPT-30B、OPT-6.7B、Llama-2-7B。
*   **对比基准（Benchmarks）**：
    *   **FlexGen**：最先进的基于层级预取的卸载框架。
    *   **vLLM-prefetch**：vLLM 的异步预取配置。
    *   **vLLM-uvm**：基于 NVIDIA 统一虚拟内存（UVM）的硬件自动分页机制。
*   **评估指标**：每生成 Token 时间（TPOT）、有效带宽（EB）、卸载比例（0%-100%）。

#### 4. 资源与算力
*   **GPU 型号**：NVIDIA GH200 (96GB HBM3) 和 RTX 6000 Pro Blackwell (96GB GDDR7)。
*   **算力细节**：实验主要集中在推理阶段（Decoding），未涉及模型训练。代码实现基于 PyTorch、CUDA C++ 和 CUTLASS 3.0。
*   **训练时长**：论文未提及训练时长，因为该研究关注的是推理优化。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括：
    *   不同卸载比例（0% 到 100%）下的性能曲线。
    *   不同 Batch Size（从 8 到 512）和序列长度（最高 1024）的组合。
    *   针对拥塞控制、TMA 组播、内核对齐等关键技术的**消融实验**。
*   **充分性评价**：实验覆盖了从高端超级芯片（GH200）到消费级/专业级显卡（Blackwell）的跨架构验证，对比了当前工业界最流行的框架（vLLM），实验设计客观且具有说服力。

#### 6. 主要结论与发现
*   **性能大幅提升**：在 GH200 上，DAK 相比预取方案实现了高达 **3 倍**的吞吐量提升；在 PCIe 系统上实现了 **1.8 倍**的提升。
*   **带宽聚合**：DAK 能够使系统达到接近理论极限的聚合带宽（HBM 带宽 + 互连带宽），而预取方案往往会导致本地带宽下降。
*   **算法有效性**：贪心卸载算法在混合负载下比统一比例卸载性能提升约 1.5 倍。
*   **硬件潜力**：证明了 TMA 引擎在处理跨节点/跨互连数据移动方面的巨大潜力。

#### 7. 优点
*   **架构创新**：打破了“预取-暂存-计算”的传统思维，利用 TMA 实现了真正的直接访存计算。
*   **数学严谨性**：为卸载比例分配提供了形式化的优化问题定义及最优性证明。
*   **系统级优化**：不仅提出了核心思想，还解决了拥塞控制、读取放大等实际工程中的关键瓶颈。
*   **易用性**：提供了 PyTorch 接口，支持 CUDA Graph，方便集成到现有推理引擎中。

#### 8. 不足与局限
*   **硬件依赖性**：DAK 深度依赖 NVIDIA Hopper 及之后架构的 TMA 硬件特性，在旧款 GPU（如 A100）或非 NVIDIA 硬件上无法直接运行。
*   **静态配置**：拥塞控制窗口和部分参数目前依赖离线分析（Parameter Sweeping），在动态波动的负载环境下可能需要更复杂的运行时自适应机制。
*   **应用范围**：主要针对 LLM 的线性层和注意力机制优化，对于非矩阵乘法类的算子覆盖较少。

（完）
