---
title: Sparsity-Aware Event-Driven Impulse Radio Transceivers for Reliable Neuromorphic Inference
title_zh: 面向可靠类脑推理的稀疏感知事件驱动脉冲无线电收发信机
authors: "Zhengzhong Guan, Jiaying Li, Kanghua Li, Bojun Cheng, Hong Xing"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23559v1"
tags: ["query:ev-cam"]
score: 9.0
evidence: 类脑推理与事件驱动收发器
tldr: 针对资源受限边缘物联网中的神经形态推理，本文提出了一种结合事件感知与超宽带（UWB）通信的宽带多用户远程推理系统。研究引入了利用脉冲稀疏性的双时间尺度重复编码，并设计了基于数字脉冲编码（利用SNN估计稀疏度）和模拟脉冲编码（端到端分类）的两种推理方案。该系统有效降低了通信延迟并提升了在衰落和干扰环境下的推理可靠性，为边缘智能提供了高效的通信与计算融合方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23559v1/fig-001.webp\", \"caption\": \"Fig. 4. Test accuracy versus SNR with Nf = 9.\", \"page\": 5, \"index\": 1, \"width\": 438, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.23559v1/fig-002.webp\", \"caption\": \"Fig. 5. Test accuracy versus SNR with Nf = 9.\", \"page\": 5, \"index\": 2, \"width\": 438, \"height\": 318}]"
motivation: 旨在解决边缘智能中高复杂度收发器在应对衰落和多用户干扰时带来的高能耗与高延迟问题。
method: 提出了一种利用脉冲稀疏性的双时间尺度重复编码，并开发了基于数字脉冲恢复和模拟脉冲输入的两种神经形态推理方案。
result: 实验表明两种方案存在信噪比相关的性能交叉，模拟编码在高中信噪比下表现更优，而数字编码在低信噪比下更具鲁棒性。
conclusion: 该研究通过整合事件驱动感知与脉冲无线电通信，为实现低延迟、高可靠的远程神经形态推理提供了有效的技术路径。
---

## 摘要
部署在资源受限网络边缘的物联网（IoT）人工智能（AI）应用日益增多，这要求从分布式前端传感器到远程推理单元建立超可靠、低延迟的数据处理流水线。与此同时，以脉冲神经网络（SNN）为特征的类脑神经形态计算已成为高效能 AI 推理的新范式。然而，用于对抗衰落和多用户干扰的高复杂度收发信机产生了巨大的能量和时间开销，阻碍了边缘智能中多用户类脑推理的实现。为了应对这一挑战，本文考虑了一种宽带多用户远程推理系统，该系统集成了基于事件的传感和基于跳时（TH）开关键控（OOK）的超宽带（UWB）通信，以实现可靠的类脑推理。具体而言，我们提出了一种新型的双时间尺度重复编码，利用帧内脉冲稀疏性来实现低延迟重复。我们还开发了两种类脑推理方案，分别基于：(i) 数字脉冲编码，通过基于 SNN 的稀疏性估计器进行阈值自适应检测，从而恢复事件帧的每个像素；(ii) 模拟脉冲编码，将接收端的含噪相关器输出转换为模拟值输入，用于端到端（E2E）分类。最后，数值结果验证了所提编码方案的有效性，并揭示了两种推理方案之间存在依赖于信噪比（SNR）的性能交叉，表明基于模拟脉冲编码的方案在中高信噪比下更具优势，而基于数字脉冲编码的方案在低信噪比条件下保持鲁棒性。

## Abstract
The growing number of Internet-of-Things (IoT) based artificial intelligence (AI) applications deployed at resource-constrained network edge call for ultra-reliable and low-latency data processing pipelines from distributed front-end sensors to remote inference units. Meanwhile, brain-inspired neuromorphic computing featuring spiking neural networks (SNNs) have arisen as a new paradigm for energy-efficient AI inference. However, significant energy and time expenses incurred in high-complexity transceivers that combat fading and multi-user interference hinder implementations of multi-user neuromorphic inference for edge intelligence. To address this challenge, we consider in this paper a broadband multi-user remote inference system that integrates event-based sensing and time-hopping (TH) on-off keying (OOK) based ultra-wideband (UWB) communications for reliable neuromorphic inference. Specifically, we propose a novel two-timescale repetition coding that leverages intra-frame pulse sparsity for low-latency repetition. We also develop two neuromorphic inference schemes based on: (i) digital spike encoding that recovers each pixel of the event-frame by threshold-adaptive detection via an SNN based sparsity estimator; and (ii) analog spike encoding that converts noisy correlator outputs at the receiver into analog-valued inputs for end-to-end (E2E) classification. Finally, numerical results validate the effectiveness of the proposed coding schemes, and reveal a signal-to-noise ratio (SNR)-dependent performance crossover between the two inference schemes, indicating that analog spike encoding based schemes are preferable with mild or high SNR while digital spike encoding based schemes remain robust in low SNR regime.

---

## 论文详细总结（自动生成）

以下是对论文《Sparsity-Aware Event-Driven Impulse Radio Transceivers for Reliable Neuromorphic Inference》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着物联网（IoT）和边缘智能的发展，低功耗、低延迟的类脑神经形态计算（如脉冲神经网络 SNN）和事件驱动传感器（如 DVS 相机）成为研究热点。
*   **核心问题**：在实际应用中，传感器与推理单元往往物理分离，需要通过无线信道传输事件流。然而，无线环境中的多径衰落和多用户干扰（MUI）会严重破坏脉冲信号，导致推理精度下降。
*   **研究动机**：现有的无线收发机设计未能充分利用事件数据的**固有稀疏性**，且在处理超宽带（UWB）复杂多径信道时的能效和可靠性不足。本文旨在设计一种“通信-计算融合”的方案，提升远程类脑推理的鲁棒性。

### 2. 论文提出的方法论
核心思想是结合**跳时开关键控（TH-OOK）**调制与**稀疏感知编码**，利用事件流的稀疏特性减少碰撞并增强信号。
*   **双时间尺度重复编码（Two-timescale Repetition Coding）**：
    *   在传统的“帧间重复”基础上，引入“帧内脉冲重复”。
    *   利用 OOK 调制下大量空闲的芯片（chips）时隙，在同一传输帧内多次发送脉冲，从而在不增加额外帧延迟的情况下提高处理增益。
*   **数字脉冲编码推理方案（Digital Spike Encoding）**：
    *   **稀疏性估计**：利用一个轻量级 SNN 估计当前事件帧的脉冲密度（稀疏度）。
    *   **自适应阈值检测**：基于最大后验概率（MAP）准则，根据估计的稀疏度动态调整检测阈值，通过“多数投票”恢复二进制事件位。
*   **模拟脉冲编码推理方案（Analog Spike Encoding）**：
    *   **端到端（E2E）处理**：不进行硬判决（即不恢复 0 或 1），而是将相关器的连续输出值通过 Sigmoid 归一化后，直接作为 SNN 的输入进行分类。

### 3. 实验设计
*   **数据集**：使用 **DVS128 Gesture** 数据集（包含 11 类手势动作）。
*   **通信场景**：模拟 16 个用户在 IEEE 802.15.4a 标准下的 UWB 多径衰落信道（CM1 模型）中竞争传输。
*   **对比方法（Benchmarks）**：
    *   **PPM-D/A**：基于脉冲位置调制（PPM）的数字/模拟方案。
    *   **Baseline**：理想通信条件（无噪声、无干扰）下的 SNN 推理精度。
    *   **OOK-D/A**：仅使用帧间重复（$N_p=1$）的对比方案。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **模型细节**：提到了使用预训练的 DVSGestureNet 架构，并针对信道损伤环境进行了微调（Fine-tuning），使用了 Arctangent 代理梯度下降法。

### 5. 实验数量与充分性
*   **实验规模**：进行了 500 次蒙特卡洛试验以获取平均测试精度。
*   **覆盖范围**：实验涵盖了不同信噪比（SNR 从 -20dB 到 10dB）、不同的重复次数（$N_p=2, 4, 8$）以及多种调制方案的对比。
*   **充分性评价**：实验设计较为客观，通过对比 PPM 方案有力地证明了 OOK 在稀疏场景下的优势。但实验仅限于单一的手势识别任务，未在更多样化的神经形态数据集（如 N-MNIST 或 DVS-CIFAR10）上验证。

### 6. 主要结论与发现
*   **稀疏性的红利**：TH-OOK 方案在性能上显著优于 TH-PPM，因为 OOK 在传输“0”时不发脉冲，极大地降低了多用户碰撞概率。
*   **双时间尺度的有效性**：增加帧内重复次数（$N_p$）能显著提升低信噪比下的精度，使其接近理想通信的基准线。
*   **性能交叉现象**：
    *   **低 SNR 环境**：数字编码方案更优，因为其阈值检测机制能有效过滤背景噪声。
    *   **中高 SNR 环境**：模拟编码方案更优，因为它保留了信号的软信息，允许 SNN 学习更丰富的特征。

### 7. 优点
*   **物理层与应用层的深度融合**：不仅考虑了通信的位错误率（BER），更关注最终的推理精度（Task-oriented）。
*   **创新的编码策略**：双时间尺度重复编码巧妙地利用了 UWB 的高时间分辨率和事件数据的稀疏性，平衡了延迟与可靠性。
*   **自适应性**：提出的 SNN 稀疏性估计器为非平稳事件流提供了动态调整的能力。

### 8. 不足与局限
*   **数据集局限性**：仅在 DVS128 Gesture 上验证，该数据集空间分辨率较低，可能无法完全代表更复杂的视觉场景。
*   **理想化假设**：假设接收端已知完美的信道状态信息（CSI），在极低信噪比下，信道估计本身的误差可能会影响方案性能。
*   **同步问题**：论文未深入讨论多用户环境下的精细时间同步挑战，这在脉冲无线电中是实现难度较大的部分。

（完）
