---
title: "Efficient, VRAM-Constrained xLM Inference on Clients"
title_zh: 客户端设备上高效且显存受限的 xLM 推理
authors: "Aditya Ukarande, Deep Shekhar, Marc Blackstein, Ram Rangan"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26334v1"
tags: ["query:evlm"]
score: 9.0
evidence: 显存受限下的LLM和VLM高效推理
tldr: 针对客户端系统显存受限的问题，本文提出了一种名为“流水线分片（Pipelined Sharding）”的CPU-GPU混合调度技术，旨在实现大语言模型（LLM）和视觉语言模型（VLM）的高效无损推理。该方法结合了子层级模型分片、CPU卸载、流水线拷贝计算及显存张量优先级放置，显著优化了首字延迟（TTFT）和吞吐量（TPS）。该技术将应用于NVIDIA的多个产品，为客户端AI创新提供了重要支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26334v1/fig-001.webp\", \"caption\": \"Figure 1. Pipelined Sharding Overview. In Step 1 , which runs at install time, we populate a profile database by running a collection\", \"page\": 2, \"index\": 1, \"width\": 484, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2604.26334v1/fig-002.webp\", \"caption\": \"Table 1. Comparison of Pipelined Sharding with Prior Hybrid Scheduling Approaches.\", \"page\": 3, \"index\": 2, \"width\": 996, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2604.26334v1/fig-003.webp\", \"caption\": \"Figure 2. Speedups from pipelined sharding for LLMs on cli3, relative to llama-cpp-baseline. Each chart shows speedups across eight VRAM budgets (2G to 32G) for four models at four context sizes (1K, 4K, 16K, 64K). (1) Time-to-first-token (TTFT) speedups average 2× (max 6.7×). (2) Tokens-per-second (TPS) speedups average 3.7×, reaching up to 30× for qwen235b at 64K context (truncated values labeled above). (3) End-to-end latency (E2EL) speedups average 2× (max 4.3×).\", \"page\": 8, \"index\": 3, \"width\": 492, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2604.26334v1/fig-004.webp\", \"caption\": \"Table 4. TPS and TTFT from pipelined sharding on cli3. Column headers are VRAM budgets.\", \"page\": 9, \"index\": 4, \"width\": 1012, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.26334v1/fig-005.webp\", \"caption\": \"Table 5. TPS and TTFT from pipelined sharding on machines cli2 and cli1 across context sizes for VRAM budget at peak capacity (which is 16G on cli2 and 12G on cli1).\", \"page\": 9, \"index\": 5, \"width\": 425, \"height\": 310}]"
motivation: 客户端系统显存有限，难以高效运行高精度的大规模语言和视觉模型。
method: 提出流水线分片技术，通过子层级分片、CPU-GPU混合调度和流水线化拷贝计算来优化推理性能。
result: 实验显示LLM的首字延迟提升达6.7倍，吞吐量提升达30倍，且VLM的显存需求降低了10倍。
conclusion: 该研究证明了通过精细化的资源调度和流水线优化，可以在显存受限的客户端设备上实现高性能的复杂模型推理。
---

## 摘要
为了开启下一轮客户端 AI 创新，迫切需要在客户端系统上实现高精度大语言模型 (LLM) 和视觉语言模型 (VLM)（统称为 xLM）的高效、无损推理。为此，我们提出了流水线分片 (pipelined sharding)，这是一种新颖的、基于基准测试分析引导的 CPU-GPU 混合调度技术，旨在为稠密型和混合专家 (MoE) LLM 实现高效且显存受限的推理。通过结合子层级模型分片、CPU 卸载、流水线拷贝-计算以及显存中的优先级张量放置，该技术优化了首字延迟 (TTFT) 和每秒生成词数 (TPS) 指标，同时能够灵活适应系统和推理条件。针对高效、高精度的 VLM 推理，我们将流水线分片与 llama.cpp 实现的三种成熟先验思想（统称为 VLMOpt）相结合，即视觉张量 CPU 卸载、Flash Attention 以及视觉与语言模型显存重叠规避。这些增强功能旨在改进 NVIDIA 两款重要产品未来版本中的客户端 xLM 推理：游戏内推理软件开发工具包 (IGI SDK) 和 Cosmos-Reason1 (CR1) 物理 AI 推理 VLM。我们在多个模型和客户端系统上进行了严格评估，结果显示：在交互式使用中，LLM 的 TTFT 提升高达 6.7 倍，TPS 提升高达 30 倍，CR1 推理的显存需求降低了 10 倍；而在批处理模式下，吞吐量提升高达 8.2 倍（均与各自激进的基准方案相比）。本文已被第九届 MLSys 会议（工业轨道，2026年）接收。代码和工件可在以下地址获取：https://github.com/deepshnv/pipeshard-mlsys26-ae。

## Abstract
To usher in the next round of client AI innovation, there is an urgent need to enable efficient, lossless inference of high-accuracy large language models (LLMs) and vision language models (VLMs), jointly referred to as xLMs, on client systems. To address this, we present pipelined sharding, a novel, benchmark-profile-guided CPU-GPU hybrid scheduling technique to achieve efficient, VRAM-constrained inference for both dense and mixture-of-experts (MoE) LLMs. Using a combination of model sharding at the sub-layer level, CPU offloading, pipelined copy-compute, and prioritized tensor placement in VRAM, it optimizes both time-to-first-token (TTFT) and tokens per second (TPS) metrics, while flexibly adapting to system and inference conditions. For efficient, high-accuracy VLM inference, we combine pipelined sharding with a llama$.$cpp implementation of three well-understood prior ideas (jointly called VLMOpt), namely, vision tensor CPU offloading, flash attention, and vision and language model VRAM overlap avoidance.   These enhancements are targeted at improving client xLM inference in future releases of two important NVIDIA products - the In-Game Inferencing software development kit (IGI SDK) and the Cosmos-Reason1 (CR1) physical AI reasoning VLM. Highlights from our rigorous evaluation spanning multiple models and client systems include: for interactive use, TTFT improves by up to 6.7x and TPS by up to 30x for LLMs, and CR1 inference's VRAM demand is down by 10x, while in batched mode, throughput improves by up to 8.2x, all compared to their respective aggressive baselines. This paper is accepted at the 9th MLSys Conference (Industry Track), 2026. Code and artifact available at: https://github.com/deepshnv/pipeshard-mlsys26-ae

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient, VRAM-Constrained xLM Inference on Clients》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在显存（VRAM）极度受限的客户端设备（如个人电脑、笔记本）上，高效且无损地运行高精度的大语言模型（LLM）和视觉语言模型（VLM，统称 xLM）。
*   **研究动机**：
    *   **隐私与成本**：云端推理成本高且存在隐私风险，用户倾向于本地推理。
    *   **硬件限制**：客户端 GPU 显存通常远小于模型权重大小（如 235B 模型需 77GB，而普通显卡仅 8-16GB）。
    *   **应用需求**：游戏 AI（IGI SDK）和物理 AI（CR1）需要低延迟（TTFT）和流畅的生成速度（TPS），且需与游戏等其他 GPU 密集型应用共存。

### 2. 论文提出的方法论
论文提出了两套核心优化方案：**Pipelined Sharding（流水线分片）** 和 **VLMOpt**。

*   **Pipelined Sharding（核心调度技术）**：
    1.  **子层级分片（Sub-layer Sharding）**：在 Attention 和 FFN 边界切分模型，比传统的按层切分粒度更细。
    2.  **安装时分析（Install-time Profiling）**：在安装阶段运行基准测试，记录 CPU/GPU 在不同量化、线程数和 PCIe 带宽下的算力特征，生成性能数据库。
    3.  **Token 阶梯计划（Token Tier Planning）**：针对不同的 Token 处理量（如 1, 64, 512...），预先生成三种调度计划：
        *   *GPU-only*：权重实时从内存流向 GPU。
        *   *Static*：部分层固定在 CPU，部分在 GPU。
        *   *Dynamic*：CPU 计算与 GPU 权重传输流水线化并行。
    4.  **优先级放置**：优先将 Attention 和 KV Cache 放入显存，FFN 视情况卸载。
*   **VLMOpt（针对视觉模型的优化）**：
    1.  **视觉张量卸载**：将视觉编码器权重置于 CPU，推理时流式传输。
    2.  **视觉 Flash Attention**：解决高分辨率图像（如 1440p）推理时注意力得分张量导致的显存爆炸。
    3.  **显存重叠规避**：确保视觉编码和语言解码阶段的显存分配在时间上错开，降低峰值需求。

### 3. 实验设计
*   **数据集与场景**：
    *   **LLM 场景**：测试了不同上下文长度（1K 到 64K）下的交互式（Batch=1）和批处理推理。
    *   **VLM 场景**：测试了不同图像分辨率（480p 到 1440p）下的端到端延迟。
*   **测试模型**：
    *   稠密模型：Mistral-Nemo (4B/8B)。
    *   MoE 模型：Qwen3-30B, Qwen3-235B。
    *   视觉模型：Nemotron-Vision-4B, Cosmos-Reason1 (CR1)。
*   **Benchmark 与对比方法**：
    *   **基准（Baseline）**：`llama.cpp` 的原生静态层划分（通过手动搜索最优 `-ngl` 参数获得最强性能）。
    *   **对比框架**：在 VLM 测试中对比了 `vLLM`。

### 4. 资源与算力
*   **硬件环境**：
    *   **cli1 (笔记本)**：Intel Ultra 7 CPU, 16GB RAM, RTX 3500 Laptop GPU (12GB VRAM)。
    *   **cli2 (主流桌面)**：Ryzen 7 CPU, 128GB RAM, RTX 5070 Ti (16GB VRAM)。
    *   **cli3 (高端桌面)**：EPYC CPU, 256GB RAM, RTX 5090 (32GB VRAM)。
*   **训练时长**：本文专注于**推理优化**，不涉及模型训练，因此未提供训练时长。安装时的 Profile 过程约需 15 分钟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 2GB 到 32GB 的 8 种显存预算。
    *   跨越了 4 种上下文长度和多种 Batch Size（1, 4, 16, 64）。
    *   进行了 105 种配置的策略有效性验证。
*   **充分性与公平性**：实验非常充分。作者不仅对比了原生框架，还进行了**灵敏度分析**（如 CPU 线程数、PCIe 带宽的影响）以及**真实应用场景测试**（与《赛博朋克 2077》游戏同时运行），证明了方案在资源竞争环境下的鲁棒性。

### 6. 主要结论与发现
*   **性能飞跃**：在 cli3 上，LLM 的首字延迟（TTFT）平均提升 2x（最高 6.7x），吞吐量（TPS）平均提升 3.7x（最高 30x）。
*   **显存压缩**：成功在仅 2GB 显存的预算下运行 235B 参数的模型，并保持交互级速度（>5 TPS）。
*   **VLM 突破**：将 Cosmos-Reason1 的显存需求降低了 10 倍，使 1440p 高清视觉推理在消费级显卡上成为可能。
*   **调度智能**：Profile 引导的调度器在 100% 的测试用例中选出了最优策略，证明了基于算力特征预测的准确性。

### 7. 优点
*   **全自动适配**：无需用户手动调整参数，自动根据当前显存预算和硬件环境寻找最优解。
*   **无损推理**：不依赖量化压缩或剪枝等可能损害模型精度的手段，纯粹通过调度优化性能。
*   **流水线并行**：巧妙利用了 CPU 计算与 PCIe 传输的重叠，极大缓解了带宽瓶颈。
*   **工业实用性**：直接集成在 `llama.cpp` 中，并针对 NVIDIA 实际产品（IGI SDK）进行了优化。

### 8. 不足与局限
*   **硬件偏向**：目前实现主要针对 NVIDIA CUDA 架构，虽然理论上架构无关，但对 AMD/Intel GPU 的支持尚未验证。
*   **热管理缺失**：调度器目前未考虑 GPU/CPU 的热节流（Thermal Throttling）导致的动态频率变化。
*   **系统开销**：虽然 Planning 阶段很快，但对于极短的单次推理任务，预分析可能存在微小开销。
*   **依赖内存带宽**：在极低显存预算下，性能高度依赖系统内存（sysRAM）的带宽，若内存频率过低，加速效果会受限。

（完）
