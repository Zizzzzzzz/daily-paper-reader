---
title: "Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation"
title_zh: 针对汽车弱势路路使用者安全的边缘人工智能：通过知识蒸馏实现可部署的检测
authors: "Akshay Karjol, Darrin M. Hanna"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26857v1"
tags: ["query:mm-trf"]
score: 9.0
evidence: 汽车弱势路人安全与目标检测
tldr: 本研究针对边缘硬件上弱势道路使用者（VRU）检测的部署难题，提出了一种基于知识蒸馏（KD）的框架。通过让轻量级的YOLOv8-S学生模型学习大型YOLOv8-L教师模型的特征，实现了3.9倍的模型压缩。实验表明，该方法在INT8量化下表现优异，不仅克服了大型模型量化后的精度骤降问题，还在保持高召回率的同时显著提升了检测精度，为安全关键型的车载边缘AI部署提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26857v1/fig-001.webp\", \"caption\": \"Fig. 3. Deployment comparison: KD INT8 achieves highest precision (0.75) and lowest FAR (0.25) among all deployable configurations.\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-002.webp\", \"caption\": \"Fig. 2. Quantization impact: FP32 (solid) vs INT8 (hatched). Teacher collapses (−23% mAP, −24% precision), students retain performance. KD gains +4% precision under INT8.\", \"page\": 4, \"index\": 2, \"width\": 512, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-003.webp\", \"caption\": \"Fig. 1. BDD100K class distribution showing severe imbalance. VRU classes (red) are significantly underrepresented compared to dominant classes such as cars.\", \"page\": 3, \"index\": 3, \"width\": 514, \"height\": 356}]"
motivation: 旨在解决大型目标检测模型在边缘设备进行INT8量化时精度大幅下降，而小型模型原生精度不足的矛盾。
method: 采用知识蒸馏技术，训练参数量仅为11.2M的YOLOv8-S学生模型去模仿43.7M参数的YOLOv8-L教师模型。
result: "在BDD100K数据集上，KD学生模型在INT8量化下的精度达到0.748，超过了教师模型的FP32精度，且误报率比量化后的教师模型降低了44%。"
conclusion: 知识蒸馏是实现在边缘硬件上部署高精度、高鲁棒性且符合安全要求的VRU检测系统的关键技术。
---

## 摘要
在边缘硬件上部署用于弱势路路使用者（VRU）安全的高精度目标检测，需要平衡模型容量与计算约束。大型模型虽然精度高，但在边缘部署所需的 INT8 量化下表现不佳，而小型模型则会牺牲检测性能。本文提出了一种知识蒸馏（KD）框架，通过训练紧凑的 YOLOv8-S 学生模型（1120 万参数）来模仿 YOLOv8-L 教师模型（4370 万参数），在实现 3.9 倍压缩的同时保持了量化鲁棒性。我们在全规模 BDD100K 数据集（7 万张训练图像）上进行了评估，并采用了训练后量化（PTQ）至 INT8。教师模型在 INT8 量化下出现了灾难性的性能下降（mAP 下降 23%），而经过 KD 的学生模型则保持了精度（mAP 仅下降 5.6%）。分析表明，KD 传递的是精度校准而非原始检测能力：在 INT8 量化下，KD 学生模型的精度达到 0.748，而直接训练的模型仅为 0.653，在同等召回率下提升了 14.5%，且相比性能崩溃的教师模型，虚警率降低了 44%。在 INT8 量化下，模型体积缩小 3.9 倍的 KD 学生模型其精度甚至超过了教师模型的 FP32 精度（0.748 对比 0.718）。这些研究结果表明，知识蒸馏是在边缘硬件上部署高精度、安全至上的 VRU 检测的必要条件。

## Abstract
Deploying accurate object detection for Vulnerable Road User (VRU) safety on edge hardware requires balancing model capacity against computational constraints. Large models achieve high accuracy but fail under INT8 quantization required for edge deployment, while small models sacrifice detection performance. This paper presents a knowledge distillation (KD) framework that trains a compact YOLOv8-S student (11.2M parameters) to mimic a YOLOv8-L teacher (43.7M parameters), achieving 3.9x compression while preserving quantization robustness. We evaluate on full-scale BDD100K (70K training images) with Post-Training Quantization to INT8. The teacher suffers catastrophic degradation under INT8 (-23% mAP), while the KD student retains accuracy (-5.6% mAP). Analysis reveals that KD transfers precision calibration rather than raw detection capacity: the KD student achieves 0.748 precision versus 0.653 for direct training at INT8, a 14.5% gain at equivalent recall, reducing false alarms by 44% versus the collapsed teacher. At INT8, the KD student exceeds the teacher's FP32 precision (0.748 vs. 0.718) in a model 3.9x smaller. These findings establish knowledge distillation as a requirement for deploying accurate, safety-critical VRU detection on edge hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心挑战**：在自动驾驶边缘硬件（如 NVIDIA Jetson）上部署弱势道路使用者（VRU，如行人、骑行者）检测模型时，面临“精度与效率”的严峻矛盾。
*   **研究背景**：大型模型（如 YOLOv8-L）在 FP32 精度下表现优异，但在边缘部署必备的 **INT8 量化**过程中会出现灾难性的精度下降（mAP 骤降）；而小型模型（如 YOLOv8-S）虽然量化鲁棒性好，但原生检测精度不足。
*   **研究目的**：探索知识蒸馏（KD）是否能让小型模型在获得量化鲁棒性的同时，继承大型模型的精度优势，从而满足安全关键型任务的需求。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：采用“教师-学生”架构，利用大型教师模型（YOLOv8-L）生成的“软标签”和中间特征来指导轻量级学生模型（YOLOv8-S）的训练。
*   **关键技术细节**：
    *   **损失函数构建**：总损失由三部分组成：$L_{total} = \alpha L_{task} + \beta L_{logit} + \gamma L_{feat}$。
    *   **Logit 蒸馏**：使用 KL 散度和温度缩放（Temperature Scaling, $T=10$）来转移类别相似度信息。
    *   **特征蒸馏**：通过 L2 距离对齐学生和教师模型骨干网络（Backbone）的中间特征图。
    *   **任务损失抑制**：通过设置 $\alpha=0.5$ 降低地面真值（Ground Truth）的权重，迫使学生模型更多地学习教师模型的置信度校准。
    *   **量化策略**：采用训练后量化（PTQ）技术，利用 TensorRT 将模型转换为 INT8 精度。

### 3. 实验设计与对比
*   **数据集**：使用大规模自动驾驶数据集 **BDD100K**（包含 7 万张训练图，1 万张验证图），重点评估行人、自行车手、摩托车手三类 VRU。
*   **对比基准（Benchmark）**：
    1.  **Teacher (L)**：直接训练的 YOLOv8-L（43.7M 参数）。
    2.  **Direct Student (S)**：直接训练的 YOLOv8-S（11.2M 参数）。
    3.  **KD Student (S)**：通过知识蒸馏训练的 YOLOv8-S。
*   **评估维度**：对比了 FP32 和 INT8 两种精度下的 mAP50、精度（Precision）、召回率（Recall）、虚警率（FAR）以及推理吞吐量（FPS）。

### 4. 资源与算力
*   **训练算力**：使用了云端 GPU 基础设施，具体型号为 **NVIDIA RTX PRO 6000 Blackwell (96GB VRAM)**。
*   **训练时长**：教师模型耗时 7 小时，直接训练的学生模型耗时 2.7 小时，KD 学生模型因需运行教师推理，耗时 4.9 小时。
*   **推理测试**：在 **NVIDIA RTX 5070 (8GB)** 上进行，以模拟接近边缘端的部署环境。

### 5. 实验数量与充分性
*   **实验规模**：基于 7 万张图像的全规模训练，实验数据具有统计学意义。
*   **消融实验**：针对任务损失权重 $\alpha$ 进行了专门的消融实验（对比 $\alpha=0.5$ 与 $\alpha=1.0$），验证了“任务损失抑制”是实现精度校准转移的关键。
*   **客观性**：实验涵盖了从 FP32 到 INT8 的完整量化路径，并对比了不同架构规模，评估指标全面（不仅看 mAP，还深入分析了虚警率），实验设计较为客观公平。

### 6. 主要结论与发现
*   **KD 提升量化鲁棒性**：教师模型量化后 mAP 下降 23.4%，而 KD 学生模型仅下降 5.6%。
*   **精度校准转移**：KD 转移的不是“召回能力”（Recall），而是“精度校准”（Precision）。在 INT8 下，KD 学生模型的精度（0.748）甚至超过了教师模型在 FP32 下的精度（0.718）。
*   **大幅降低虚警**：相比量化后崩溃的教师模型，KD 学生模型减少了 44% 的虚警，这对于建立自动驾驶系统的用户信任至关重要。
*   **高效部署**：KD 学生模型实现了 3.9 倍的参数压缩和 2.37 倍的推理加速，且完全满足实时性要求。

### 7. 优点与亮点
*   **实用性强**：直接针对工业界边缘部署的痛点（INT8 量化失效）提出解决方案。
*   **深入的机制分析**：通过消融实验揭示了 KD 并非简单提升性能，而是通过软标签转移了置信度校准，这一发现对理解蒸馏机制有学术价值。
*   **安全导向**：将虚警率（FAR）作为核心指标，紧扣 VRU 安全这一应用场景。

### 8. 不足与局限
*   **架构单一**：实验仅限于 YOLOv8 系列，未验证在其他先进架构（如 Transformer-based 检测器）上的普适性。
*   **硬件偏差**：虽然使用了 RTX 5070 模拟边缘，但尚未在真正的嵌入式 SoC（如 Jetson Orin）上进行功耗和热管理测试。
*   **数据集局限**：仅在 BDD100K 上验证，缺乏在 KITTI 或 nuScenes 等其他主流数据集上的交叉验证。
*   **量化方法限制**：仅使用了 PTQ（训练后量化），未探讨量化感知训练（QAT）可能带来的进一步提升。

（完）
