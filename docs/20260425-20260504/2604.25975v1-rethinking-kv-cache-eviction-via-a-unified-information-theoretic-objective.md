---
title: Rethinking KV Cache Eviction via a Unified Information-Theoretic Objective
title_zh: 通过统一的信息论目标重新审视 KV 缓存逐出机制
authors: "Jiaming Yang, Chenwei Tang, Liangli Zhen, Jiancheng Lv"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25975v1"
tags: ["query:evlm"]
score: 10.0
evidence: 通过信息瓶颈进行长上下文生成的KV缓存剔除
tldr: 针对大语言模型推理中KV缓存带来的内存瓶颈，本文从信息瓶颈原理出发，为KV缓存剔除建立了统一的理论框架。研究者推导出了表征缓存子集信息容量的互信息目标函数，并据此提出CapKV方法。该方法利用统计杠杆得分近似对数行列式，以最大化保留预测信号。实验证明，CapKV在长文本生成中优于现有启发式方法，实现了更优的内存效率与生成质量平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25975v1/fig-001.webp\", \"caption\": \"Figure 3. Capacity preservation and capacity–performance correlation on Qasper.Top: Retained capacity under increasing cache compression ratios for different KV cache eviction methods, shown for three simplified capacity proxies.Bottom: Scatter plots of retained capacity versus task performance across eviction methods and compression ratios, with Spearman rank correlation coefficients and corresponding p-values annotated.Results on additional datasets are provided in Appendix C.2.\", \"page\": 6, \"index\": 1, \"width\": 1003, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2604.25975v1/fig-002.webp\", \"caption\": \"Table 1. Experimental results of Qwen3-8B and Qwen3-14B on Longbench Benchmark.\", \"page\": 7, \"index\": 2, \"width\": 1031, \"height\": 879}, {\"url\": \"assets/figures/arxiv/2604.25975v1/fig-003.webp\", \"caption\": \"Figure 5. Runtime comparison of KV cache eviction methods under varying context lengths and compression ratios on Qwen3-8B.\", \"page\": 14, \"index\": 3, \"width\": 1006, \"height\": 563}, {\"url\": \"assets/figures/arxiv/2604.25975v1/fig-004.webp\", \"caption\": \"Figure 2. Information-Theoretic Perspective of KV Cache Eviction. We model cached key–value pairs as communication channels mapping future queries to attention outputs. Query as Source: Future queries are characterized as stochastic signals sampled from q ∼ N (µQ,ΛQ). KV Cache Channels: Each cached entities induces an effective channel, the vector direction signifies its contribution to the output space, while color intensity encodes query–key alignment. Given a fixed budget |C| = B, diverse channel orientations maximize the information capacity (Theorem 3.2), whereas redundant directions lead to capacity loss. The full KV cache serves as the theoretical upper bound. Output as Destination: Output fidelity is evaluated against the full-cache reference, demonstrating that higher effective capacity directly correlates with lower representational distortion.\", \"page\": 4, \"index\": 4, \"width\": 965, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.25975v1/fig-005.webp\", \"caption\": \"Figure 6. Capacity–Performance Correlation analysis on 2WIKIMQA (top two rows), MULTIFIELDQA (middle two rows), and PASSAGERETRIEVAL (bottom two rows). For each dataset, the top row shows the retained KV cache capacity under increasing compression ratios for different eviction methods, evaluated using three simplified capacity proxies (K, U, and KU-Capacity). The bottom row plots downstream task performance against the corresponding retained capacity across methods and compression ratios, with Spearman rank correlation coefficients and associated p-values annotated.\", \"page\": 16, \"index\": 5, \"width\": 873, \"height\": 1229}]"
motivation: 现有的KV缓存剔除策略大多依赖经验启发式方法，缺乏严谨的理论基础来指导如何保留最关键的信息。
method: 提出CapKV方法，通过线性高斯代理模型推导出互信息目标，并利用统计杠杆得分来最大化KV缓存的信息容量。
result: 在多个模型和长文本基准测试中，CapKV在保持生成保真度的同时，显著提升了内存使用效率，超越了现有主流方法。
conclusion: 该研究证明了信息论目标可以统一并改进KV缓存剔除策略，为长文本推理优化提供了坚实的理论支撑。
---

## 摘要
键值（KV）缓存对于大语言模型推理至关重要，但其内存开销构成了长文本生成的关键瓶颈。现有的逐出策略主要依赖于经验启发式方法，缺乏严谨的理论基础。本研究从信息瓶颈（Information Bottleneck）原则的角度重新审视了 KV 缓存逐出。在注意力机制的线性高斯替代模型下，我们推导出了一个闭式互信息目标，用于表征保留的 KV 缓存子集的有效信息容量。这一公式表明，多种现有的逐出策略都可以被解释为同一容量最大化原则的不同近似。在此见解的指导下，我们提出了 CapKV，这是一种容量感知逐出方法，通过使用统计杠杆得分进行对数行列式近似，直接以信息保留为目标。该方法用具有理论依据的机制取代了启发式选择，从而保留了最大的预测信号。在多个模型和长文本基准测试上的广泛实验表明，CapKV 一贯优于先前的方法，在内存效率和生成保真度之间实现了更好的权衡。

## Abstract
Key-value (KV) caching is essential for large language model inference, yet its memory overhead poses a critical bottleneck for long-context generation. Existing eviction policies predominantly rely on empirical heuristics, lacking a rigorous theoretical foundation. This work rethinks KV cache eviction through the lens of the Information Bottleneck principle. Under a linear-Gaussian surrogate of attention, we derive a closed-form mutual information objective that characterizes the effective information capacity of a retained KV cache subset. This formulation reveals that a wide range of existing eviction strategies can be interpreted as different approximations of the same capacity-maximization principle. Guided by this insight, we introduce CapKV, a capacity-aware eviction method that directly targets information preservation via a log-determinant approximation using statistical leverage scores. This approach replaces heuristic selection with a theoretically grounded mechanism that preserves the maximum predictive signal. Extensive experiments across multiple models and long-context benchmarks show that CapKV consistently outperforms prior methods, achieving a better trade-off between memory efficiency and generational fidelity.

---

## 论文详细总结（自动生成）

### 论文总结：通过统一的信息论目标重新审视 KV 缓存逐出机制

#### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）推理中，KV Cache（键值缓存）随上下文长度线性增长，导致巨大的内存压力。现有的缓存逐出（Eviction）策略（如 SnapKV, H2O, KeyDiff 等）大多依赖于经验性的启发式规则，缺乏严谨的理论基础。
*   **研究动机**：现有的方法往往孤立地评估每个 Token 的重要性，忽略了缓存条目之间的全局冗余性和互补性。作者旨在建立一个统一的理论框架，从信息论的角度解释并优化 KV 缓存的保留策略。

#### 2. 方法论：CapKV 及其理论基础
*   **核心思想**：基于**信息瓶颈（Information Bottleneck）**原则，将 KV 缓存逐出建模为：在固定容量预算下，最大化保留的缓存子集与未来查询（Query）之间的互信息。
*   **关键技术细节**：
    *   **线性高斯代理模型**：为了使分析可解，将注意力机制简化为线性高斯传输信道，推导出闭式的互信息目标函数（对数行列式形式）。
    *   **统一目标函数**：推导出的公式显示，信息保留受 Key 的多样性、Value 的输出贡献、未来 Query 的统计分布以及噪声水平四个因素共同驱动。
    *   **CapKV 算法**：
        1.  **容量矩阵构建**：利用 Value 向量作为输出方向的代理，结合 Query 感知权重构建容量矩阵。
        2.  **统计杠杆得分（Leverage Scores）**：利用矩阵行列式引理，通过计算每个 KV 对对全局容量的边际贡献（即杠杆得分）来评估其重要性。
        3.  **贪婪逐出**：移除杠杆得分最低的 KV 对，从而以低计算开销近似实现信息容量最大化。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LongBench**：包含 16 个任务，涵盖单文档 QA、多文档 QA、摘要、少样本学习、合成任务和代码理解。
    *   **AIME25**：用于评估数学推理模型（Nemotron-7B）在动态解码过程中的表现。
*   **对比方法**：
    *   **基于注意力的策略**：SnapKV, EA (Expected Attention), Sink, H2O。
    *   **基于结构的策略**：KeyDiff（基于几何多样性）, Knorm（基于向量范数）。
*   **压缩率**：测试了 0.25, 0.5, 0.75, 0.9 等多种压缩比例。

#### 4. 资源与算力
*   **硬件环境**：实验使用了 **4 张 NVIDIA L40S GPU**。
*   **算力说明**：文中未明确提及总训练时长（因为该方法主要是推理侧的剔除策略，无需重新训练），但提供了运行效率分析。结果显示 CapKV 的计算复杂度为 $O(Nd^2)$，在 64k 上下文下的推理延迟与现有启发式方法处于同一数量级。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 种主流模型架构（Qwen3-4B/8B/14B, Llama 3.1-8B, Mistral-7B, Nemotron-7B）上进行了测试。
*   **充分性**：
    *   涵盖了从长文本理解到复杂逻辑推理的多种任务。
    *   进行了**相关性分析实验**，验证了理论推导的“信息容量”指标与下游任务性能之间存在显著的正相关性（Spearman 秩相关系数达 0.73-0.87）。
    *   包含消融实验，探讨了 Query 权重参数 $\tau$ 的影响。实验设计客观、对比公平。

#### 6. 主要结论与发现
*   **理论统一性**：证明了现有的多种启发式逐出策略（如基于范数的 Knorm 或基于多样性的 KeyDiff）实际上都是该统一信息容量目标的特定简化近似。
*   **性能优越性**：CapKV 在所有测试的模型和数据集上均一致优于先前的 SOTA 方法，尤其在极端压缩情况下，其性能下降更为平缓。
*   **鲁棒性**：在推理链条极长的数学任务中，CapKV 表现出更强的稳定性，证明了其在处理复杂推理时的信息保留能力。

#### 7. 优点
*   **理论严谨**：为 KV 缓存管理提供了首个基于信息论的统一解释框架。
*   **全局优化**：通过杠杆得分考虑了 Token 之间的相互关系，而非仅仅是孤立的重要性。
*   **即插即用**：无需模型微调，计算开销低，易于集成到现有的推理流水线中。

#### 8. 不足与局限
*   **线性假设**：理论推导依赖于线性高斯假设，虽然实验证明有效，但可能无法完全捕捉 Transformer 内部高度非线性的动态特性。
*   **Query 依赖**：Query 权重的计算依赖于历史 Query 的统计量，如果生成过程中的 Query 分布发生剧烈偏移（Distribution Shift），效果可能会受限。
*   **超参数**：虽然 $\tau=5$ 在多数实验中表现良好，但在特定极端场景下可能仍需微调。

（完）
