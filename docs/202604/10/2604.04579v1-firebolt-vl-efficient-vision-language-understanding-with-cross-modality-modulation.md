---
title: "Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation"
title_zh: Firebolt-VL：基于跨模态调制的高效视觉-语言理解
authors: "Quoc-Huy Trinh, Mustapha Abdullahi, Bo Zhao, Debesh Jha"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04579v1"
tags: ["query:evlm"]
score: 9.0
evidence: 替换 Transformer 交叉注意力的高效视觉语言模型
tldr: 针对多模态大模型计算成本高及小模型细粒度理解能力弱的问题，本文提出Firebolt-VL。该模型采用Liquid Foundation Model (LFM) 替代传统Transformer解码器以实现线性推理效率，并引入Token-Grid相关性模块，通过状态空间模型和FiLM调节机制增强文本与图像区域的关联，从而在保持高效的同时提升了细粒度视觉推理性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04579v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison of the proposed Firebolt-VL model with existing MLLMs across seven benchmarks. The superscript p denotes the perception score on the MME benchmark, while SQAI refers the ScienceQA-IMG [30]. The best results are shown in bold, and the second-best results are underlined. “–” indicates results not reported in the original papers.\", \"page\": 6, \"index\": 1, \"width\": 1038, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.04579v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of responses from Firebolt-VL with recent efficient vision–language models, including MobileVLM V2 [10] and SmolVLM2 [31], on detail-dependent question-answering tasks. Firebolt-VL demonstrates stronger fine-grained grounding and more accurate, instruction-aligned responses.\", \"page\": 6, \"index\": 2, \"width\": 891, \"height\": 589}, {\"url\": \"assets/figures/arxiv/2604.04579v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Firebolt-VL architecture. The Cross-Modal Modulator (CMM) fuses textual instructions with the visual representations of the query image to produce conditioned tokens, which are then processed by the Liquid Foundation Model (LFM). The model is trained in two stages: (1) CMM pre-training to initialize modulation parameters, and (2) end-to-end training of the full framework.\", \"page\": 4, \"index\": 3, \"width\": 949, \"height\": 448}]"
motivation: 现有模型受限于Transformer的二次方复杂度且难以精准捕捉任务相关的细粒度视觉区域。
method: 采用LFM解码器并结合Token-Grid相关性模块，利用SSM和FiLM技术对视觉特征进行跨模态调制。
result: 在多个基准测试中，Firebolt-VL在显著提升推理效率的同时，实现了更准确的细粒度视觉理解。
conclusion: 该研究证明了结合线性时间解码器与轻量化跨模态调制机制是构建高效、高性能视觉语言模型的有效路径。
---

## 摘要
多模态大语言模型 (MLLMs) 的最新进展在视觉-语言理解方面取得了显著进步，但其高昂的计算成本限制了在个人助手、文档理解和智能摄像头等资源受限场景中的部署。大多数现有方法依赖于基于 Transformer 的交叉注意力机制，其二次方复杂度阻碍了效率。此外，小型视觉-语言模型通常难以精确捕捉细粒度的、与任务相关的视觉区域，导致在细粒度推理任务上的性能下降，限制了其在现实世界中的有效性。为了解决这些问题，我们提出了 Firebolt-VL，这是一种高效的视觉-语言模型，它使用液体基础模型 (LFM) 解码器取代了基于 Transformer 的解码器。为了进一步增强视觉定位能力，我们提出了一个 Token-Grid 相关性模块，该模块计算文本标记与图像块之间的轻量级相关性，并通过带有 FiLM 条件的状态空间模型进行调制。这使得模型能够在保持线性时间推理的同时，选择性地强调与文本提示相关的视觉区域。在多个基准测试上的实验结果表明，Firebolt-VL 在显著提高效率的同时，实现了准确的细粒度理解。我们的模型和代码可在 https://fireboltvl.github.io 获取。

## Abstract
Recent advances in multimodal large language models (MLLMs) have enabled impressive progress in vision-language understanding, yet their high computational cost limits deployment in resource-constrained scenarios such as personal assistants, document understanding, and smart cameras. Most existing methods rely on Transformer-based cross-attention, whose quadratic complexity hinders efficiency. Moreover, small vision-language models often struggle to precisely capture fine-grained, task-relevant visual regions, leading to degraded performance on fine-grained reasoning tasks that limit their effectiveness in the real world. To address these issues, we introduce Firebolt-VL, an efficient vision-language model that replaces the Transformer-based decoder with a Liquid Foundation Model (LFM) decoder. To further enhance visual grounding, we propose a Token-Grid Correlation Module, which computes lightweight correlations between text tokens and image patches and modulates via the state-space model with FiLM conditioning. This enables the model to selectively emphasize visual regions relevant to the textual prompt while maintaining linear-time inference. Experimental results across multiple benchmarks demonstrate that Firebolt-VL achieves accurate, fine-grained understanding with significantly improved efficiency. Our model and code are available at: https://fireboltvl.github.io

---

## 论文详细总结（自动生成）

以下是对论文《Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：多模态大语言模型（MLLM）在视觉问答、OCR等领域表现优异，但主流模型基于 Transformer 架构，其注意力机制具有**二次方计算复杂度**，导致在资源受限设备（如智能相机、个人助手）上部署困难。
*   **核心问题**：
    1.  **效率瓶颈**：Transformer 的推理延迟随序列长度增加而剧增。
    2.  **精度缺失**：小型视觉语言模型（VLM）往往难以精准定位与任务相关的细粒度视觉区域，导致在处理细节推理任务时表现不佳。
*   **整体含义**：本文旨在通过引入线性复杂度的解码器和高效的跨模态调制机制，构建一个既能保持高性能细粒度理解，又能实现极低推理延迟的轻量化 MLLM。

### 2. 方法论：核心思想与关键技术
Firebolt-VL 的架构由三个核心组件构成：
*   **Liquid Foundation Model (LFM) 解码器**：
    *   放弃了传统的 Transformer，采用基于“液体假设”（Liquid hypothesis）的 LFM2 作为语言骨干。
    *   LFM 具有**线性时间复杂度**，消除了 Transformer 的 KV 缓存负担，显著提升了长序列处理效率。
*   **跨模态调制器 (Cross-Modal Modulator, CMM)**：
    *   **Token-Grid 相关性计算**：计算文本 Token 与图像 Grid（来自 SigLIP 编码器）之间的点积相关性矩阵，并仅保留 Top-k 个最相关的视觉块，以减少噪声。
    *   **FiLM 调制**：利用特征线性调制（FiLM）技术，根据文本指令动态地对视觉特征进行缩放（Scale）和偏移（Shift）。
    *   **SSM 序列建模**：引入结构化状态空间模型（SSM，如 S4），在 O(T) 线性时间内捕捉视觉与文本融合后的隐藏状态，增强模型对空间几何关系的建模能力。
*   **训练流程**：分为两个阶段：(1) CMM 预训练（冻结视觉和语言模型，仅训练调制器）；(2) 全框架端到端微调（使用指令微调数据集）。

### 3. 实验设计
*   **数据集**：
    *   预训练：CC3M。
    *   指令微调：LLaVA-CoT、经过处理的 MMPR-v1.2（包含思维链格式）。
*   **基准测试 (Benchmarks)**：涵盖了 VQAv2、POPE（物体幻觉）、AI2D（图表理解）、MMMU（多学科推理）、MME（综合感知）、ScienceQA-IMG 和 MMBench。
*   **对比方法**：
    *   **大型模型 (>7B)**：IDEFICS、OpenFlamingo v2、MiniGPT-4、Chameleon、FastV。
    *   **小型模型 (<3B)**：Kosmos-2、MobileVLM (V1/V2)、MoE-LLaVA、LLaVA-Phi、SmolVLM2。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H100 (80GB) GPU**。
*   **训练参数**：
    *   第一阶段：Batch size 128，学习率 5e-4。
    *   第二阶段：Batch size 8，学习率 1e-4。
*   **时长**：每个阶段训练 5 个 Epoch，通常在第 2 个 Epoch 即可选出最佳模型（基于验证集 Perplexity）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个主流多模态基准上进行了定量对比，并提供了丰富的定性分析（Case Study）。
*   **消融实验**：
    *   对比了不同的 SSM 变体（Mamba vs S4D vs S4），发现 S4 结构化建模效果最好。
    *   对比了不同的连接器（Prepend vs Cross-attention vs CMM），证明了 CMM 的优越性。
    *   评估了 Top-k 视觉块选择的影响（k=1 到 5）。
*   **充分性评价**：实验设计较为全面，涵盖了感知、推理、效率（吞吐量与延迟）等多个维度，对比对象包含了从 0.3B 到 9B 的多种模型，评价客观且具有说服力。

### 6. 主要结论与发现
*   **性能卓越**：Firebolt-VL (0.8B) 在 MME 感知得分上达到了 1376.2，超过了许多 7B 甚至 9B 的模型。
*   **效率极高**：吞吐量达到 **46.67 tokens/s**，在同类轻量级模型中处于领先地位，且在准确率-延迟权衡（Accuracy-Latency trade-off）中表现最优。
*   **细粒度能力**：定性分析显示，CMM 模块使模型能精准定位图像中的微小细节（如搜索栏文字、食品标签），减少了小型模型常见的泛泛而谈现象。

### 7. 优点与亮点
*   **架构创新**：首次将 Liquid Foundation Model 与 SSM/FiLM 调制结合用于多模态任务，打破了 Transformer 的复杂度限制。
*   **轻量且精准**：通过 Top-k 机制和跨模态调制，在极小的参数量（0.8B）下实现了极强的视觉定位能力。
*   **推理友好**：线性复杂度的特性使其非常适合实时视频流处理或长文本多模态交互。

### 8. 不足与局限
*   **物体幻觉风险**：在 POPE 基准测试中表现略逊于部分模型，作者推测是因为 SSM 的隐式状态传播在抑制幻觉方面可能不如显式注意力机制有效。
*   **输入限制**：目前仅支持单图输入，尚未扩展到多图或原生视频序列（虽然理论上 SSM 适合处理视频）。
*   **参数规模单一**：目前仅展示了 0.8B 规模的模型，尚未验证该架构在大规模参数（如 7B+）下的扩展性（Scaling Law）。

（完）
