---
title: "Don't Waste Bits! Adaptive KV-Cache Quantization for Lightweight On-Device LLMs"
title_zh: 别浪费比特！面向轻量化端侧大语言模型的自适应 KV 缓存量化
authors: "Sayed Pedram Haeri Boroujeni, Niloufar Mehrabi, Patrick Woods, Gabriel Hillesheim, Abolfazl Razi"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04722v1"
tags: ["query:evlm"]
score: 10.0
evidence: 为轻量级端侧大模型提出自适应KV缓存量化方案
tldr: 针对移动端和边缘设备上大语言模型推理受限于KV缓存内存和带宽的问题，本文提出了一种自适应KV缓存量化方案。受哈夫曼编码启发，该方法通过轻量级控制器动态评估Token重要性，并为其分配不同的位宽（2/4/8-bit或FP16）。实验表明，该方法在SmolLM系列模型上显著降低了推理延迟并减少了内存占用，同时保持了接近全精度的模型准确率，为端侧LLM的高效部署提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04722v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 2048, \"height\": 1116}, {\"url\": \"assets/figures/arxiv/2604.04722v1/fig-002.webp\", \"caption\": \"\", \"page\": 1, \"index\": 2, \"width\": 2048, \"height\": 1569}]"
motivation: 现有的KV缓存量化方案多采用固定精度或手工启发式规则，导致重要Token被过度压缩或不重要Token浪费位宽，限制了端侧LLM的性能表现。
method: 提出一种基于学习的自适应策略，通过提取Token频率、注意力方差和信息熵等特征，利用紧凑控制器动态选择KV缓存的量化精度。
result: "在SmolLM-360M模型上，该方法相比静态量化降低了17.75%的解码延迟，准确率提升了7.60个百分点，且与FP16推理结果仅差0.30分。"
conclusion: 自适应KV缓存量化能有效优化端侧LLM的推理效率与精度权衡，是实现轻量化、高性能端侧部署的有效途径。
---

## 摘要
大语言模型（LLMs）在推理、生成和决策任务中取得了显著进展，但在移动、嵌入式和边缘设备上部署它们仍然极具挑战性。端侧 LLM 推理受到键值（KV）缓存的内存和带宽开销的严重限制，该开销随上下文长度线性增长，且通常在解码成本中占主导地位。现有的 KV 缓存量化方案通常依赖于固定精度或手工设计的启发式方法，从而在低影响的 token 上浪费比特，同时对信息量大的 token 过度压缩，导致了本可避免的精度下降。受哈夫曼编码（Huffman coding）变长分配原则的启发，我们提出了自适应 KV 缓存量化，这是一种学习策略，根据 token 的重要性按比例分配位宽，在不牺牲竞争性精度的前提下最小化预期的内存和延迟。我们的框架提取轻量级的 token 级特征，包括 token 频率、质量得分、注意力方差和基于熵的不确定性，并将它们输入到一个紧凑的数据驱动控制器中，该控制器在解码过程中从 {2-bit, 4-bit, 8-bit, FP16} 中动态选择 KV 精度。与静态 KV 量化和基于规则的基准相比，这种自适应精度策略减少了 KV 内存占用和延迟，同时提高了准确性，并在标准 LLM 基准测试中保持了接近 FP16 推理的竞争性精度。在 SmolLM-135M、SmolLM-360M 和 SmolLM-1.7B 上进行的多个常识推理基准测试的大量实验表明，我们的控制器持续改进了准确性与延迟之间的权衡。例如，在 HellaSwag 基准上使用 SmolLM-360M 时，我们的方法相对于静态 KV 量化将解码延迟（ms/token）降低了 17.75%，准确率提高了 7.60 个百分点，且与 FP16 推理的差距仅在 0.30 个百分点以内。

## Abstract
Large Language Models (LLMs) have achieved remarkable progress across reasoning, generation, and decision-making tasks, yet deploying them on mobile, embedded, and edge devices remains particularly challenging. On-device LLM inference is heavily constrained by the memory and bandwidth overhead of the key-value (KV) cache, which grows linearly with context length and often dominates decoding cost. Existing KV-cache quantization schemes typically rely on fixed precision or hand-crafted heuristics, thereby wasting bits on low-impact tokens while over-compressing informative ones, leading to avoidable accuracy degradation. Inspired by Huffman coding's principle of variable-length allocation, we propose adaptive KV-cache quantization, a learned policy that assigns bit-width proportional to token importance, minimizing expected memory and latency without sacrificing competitive accuracy. Our framework extracts lightweight token-level features, including token frequency, quality score, attention variance, and entropy-based uncertainty, and feeds them into a compact data-driven controller that dynamically selects KV precision from {2-bit, 4-bit, 8-bit, FP16} during decoding. This adaptive precision policy reduces KV memory footprint and latency while improving accuracy compared to static KV quantization and rule-based baselines, and maintaining competitive accuracy close to FP16 inference across standard LLM benchmarks. Extensive experiments across multiple commonsense reasoning benchmarks using SmolLM-135M, SmolLM-360M, and SmolLM-1.7B demonstrate that our controller consistently improves the accuracy-latency trade-off. For instance, with SmolLM-360M on HellaSwag, our method reduces decoding latency (ms/token) by 17.75% relative to static KV quantization, improves accuracy by 7.60 points, and remains within only 0.30 points of FP16 inference.

---

## 论文详细总结（自动生成）

以下是对论文《Don't Waste Bits! Adaptive KV-Cache Quantization for Lightweight On-Device LLMs》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在自回归解码过程中，KV 缓存（Key-Value Cache）会随上下文长度线性增长，成为端侧设备（如手机、嵌入式设备）推理时的内存和带宽瓶颈。
*   **核心问题**：现有的 KV 缓存量化方法大多采用**静态精度**（所有 Token 使用相同位宽）或**手工启发式规则**。这导致不重要的 Token 浪费了存储比特，而关键 Token 却因过度压缩而损失精度，限制了模型在资源受限环境下的推理表现。
*   **研究动机**：受哈夫曼编码（Huffman Coding）变长分配原则的启发，作者认为应将精度视为有限资源，根据 Token 的重要性动态分配位宽，实现“按需分配”。

### 2. 方法论
*   **核心思想**：引入一个数据驱动的轻量级控制器，在解码阶段实时预测每个 Token 的重要性，并从 `{2, 4, 8, 16}` 比特中动态选择最适合的 KV 存储精度。
*   **关键技术细节**：
    *   **特征提取（Token-level Saliency Features）**：提取四个轻量级信号作为输入：
        1.  **信息熵（Entropy）**：反映预测分布的不确定性。
        2.  **稀有度（Rarity）**：基于 Token 频率的自信息得分。
        3.  **注意力方差（Attention Variance）**：衡量 Token 在结构上的影响力。
        4.  **置信度（Confidence）**：模型对当前预测的确定程度。
    *   **控制器设计**：采用一个极简的 3 层多层感知机（MLP），隐藏层维度仅为 128。它将 4 维特征映射为 4 个精度等级的概率分布。
    *   **训练目标**：设计了一个复合损失函数 $L = \alpha L_{ce} + \beta L_{lat} + \gamma L_{qual}$，同时优化分类准确性、预期延迟（Efficiency）和预期生成质量（Quality）。
    *   **自适应量化流程**：在解码每一步，控制器决定当前 Token 的位宽，随后对 K 和 V 张量进行量化处理并存入缓存，供后续步骤调用。

### 3. 实验设计
*   **数据集/场景**：
    *   **HellaSwag**：常识推理任务。
    *   **OpenBookQA (OBQA)**：科学问答任务。
    *   **ARC-Challenge (ARC-C)**：挑战性科学推理任务。
*   **Benchmark 模型**：使用了 HuggingFace 的轻量级模型系列 **SmolLM**（包含 135M、360M 和 1.7B 三种规模）。
*   **对比方法**：
    *   **FP16**：全精度推理（性能上限）。
    *   **Static 4-bit KV**：统一使用 4 比特量化的静态基准。
    *   **Rule-Based Dynamic KV**：基于手工启发式规则的动态量化方法。
    *   **横向对比**：与 OPT、Pythia、Galactica、Cerebras-GPT 等同量级模型在标准基准上的表现进行对比。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 4090 GPU (24 GB)** 的工作站上完成。
*   **训练细节**：针对不同规模的模型设置了不同的学习率和 Batch Size（如 1.7B 模型使用 2M BS）。虽然文中未明确给出控制器的具体训练时长，但由于控制器仅为极小的 MLP 且特征维度极低，其训练和推理开销在 LLM 整体计算中几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 135M 到 1.7B 的三个模型量级，并在三个主流推理数据集上进行了测试。
*   **充分性评价**：实验设计较为充分。作者不仅对比了不同量化策略，还通过横向对比证明了 SmolLM 在搭载该方法后，其综合性能（精度/延迟比）优于许多同参数规模的知名模型。
*   **客观性**：采用了零样本（Zero-shot）评测设置，并提供了详细的延迟（ms/token）与准确率对比数据，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **精度-延迟权衡优化**：该方法在显著降低延迟的同时，保持了极高的准确率。例如，SmolLM-360M 在 HellaSwag 上比静态 4-bit 量化准确率提升了 **7.60%**，延迟降低了 **17.75%**。
*   **逼近全精度表现**：在多个任务中，自适应量化的准确率与 FP16 相比差距极小（通常在 0.3-1.0% 以内），但内存占用大幅减少。
*   **端侧适用性**：实验证明该框架能有效解决端侧设备因内存带宽受限导致的推理缓慢问题。

### 7. 优点
*   **理论与实践结合**：将信息论中的哈夫曼编码思想巧妙应用于 KV 缓存管理，逻辑自洽。
*   **极低开销**：控制器极其轻量，特征提取均来自模型原生输出，无需昂贵的辅助计算。
*   **非侵入性**：不需要对 Transformer 核心架构进行修改，易于集成到现有的推理引擎中。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 2B 以下的轻量级模型，尚未验证该方法在 7B、70B 等大规模模型上的表现及控制器的泛化能力。
*   **硬件底层实现**：论文主要讨论了算法层面的延迟减少，但在实际底层硬件（如 NPU 或特定 DSP）上，处理异构位宽（混合 2/4/8-bit）的内存对齐和并行计算可能存在工程实现上的挑战。
*   **特征依赖性**：特征提取依赖于 Softmax 后的完整分布，在某些极致优化的推理框架中，获取这些中间特征可能会产生额外的同步开销。

（完）
