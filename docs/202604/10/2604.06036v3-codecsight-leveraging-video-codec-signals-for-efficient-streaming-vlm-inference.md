---
title: "CodecSight: Leveraging Video Codec Signals for Efficient Streaming VLM Inference"
title_zh: CodecSight：利用视频编解码信号实现高效流式 VLM 推理
authors: "Yulin Zou, Yan Chen, Wenyan Chen, JooYoung Park, Shivaraman Nitin, Luo Tao, Francisco Romero, Dmitrii Ustiugov"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06036v3"
tags: ["query:evlm"]
score: 10.0
evidence: 利用时空冗余实现高效的流式视觉语言模型推理
tldr: CodecSight 针对视频流视觉语言模型（VLM）推理的高成本问题，提出利用视频编解码器（Codec）元数据作为低成本信号。该系统通过编解码器引导的补丁剪枝和选择性 KV 缓存刷新，实现了视频解码、视觉处理及 LLM 预填充的端到端优化。它无需离线训练，在保持高精度的同时显著提升了吞吐量并降低了计算开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06036v3/fig-001.webp\", \"caption\": \"Figure 8. System architecture overview of CodecSight.\", \"page\": 6, \"index\": 1, \"width\": 501, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.06036v3/fig-002.webp\", \"caption\": \"Figure 9. Illustration of the motion-guided token pruning policy. Each (2 × 2) group of neighboring patches is projected into a vision token. A token is retained if any of its constituent patches are marked dynamic, otherwise it will be discarded.\", \"page\": 7, \"index\": 2, \"width\": 502, \"height\": 266}]"
motivation: 现有的视频 VLM 推理优化方法往往只针对单一组件且计算开销大，难以应对动态实时流的效率挑战。
method: 利用视频压缩过程中产生的编解码信号，在 ViT 编码前进行补丁剪枝，并在 LLM 预填充阶段实施选择性 KV 缓存刷新。
result: "实验表明，CodecSight 相比现有基准可提升高达 3 倍的吞吐量，减少 87% 的 GPU 计算量，且 F1 分数仅下降 0-8%。"
conclusion: CodecSight 证明了直接利用编解码器元数据是实现高效、实时且端到端视频流 VLM 推理的有效途径。
---

## 摘要
视频流分析是视觉语言模型（VLM）服务中的关键工作负载，但多模态推理的高昂成本限制了其可扩展性。现有系统通过利用视频流中的时空冗余来降低推理成本，但它们往往仅针对视觉 Transformer (ViT) 或大语言模型 (LLM) 进行局部优化，未能充分挖掘端到端的优化潜力。此外，现有方法在识别冗余时会产生显著开销，无论是通过离线分析和训练，还是通过高昂的在线计算，这使得它们难以适应动态实时流。我们提出了 CodecSight，这是一个由编解码器引导的流式视频分析系统。该系统基于一个关键观察：视频编解码器在压缩过程中已经作为副产品提取了每条视频流的时空结构。CodecSight 将这些编解码元数据视为低成本的运行时信号，以统一优化视频解码、视觉处理和 LLM 预填充，而直接在压缩比特流上操作也带来了传输量减少的固有优势。这驱动了 ViT 编码前的编解码引导补丁剪枝以及 LLM 预填充期间的选择性键值缓存刷新，两者均为完全在线运行且无需离线训练。实验表明，与最先进的基准方法相比，CodecSight 的吞吐量提升了高达 3 倍，GPU 计算量减少了高达 87%，同时保持了具有竞争力的准确率，F1 分数仅下降 0~8%。

## Abstract
Video streaming analytics is a crucial workload for vision-language model serving, but the high cost of multimodal inference limits scalability. Prior systems reduce inference cost by exploiting temporal and spatial redundancy in video streams, but they target either the vision transformer (ViT) or the LLM with a limited view, leaving end-to-end opportunities untapped. Moreover, existing methods incur significant overhead to identify redundancy, either through offline profiling and training or costly online computation, making them ill-suited for dynamic real-time streams.   We present CodecSight, a codec-guided streaming video analytics system, built on a key observation that video codecs already extract the temporal and spatial structure of each stream as a byproduct of compression. CodecSight treats this codec metadata as a low-cost runtime signal to unify optimization across video decoding, visual processing, and LLM prefilling, with transmission reduction as an inherent benefit of operating directly on compressed bitstreams. This drives codec-guided patch pruning before ViT encoding and selective key-value cache refresh during LLM prefilling, both of which are fully online and do not require offline training. Experiments show that CodecSight achieves an improvement in throughput of up to 3$\times$, and a reduction of up to 87% in GPU compute over state-of-the-art baselines, maintaining competitive accuracy with only 0$\sim$8% F1 drop.

---

## 论文详细总结（自动生成）

这是一份关于论文《CodecSight: Leveraging Video Codec Signals for Efficient Streaming VLM Inference》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景：** 随着监控、工业自动化等领域视频传感器的爆炸式增长，使用视觉语言模型（VLM）进行实时视频流分析的需求激增。
*   **核心问题：** VLM 推理成本极高，尤其是处理连续视频流时，滑动窗口机制导致了大量的重复计算。
*   **痛点：** 
    1.  **现有优化碎片化：** 现有系统要么只优化视觉编码器（ViT），要么只优化语言模型（LLM），缺乏端到端的协同。
    2.  **识别冗余开销大：** 现有的冗余识别方法通常需要昂贵的离线训练或高成本的在线计算（如计算光流或令牌重要性），难以应对动态实时流。
*   **研究动机：** 视频编解码器（Codec）在压缩过程中已经提取了时空结构（如运动矢量和残差），这些是“免费”的运行时信号，可以用来指导 VLM 的高效推理。

### 2. 方法论
CodecSight 的核心思想是将编解码器元数据作为低成本信号，统一优化视频分析的全生命周期。
*   **硬件加速编解码处理：** 利用 GPU 硬件解码器（如 NVIDIA NVDEC）进行单次解码并缓冲，消除滑动窗口重叠部分的重复解码，并直接提取运动矢量（MV）。
*   **运动矢量引导的补丁剪枝（Patch Pruning）：**
    *   **原理：** 将编解码器中的块级运动矢量映射到 ViT 的补丁（Patch）网格上。
    *   **逻辑：** 计算补丁的运动强度 $M_t(i) = V_t(i) + \alpha R_t(i)$（$V$ 为运动矢量，$R$ 为残差）。若强度低于阈值，则视为静态区域并剪枝，仅对动态区域进行 ViT 编码。
    *   **策略：** 在一个 GOP（图像组）内累积动态掩码，确保时间一致性。
*   **选择性 KV 缓存刷新（Selective KVC Refresh）：**
    *   **原理：** 针对滑动窗口的重叠部分，不进行全量重计算，也不盲目重用。
    *   **逻辑：** 将 I 帧（关键帧）生成的令牌视为“锚点”，在窗口滑动后强制刷新其 KV 状态以对齐新上下文；对于 P 帧令牌，则直接重用缓存。
    *   **位置校正：** 使用基于 RoPE（旋转位置嵌入）的公式对重用的 Key 状态进行位置偏移修正，确保语义一致性。

### 3. 实验设计
*   **数据集：** 使用 **UCF-Crime** 数据集，包含 1900 个真实世界的长视频，涵盖多种异常场景和运动特征。
*   **模型（Backbones）：** 
    *   **InternVL3-14B** (ViT 300M + Qwen2.5-14B)
    *   **Qwen3-VL-32B** (ViT 600M + Qwen3-32B)
*   **Benchmark 与对比方法：**
    *   **Full Comp：** 无优化的标准 vLLM 推理。
    *   **Déjà Vu：** 针对 ViT 补丁重用的优化方法。
    *   **CacheBlend：** 针对 RAG 场景的 KV 缓存融合方法。
    *   **VLCache：** 针对多模态输入的层级缓存重用框架。

### 4. 资源与算力
*   **硬件环境：** 4 张 **NVIDIA A100 (40GB, SXM4)** GPU，配备 AMD EPYC 7713 64 核 CPU。
*   **并行策略：** InternVL3 使用张量并行（TP=2），Qwen3-VL 使用 TP=4。
*   **训练时长：** **无需训练**。该方法是完全的推理侧优化（Training-free），直接在预训练模型上运行。

### 5. 实验数量与充分性
*   **实验维度：** 
    *   **端到端性能：** 测量了不同阶段（传输、解码、预处理、ViT、LLM）的延迟。
    *   **准确率评估：** 测量了 Precision、Recall 和 F1 分数。
    *   **资源分析：** 统计了令牌减少比例和 FLOPs 节省量。
    *   **消融实验：** 分别验证了补丁剪枝和 KV 刷新的贡献。
    *   **敏感性分析：** 探讨了步长比例、运动阈值、GOP 大小对结果的影响。
*   **充分性评价：** 实验设计非常充分。作者不仅在不同规模的模型上进行了验证，还针对不同运动强度（低、中、高）的视频进行了分类讨论，证明了系统在各种动态环境下的鲁棒性。

### 6. 主要结论与发现
*   **性能提升：** 相比基准方法，端到端吞吐量提升高达 **3 倍**。
*   **计算节省：** GPU 计算量（FLOPs）减少了高达 **87%**，令牌处理量显著下降。
*   **准确率保持：** 在大幅提速的同时，F1 分数仅下降 **0~8%**，在 Qwen3-VL 上甚至实现了零精度损失。
*   **低开销：** 优化逻辑本身的运行时开销极低，仅占总延迟的约 **4%**。

### 7. 优点
*   **端到端协同：** 首次将视频编解码信号贯穿到从传输、解码到 ViT 和 LLM 的全流程优化中。
*   **无需训练：** 具有极强的即插即用性，能够快速部署到不同的 VLM 架构中。
*   **硬件友好：** 充分利用了现代 GPU 闲置的硬件解码单元，将预处理压力从 CPU 转移到 GPU。
*   **动态适应：** 能够根据视频内容的实际运动强度自动调整计算量。

### 8. 不足与局限
*   **场景局限性：** 实验主要集中在监控类视频（背景相对固定），对于背景剧烈晃动（如手持摄影）或镜头频繁切换的视频，补丁剪枝的效果可能会打折扣。
*   **硬件依赖：** 目前的实现深度依赖 NVIDIA 的 NVDEC 接口，在其他硬件平台（如 AMD 或国产 AI 芯片）上的移植可能需要额外开发。
*   **精度权衡：** 虽然平均 F1 下降较小，但在某些对极微小动作敏感的检测任务中，剪枝可能会导致关键细节丢失。

（完）
