---
title: Does a Global Perspective Help Prune Sparse MoEs Elegantly?
title_zh: 全局视角是否能帮助优雅地剪枝稀疏混合专家模型？
authors: "Zeliang Zhang, Nikhil Ghosh, Jiani Liu, Bin Yu, Xiaodong Liu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06542v1"
tags: ["query:evlm"]
score: 9.0
evidence: 针对稀疏混合专家语言模型的全局剪枝策略
tldr: 针对稀疏混合专家模型（MoE）因专家参数量巨大而导致的内存占用问题，本文提出了GRAPE（全局冗余感知专家剪枝）。该方法打破了传统剪枝在各层均匀分配预算的局限，通过全局视角动态评估并分配跨层的剪枝预算，以应对MoE模型中异构的冗余分布。在Mixtral、DeepSeek等多个主流模型上的实验表明，GRAPE在相同剪枝比例下显著优于现有局部剪枝方法，有效平衡了模型规模与性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06542v1/fig-001.webp\", \"caption\": \"Table 1: Accuracy (%) on pruning Mixtral-8x22B, and DeepSeek-MoE with 2 and 4 experts per MoE layer. Each cell reports accuracy in the format 2e/4e, where we denote e as the number of experts to prune in each layer.\", \"page\": 4, \"index\": 1, \"width\": 950, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.06542v1/fig-002.webp\", \"caption\": \"Table 2: Accuracy (%) on pruning GPT-OSS with 2 and 4 experts per MoE layer. Each cell reports accuracy in the format 2e/4e, where we denote e as the number of experts to prune in each layer.\", \"page\": 5, \"index\": 2, \"width\": 943, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2604.06542v1/fig-003.webp\", \"caption\": \"Figure 2: Results of Mixtral-8x7B and Qwen-MoE.\", \"page\": 7, \"index\": 3, \"width\": 950, \"height\": 293}]"
motivation: 现有的MoE剪枝方法通常在各层间均匀分配预算，忽略了不同层专家之间存在的异构冗余特性。
method: 提出GRAPE策略，通过全局视角动态评估跨层冗余，并据此为各层分配差异化的专家剪枝预算。
result: "在Mixtral、DeepSeek-MoE等模型上的实验显示，GRAPE比最强局部基准平均准确率提升1.40%，最高提升达2.45%。"
conclusion: 采用全局冗余感知的动态预算分配策略能更优雅且高效地压缩稀疏MoE模型，显著优于传统的局部剪枝方法。
---

## 摘要
语言模型的经验性缩放法则促使了超大规模大语言模型（LLM）的发展，尽管其计算和内存成本不断增加。稀疏混合专家模型（MoEs）通过在每次前向传播中仅激活部分专家，提供了一种极具前景的替代方案，在不牺牲性能的前提下提升了效率。然而，庞大的专家参数量仍会导致显著的内存消耗。现有的剪枝方法通常在各层之间均匀分配预算，忽略了稀疏 MoE 中存在的异构冗余。我们提出了 GRAPE（全局冗余感知专家剪枝），这是一种根据跨层冗余动态分配剪枝预算的全局剪枝策略。在 Mixtral-8x7B、Mixtral-8x22B、DeepSeek-MoE、Qwen-MoE 和 GPT-OSS 上的实验表明，在相同的剪枝预算下，GRAPE 始终能取得最佳的平均性能。在论文报告的三个主要模型中，GRAPE 在各种剪枝设置下比最强的局部基准方法平均准确率提升了 1.40%，最高提升达 2.45%。

## Abstract
Empirical scaling laws for language models have encouraged the development of ever-larger LLMs, despite their growing computational and memory costs. Sparse Mixture-of-Experts (MoEs) offer a promising alternative by activating only a subset of experts per forward pass, improving efficiency without sacrificing performance. However, the large number of expert parameters still leads to substantial memory consumption.   Existing pruning methods typically allocate budgets uniformly across layers, overlooking the heterogeneous redundancy that arises in sparse MoEs. We propose GRAPE (Global Redundancy-Aware Pruning of Experts, a global pruning strategy that dynamically allocates pruning budgets based on cross-layer redundancy. Experiments on Mixtral-8x7B, Mixtral-8x22B, DeepSeek-MoE, Qwen-MoE, and GPT-OSS show that, under the same pruning budget, GRAPE consistently achieves the best average performance. On the three main models reported in the paper, it improves average accuracy over the strongest local baseline by 1.40% on average across pruning settings, with gains of up to 2.45%.

---

## 论文详细总结（自动生成）

这是一份关于论文《Does a Global Perspective Help Prune Sparse MoEs Elegantly?》的结构化深度总结：

### 1. 核心问题与研究动机
*   **背景**：稀疏混合专家模型（MoE）通过仅激活部分专家来提升推理效率，但其庞大的参数总量带来了巨大的内存压力。
*   **核心问题**：现有的 MoE 剪枝方法（如基于访问频率、路由权重或特征相似度的方法）通常采用**局部均匀分配**策略，即在每一层剪掉相同数量的专家。
*   **动机**：作者观察到 MoE 模型不同层之间的专家冗余度存在显著差异（异构性）。例如，早期层通常冗余度较低，而后期层冗余度较高。因此，统一的剪枝预算分配是不合理的，需要一种从全局视角出发的动态分配策略。

### 2. 方法论：GRAPE (Global Redundancy-Aware Pruning of Experts)
*   **核心思想**：根据各层的冗余程度动态分配剪枝预算，在保持总专家数不变的前提下，冗余度高的层多剪，冗余度低的层少剪。
*   **关键技术细节**：
    *   **冗余度度量**：利用中心核对齐（CKA）或其他相似度度量构建每层的专家相似度矩阵 $D_l$，计算层内平均冗余得分 $R_l$。
    *   **全局熵正则化**：为了防止剪枝过度集中在某些极高冗余的层（导致模型崩溃），引入了“全局熵” $E$。熵值越大表示保留的专家在各层分布越均匀。
    *   **算法流程**：
        1.  初始化各层专家簇，设定全局熵阈值。
        2.  **贪婪合并**：在所有未冻结的层中，选择冗余得分最高的层，合并该层中最相似的一对专家。
        3.  **熵控冻结**：合并后重新计算全局熵，若熵值低于阈值，则冻结该层，防止进一步剪枝。
        4.  **重启机制**：若所有层都被冻结但尚未达到目标预算，则重置冻结状态继续剪枝，确保达到压缩目标。

### 3. 实验设计
*   **实验模型**：Mixtral-8x7B、Mixtral-8x22B、DeepSeek-MoE-16B、Qwen-MoE、GPT-OSS（20B）。
*   **评估任务**：MMLU（多任务理解）、BoolQ（布尔问答）、OpenBookQA、RTE（蕴含识别）等。
*   **基准方法 (Baselines)**：
    *   **Router-guided**：基于路由策略的合并。
    *   **Count-guided (Expert Trimming)**：基于访问频率的剪枝。
    *   **Enumerate**：基于损失景观搜索的剪枝。
    *   **DEK**：基于输出表示多样性的局部剪枝。
*   **剪枝设置**：分别测试了每层平均剪掉 2 个专家（2e）和 4 个专家（4e）的场景。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及具体的运行耗时。
*   **实现细节**：由于该方法属于“一键式（One-shot）”剪枝且禁用了微调（Task-agnostic setting），其计算开销主要集中在计算专家相似度矩阵上，相比需要重新训练或大规模微调的方法，算力需求较低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种主流 MoE 架构，并在多个标准 NLP 任务上进行了验证。
*   **充分性**：实验对比了四种不同类型的先进局部剪枝基准，涵盖了从 7B 到 120B 不同规模的模型。
*   **客观性**：通过在相同总预算下对比“局部均匀”与“全局动态”策略，直接证明了全局视角的优越性。附录中还补充了 Mixtral-8x7B 和 Qwen-MoE 的保留性能分析，进一步增强了结论的普适性。

### 6. 主要结论与发现
*   **全局视角更优**：GRAPE 在所有测试模型和预算下均优于局部基准。在 Mixtral-8x22B 上，平均准确率比最强局部基准高出 1.79% - 2.45%。
*   **层间冗余异构性**：实验证实了 MoE 模型中不同层的冗余度确实不同，通常后层比前层更冗余，但并非严格单调。
*   **稳定性**：在大幅度剪枝（如 4e 设置）时，局部剪枝往往导致性能骤降，而 GRAPE 通过全局优化能更好地保留核心能力。

### 7. 优点
*   **策略优雅**：打破了“每层必剪”的思维定式，通过简单的熵控制实现了复杂的跨层预算分配。
*   **通用性强**：作为一种任务无关（Task-agnostic）的方法，不需要下游任务微调，易于部署到各种 MoE 架构中。
*   **性能提升显著**：在不增加额外推理开销的前提下，仅通过优化剪枝位置就获得了明显的精度增益。

### 8. 不足与局限
*   **模型崩溃风险**：在 DeepSeek-MoE 等极端异构的模型上，如果熵控制不当，全局剪枝仍可能导致某些层被过度削弱。
*   **度量指标依赖**：方法的有效性高度依赖于相似度度量（如 CKA）的准确性，论文提到未来需要研究更鲁棒的冗余评估指标。
*   **缺乏微调对比**：实验主要在无微调设置下进行，未探讨在剪枝后进行恢复性微调时，全局策略是否依然保持大幅领先。

（完）
