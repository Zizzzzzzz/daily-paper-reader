---
title: "QAPruner: Quantization-Aware Vision Token Pruning for Multimodal Large Language Models"
title_zh: QAPruner：面向多模态大语言模型的量化感知视觉 Token 剪枝
authors: "Xinhao Wang, Zhonyu Xia, Zhiwei Lin, Zhe Li, Yongtao Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02816v1"
tags: ["query:evlm"]
score: 10.0
evidence: 针对多模态大语言模型的量化感知视觉标记剪枝
tldr: 针对多模态大语言模型（MLLM）部署成本高的问题，本文提出了QAPruner框架。研究发现传统的视觉Token剪枝会移除对量化稳定性至关重要的激活离群值，导致低比特量化（如W4A4）性能下降。QAPruner通过引入结合量化误差、离群值强度和语义相关性的混合敏感度指标，实现了量化感知的Token剪枝。实验证明，该方法在极高剪枝率下仍能保持高精度，是首个协同优化剪枝与量化的MLLM推理加速方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02816v1/fig-001.webp\", \"caption\": \"Table 2. Ablation study about methods to calculate quantization sensitivity\", \"page\": 10, \"index\": 1, \"width\": 562, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.02816v1/fig-002.webp\", \"caption\": \"Fig. 3. Normalized accuracy retention versus retained visual-token ratio for LLaVA7B and LLaVA-13B under W4A4 PTQ. Each curve is normalized by the dense W4A4 baseline of its model. As the token budget shrinks, semantic-only pruning degrades more noticeably, while our method remains consistently closer to, and often above, the dense PTQ baseline.\", \"page\": 10, \"index\": 2, \"width\": 691, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2604.02816v1/fig-003.webp\", \"caption\": \"Table 1. Comparisons with quantization methods and vision tokens pruning methods for LLaVA-v1.3 and LLaVA-v1.5 7B models on the ScienceQA dataset. Question classes: NAT = natural science, SOC = social science, LAN = language science, TXT = text context, IMG = image context, NO = no context.\", \"page\": 9, \"index\": 3, \"width\": 725, \"height\": 465}, {\"url\": \"assets/figures/arxiv/2604.02816v1/fig-004.webp\", \"caption\": \"Fig. 1. Motivation teaser of quantization-aware vision token pruning on a real ScienceQA sample. Panel (a) shows the input image. Panel (b) shows the token-level outlier scores, with darker red cells indicating greater quantization sensitivity. Panel (c) shows the tokens kept by semantic-only pruning, which misses the highest-scoring outlier token and leads the quantized model to predict Rhode Island. Panel (d) shows our selection, which preserves that token and recovers the correct answer South Carolina.\", \"page\": 3, \"index\": 4, \"width\": 698, \"height\": 367}]"
motivation: 传统的视觉Token剪枝会误删对量化稳定性至关重要的激活离群值，导致低比特量化下的MLLM性能大幅下降。
method: 提出一种量化感知的剪枝框架，利用结合了模拟量化误差、离群值强度和语义相关性的混合敏感度指标来筛选Token。
result: "在LLaVA架构上，仅保留12.5%视觉Token的情况下，准确率比基线提升2.24%，甚至优于无剪枝的密集量化结果。"
conclusion: 本文证明了剪枝与量化的强耦合性，并通过协同优化成功实现了高效且准确的低比特MLLM推理。
---

## 摘要
多模态大语言模型 (MLLMs) 展现出强大的推理能力，但其高昂的计算和内存开销阻碍了在资源受限环境下的部署。虽然训练后量化 (PTQ) 和视觉 Token 剪枝是标准的压缩技术，但它们通常被视为独立的优化手段。在本文中，我们证明了这两种技术是强耦合的：在经过 PTQ 优化的 MLLM 上简单地应用基于语义的 Token 剪枝，可能会丢弃对数值稳定性至关重要的激活离群值 (outliers)，从而加剧低比特方案（例如 W4A4）下的量化误差。为了解决这一问题，我们提出了一个量化感知的视觉 Token 剪枝框架。该方法引入了一种轻量级的混合敏感度指标，结合了模拟的分组量化误差与离群值强度。通过将此指标与标准的语义相关性分数相结合，该方法能够保留既具有语义信息量又对量化具有鲁棒性的 Token。在标准 LLaVA 架构上的实验表明，我们的方法始终优于简单的集成基准。在仅保留 12.5% 视觉 Token 的激进剪枝比例下，我们的框架比基准模型提高了 2.24% 的准确率，甚至超过了不进行剪枝的密集量化模型。据我们所知，这是首个显式协同优化视觉 Token 剪枝和 PTQ 以实现精确低比特 MLLM 推理的方法。

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong reasoning ability, but their high computational and memory costs hinder deployment in resource-constrained settings. While Post-Training Quantization (PTQ) and vision token pruning are standard compression techniques, they are usually treated as independent optimizations. In this paper, we show that these two techniques are strongly coupled: naively applying semantic-based token pruning to PTQ-optimized MLLMs can discard activation outliers that are important for numerical stability and thus worsen quantization errors in low-bit regimes (\textit{e.g.}, W4A4). To address this issue, we propose a quantization-aware vision token pruning framework. Our method introduces a lightweight hybrid sensitivity metric that combines simulated group-wise quantization error with outlier intensity. By combining this metric with standard semantic relevance scores, the method retains tokens that are both semantically informative and robust to quantization. Experiments on standard LLaVA architectures show that our method consistently outperforms naive integration baselines. At an aggressive pruning ratio that retains only 12.5\% of visual tokens, our framework improves accuracy by 2.24\% over the baseline and even surpasses dense quantization without pruning. To the best of our knowledge, this is the first method that explicitly co-optimizes vision token pruning and PTQ for accurate low-bit MLLM inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《QAPruner: Quantization-Aware Vision Token Pruning for Multimodal Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：多模态大语言模型（MLLM）虽然性能强大，但其庞大的视觉 Token 序列和模型参数量导致了极高的计算与内存开销，难以在边缘设备部署。
*   **核心问题**：目前主流的压缩手段包括**训练后量化（PTQ）**和**视觉 Token 剪枝**，但两者通常被独立研究。
*   **关键发现**：论文指出这两者存在**强耦合性**。传统的剪枝方法仅关注“语义相关性”，往往会误删包含“激活离群值（Outliers）”的 Token。这些离群值虽然语义贡献可能较小，但对维持低比特量化（如 W4A4）的数值稳定性至关重要。一旦删除，量化误差会剧增，导致模型精度大幅崩塌。

### 2. 方法论：核心思想与关键技术
QAPruner 提出了一个**量化感知的视觉 Token 剪枝框架**，其核心思想是在筛选 Token 时同时考虑语义重要性和量化敏感度。
*   **混合量化敏感度指标 ($S_Q$)**：由两个子指标融合而成：
    1.  **分组量化模拟误差 ($E$)**：模拟实际部署中的分组量化过程，计算每个 Token 在量化前后的 $L_2$ 重建残差，识别局部难以量化的 Token。
    2.  **全局离群值强度 ($R$)**：计算 Token 在所有通道上的动态范围（最大值减最小值），识别包含极端离群值的 Token，以保护模型的数值结构。
*   **分数融合与筛选**：
    *   将上述量化敏感度 $S_Q$ 与传统的语义相关性分数 $S_P$（如基于注意力或相似度的分数）进行加权融合：$S_{Final} = \alpha S_P + (1 - \alpha) S_Q$。
    *   根据最终得分 $S_{Final}$ 选取 Top-K 个 Token 进入后续的 LLM 推理。
*   **特性**：该方法是 **Training-free（无需训练）** 的，能够直接应用于现有的预训练 MLLM。

### 3. 实验设计
*   **模型架构**：选择了代表性的 LLaVA 系列模型，包括 LLaVA-1.3 (7B/13B) 和 LLaVA-1.5 (7B)。
*   **数据集**：主要在 **ScienceQA** 基准测试上进行评估，该任务涉及复杂的科学图表推理，对视觉细节敏感。
*   **量化配置**：采用 **W4A4**（4比特权重、4比特激活）的极低比特配置，这是目前 PTQ 的挑战性场景。
*   **对比基准 (Baselines)**：
    *   FP（全精度模型）。
    *   Dense W4A4（仅量化，不剪枝）。
    *   Naive Pruning（直接在量化模型上应用传统的语义剪枝方法，如 CDPruner）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及具体的推理/校准时长。
*   **算法开销**：由于是 PTQ 和启发式剪枝，且敏感度计算属于轻量级操作，预计其计算开销远低于模型本身的推理开销，且无需昂贵的微调过程。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同规模和版本的模型（7B 到 13B）。
    *   测试了多种剪枝比例（如保留 128 Token、32 Token 等，对应 50% 到 12.5% 的保留率）。
    *   进行了详细的**消融实验**，对比了 $L_\infty$ 范数、Token 方差、绝对最大值等 9 种不同的敏感度计算指标。
*   **充分性评价**：实验设计较为充分，通过跨模型、跨比例的对比验证了方法的普适性。对比实验遵循了公平性原则，使用了相同的量化框架（QVLM）和剪枝基准（CDPruner）。

### 6. 主要结论与发现
*   **协同效应显著**：在 W4A4 模式下，QAPruner 显著优于传统的语义剪枝。在 12.5% 的极低保留率下，准确率比基线提升了 **2.24%**。
*   **“少即是多”**：在多个设置中，QAPruner 剪枝后的模型精度甚至**超过了不剪枝的密集量化模型**。这表明通过移除难以量化且语义冗余的 Token，反而能提升量化模型的整体稳定性。
*   **离群值保护是关键**：消融实验证明，结合局部量化误差和全局离群值强度的混合指标效果最好。

### 7. 优点与亮点
*   **视角独特**：首次揭示了 MLLM 中视觉 Token 剪枝与量化之间的相互干扰机制。
*   **高效实用**：无需重新训练或微调，易于集成到现有的推理加速流水线中。
*   **性能卓越**：在大幅降低计算量（减少 87.5% 视觉 Token）的同时，实现了比全量化模型更高的精度。

### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在 ScienceQA 这一类任务上，对于视频理解、长文本多模态对话等更广泛场景的泛化性有待进一步验证。
*   **超参数依赖**：融合权重 $\alpha$ 可能需要针对不同模型或量化比特数进行手动调优。
*   **硬件实测缺失**：论文主要报告了算法精度，缺乏在实际硬件（如手机端 SoC 或特定加速器）上的端到端延迟（Latency）实测数据。

（完）
