---
title: Progressive Video Condensation with MLLM Agent for Long-form Video Understanding
title_zh: 基于 MLLM 智能体的渐进式视频浓缩用于长视频理解
authors: "Yufei Yin, Yuchen Xing, Qianke Meng, Minghao Chen, Yan Yang, Zhou Yu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02891v1"
tags: ["query:evlm"]
score: 9.0
evidence: 用于高效多模态大模型的多级视频压缩
tldr: 针对长视频理解中计算开销大和视觉细节丢失的问题，本文提出ProVCA，一种基于多模态大模型（MLLM）代理的渐进式视频压缩方法。该方法通过从粗到细的三个阶段（片段定位、片段选择、关键帧精炼）迭代筛选关键帧，在显著减少输入帧数的同时保留了关键视觉信息。实验表明，ProVCA在EgoSchema等多个长视频问答基准上达到了SOTA性能，且无需额外训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02891v1/fig-001.webp\", \"caption\": \"TABLE I: Comparison with other training-free methods on EgoSchema, NExT-QA, and IntentQA.\", \"page\": 5, \"index\": 1, \"width\": 1076, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-002.webp\", \"caption\": \"TABLE II: Ablation experiments for ProVCA. All results are on the val subset of NExT-QA. Best results are bolded, and\", \"page\": 5, \"index\": 2, \"width\": 1093, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-003.webp\", \"caption\": \"Fig. 2: Overview of ProVCA in video understanding based on MLLM.\", \"page\": 3, \"index\": 3, \"width\": 1076, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-004.webp\", \"caption\": \"Fig. 1: Conceptual comparisons of three Video understanding methods based on LLM (MLLM). Existing approaches either (a) directly reason with pre-trained video-based MLLMs at high computational cost, or (b) use LLM agents that convert frames to captions and rely on VLMs (e.g., CLIP) to filter key frames, discarding fine-grained visual cues. In contrast, our ProVCA (c) progressively condenses videos by using an MLLM to narrow down query-relevant content from coarse segments to fine frames, then feeds the selected key frames into the MLLM to generate the final answer.\", \"page\": 1, \"index\": 4, \"width\": 518, \"height\": 356}]"
motivation: 现有的长视频理解方法在保留细粒度视觉细节与降低计算成本之间难以平衡。
method: 提出一种渐进式压缩代理，通过片段定位、片段选择和关键帧精炼三个模块，由粗到细地提取与查询相关的关键帧。
result: 在EgoSchema、NExT-QA和IntentQA等数据集上取得了领先的零样本准确率，且使用的帧数少于以往的免训练方法。
conclusion: 渐进式视频压缩策略能有效提升MLLM处理长视频的效率和准确性，为高效长视频理解提供了新思路。
---

## 摘要
理解长视频需要在有限的计算预算下，从长序列中提取与查询相关的信息。现有的“先文本后大语言模型（LLM）”流水线会丢失细粒度的视觉线索，而基于视频的多模态大语言模型（MLLMs）虽然能保留视觉细节，但对帧数需求过高且计算开销巨大。在这项工作中，我们旨在利用 MLLMs 实现高效的视频理解。我们提出了 ProVCA，一种渐进式视频浓缩智能体，它在多个粒度上迭代定位关键视频帧。ProVCA 首先采用片段定位模块来识别与查询相关的视频片段，接着通过片段选择模块基于相似度选择重要片段，最后利用关键帧细化模块在这些片段中精准定位特定的关键帧。通过将范围从粗粒度片段逐步缩小到细粒度帧，ProVCA 识别出一组少量的关键帧，用于基于 MLLM 的推理。ProVCA 在 EgoSchema 上达到了 69.3%、在 NExT-QA 上达到了 80.5%、在 IntentQA 上达到了 77.7% 的最先进零样本准确率，且使用的帧数少于以往的免训练方法。

## Abstract
Understanding long videos requires extracting query-relevant information from long sequences under tight compute budgets. Existing text-then-LLM pipelines lose fine-grained visual cues, while video-based multimodal large language models (MLLMs) can keep visual details but are too frame-hungry and computationally expensive. In this work, we aim to harness MLLMs for efficient video understanding. We propose ProVCA, a progressive video condensation agent that iteratively locates key video frames at multiple granularities. ProVCA first adopts a segment localization module to identify the video segment relevant to the query, then a snippet selection module to select important snippets based on similarity, and finally a keyframe refinement module to pinpoint specific keyframes in those snippets. By progressively narrowing the scope from coarse segments to fine frames, ProVCA identifies a small set of keyframes for MLLM-based reasoning. ProVCA achieves state-of-the-art zero-shot accuracies of 69.3\% on EgoSchema, 80.5\% on NExT-QA, and 77.7\% on IntentQA, while using fewer frames than previous training-free methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Progressive Video Condensation with MLLM Agent for Long-form Video Understanding》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在有限的计算预算下，让多模态大模型（MLLM）高效且准确地理解长视频。
*   **背景与动机**：
    *   **现有方案的缺陷**：传统的“视频转文本+LLM”流水线（如 VideoTree）会丢失细粒度的视觉细节；而直接输入多帧的视频 MLLM（如视频版 LLaVA）计算成本极高，且存在大量冗余信息干扰推理。
    *   **人类启发**：人类理解长视频类似于阅读理解，先定位相关段落，再关注细节。
    *   **目标**：开发一种无需额外训练、能够自动从长视频中“浓缩”出极少数关键帧，并保持高精度推理的智能体框架。

### 2. 方法论：ProVCA 框架
ProVCA（Progressive Video Condensation Agent）采用**由粗到细（Top-down）**的渐进式筛选策略，核心流程分为四个阶段：

*   **片段定位 (Segment Localization)**：
    *   将视频划分为 $n$ 个大段，提取每段的首帧作为全局上下文。
    *   调用 MLLM（如 GPT-4o）根据查询（Query）识别出最相关的粗粒度时间区间。
*   **片段选择 (Snippet Selection)**：
    *   **时序感知聚类**：在定位到的区间内，利用轻量级描述模型（Captioner）生成每帧描述，通过计算文本嵌入的余弦相似度进行动态聚类，将相似帧归为“片段（Snippet）”。
    *   **重要性评估**：MLLM 评估每个片段代表帧的关联度，并给出置信度评分。
*   **关键帧精炼 (Keyframe Refinement)**：
    *   对于低置信度的片段，系统会回到原始图像域，让 MLLM 在该片段内进行细粒度视觉分析，精准锁定最关键的一帧。
*   **MLLM 推理 (Reasoning)**：
    *   将最终筛选出的极少量关键帧（通常仅 4-7 帧）按时序输入 MLLM，配合思维链（CoT）提示词生成最终答案。

### 3. 实验设计
*   **数据集**：
    *   **NExT-QA**：侧重因果和时序动作解释（平均 44 秒）。
    *   **EgoSchema**：超长第一视角视频理解（平均 180 秒）。
    *   **IntentQA**：侧重视频意图推理。
*   **对比方法**：
    *   **基于开源 MLLM**：SeViLA, MVU, LangRepo 等。
    *   **基于闭源 LLM/MLLM**：LLoVi, VideoAgent, VideoTree, IG-VLM, LVNet 等。
*   **基准测试**：采用零样本（Zero-shot）准确率作为核心指标。

### 4. 资源与算力
*   **模型使用**：核心推理引擎采用 **GPT-4o** (gpt-4o-2024-08-06)。
*   **辅助模型**：使用 LaViLa 或 CogAgent 作为视频帧描述生成器；使用 `all-MiniLM-L6-v2` 计算文本相似度。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或推理总时长。由于该方法是免训练（Training-free）的，其主要开销在于调用商业模型 API 的 Token 消耗和视频解码。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流长视频基准上进行了全面测试，并在 NExT-QA 子集上进行了详尽的消融实验。
*   **消融实验覆盖面**：
    *   测试了不同 MLLM 底座（LLaVA 系列 vs GPT 系列）的影响。
    *   对比了不同输入模态（仅文本、图像网格、图像+文本）的效果。
    *   验证了三个核心模块（定位、选择、精炼）各自的增益。
    *   探讨了聚类策略和分段数量对性能的影响。
*   **评价**：实验设计客观、公平，通过控制变量法（如在相同 GPT-4o 底座下对比）证明了算法逻辑本身的优越性，而非单纯依赖更强的模型。

### 6. 主要结论与发现
*   **性能领先**：ProVCA 在 EgoSchema 全量榜单上达到 74.2% 的准确率，比之前的 SOTA（LVNet）高出 8.2%。
*   **高效浓缩**：在保持高精度的同时，ProVCA 平均每段视频仅需处理 4.2 到 7.3 帧，远低于同类方法（如 LVNet 的 12 帧）。
*   **视觉反馈的重要性**：实验证明，在精炼阶段回归“图像域”比单纯依赖“文本描述”能显著提升对细微动作的捕捉能力。

### 7. 优点与亮点
*   **渐进式逻辑**：模仿人类“先看大概，再找细节”的认知过程，有效解决了长视频中“大海捞针”的难题。
*   **免训练特性**：作为一种 Plug-and-play 框架，可以随 MLLM 能力的提升而自动增强，具有极强的通用性。
*   **精度与效率平衡**：通过动态阈值聚类和置信度机制，实现了极低帧数下的高性能推理。

### 8. 不足与局限
*   **API 依赖与成本**：高度依赖 GPT-4o 等闭源模型，对于大规模离线处理可能产生较高的 API 调用费用。
*   **多阶段延迟**：由于采用多轮迭代（定位 -> 选择 -> 精炼 -> 推理），相比单次推理的方法，其端到端响应延迟（Latency）可能会增加。
*   **描述模型限制**：第一阶段的片段化依赖于 Caption 模型的质量，如果描述模型无法捕捉到某些关键视觉特征，后续的 MLLM 筛选可能会漏掉重要信息。

（完）
