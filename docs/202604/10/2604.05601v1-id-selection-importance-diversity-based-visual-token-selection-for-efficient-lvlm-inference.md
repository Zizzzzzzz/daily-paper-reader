---
title: "ID-Selection: Importance-Diversity Based Visual Token Selection for Efficient LVLM Inference"
title_zh: ID-Selection：基于重要性与多样性的视觉 Token 选择，助力高效 LVLM 推理
authors: "Zhaohong Huang, Wenjing Liu, Yuxin Zhang, Fei Chao, Rongrong Ji"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05601v1"
tags: ["query:evlm"]
score: 10.0
evidence: 用于高效LVLM推理的视觉标记剪枝
tldr: 针对大语言视觉模型（LVLM）推理效率问题，本文提出ID-Selection策略。该方法通过结合重要性评估与多样性感知迭代选择，在保留关键信息的同时有效减少冗余视觉Token。实验表明，该方法在无需额外训练的情况下，能在极高剪枝率下保持优异性能，显著降低推理计算量，为高效LVLM推理提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05601v1/fig-001.webp\", \"caption\": \"Table 1. Comparison with previous approaches on LLaVA-1.5-7B [20] across 9 image understanding benchmarks. The highest score is denoted in bold. Average are computed by calculating the mean accuracy across all 9 datasets. †: importance from LLM second-layer cross-modal attention (Eq. (3)); ‡: importance from visual encoder [CLS] attention [30]; §: importance from the unified score in Eq. (6).\", \"page\": 6, \"index\": 1, \"width\": 1004, \"height\": 965}, {\"url\": \"assets/figures/arxiv/2604.05601v1/fig-002.webp\", \"caption\": \"Table 2. Comparison with previous approaches on LLaVA-Next-7B [21] across 9 image understanding benchmarks. The highest score is denoted in bold. Average are computed by calculating the mean accuracy across all 9 datasets. †: importance from LLM second-layer cross-modal attention (Eq. (3)); ‡: importance from visual encoder [CLS] attention [30]; §: importance from the unified score in Eq. (6).\", \"page\": 7, \"index\": 2, \"width\": 1018, \"height\": 640}, {\"url\": \"assets/figures/arxiv/2604.05601v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of ID-Selection. Each token is assigned an importance score. For the LLaVA series [19–21], we adopt a unified score (Eq. (6)) that combines visual saliency ([CLS] attention [40]) and instruction relevance (Eq. (4)). Tokens with high scores are iteratively selected while suppressing similar ones to maintain diversity, until the token budget is reached, and the remaining tokens are pruned.\", \"page\": 3, \"index\": 3, \"width\": 1036, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2604.05601v1/fig-004.webp\", \"caption\": \"Table 4. Comparison of Qwen2.5-VL-7B’s and InternVL2.5-8B’s performance [4, 8] on 5 image understanding benchmarks [9, 18, 22, 24, 32]. The highest score is denoted in bold. †: importance from LLM second-layer cross-modal attention (Eq. (3)).\", \"page\": 8, \"index\": 4, \"width\": 502, \"height\": 472}, {\"url\": \"assets/figures/arxiv/2604.05601v1/fig-005.webp\", \"caption\": \"Table 5. Comparison of Qwen2.5-VL-7B’s and InternVL2.5-8B’s performance [4, 8] on 4 text-focused image understanding benchmarks [23, 26–28]. The highest score is denoted in bold. †: importance from LLM second-layer cross-modal attention (Eq. (3)).\", \"page\": 8, \"index\": 5, \"width\": 500, \"height\": 472}, {\"url\": \"assets/figures/arxiv/2604.05601v1/fig-006.webp\", \"caption\": \"Table 3. Comparison of Video-LLaVA’s performance [19] on 3 video understanding benchmarks [5, 15, 37]. The highest score is denoted in bold. ‡: importance from visual encoder [CLS] attention [30]; §: importance from the unified score in Eq. (6).\", \"page\": 8, \"index\": 6, \"width\": 515, \"height\": 535}, {\"url\": \"assets/figures/arxiv/2604.05601v1/fig-007.webp\", \"caption\": \"Figure 4. Ablation of Unified Importance.\", \"page\": 9, \"index\": 7, \"width\": 469, \"height\": 283}]"
motivation: 现有的视觉Token剪枝方法难以平衡Token的重要性与多样性，导致在高压缩率下容易出现信息丢失或冗余。
method: 提出一种耦合重要性评分与多样性抑制的迭代选择策略，在选取高分Token的同时动态降低其相似Token的评分以减少冗余。
result: "在LLaVA-1.5-7B上，该方法在剪枝掉97.2%的Token（仅剩16个）时仍能保留91.8%的性能，并减少超过97%的推理计算量。"
conclusion: ID-Selection是一种简单高效、无需训练的即插即用方案，在极端剪枝比例下依然能保持极强的性能竞争力。
---

## 摘要
近期的研究进展探索了通过视觉 Token 剪枝来加速大语言视觉模型（LVLMs）的推理。然而，现有方法往往难以平衡 Token 的重要性与多样性：基于重要性的方法倾向于保留冗余 Token，而基于多样性的方法则可能忽略包含信息的 Token。这种权衡在极高缩减率下变得尤为突出，因为此时仅保留极小部分的视觉 Token 至关重要。为解决这一问题，我们提出了 ID-Selection，这是一种简单且有效的 Token 选择策略，旨在实现高效的 LVLM 推理。其核心思想是将重要性评估与多样性感知迭代选择相结合：首先为每个 Token 分配一个重要性得分，随后逐一选择高分 Token，同时逐步抑制相似 Token 的得分。通过这种方式，ID-Selection 在统一的选择过程中既保留了关键信息，又减少了冗余。在 5 个 LVLM 骨干网络和 16 个主流基准测试上的广泛实验表明，ID-Selection 始终能获得卓越的性能和效率，尤其是在极端剪枝率下。例如，在 LLaVA-1.5-7B 上，ID-Selection 剪枝了 97.2% 的视觉 Token，仅保留 16 个 Token，在减少超过 97% 的推理 FLOPs 的同时，保留了 91.8% 的原始性能，且无需任何额外训练。

## Abstract
Recent advances have explored visual token pruning to accelerate the inference of large vision-language models (LVLMs). However, existing methods often struggle to balance token importance and diversity: importance-based methods tend to retain redundant tokens, whereas diversity-based methods may overlook informative ones. This trade-off becomes especially problematic under high reduction ratios, where preserving only a small subset of visual tokens is critical. To address this issue, we propose ID-Selection, a simple yet effective token selection strategy for efficient LVLM inference. The key idea is to couple importance estimation with diversity-aware iterative selection: each token is first assigned an importance score, after which high-scoring tokens are selected one by one while the scores of similar tokens are progressively suppressed. In this way, ID-Selection preserves informative tokens while reducing redundancy in a unified selection process. Extensive experiments across 5 LVLM backbones and 16 main benchmarks demonstrate that ID-Selection consistently achieves superior performance and efficiency, especially under extreme pruning ratios. For example, on LLaVA-1.5-7B, ID-Selection prunes 97.2% of visual tokens, retaining only 16 tokens, while reducing inference FLOPs by over 97% and preserving 91.8% of the original performance, all without additional training.

---

## 论文详细总结（自动生成）

这是一份关于论文《ID-Selection: Importance-Diversity Based Visual Token Selection for Efficient LVLM Inference》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：大语言视觉模型（LVLMs）在处理高分辨率图像或长视频时，会产生海量的视觉 Token（如 Qwen-2.5-VL 可达 1.6 万个），导致推理计算开销（FLOPs）和显存占用（KV Cache）剧增。
*   **核心问题**：现有的 Token 剪枝方法存在“重要性”与“多样性”的失衡。
    *   **基于重要性**的方法（如关注注意力分数）容易选出空间上高度聚集、特征冗余的 Token。
    *   **基于多样性**的方法（如特征聚类或去重）虽然分布均匀，但可能漏掉与指令最相关的关键视觉信息。
*   **目标**：在极端剪枝比例下（如保留率低于 3%），寻找一种既能保留关键信息又能消除冗余的简单、高效、无需训练的 Token 选择策略。

### 2. 方法论：ID-Selection
ID-Selection 的核心思想是**耦合重要性评估与多样性感知的迭代抑制选择**。
*   **重要性评估（Importance Estimation）**：
    *   针对不同模型采用不同指标：包括视觉编码器的 `[CLS]` 注意力分数（代表视觉显著性）和 LLM 早期层的跨模态注意力分数（代表指令相关性）。
    *   提出**统一重要性得分**：将视觉显著性与指令相关性（通过计算指令文本特征与视觉 Token 的余弦相似度并归一化）进行元素级相乘，从而精确定位既好看又相关的区域。
*   **多样性感知迭代选择（Diversity-aware Iterative Selection）**：
    *   **初始化**：计算所有 Token 的初始重要性得分。
    *   **迭代过程**：每次选择当前得分最高的 Token 加入保留集。
    *   **动态抑制**：根据新选出的 Token，利用一个基于距离的抑制函数（指数衰减函数）降低剩余 Token 中与其相似度高的 Token 得分。
    *   **效果**：强制模型在选出一个关键点后，去寻找其他区域的“次高分”关键点，从而在特征空间实现均匀覆盖。

### 3. 实验设计
*   **数据集与场景**：涵盖 16 个基准测试，分为三大类：
    1.  **通用图像理解**：VQA v2, GQA, VizWiz, SQA, POPE, MME, MMB 等。
    2.  **细粒度/文本理解**：DocVQA, InfoVQA, ChartQA, OCRBench。
    3.  **视频问答**：TGIF, MSVD, MSRVTT。
*   **基门模型（Backbones）**：LLaVA-1.5, LLaVA-Next, Video-LLaVA, Qwen2.5-VL, InternVL2.5。
*   **对比方法**：对比了 9 种 SOTA 方法，包括 FastV, FasterVLM, SparseVLM, PruMerge, VisionZip, DART, DivPrune, VisPruner, CDPruner。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA A800-80GB GPU** 上进行。
*   **效率指标**：论文详细记录了理论 FLOPs、实际运行时间（Total Time）和 KV Cache 占用。
*   **训练时长**：该方法为**无需训练（Training-free）**的即插即用方案，因此无训练算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 种主流架构和 16 个数据集上进行了验证，实验量非常大。
*   **消融实验**：针对迭代选择机制的有效性、统一重要性得分的构成、以及超参数 $\gamma$（抑制强度）进行了详细的消融研究。
*   **客观性**：通过在不同剪枝率（如 88.9%、94.4%、甚至 97.2%）下进行对比，证明了该方法在极端情况下的稳健性。对比实验涵盖了从早期的简单注意力剪枝到最新的基于 DPP（行列式点过程）的复杂方法，对比具有公信力。

### 6. 主要结论与发现
*   **极端压缩下的高性能**：在 LLaVA-1.5 上仅保留 16 个 Token（剪枝率 97.2%）时，仍能保持 91.8% 的原始性能，远超同类方法。
*   **效率提升显著**：在 LLaVA-Next 上实现了约 10 倍的 FLOPs 减少和 2.5 倍的推理加速，同时 KV Cache 减少超过 89%。
*   **通用性强**：不仅适用于 LLaVA 系列，在 Qwen2.5-VL 和 InternVL2.5 等最新模型上也表现优异，尤其在处理文本密集的 OCR 任务时，比传统方法更具优势。

### 7. 优点与亮点
*   **简单高效**：相比于 CDPruner 等需要复杂子集优化（如 DPP）的方法，ID-Selection 仅涉及轻量级的矩阵运算和迭代更新，推理延迟极低。
*   **即插即用**：无需对模型进行微调或重新训练，适合快速部署。
*   **多模态融合**：通过结合视觉显著性和指令相关性，解决了剪枝过程中“答非所问”的问题。

### 8. 不足与局限
*   **迭代开销**：虽然单步计算简单，但其复杂度为 $O(NT)$（$N$ 为原始 Token 数，$T$ 为保留数）。虽然在 $T \ll N$ 时开销可忽略，但在保留 Token 较多时，迭代过程可能比一次性 Top-k 选择略慢。
*   **超参数依赖**：抑制函数中的 $\gamma$ 需要预设，虽然实验证明其在不同模型间具有一定的通用性，但在极端异构的数据分布下可能需要微调。
*   **应用限制**：主要针对视觉 Token 剪枝，未涉及文本 Token 的优化，且对于极小尺寸目标的捕捉能力仍受限于原始视觉编码器的分辨率。

（完）
