---
title: "EventFace: Event-Based Face Recognition via Structure-Driven Spatiotemporal Modeling"
title_zh: EventFace：通过结构驱动的时空建模实现基于事件的人脸识别
authors: "Qingguo Meng, Xingbo Dong, Zhe Jin, Massimo Tistarelli"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06782v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 利用事件流和时空建模的事件相机人脸识别
tldr: 针对事件相机在人脸识别中缺乏稳定光度外观的挑战，本文提出了EventFace框架。该研究构建了EFace数据集，通过LoRA技术将预训练RGB模型的空间结构先验迁移至事件领域，并结合运动提示编码器（MPE）与时空调制器（STM）融合动态特征。实验证明该方法在提升识别精度的同时，具有极强的光照鲁棒性和隐私保护能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06782v1/fig-001.webp\", \"caption\": \"Fig. 1. (a) Traditional RGB face recognition is sensitive to lighting variations and vulnerable to template reconstruction attacks. (b) By contrast, event-based face recognition leverages structure-driven spatiotemporal patterns generated by rigid facial motion, achieving more stable recognition under varying illumination while reducing reconstructability from leaked templates.\", \"page\": 1, \"index\": 1, \"width\": 523, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2604.06782v1/fig-002.webp\", \"caption\": \"Fig. 5. Illustration of the Spatiotemporal Interleaved WKV (ST-WKV). Top: The overall data flow. The module takes spatial and motion features as input, concatenates them in an interleaved manner, and processes them through cascaded Bi-WKV layers. Bottom: Visualization of the Row-Major and Column-Major Spatiotemporal Interleaved Scans.\", \"page\": 6, \"index\": 2, \"width\": 524, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2604.06782v1/fig-003.webp\", \"caption\": \"Fig. 9. Visualization of matching examples under the degraded illumination challenge. The Gallery samples are captured under standard indoor lighting, while the Probe samples correspond to varying low-light levels (Levels 1–3) and sidelight conditions. The computed cosine similarity scores are annotated for both RGB and Event modalities.\", \"page\": 11, \"index\": 3, \"width\": 370, \"height\": 675}, {\"url\": \"assets/figures/arxiv/2604.06782v1/fig-004.webp\", \"caption\": \"Fig. 7. CMC, DET, and ROC curves of EventFace and competing methods on the EFace benchmark. EventFace achieves the best overall identification and verification performance among the evaluated methods.\", \"page\": 10, \"index\": 4, \"width\": 1060, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2604.06782v1/fig-005.webp\", \"caption\": \"Fig. 2. Overview of the EFace dataset acquisition setup. Subjects perform natural rigid facial motions (e.g., nodding and shaking) to induce intensity changes captured by the event camera, producing spatiotemporal event streams. For the challenging test subset, an RGB camera is mounted in close proximity to the event camera, and the two modalities are acquired in a synchronized manner to ensure consistent illumination conditions.\", \"page\": 3, \"index\": 5, \"width\": 526, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2604.06782v1/fig-006.webp\", \"caption\": \"Fig. 11. Privacy evaluation under a worst-case raw data leakage scenario. We employ representative event-to-video reconstruction algorithms, E2VID and Sparse-E2VID, to recover visual information directly from the 200 ms raw event streams.\", \"page\": 12, \"index\": 6, \"width\": 420, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.06782v1/fig-007.webp\", \"caption\": \"Fig. 3. Overview of the proposed EventFace framework. The training process is structured into two progressive stages. In Stage I, the pretrained RGB backbone is frozen to retain structural priors, while LoRA modules are trained to align event features with the RGB domain. In Stage II, the adapted backbone is frozen. The Motion Prompt Encoder (MPE) and Spatiotemporal Modulator (STM) are introduced to work collaboratively at each stage: the MPE explicitly extracts motion prompts from event streams, which are then fused with spatial features via the STM module. This mechanism facilitates bidirectional interaction, allowing both spatial and temporal representations to be mutually refined before they are propagated to the next stage.\", \"page\": 5, \"index\": 7, \"width\": 964, \"height\": 635}]"
motivation: 事件相机缺乏传统RGB图像的纹理信息，需通过建模由面部运动和几何结构驱动的时空特征来实现可靠的身份识别。
method: 利用LoRA迁移RGB人脸先验建立空间基础，并引入MPE编码时间特征及STM进行时空融合，增强身份相关的事件模式表征。
result: "在EFace数据集上实现了94.19%的Rank-1识别率和5.35%的等错误率，且在低光照等恶劣环境下表现优于现有方法。"
conclusion: EventFace通过整合空间结构与时间动态，为事件驱动的人脸识别提供了一种兼顾高精度、强鲁棒性与隐私安全性的有效方案。
---

## 摘要
由于事件相机在光照鲁棒性和隐私友好性方面的固有优势，其为人脸识别提供了一种极具前景的感知模态。然而，由于事件流缺乏传统基于 RGB 的人脸识别系统所依赖的稳定光度外观，我们认为基于事件的人脸识别应当建模由刚性面部运动和个体面部几何形状所塑造的结构驱动型时空身份表示。由于目前仍缺乏专门用于基于事件的人脸识别的数据集，我们构建了 EFace，这是一个在刚性面部运动下采集的小规模基于事件的人脸数据集。为了从这些有限的事件数据中进行有效学习，我们进一步提出了 EventFace，这是一个集成空间结构和时间动态进行身份建模的基于事件的人脸识别框架。具体而言，我们采用低秩自适应（LoRA）将预训练 RGB 人脸模型的结构化面部先验迁移到事件领域，从而为身份建模建立可靠的空间基础。在此基础上，我们进一步引入了运动提示编码器（MPE）来显式编码时间特征，并引入了时空调制器（STM）将其与空间特征融合，从而增强了与身份相关的事件模式的表示。广泛的实验表明，EventFace 在评估的基准模型中实现了最佳性能，Rank-1 识别率为 94.19%，等错误率（EER）为 5.35%。结果进一步表明，在光照退化的情况下，EventFace 比竞争方法表现出更强的鲁棒性。此外，学习到的表示表现出更低的模板可重构性。

## Abstract
Event cameras offer a promising sensing modality for face recognition due to their inherent advantages in illumination robustness and privacy-friendliness. However, because event streams lack the stable photometric appearance relied upon by conventional RGB-based face recognition systems, we argue that event-based face recognition should model structure-driven spatiotemporal identity representations shaped by rigid facial motion and individual facial geometry. Since dedicated datasets for event-based face recognition remain lacking, we construct EFace, a small-scale event-based face dataset captured under rigid facial motion. To learn effectively from this limited event data, we further propose EventFace, a framework for event-based face recognition that integrates spatial structure and temporal dynamics for identity modeling. Specifically, we employ Low-Rank Adaptation (LoRA) to transfer structural facial priors from pretrained RGB face models to the event domain, thereby establishing a reliable spatial basis for identity modeling. Building on this foundation, we further introduce a Motion Prompt Encoder (MPE) to explicitly encode temporal features and a Spatiotemporal Modulator (STM) to fuse them with spatial features, thereby enhancing the representation of identity-relevant event patterns. Extensive experiments demonstrate that EventFace achieves the best performance among the evaluated baselines, with a Rank-1 identification rate of 94.19% and an equal error rate (EER) of 5.35%. Results further indicate that EventFace exhibits stronger robustness under degraded illumination than the competing methods. In addition, the learned representations exhibit reduced template reconstructability.

---

## 论文详细总结（自动生成）

这是一份关于论文《EventFace: Event-Based Face Recognition via Structure-Driven Spatiotemporal Modeling》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统基于RGB图像的人脸识别高度依赖光度外观（纹理、颜色），在极端光照（低光、强侧光）下表现不佳，且容易受到模板重建攻击，存在隐私风险。
*   **核心问题**：事件相机（Event Camera）具有高动态范围（HDR）和隐私友好性，但其输出是稀疏、异步的事件流，缺乏稳定的光度信息。
*   **核心思想**：论文认为基于事件的人脸识别不应依赖外观匹配，而应建模由**刚性面部运动**（如点头、摇头）和**个体面部几何结构**共同驱动的**时空身份表示**。

### 2. 方法论：核心思想与关键技术
EventFace 采用两阶段学习策略，旨在将 RGB 领域的结构先验迁移至事件领域，并融合动态特征：
*   **输入转换**：将原始异步事件流按固定时间间隔（50ms）累积，转换为双极性（正、负）的 2D 事件帧序列，使其兼容深度学习架构。
*   **第一阶段：结构感知空间自适应（Stage I）**
    *   利用在大型 RGB 数据集上预训练的人脸识别模型（如 AdaFace）作为骨干网络。
    *   引入 **LoRA（低秩自适应）** 技术，冻结骨干网络，仅训练轻量级的低秩矩阵，将 RGB 空间结构先验迁移到事件模态，缓解事件数据稀缺导致的过拟合。
*   **第二阶段：运动诱导时空学习（Stage II）**
    *   **运动提示编码器（MPE）**：通过计算相邻事件帧之间的特征差异（使用不同尺度的深度卷积），显式提取由头部运动产生的动态提示。
    *   **时空调制器（STM）**：基于 RWKV 架构改进，包含 **八向令牌偏移（Octa-Shift）** 以捕捉全向空间上下文，以及 **时空交错 WKV（ST-WKV）** 机制，通过交错扫描实现静态空间特征与动态运动特征的深度融合。

### 3. 实验设计
*   **数据集**：
    *   **EFace（自建）**：包含 131 个身份。训练集 100 人；测试集 31 人（包含标准光照子集和极端光照子集）。
    *   **场景**：涵盖室内外标准光照、极低光照（Level 1-3）和侧光环境。
*   **Benchmark 与对比方法**：
    *   对比了 9 种最先进的 RGB 人脸识别方法（重训练于 EFace）：AdaFace, MagFace, SphereFace2, UniFace, UniTSFace, RVFace, TopoFace, TransFace, TransFace++。
    *   评价指标：Rank-1 识别率、EER（等错误率）、AUC、TAR@FAR。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA GeForce RTX 3090 GPU (24 GB)**。
*   **训练细节**：
    *   Batch size 为 80。
    *   第一阶段训练 60 个 epoch，第二阶段训练 20 个 epoch。
    *   输入序列长度 $T=4$（总时长 200ms），计算量为 29.82G FLOPs。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **消融实验**：针对两阶段框架的必要性、不同微调策略（全量 vs LoRA）、MPE/STM 内部组件设计、输入帧数影响等做了详细测试。
    *   **性能对比**：在标准测试集上进行了完整的 CMC、DET 和 ROC 曲线分析。
    *   **鲁棒性分析**：专门针对 4 种退化光照场景进行了定量和定性对比。
    *   **隐私分析**：进行了特征模板重建实验和原始事件流重建实验。
*   **充分性评价**：实验设计较为全面，涵盖了从算法有效性到实际应用挑战（光照、隐私）的多个维度。对比基准均为近年顶级会议成果，实验过程客观公平。

### 6. 主要结论与发现
*   **性能领先**：EventFace 达到了 **94.19% 的 Rank-1 识别率** 和 **5.35% 的 EER**，显著优于所有适配后的 RGB 基准模型。
*   **光照鲁棒性**：在 RGB 图像完全失效（近乎全黑）的 Level 1 低光环境下，EventFace 仍能保持稳定的识别能力（余弦相似度远高于随机水平）。
*   **隐私保护**：由于事件数据不含精细纹理且经过深度时空交互建模，从其特征模板中重建人脸图像极其困难，重建结果仅具模糊轮廓，无法识别身份。

### 7. 优点（亮点）
*   **跨模态迁移策略**：巧妙利用 LoRA 将成熟的 RGB 人脸识别先验引入事件领域，解决了事件数据集规模小、难以从头训练深层网络的问题。
*   **时空深度融合**：STM 模块通过交错扫描机制，让运动特征动态地调制空间结构特征，比简单的特征拼接更具判别力。
*   **实际应用价值**：通过 EFace 数据集的构建和极端环境测试，证明了事件相机在安全监控、车载人脸识别等特定场景下的巨大潜力。

### 8. 不足与局限
*   **数据集规模**：虽然自建了 EFace，但 131 个身份的规模相对于 RGB 领域的百万级数据集仍然较小，模型的泛化能力在大规模人群中尚待验证。
*   **运动依赖性**：该方法依赖于“刚性面部运动”来产生事件。如果人脸完全静止且环境光照无变化，事件相机将无法捕获有效信息，这限制了其在某些静态场景下的应用。
*   **硬件普及度**：事件相机目前成本较高且分辨率（640x480）低于主流 RGB 传感器，大规模商用仍受硬件限制。

（完）
