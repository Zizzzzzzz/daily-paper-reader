---
title: Spatial Orthogonal Refinement for Robust RGB-Event Visual Object Tracking
title_zh: 空间正交细化用于鲁棒的 RGB-事件视觉目标跟踪
authors: "Dexing Huang, Shiao Wang, Fan Zhang, Xiao Wang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27913v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 使用事件相机进行鲁棒的RGB-事件视觉目标跟踪
tldr: 针对传统RGB相机在高速运动下易产生运动模糊的问题，本文提出SOR-Track框架。该方法利用事件相机的高时间分辨率特性，通过空间正交细化（SOR）模块提取事件流中的定向几何先验。SOR模块利用动态正交方向滤波器捕捉运动一致的结构响应，并采用非对称结构调制机制修正退化的RGB纹理。实验表明，该方法在FE108基准上表现优异，特别是在模糊和弱光环境下，为多模态特征对齐提供了物理依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27913v1/fig-001.webp\", \"caption\": \"Fig. 2. Qualitative comparison of SOR-Track against the baseline CEUTrack on the FE108 dataset. In this HDR scenario, the baseline (red) drifts to the overexposed background, while our SOR-Track (blue) stays robust.\", \"page\": 5, \"index\": 1, \"width\": 529, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2603.27913v1/fig-002.webp\", \"caption\": \"Fig. 1. An overview of our proposed Spatial Orthogonal Refinement for a robust RGB-Event visual object tracking framework.\", \"page\": 3, \"index\": 2, \"width\": 1076, \"height\": 391}]"
motivation: 传统RGB-Event融合方法多采用黑盒策略，未能有效利用事件流中蕴含的定向几何先验来修复RGB图像的运动模糊。
method: 提出空间正交细化（SOR）模块，通过动态运动引导的正交滤波器提取事件结构，并利用非对称调制机制精细化RGB特征。
result: 在大规模FE108基准测试中，SOR-Track在运动模糊和低光照等挑战性场景下的性能显著优于现有融合跟踪算法。
conclusion: 该研究为RGB与事件数据的多模态融合提供了一种基于物理原理的特征对齐与纹理修复新方案。
---

## 摘要
在高速运动场景下，鲁棒的视觉目标跟踪（VOT）仍然具有挑战性，因为传统的 RGB 传感器会遭受严重的运动模糊和性能下降。事件相机具有微秒级的时间分辨率和高动态范围，能够提供互补的结构线索，有望弥补这些局限性。然而，现有的 RGB-事件融合方法通常将事件数据视为密集的强度表示，并采用黑盒融合策略，未能显式地利用事件流中固有的方向几何先验来纠正退化的 RGB 特征。为了解决这一局限性，我们提出了 SOR-Track，这是一个基于空间正交细化（SOR）的鲁棒 RGB-事件跟踪精简框架。核心 SOR 模块采用了一组正交方向滤波器，这些滤波器由局部运动方向动态引导，从而从事件流中提取清晰且运动一致的结构响应。这些响应作为几何锚点，通过非对称结构调制机制来调制和细化混叠的 RGB 纹理，从而显式地弥合两种模态之间的结构差异。在大规模 FE108 基准测试上的广泛实验表明，SOR-Track 的性能始终优于现有的基于融合的跟踪器，特别是在运动模糊和低光照条件下。尽管该方法简单，但它为多模态特征对齐和纹理纠正提供了一种有原则且基于物理的方法。本文的源代码将发布在 https://github.com/Event-AHU/OpenEvTracking。

## Abstract
Robust visual object tracking (VOT) remains challenging in high-speed motion scenarios, where conventional RGB sensors suffer from severe motion blur and performance degradation. Event cameras, with microsecond temporal resolution and high dynamic range, provide complementary structural cues that can potentially compensate for these limitations. However, existing RGB-Event fusion methods typically treat event data as dense intensity representations and adopt black-box fusion strategies, failing to explicitly leverage the directional geometric priors inherently encoded in event streams to rectify degraded RGB features. To address this limitation, we propose SOR-Track, a streamlined framework for robust RGB-Event tracking based on Spatial Orthogonal Refinement (SOR). The core SOR module employs a set of orthogonal directional filters that are dynamically guided by local motion orientations to extract sharp and motion-consistent structural responses from event streams. These responses serve as geometric anchors to modulate and refine aliased RGB textures through an asymmetric structural modulation mechanism, thereby explicitly bridging structural discrepancies between two modalities. Extensive experiments on the large-scale FE108 benchmark demonstrate that SOR-Track consistently outperforms existing fusion-based trackers, particularly under motion blur and low-light conditions. Despite its simplicity, the proposed method offers a principled and physics-grounded approach to multi-modal feature alignment and texture rectification. The source code of this paper will be released on https://github.com/Event-AHU/OpenEvTracking

---

## 论文详细总结（自动生成）

以下是对论文《Spatial Orthogonal Refinement for Robust RGB-Event Visual Object Tracking》的结构化深入总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：在高速运动或极端光照（如高动态范围 HDR）场景下，传统 RGB 相机因曝光时间长产生严重的**运动模糊**或**细节丢失**，导致视觉目标跟踪（VOT）性能大幅下降。
*   **研究背景**：事件相机（Event Camera）具有微秒级响应和高动态范围，能捕捉清晰的结构线索。
*   **动机**：现有 RGB-Event 融合方法多将事件流视为类似图像的密集表示，并采用“黑盒”融合（如简单的拼接或注意力机制），忽略了事件流中蕴含的**方向性几何先验**。这导致无法显式地利用事件的结构信息来修复退化的 RGB 纹理。

### 2. 核心方法论：SOR-Track 框架
论文提出了“**先细化，后跟踪**”（Refine-then-Track）的范式，核心是**空间正交细化（SOR）模块**：
*   **颗粒化特征提取（Granular Feature Stem）**：为了避免传统步长卷积（Strided Conv）作为低通滤波器滤除事件流中的高频结构，采用“空间到深度”（Space-to-Depth）变换，保留像素级的细微边界。
*   **正交方向模块（ODM）**：
    *   受生物视觉皮层 Gabor 响应启发，根据局部运动方向 $\phi$ 生成一组在正交空间均匀分布的方向滤波器（如 $K=4$ 个方向）。
    *   利用可学习的 Gabor 核参数（$\sigma, \lambda, \gamma, \psi$）动态捕捉运动一致的结构原语。
*   **非对称结构调制**：
    *   将事件流提取的响应作为“结构蓝图”，生成空间感知调制掩码（Modulation Mask）。
    *   通过该掩码对模糊的 RGB 特征进行元素级乘法（Hadamard product），放大与事件边界对齐的纹理，抑制背景噪声。
*   **预测头**：基于 Vision Transformer (ViT) 主干网络，通过分类（Focal Loss）和回归（L1 + GIoU Loss）定位目标。

### 3. 实验设计与对比
*   **数据集**：主要在大型 RGB-Event 跟踪基准 **FE108** 上进行评估，该数据集包含 108 个涵盖高速运动、低光照和复杂背景的挑战性序列。
*   **对比方法**：
    *   **仅 RGB 跟踪器**：SiamRPN++、ATOM、DiMP、PrDiMP。
    *   **RGB-Event 融合跟踪器**：ATOM+Event、DiMP+Event、PrDiMP+Event，以及基于 Transformer 的前沿模型 **CEUTrack**。
*   **评价指标**：成功率（AUC/RSR）、精确度（PR/RPR）、重叠精度（OP50, OP75）及归一化精确度（NPR）。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA RTX 4090 GPU**。
*   **训练配置**：总 Batch Size 为 32，训练 50 个 Epoch。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到每个 Epoch 包含 60,000 个样本。
*   **推理速度**：强调了计算效率，支持实时定向细化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 FE108 全集上进行了完整的定量对比。
    *   进行了**消融实验**，验证了“颗粒化特征提取（StemNet）”对高频信息保留的有效性（AUC 提升 3.93%）。
    *   提供了**定性可视化分析**，包括跟踪轨迹对比和特征能量图（Saliency Map）分析。
*   **客观性与公平性**：实验遵循标准评估协议，对比了同类最先进（SOTA）模型，且在不使用数据增强的情况下取得了优异结果，证明了架构本身的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：SOR-Track 在所有成功率指标上均优于 CEUTrack，尤其在 **OP75（高精度定位）上实现了 3.83% 的绝对增益**。
*   **结构修复能力**：可视化证明 SOR 模块能有效抑制过曝区域的背景噪声，将弥散的特征能量重新聚焦到目标的真实边界上。
*   **物理一致性**：通过引入 Gabor 滤波器，证明了将物理先验（方向性）引入深度学习模型比纯粹的黑盒特征融合更有效。

### 7. 优点与亮点
*   **物理启发设计**：不同于盲目的特征拼接，ODM 模块具有明确的物理含义，利用事件相机的特性解决 RGB 的运动模糊。
*   **高精度定位**：OP75 指标的显著提升表明该方法在估计目标边界框方面非常精确。
*   **简洁高效**：框架逻辑清晰（增强后跟踪），且在特征提取阶段避免了信息丢失。

### 8. 不足与局限
*   **方向数固定**：目前 ODM 模块使用固定数量（K=4）的方向，可能无法完美覆盖极其复杂的非线性运动轨迹。
*   **模态对齐假设**：实验基于已同步对齐的 RGB-Event 数据，未深入探讨在空间非对齐（Unaligned）情况下的表现。
*   **场景覆盖**：虽然在 FE108 表现优异，但尚未在更多样化的跨模态数据集（如 VisEvent 或其他极端天气数据集）上进行广泛验证。

（完）
