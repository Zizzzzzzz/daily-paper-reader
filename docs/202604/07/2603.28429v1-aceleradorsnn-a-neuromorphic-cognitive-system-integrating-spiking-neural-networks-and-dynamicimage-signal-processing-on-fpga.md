---
title: "AceleradorSNN: A Neuromorphic Cognitive System Integrating Spiking Neural Networks and DynamicImage Signal Processing on FPGA"
title_zh: AceleradorSNN：一种在 FPGA 上集成脉冲神经网络与动态图像信号处理的神经形态认知系统
authors: "Daniel Gutierrez, Ruben Martinez, Leyre Arnedo, Antonio Cuesta, Soukaina El Hamry"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28429v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 处理来自动态视觉传感器（DVS）异步数据的类脑系统
tldr: 针对自动驾驶、无人机及工业4.0对高效目标检测的需求，本文推出了AceleradorSNN，这是一种集成脉冲神经网络（SNN）与动态图像信号处理（ISP）的FPGA神经形态认知系统。该系统通过硬件优化的神经形态处理单元（NPU）处理DVS异步数据，并结合可重构ISP处理RGB图像，实现了低延迟、高能效的实时视觉感知，为第三代人工智能硬件架构提供了创新方案。
source: arxiv
selection_source: fresh_fetch
motivation: 传统卷积神经网络在自动驾驶等实时应用中存在高延迟和高能耗的局限性，难以满足高性能嵌入式系统的需求。
method: 设计并实现了一种基于FPGA的硬件架构，集成处理事件相机数据的SNN处理单元和支持动态重构的认知图像信号处理器。
result: 成功开发了面向硬件的IP核，并验证了采用代理梯度训练的SNN骨干网络在FPGA上的实时流式处理性能。
conclusion: AceleradorSNN通过结合SNN的异步处理优势与可重构ISP，为自主系统提供了一种兼具高速度与低功耗的神经形态认知解决方案。
---

## 摘要
自主系统（如高级驾驶辅助系统 (ADAS)、无人机 (UAV) 和工业 4.0 机器人）对高速、低延迟和高能效目标检测的需求，暴露了传统卷积神经网络 (CNN) 的局限性。为了应对这些挑战，我们开发了 AceleradorSNN，这是一种第三代人工智能认知系统。该架构集成了一个基于脉冲神经网络 (SNN) 的神经形态处理单元 (NPU)，用于处理来自动态视觉传感器 (DVS) 的异步数据，以及一个用于 RGB 摄像头的动态可重构认知图像信号处理器 (ISP)。本文详细介绍了这两个 IP 核的面向硬件的设计、基于代理梯度训练的 SNN 骨干网络的评估，以及在现场可编程门阵列 (FPGA) 上实现的实时流式 ISP 架构。

## Abstract
The demand for high-speed, low-latency, and energy-efficient object detection in autonomous systems -- such as advanced driver-assistance systems (ADAS), unmanned aerial vehicles (UAVs), and Industry 4.0 robotics -- has exposed the limitations of traditional Convolutional Neural Networks (CNNs). To address these challenges, we have developed AceleradorSNN, a third-generation artificial intelligence cognitive system. This architecture integrates a Neuromorphic Processing Unit (NPU) based on Spiking Neural Networks (SNNs) to process asynchronous data from Dynamic Vision Sensors (DVS), alongside a dynamically reconfigurable Cognitive Image Signal Processor (ISP) for RGB cameras. This paper details the hardware-oriented design of both IP cores, the evaluation of surrogate-gradienttrained SNN backbones, and the real-time streaming ISP architecture implemented on Field-Programmable Gate Arrays (FPGA).

---

## 论文详细总结（自动生成）

这是一份关于论文《AceleradorSNN: A Neuromorphic Cognitive System Integrating Spiking Neural Networks and Dynamic Image Signal Processing on FPGA》的深度结构化总结：

### 1. 研究动机与背景
在自动驾驶（ADAS）、无人机（UAV）和工业4.0机器人等领域，传统基于卷积神经网络（CNN）的视觉系统面临高延迟、高功耗以及在极端光照/高速运动下表现不佳的局限。虽然动态视觉传感器（DVS，即事件相机）具有微秒级延迟和高动态范围，但其稀疏数据缺乏纹理和颜色信息。论文的核心问题是如何结合 DVS 的速度优势与 RGB 相机的细节优势，构建一个低功耗、高实时性的嵌入式认知系统。

### 2. 核心方法论
论文提出了 **AceleradorSNN** 架构，由两个协同工作的 FPGA IP 核组成：
*   **神经形态处理单元 (NPU)：**
    *   **事件编码：** 将异步 DVS 事件 $(t, x, y, p)$ 在固定时间窗口内聚合，转换为时空体素网格（Voxel Grid）张量。
    *   **神经元模型：** 采用漏积分发放（LIF）模型，通过微分方程模拟电荷衰减与脉冲发放。
    *   **训练技术：** 针对脉冲不可导问题，使用**代理梯度（Surrogate Gradients）**结合随时间反向传播（BPTT）算法进行训练。
*   **认知图像信号处理器 (ISP)：**
    *   **闭环控制：** NPU 作为认知控制器，根据场景的光照和运动特征，实时生成指令动态调整 ISP 参数。
    *   **硬件流水线：** 采用全流水线 HDL 设计，基于 AXI4-Stream 协议实现像素级流处理，无需外部帧缓存。
    *   **功能模块：** 包含动态坏点校正（DPC）、自动白平衡（AWB）、基于 Malvar-He-Cutler 算法的去马赛克、非局部均值（NLM）去噪以及伽马校正。

### 3. 实验设计与基准
*   **数据集：** 使用了 Prophesee 的 **GEN1 汽车检测数据集**（Prophesee GEN1），这是目前事件视觉领域的主流基准。
*   **对比骨干网络：** 评估了四种 SNN 架构：
    1.  **Spiking-VGG**：层次化特征提取。
    2.  **Spiking-DenseNet**：特征复用，防止梯度消失。
    3.  **Spiking-MobileNet**：利用深度可分离卷积降低参数量。
    4.  **Spiking-YOLO**：将 YOLO 转换为脉冲域，用于高性能目标检测。
*   **评估指标：** 平均精度（AP @ IoU 0.50）和网络稀疏度（Sparsity）。

### 4. 资源与算力
*   **硬件平台：** 整个系统在 **FPGA** 上进行了综合与验证，并为未来的 ASIC 制造做了映射准备。
*   **训练细节：** 文中提到使用了 AdamW 优化器进行训练，但**未明确说明**具体的 GPU 型号、数量及训练时长。
*   **硬件效率：** 强调了 ISP 模块通过流式处理（Streaming）极大地减少了硬件面积占用。

### 5. 实验数量与充分性
*   **实验规模：** 论文对比了四种主流深度学习架构在脉冲域的表现，并给出了量化结果。
*   **充分性评价：** 实验涵盖了精度（YOLO 表现最佳）与能效（MobileNet 稀疏度最高）两个维度的权衡，具有一定的参考价值。然而，由于这是一篇短篇论文（3页），缺乏与传统 CNN 在相同 FPGA 平台上的功耗对比数据，也未展示 ISP 动态调整前后的视觉对比效果，实验的深度略显不足。

### 6. 主要结论与发现
*   **性能最优：** **Spiking-YOLO** 在 GEN1 数据集上取得了最高的精度（AP 为 0.4726），是精度与计算成本的最佳平衡点。
*   **能效最优：** **Spiking-MobileNet** 表现出最高的神经元稀疏度（48.08%），意味着约一半的神经元在处理时保持静默，极利于降低边缘设备的功耗。
*   **系统协同：** 证明了 NPU 与 ISP 闭环协作的可行性，能够克服单一传感器的局限。

### 7. 优点与亮点
*   **异构融合：** 创新性地将 SNN（处理异步事件）与传统 ISP（处理同步 RGB）结合，实现了“认知控制”闭环。
*   **硬件友好：** ISP 设计完全基于流处理，无需昂贵的外部存储器（如 DDR），显著降低了系统延迟和面积。
*   **高成熟度：** 系统达到了 TRL 6/7 等级，展示了从理论模型到工业级 FPGA 部署的完整路径。

### 8. 不足与局限
*   **数据支撑较薄弱：** 论文篇幅较短，缺乏具体的硬件资源占用表（如 LUT、FF、BRAM 的使用量）。
*   **应用限制：** 虽然提出了 NPU 控制 ISP 的概念，但未详细说明具体的控制逻辑算法（即 NPU 如何精准映射到 ISP 的增益参数）。
*   **对比范围：** 未能提供与现有的神经形态芯片（如 Intel Loihi 或 SynSense Speck）在性能和功耗上的直接对比。

（完）
