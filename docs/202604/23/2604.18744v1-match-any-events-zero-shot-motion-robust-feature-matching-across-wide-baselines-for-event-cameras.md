---
title: "Match-Any-Events: Zero-Shot Motion-Robust Feature Matching Across Wide Baselines for Event Cameras"
title_zh: Match-Any-Events：事件相机跨宽基线的零样本运动鲁棒特征匹配
authors: "Ruijun Zhang, Hang Su, Kostas Daniilidis, Ziyun Wang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18744v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 事件相机的零样本特征匹配
tldr: "针对事件相机在宽基线场景下因运动导致外观剧烈变化且缺乏标注数据的问题，本文提出了首个零样本事件匹配模型Match-Any-Events。该模型采用运动鲁棒的注意力骨干网络，结合多时间尺度特征与稀疏感知令牌选择，并利用新开发的事件运动合成框架进行大规模训练。实验表明，该方法在无需微调的情况下，在多个基准测试中性能较此前最优方法提升了37.7%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18744v1/fig-001.webp\", \"caption\": \"Fig. 2: Pipeline. Event slices are binned to multi-scale voxel preserving rich temporal information (Sec. 4.1). The voxel input are tokenized and processed by a separable Temporal Aggregation Transformer module (Sec. 4.2), and then with a sparsity-aware token selection (Sec. 4.3) module. Multi-scale and dense features from ViT followed by DPT are used for iterative matching in the final MNN matching module (Sec. 4.4).\", \"page\": 6, \"index\": 1, \"width\": 706, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2604.18744v1/fig-002.webp\", \"caption\": \"Table 1: Quantitative comparison for event-to-event and event-to-image matching on ECM and M3ED.\", \"page\": 7, \"index\": 2, \"width\": 723, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2604.18744v1/fig-003.webp\", \"caption\": \"Fig. 8: Motion robustness study.\", \"page\": 14, \"index\": 3, \"width\": 675, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.18744v1/fig-004.webp\", \"caption\": \"Fig. 3: Qualitative results of Match-Any-Events. We compared with VGGT [65], MatchAnything [28] and SuperEvent [9]. Match-Any-Events computes denser matches and achieves lower pose estimation errors. Green and Red indicate inliers and outliers for pose estimation respectively. Methods marked with ⋆ use event frames as input.\", \"page\": 8, \"index\": 4, \"width\": 721, \"height\": 351}]"
motivation: 解决事件相机在宽基线视角下因运动导致的外观显著变化以及缺乏大规模、多样化宽基线标注数据导致的泛化性差问题。
method: 提出一种高效的运动鲁棒注意力骨干网络，结合多时间尺度特征和稀疏令牌选择，并开发了事件运动合成框架以生成大规模训练数据。
result: "在多个基准测试中实现了卓越的零样本跨数据集泛化能力，匹配性能较现有最先进方法提升了37.7%。"
conclusion: 通过创新的架构设计和数据合成方案，该研究成功实现了事件相机在宽基线场景下鲁棒且高效的零样本特征匹配。
---

## 摘要
事件相机由于其对低光照和快速运动的鲁棒性，最近在瞬时运动估计方面展现出巨大的潜力。然而，计算两个任意视图之间的宽基线对应关系仍然是一个重大挑战，因为事件的外观随运动而发生显著变化，且基于学习的方法受到可扩展性和有限的宽基线监督的约束。因此，我们推出了首个以零样本方式实现跨数据集宽基线对应关系的事件匹配模型：一个经过一次训练的模型即可部署在未见数据集上，无需任何目标域微调或适配。为了实现这一能力，我们引入了一个运动鲁棒且计算高效的注意力骨干网络，该网络从事件流中学习多时间尺度特征，并辅以稀疏感知的事件令牌选择，使得在多样化的宽基线监督下进行大规模训练在计算上变得可行。为了提供宽基线泛化所需的监督，我们开发了一个鲁棒的事件运动合成框架，用于生成具有增强视角、模态和运动的大规模事件匹配数据集。在多个基准测试上的广泛实验表明，我们的框架比之前的最佳事件特征匹配方法提高了 37.7%。代码和数据可在以下网址获取：https://github.com/spikelab-jhu/Match-Any-Events。

## Abstract
Event cameras have recently shown promising capabilities in instantaneous motion estimation due to their robustness to low light and fast motions. However, computing wide-baseline correspondence between two arbitrary views remains a significant challenge, since event appearance changes substantially with motion, and learning-based approaches are constrained by both scalability and limited wide-baseline supervision. We therefore introduce the first event matching model that achieves cross-dataset wide-baseline correspondence in a zero-shot manner: a single model trained once is deployed on unseen datasets without any target-domain fine-tuning or adaptation. To enable this capability, we introduce a motion-robust and computationally efficient attention backbone that learns multi-timescale features from event streams, augmented with sparsity-aware event token selection, making large-scale training on diverse wide-baseline supervision computationally feasible. To provide the supervision needed for wide-baseline generalization, we develop a robust event motion synthesis framework to generate large-scale event-matching datasets with augmented viewpoints, modalities, and motions. Extensive experiments across multiple benchmarks show that our framework achieves a 37.7% improvement over the previous best event feature matching methods. Code and data are available at: https://github.com/spikelab-jhu/Match-Any-Events.

---

## 论文详细总结（自动生成）

这是一份关于论文《Match-Any-Events: Zero-Shot Motion-Robust Feature Matching Across Wide Baselines for Event Cameras》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：事件相机在处理宽基线（Wide-baseline）特征匹配时面临两大难题：
    1.  **外观不一致性**：事件的生成高度依赖于相对运动，同一场景在不同运动速度或视角下，其事件流的外观差异巨大。
    2.  **数据匮乏**：现有的事件数据集多为窄基线（如自动驾驶场景），缺乏大规模、多样化视角且带有精确对应关系标注的宽基线数据。
*   **研究目标**：开发首个能够实现**零样本（Zero-shot）**跨数据集泛化的事件匹配模型，使其在未见过的场景、不同的运动速度以及跨模态（事件对图像）任务中均能保持鲁棒性。

### 2. 方法论：核心思想与关键技术
*   **多时间尺度事件表示**：采用对数时间窗口将事件流划分为多个时间仓（Bins），构建事件体素（Voxel），以保留从瞬时到长期的丰富时间信息。
*   **时间聚合 Transformer (TAg)**：
    *   **可分离时空注意力**：为了降低计算复杂度，将全时空注意力分解为空间自注意力和时间自注意力，将复杂度从 $O((THW)^2)$ 降低到 $O(T(HW)^2 + HWT^2)$。
    *   **聚合与精炼**：通过查询最精细时间尺度的特征，并结合其他尺度的键值对，自适应地融合多尺度特征。
*   **稀疏感知事件令牌选择 (SETS)**：
    *   利用事件数据的固有稀疏性，引入一个轻量级 MLP 预测“停止分数”（Halting score）。
    *   通过 Ponder Loss 鼓励模型尽早停止对无信息区域（如模糊或无纹理区）的计算，将空间注意力计算量进一步降低。
*   **匹配阶段**：采用粗到精（Coarse-to-fine）的策略。先在低分辨率特征图上通过双向 Softmax 和互邻域（MNN）筛选匹配点，再在局部窗口内进行亚像素级的精细化回归。

### 3. 实验设计
*   **数据集与场景**：
    *   **E-MegaDepth（合成）**：基于 MegaDepth 图像数据集，通过随机 6-DoF 变换和 Vid2E 模拟生成 300 万对具有宽基线和多样运动的事件流。
    *   **ECM（真实，新收集）**：使用 Prophesee Gen4 和 Flir RGB 相机搭建的异构双目系统，提供真实世界中高精度的事件-图像对应关系。
    *   **M3ED & EDS**：用于验证跨数据集泛化能力的公开基准。
*   **对比方法**：
    *   **事件专用模型**：SuperEvent, EventPoint, RATE。
    *   **图像/跨模态大模型**：MatchAnything, VGGT（通过 E2VID 重建图像后进行对比）。
*   **Benchmark**：主要评估位姿估计的 AUC（5°/10°/20°）和匹配精度（Precision）。

### 4. 资源与算力
*   **训练配置**：使用单块 **NVIDIA H100 (96G)** GPU。
*   **训练时长**：约 **3 天**。
*   **超参数**：Batch size 为 16，输入分辨率为 560×336。
*   **推理效率**：在 NVIDIA RTX 4080 上，处理一对 350×630 的事件数据耗时约 **49ms**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同的数据集上进行了定量评估，涵盖了室内、室外、合成及真实场景。
*   **消融实验**：针对 TAg 模块、多尺度输入、SETS 令牌选择等核心组件进行了详细的对比实验。
*   **鲁棒性测试**：专门设计了不同时间窗口（5ms 到 1000ms）下的性能测试，验证模型对运动速度变化的适应力。
*   **充分性评价**：实验设计较为全面，不仅对比了同类事件算法，还跨领域对比了图像匹配大模型，并展示了在 3D 重建（SfM）中的实际应用效果，客观性较强。

### 6. 主要结论与发现
*   **性能飞跃**：在零样本设置下，该方法在多个基准测试中比此前最先进的事件匹配方法（如 SuperEvent）性能提升了 **37.7%**。
*   **运动鲁棒性**：得益于 TAg 架构，模型在输入时间间隔大幅变化时，性能表现极其平稳，远优于基于固定时间窗口的方法。
*   **跨模态能力**：模型在“事件对图像”匹配任务中同样表现出色，超越了专门针对跨模态设计的 MatchAnything。

### 7. 优点（亮点）
*   **零样本泛化**：解决了事件相机算法难以在不同设备和场景间迁移的痛点。
*   **高效架构**：通过可分离注意力和令牌选择机制，在保持高性能的同时显著降低了 Transformer 处理 3D 时空数据的计算开销。
*   **数据贡献**：发布的 E-MegaDepth 和 ECM 数据集填补了宽基线事件匹配训练数据的空白。

### 8. 不足与局限
*   **极短时间窗口限制**：当输入时间间隔低于 20ms 时，由于事件数量过少、纹理信息严重缺失，模型性能仍会出现明显下降。
*   **计算开销**：尽管进行了优化，但 49ms 的推理时间对于某些超高频（如 >200Hz）的实时闭环控制任务可能仍显不足。
*   **场景依赖**：在完全随机、非结构化的动态场景中，匹配的稳定性可能受到挑战（虽然文中未详细展开，但这是所有特征匹配方法的共性问题）。

（完）
