---
title: Revisiting Token Compression for Accelerating ViT-based Sparse Multi-View 3D Object Detectors
title_zh: 重新审视用于加速基于ViT的稀疏多视图3D目标检测器的Token压缩技术
authors: "Mingqian Ji, Shanshan Zhang, Jian Yang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14563v1"
tags: ["query:evlm"]
score: 9.0
evidence: 用于加速基于ViT的3D目标检测器的标记压缩
tldr: "本研究针对基于ViT的稀疏多视图3D目标检测器推理延迟高的问题，提出了SEPatch3D框架。现有Token压缩方法常导致背景信息丢失和语义不连贯，SEPatch3D通过时空感知补丁大小选择（SPSS）动态调整补丁粒度，并结合信息补丁选择（IPS）与跨粒度特征增强（CGFE）来保留细粒度细节。实验证明，该方法在保持精度的同时，推理速度比StreamPETR提升57%，显著优于现有SOTA方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14563v1/fig-001.webp\", \"caption\": \"Table 1. Comparisons with the state of the art on the nuScenes validation set. We report the inference time of the whole model as shown in the column “Infe. Time” and the time of the backbone only as shown in the column “Back. Time”. The default image resolution is 320 × 800. * and † denote image resolutions of 512 × 1408 and 640 × 1600, respectively. # indicates acceleration in the 3D decoder structure. The difference between our fast and faster variants lies in the different patch sizes, as detailed in the implementation details.\", \"page\": 6, \"index\": 1, \"width\": 1035, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.14563v1/fig-002.webp\", \"caption\": \"Figure 3. Temporal distribution.\", \"page\": 11, \"index\": 2, \"width\": 1030, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2604.14563v1/fig-003.webp\", \"caption\": \"Figure 13. Visualization of detection results between StreamPETR (baseline) and our SEPatch3D-faster. Bounding box colors indicate different object categories.\", \"page\": 12, \"index\": 3, \"width\": 494, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.14563v1/fig-004.webp\", \"caption\": \"Figure 4. Illustration of the proposed Informative Patch Selection (IPS) and Cross-Granularity Feature Enhancement (CGFE) modules. IPS identifies informative patches based on entropy computed from temporally enhanced features. CGFE refines coarse patches by integrating fine-grained details from fine patches.\", \"page\": 5, \"index\": 4, \"width\": 492, \"height\": 339}]"
motivation: 现有的Token压缩策略在加速ViT 3D检测时，容易丢失关键背景信息和细粒度语义，导致检测精度下降。
method: 提出了SEPatch3D框架，利用SPSS动态调整补丁大小，并通过IPS和CGFE技术对粗粒度补丁进行特征增强和细节注入。
result: "在nuScenes和Argoverse 2数据集上，SEPatch3D比StreamPETR基准快57%，比ToC3D-faster效率高20%，且精度相当。"
conclusion: SEPatch3D通过动态补丁调整和跨粒度特征融合，有效平衡了3D目标检测的推理效率与检测精度。
---

## 摘要
基于Vision Transformer (ViT)的稀疏多视图3D目标检测器虽然取得了显著的准确率，但由于繁重的Token处理，仍面临高推理延迟的问题。为了加速这些模型，Token压缩技术已被广泛探索。然而，我们对现有策略（如Token剪枝、合并和增大Patch尺寸）的重新审视发现，它们往往会丢弃有信息量的背景线索，破坏上下文一致性，并丢失细粒度语义，从而对3D检测产生负面影响。为了克服这些局限性，我们提出了SEPatch3D，这是一个在保留粗粒度Patch中关键语义信息的同时，动态调整Patch尺寸的新型框架。具体而言，我们设计了时空感知Patch尺寸选择（SPSS），它为包含近处物体的场景分配小Patch以保留精细细节，为背景主导的场景分配大Patch以降低计算成本。为了进一步减轻潜在的细节损失，信息Patch选择（IPS）选取具有信息量的Patch进行特征精炼，而跨粒度特征增强（CGFE）将细粒度细节注入选定的粗粒度Patch中，从而丰富语义特征。在nuScenes和Argoverse 2验证集上的实验表明，SEPatch3D的推理速度比StreamPETR基准模型快57%，效率比最先进的ToC3D-faster高20%，同时保持了相当的检测准确率。代码可在 https://github.com/Mingqj/SEPatch3D 获取。

## Abstract
Vision Transformer (ViT)-based sparse multi-view 3D object detectors have achieved remarkable accuracy but still suffer from high inference latency due to heavy token processing. To accelerate these models, token compression has been widely explored. However, our revisit of existing strategies, such as token pruning, merging, and patch size enlargement, reveals that they often discard informative background cues, disrupt contextual consistency, and lose fine-grained semantics, negatively affecting 3D detection. To overcome these limitations, we propose SEPatch3D, a novel framework that dynamically adjusts patch sizes while preserving critical semantic information within coarse patches. Specifically, we design Spatiotemporal-aware Patch Size Selection (SPSS) that assigns small patches to scenes containing nearby objects to preserve fine details and large patches to background-dominated scenes to reduce computation cost. To further mitigate potential detail loss, Informative Patch Selection (IPS) selects the informative patches for feature refinement, and Cross-Granularity Feature Enhancement (CGFE) injects fine-grained details into selected coarse patches, enriching semantic features. Experiments on the nuScenes and Argoverse 2 validation sets show that SEPatch3D achieves up to \textbf{57\%} faster inference than the StreamPETR baseline and \textbf{20\%} higher efficiency than the state-of-the-art ToC3D-faster, while preserving comparable detection accuracy. Code is available at https://github.com/Mingqj/SEPatch3D.

---

## 论文详细总结（自动生成）

### 论文总结：重新审视用于加速基于 ViT 的稀疏多视图 3D 目标检测器的 Token 压缩技术 (SEPatch3D)

#### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶领域，基于 Vision Transformer (ViT) 的稀疏多视图 3D 目标检测器（如 StreamPETR）虽然精度高，但由于 ViT 处理大量 Token 带来的计算开销，推理延迟极大。
*   **现有压缩技术的缺陷**：
    *   **Token 剪枝**：容易丢弃背景区域，而背景对于学习“困难负样本（Hard Negatives）”至关重要。
    *   **Token 合并**：不规则的合并会破坏语义一致性。
    *   **增大 Patch 尺寸**：虽然能显著提速，但会导致细粒度语义丢失，影响小目标或远处目标的定位。
*   **研究目标**：在不牺牲背景信息和关键细节的前提下，通过动态调整 Patch 粒度来平衡 3D 检测的精度与速度。

#### 2. 论文提出的方法论：SEPatch3D
论文提出了 **SEPatch3D** 框架，核心思想是“动态调整 Patch 大小 + 关键区域特征增强”。

*   **时空感知 Patch 尺寸选择 (SPSS)**：
    *   利用上一帧目标查询（Object Queries）的 3D 深度信息作为代理指标。
    *   **逻辑**：如果场景中物体较近且正在变大（深度减小），则分配小 Patch（如 18x18）以保留细节；如果物体较远且主导背景，则分配大 Patch（如 22x22）以减少计算。
    *   通过线性回归模型预测深度趋势，确保 Patch 尺寸在帧间切换的平滑性。
*   **信息 Patch 选择 (IPS)**：
    *   在浅层特征阶段，利用**信息熵（Entropy）**来识别包含丰富纹理或边缘的 Patch。
    *   相比于学习分数或注意力权重，熵在语义尚不明确的浅层阶段更能可靠地反映信息量。
*   **跨粒度特征增强 (CGFE)**：
    *   将选中的粗粒度 Patch 作为 Query，对应的原始细粒度（16x16）特征作为 Key/Value，通过**交叉注意力（Cross-Attention）**机制将细节注入粗粒度特征中，弥补因 Patch 增大带来的损失。

#### 3. 实验设计
*   **数据集**：nuScenes（主流自动驾驶基准）和 Argoverse 2（长距离检测基准）。
*   **基准模型 (Benchmark)**：以 **StreamPETR (ViT-L)** 为主要基准。
*   **对比方法**：
    *   经典 3D 检测器：DETR3D, Sparse4D, SparseBEV 等。
    *   SOTA 压缩方法：ToC3D (ECCV 2024), tgGBC (ICCV 2025)。
    *   2D 压缩迁移方法：SparseDETR, Cropr, SViT。

#### 4. 资源与算力
*   **训练算力**：使用 **4 张 NVIDIA RTX 3090 GPU**。
    *   nuScenes 训练 36 个 epoch（前 24 轮训练基础检测器，后 12 轮训练新增模块）。
    *   Argoverse 2 训练 12 个 epoch。
*   **推理算力**：所有推理延迟测试均在**单张 RTX 3090 GPU** 上完成，以确保公平性。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的实验，包括在两个大型数据集上的性能对比、针对不同分辨率（320x800 和 640x1600）的测试。
*   **消融实验**：非常充分，涵盖了 SPSS、IPS、CGFE 各组件的贡献、Patch 尺寸多样性的影响、熵选择策略的优越性、以及 IPS 模块放置位置的讨论。
*   **泛化性验证**：作者将该方法应用于 DETR3D 和 Sparse4Dv2 等不同检测框架，并测试了 ViT-B 和 ViT-L 等不同骨干网络，证明了方法的通用性。实验设计客观、对比基准统一。

#### 6. 主要结论与发现
*   **提速显著**：在 nuScenes 高分辨率下，SEPatch3D 比 StreamPETR 基准提速 **57.7%**，且 NDS 仅下降 0.3%。
*   **效率领先**：比目前最先进的 ToC3D-faster 还要快 **20%**，且精度更优。
*   **背景的重要性**：定量实验证明，保留背景 Patch（而非剪枝）能显著降低误报率（mFP），验证了作者“不剪枝、只变粗”的策略正确性。
*   **动态平衡**：SPSS 模块能根据场景深度自动适配计算资源，实现了计算量的按需分配。

#### 7. 优点（亮点）
*   **策略创新**：避开了主流的“剪枝/合并”路线，通过“动态 Patch 尺寸 + 跨粒度增强”解决了 3D 检测中背景与细节并存的难题。
*   **时空耦合**：巧妙利用 3D 检测特有的 Query 深度信息来指导 2D 图像层的 Token 压缩，实现了任务层与特征层的联动。
*   **实用性强**：提出了“预算感知分析（Budget-Aware Analysis）”，通过多项式回归拟合精度-速度曲线，帮助开发者在部署时快速选择最优 Patch 配置。

#### 8. 不足与局限
*   **启发式阈值**：Patch 尺寸的选择仍依赖于预设的深度阈值 $\theta$，在极端复杂或罕见场景下，这种启发式规则可能不是最优的。
*   **粒度限制**：目前仅在两种预设的 Patch 尺寸间切换，未来可探索更连续或更自动化的粒度生成方式。
*   **硬件适配**：虽然在 GPU 上表现优异，但在端侧专用芯片（如 DSP/NPU）上的算子支持和加速效果尚未验证。

（完）
