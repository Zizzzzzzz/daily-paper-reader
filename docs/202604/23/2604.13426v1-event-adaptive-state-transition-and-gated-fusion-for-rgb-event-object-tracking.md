---
title: Event-Adaptive State Transition and Gated Fusion for RGB-Event Object Tracking
title_zh: 用于 RGB-事件目标跟踪的事件自适应状态转移与门控融合
authors: "Jinlin You, Muyu Li, Xudong Zhao"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13426v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 用于 RGB-事件对象跟踪的事件自适应状态转移
tldr: 针对现有基于Vision Mamba的RGB-Event跟踪方法中静态状态转移矩阵无法适应事件流稀疏度变化的问题，本文提出MambaTrack框架。该框架引入动态状态空间模型，通过事件自适应状态转移机制动态调节演化速率，并结合门控投影融合模块，利用事件密度和RGB置信度实现鲁棒的跨模态特征集成，在保持轻量化的同时显著提升了跟踪性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13426v1/fig-001.webp\", \"caption\": \"Figure 1: Illustration of the proposed DSSM module with the event-adaptive state transition mechanism. Based on the standard State Space Model (SSM), our DSSM module incorporates an eventadaptive state transition mechanism (highlighted in red), which dynamically modulates the transition dynamics according to the event stream, enabling better temporal modeling under varying event sparsity.\", \"page\": 2, \"index\": 1, \"width\": 498, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.13426v1/fig-002.webp\", \"caption\": \"Table 2: Tracking results on FE108 dataset\", \"page\": 8, \"index\": 2, \"width\": 490, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.13426v1/fig-003.webp\", \"caption\": \"Figure 2: Overall architecture of our proposed MambaTrack. The framework consists of two main components: feature extraction and feature fusion. The feature extraction branch includes (a) the RGB Mamba Module and (b) the Event Mamba Module, which are designed to extract modalityspecific features. These features are then adaptively integrated in (c) the Gated Projection Fusion (GPF) Module to enhance cross-modal representation.\", \"page\": 4, \"index\": 3, \"width\": 829, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.13426v1/fig-004.webp\", \"caption\": \"Figure 3: Tracking results of our MambaTrack under 4 challenging conditions on FELT SOT dataset.\", \"page\": 9, \"index\": 4, \"width\": 498, \"height\": 467}]"
motivation: 现有的Mamba跟踪方法因使用静态状态转移矩阵，难以应对事件流稀疏度变化导致的建模失衡和融合鲁棒性下降问题。
method: 提出了包含事件自适应状态转移机制和门控投影融合模块的动态状态空间模型框架MambaTrack。
result: 实验表明MambaTrack在FE108和FELT数据集上达到了最先进的性能，且具有实时嵌入式部署的潜力。
conclusion: 该研究通过动态调节状态演化和跨模态融合强度，有效解决了事件流密度波动带来的挑战，提升了多模态跟踪的鲁棒性。
---

## 摘要
现有的基于 Vision Mamba 的 RGB-事件（RGBE）跟踪方法由于使用静态状态转移矩阵，无法适应事件稀疏性的变化。这种僵化导致建模不平衡——对稀疏事件流欠拟合，而对密集事件流过拟合——从而降低了跨模态融合的鲁棒性。为解决这些局限性，我们提出了 MambaTrack，这是一个基于动态状态空间模型（DSSM）的多模态高效跟踪框架。我们的贡献主要包括两个方面：首先，我们引入了一种事件自适应状态转移机制，根据事件流密度动态调节状态转移矩阵。一个可学习的标量控制状态演化速率，从而实现对稀疏和密集事件流的差异化建模。其次，我们开发了一个门控投影融合（GPF）模块，用于鲁棒的跨模态集成。该模块将 RGB 特征投影到事件特征空间，并根据事件密度和 RGB 置信度分数生成自适应门控。这些门控精确控制融合强度，在抑制噪声的同时保留互补信息。实验表明，MambaTrack 在 FE108 和 FELT 数据集上达到了最先进的性能。其轻量化设计表明了在实时嵌入式部署方面的潜力。

## Abstract
Existing Vision Mamba-based RGB-Event(RGBE) tracking methods suffer from using static state transition matrices, which fail to adapt to variations in event sparsity. This rigidity leads to imbalanced modeling-underfitting sparse event streams and overfitting dense ones-thus degrading cross-modal fusion robustness. To address these limitations, we propose MambaTrack, a multimodal and efficient tracking framework built upon a Dynamic State Space Model(DSSM). Our contributions are twofold. First, we introduce an event-adaptive state transition mechanism that dynamically modulates the state transition matrix based on event stream density. A learnable scalar governs the state evolution rate, enabling differentiated modeling of sparse and dense event flows. Second, we develop a Gated Projection Fusion(GPF) module for robust cross-modal integration. This module projects RGB features into the event feature space and generates adaptive gates from event density and RGB confidence scores. These gates precisely control the fusion intensity, suppressing noise while preserving complementary information. Experiments show that MambaTrack achieves state-of-the-art performance on the FE108 and FELT datasets. Its lightweight design suggests potential for real-time embedded deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Event-Adaptive State Transition and Gated Fusion for RGB-Event Object Tracking》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：传统的 RGB 摄像头在高速运动（运动模糊）和极端光照（低动态范围）下表现不佳。事件相机（Event Camera）具有微秒级响应和超高动态范围，能提供互补的运动信息。
*   **核心问题**：现有的基于 Vision Mamba 的 RGB-Event 跟踪方法使用**静态状态转移矩阵**。由于事件流的密度随运动剧烈变化（静止时稀疏，运动时密集），静态矩阵会导致对稀疏流欠拟合、对密集流过拟合，从而削弱跨模态融合的鲁棒性。
*   **研究目标**：开发一种能够根据事件流密度动态调整建模粒度，并能智能融合异构数据的轻量化跟踪框架。

### 2. 方法论：MambaTrack 框架
*   **核心思想**：构建一个基于**动态状态空间模型（DSSM）**的框架，通过感知事件密度来实时调整 Mamba 的状态演化速率，并利用门控机制实现双向特征融合。
*   **关键技术细节**：
    *   **事件自适应状态转移机制**：引入事件密度参数 $\rho_t$，通过可学习矩阵投影和 Sigmoid 激活生成动态缩放因子 $\beta$。结合历史状态 $A_{t-1}$ 和基础矩阵 $A_{base}$，利用可学习标量 $\alpha$ 进行加权融合，生成当前的状态转移矩阵 $A_t$。这使得模型能根据事件脉冲的疏密程度差异化建模。
    *   **门控投影融合（GPF）模块**：
        1.  **空间对齐**：使用轻量级 MLP 将 RGB 特征投影到事件特征空间。
        2.  **自适应门控**：根据事件密度和 RGB 特征的 $L_2$ 范数（代表置信度）生成门控系数 $G$。
        3.  **双向融合**：利用门控系数精确控制融合强度，在抑制噪声（如事件相机在静止时的热噪声）的同时保留互补的纹理和运动信息。
    *   **输入表示**：将异步事件流通过线性插值转化为时空体（Voxel Grids），实现与 RGB 帧的时间对齐。

### 3. 实验设计
*   **数据集**：
    *   **FELT**：目前最大的长时帧-事件跟踪数据集，包含 742 个序列，涵盖 45 个类别和 14 种挑战属性。
    *   **FE108**：包含 108 个视频对，主要针对室内挑战（如光照变化、运动模糊）。
*   **评估指标**：成功率（SR）、精确率（PR）和归一化精确率（NPR）。
*   **对比方法**：包括传统的 Transformer 跟踪器（如 TransT, OSTrack, ViPT）和专门的 RGB-E 跟踪器（如 AFNet, CMT-ATOM, Mamba-FETrack 等）。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA RTX 4090 GPU** 进行训练。
*   **软件框架**：基于 PyTorch 实现。
*   **超参数**：使用 AdamW 优化器，初始学习率 0.0004，Batch Size 为 48。
*   **训练时长**：文中未明确提及具体的总训练时长，但强调了其轻量化设计和高效的推理潜力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准数据集上进行了完整的性能对比。
    *   **消融实验**：针对单模态（仅 RGB/仅 Event）、DSSM 模块的有效性、GPF 融合模块的有效性分别进行了 5 组对比实验。
    *   **可视化分析**：展示了在小目标、背景干扰、部分遮挡和快速运动四种挑战场景下的跟踪效果。
*   **充分性评价**：实验设计较为客观，涵盖了从宏观性能到微观模块贡献的验证。对比对象既包含经典的 Transformer 架构，也包含最新的 Mamba 变体，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：MambaTrack 在 FELT 数据集上 SR 达到 42.5%，PR 达到 54.0%，显著优于 ViPT 等强力竞争者。在 FE108 上也表现出极强的鲁棒性。
*   **动态建模的必要性**：消融实验证明，引入 DSSM 动态状态转移机制后，模型处理运动突变和事件稀疏性的能力显著增强。
*   **融合策略的有效性**：GPF 模块通过密度感知门控，有效解决了异构数据间的噪声干扰问题。

### 7. 优点与亮点
*   **创新性建模**：首次在 RGB-E 跟踪中提出“事件自适应”的状态转移概念，解决了 Mamba 静态矩阵与事件流动态性不匹配的痛点。
*   **轻量化与高效**：相比 Transformer 的二次方复杂度，Mamba 的线性复杂度使其更适合嵌入式和实时应用。
*   **鲁棒性强**：通过门控机制智能平衡两种模态，在极端光照和高速运动下表现稳定。

### 8. 不足与局限
*   **事件表示单一**：目前主要依赖线性插值的时空体表示，可能未完全挖掘事件流的原始异步特性（如使用脉冲神经网络 SNN 可能更契合）。
*   **极端环境下的偏差**：虽然引入了门控，但在极低光照且极高速运动（双模态均受损）的极端边缘情况下，模型性能仍有提升空间。
*   **跟踪头设计**：目前沿用了 OSTrack 的标准跟踪头，未针对事件数据的特性专门优化预测分支。

（完）
