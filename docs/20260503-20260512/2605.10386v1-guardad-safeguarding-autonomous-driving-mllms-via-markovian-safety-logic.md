---
title: "GuardAD: Safeguarding Autonomous Driving MLLMs via Markovian Safety Logic"
title_zh: GuardAD：通过马尔可夫安全逻辑保障自动驾驶多模态大语言模型
authors: "Tianyuan Zhang, Peng Yue, Zihao Peng, Jiangfan Liu, Zonghao Ying, Jiakai Wang, Tianlin Li, Jian Yang, Yaodong Yang, Aishan Liu, Xianglong Liu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10386v1"
tags: ["query:mm-trf"]
score: 10.0
evidence: 在交通环境中保护自动驾驶多模态大模型
tldr: 针对多模态大语言模型（MLLM）在自动驾驶中面临的安全威胁及现有静态防御机制缺乏时序推理的问题，本文提出GuardAD。该框架将自动驾驶安全建模为演化的马尔可夫逻辑状态，通过神经符号逻辑形式化和高阶马尔可夫逻辑归纳，实现对动态交通场景中潜在风险的持续推理。GuardAD不仅能识别危险，还能通过逻辑驱动的动作修正引导模型优化决策，在显著降低事故率的同时提升了任务性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10386v1/fig-001.webp\", \"caption\": \"Figure 1: Illustration of GUARDAD, a safeguard that captures the dynamic nature of autonomous driving and mitigates accident-prone decisions.\", \"page\": 1, \"index\": 1, \"width\": 450, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.10386v1/fig-002.webp\", \"caption\": \"Table 2: Main results on the DriveLM benchmark for different AD-MLLMs, comparing the vanilla model with safeguard methods (RF, CM, SA, GA). We report the standard deviation (σ) and the change (∆). The GA (ours) columns are shaded in gray. Improvements are highlighted in red.\", \"page\": 7, \"index\": 2, \"width\": 946, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2605.10386v1/fig-003.webp\", \"caption\": \"Figure 2: Framework of GUARDAD. GUARDAD is a model-agnostic safeguard that formulates AD safety as an evolving Markovian logical state. It constructs the safety state via Neuro-Symbolic Logic Formalization, induces a temporally consistent state through n-th order Markovian Logic Induction, and then performs Logic-Driven Action Revision to correct unsafe actions without modifying the AD-MLLMs.\", \"page\": 4, \"index\": 3, \"width\": 926, \"height\": 458}]"
motivation: 现有的自动驾驶大模型安全机制多依赖静态逻辑，难以在动态演化的交通环境中进行有效的时序安全推理。
method: 提出一种模型无关的GuardAD框架，利用神经符号逻辑和马尔可夫逻辑归纳来推理潜在危险，并据此修正不安全动作。
result: "实验表明GuardAD在多个基准测试中将事故率降低了32.07%，同时将任务性能提升了6.85%。"
conclusion: GuardAD通过引入动态逻辑推理，有效增强了自动驾驶大模型在复杂动态环境下的安全性和鲁棒性。
---

## 摘要
多模态大语言模型（MLLMs）正越来越多地集成到自动驾驶（AD）系统中；然而，它们在面对各种安全威胁时仍然十分脆弱，特别是在易发生事故的场景中。最近的保障机制通过引入逻辑约束展现出了潜力，但大多数机制依赖于静态公式，缺乏对不断演变的交通交互进行基于时间的安全性推理，导致在动态驾驶环境中的鲁棒性有限。为了解决这些局限性，我们提出了 GuardAD，这是一种与模型无关的保障机制，它将自动驾驶安全性建模为一种演化的马尔可夫逻辑状态。GuardAD 引入了神经符号逻辑形式化，它表示了针对异构交通参与者的安全谓词，并通过 n 阶马尔可夫逻辑归纳对其进行持续归纳。这种设计使得能够推断出超出单步观测范围的突发和潜在危险。GuardAD 并非简单地否决不安全动作，而是执行逻辑驱动的动作修正，其中推断出的安全状态在不修改底层 MLLM 的情况下主动引导动作优化。在多个基准测试和自动驾驶 MLLM 上进行的大量实验表明，GuardAD 在显著降低事故率（-32.07%）的同时，还略微提升了任务性能（+6.85%）。此外，闭环仿真评估以及物理世界车辆研究进一步验证了 GuardAD 的有效性和潜力。

## Abstract
Multimodal large language models (MLLMs) are increasingly integrated into autonomous driving (AD) systems; however, they remain vulnerable to diverse safety threats, particularly in accident-prone scenarios. Recent safeguard mechanisms have shown promise by incorporating logical constraints, yet most rely on static formulations that lack temporally grounded safety reasoning over evolving traffic interactions, resulting in limited robustness in dynamic driving environments. To address these limitations, we propose GuardAD, a model-agnostic safeguard that formulates AD safety as an evolving Markovian logical state. GuardAD introduces Neuro-Symbolic Logic Formalization, which represents safety predicates over heterogeneous traffic participants and continuously induces them via n-th order Markovian Logic Induction. This design enables the inference of emerging and latent hazards beyond single-step observations. Rather than simply vetoing unsafe actions, GuardAD performs Logic-Driven Action Revision, where inferred safety states actively guide action refinement without modifying the underlying MLLM. Extensive experiments on multiple benchmarks and AD-MLLMs demonstrate that GuardAD substantially reduces accident rates (-32.07%) while slightly improving task performance (+6.85%). Moreover, closed-loop simulation evaluations, together with physical-world vehicle studies, further validate the effectiveness and potential of GuardAD.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GuardAD** 的模型无关（model-agnostic）安全保障框架，旨在增强自动驾驶多模态大语言模型（AD-MLLMs）在动态交通环境中的安全性。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：虽然 AD-MLLMs 在感知和规划方面表现出色，但在面对复杂、动态的交通场景时仍存在显著的安全风险。
*   **研究动机**：现有的逻辑驱动安全机制大多依赖于**静态**的预定义约束（如“红灯停”），缺乏对交通交互演化过程的**时序推理**能力。这种局限性导致模型难以识别那些由多个时间步累积而成的潜在危险（如突然横穿的行人或侧方车辆的逼近）。
*   **整体含义**：GuardAD 将自动驾驶安全重新定义为一个演化的**马尔可夫逻辑状态**，通过引入时间维度，使模型能够识别并修正单帧观测无法发现的隐性风险。

### 2. 方法论
GuardAD 的核心流程分为三个阶段：
*   **神经符号逻辑形式化（Neuro-Symbolic Logic Formalization）**：
    *   将神经网络的感知信号（如目标检测、运动趋势）转化为结构化的符号谓词。
    *   谓词分为四类：动作谓词（如减速）、环境谓词（如红灯）、目标存在谓词（如左前方有车）和目标运动谓词（如自行车靠近）。
*   **n 阶马尔可夫逻辑归纳（n-th order Markovian Logic Induction）**：
    *   利用马尔可夫逻辑网络（MLN）处理过去 $n$ 个时间步的逻辑状态。
    *   通过聚合多步证据，推断出当前时刻应激活的安全约束（$\hat{Z}_t$），从而捕捉动态演变的风险模式。
*   **逻辑驱动的动作修正（Logic-Driven Action Revision）**：
    *   **违规检测**：检查 AD-MLLM 提出的初始动作是否违反了推断出的安全约束。
    *   **最小干预修正**：若发生违规，GuardAD 不直接否决，而是将约束转化为辅助提示词（Prompt），引导 MLLM 在不修改模型参数的情况下重新生成安全的动作。

### 3. 实验设计
*   **数据集/场景**：
    *   **VRU-Accident**：专注于易发生事故的场景（涉及行人、骑行者等弱势群体）。
    *   **DriveLM**：用于评估通用驾驶查询和决策性能。
*   **对比方法（Baselines）**：
    *   **SafeAuto (SA)**：最相关的自动驾驶逻辑保障基准。
    *   **RoboFactory (RF)** 与 **Code-as-Monitor (CM)**：具身智能领域的代表性保障机制。
*   **评估模型**：DriveLM、Dolphins、EM-VLM4AD。
*   **评估指标**：GPT/Qwen 评分（任务性能）、GPT/Qwen 事故率（安全性）。
*   **闭环与物理验证**：在 CARLA 仿真器中进行闭环测试，并在实验室环境下使用物理车辆进行人机共驾（Human-in-the-loop）研究。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **8 张 NVIDIA A800 80GB PCIe GPU** 的服务器上运行。
*   **其他参数**：拥有 128 核 Intel Xeon 8358 CPU 和 1TB RAM。
*   **训练/推理时长**：文中未明确给出总训练时长，但提供了效率分析：在 DriveLM 上平均推理延迟约为 1.77 秒，在 Dolphins 上约为 3.54 秒，表明其引入的计算开销较小。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流 AD-MLLM 模型，在 2 个大型基准测试上进行了对比。
*   **重复性**：每组实验重复 10 次并取平均值，提供了标准差（$\sigma$），确保了结果的统计显著性。
*   **消融实验**：针对不同模块（谓词类型、修正策略）、提示词生成器（GPT-4o, Qwen-3 等）以及超参数（窗口长度 $n$ 和 $k$）进行了详尽的消融研究。
*   **客观性**：实验不仅包含离线数据集评估，还扩展到了闭环仿真、对抗攻击防御测试以及物理世界验证，评估维度全面且客观。

### 6. 主要结论与发现
*   **安全性显著提升**：GuardAD 将平均事故率降低了 **32.07%**。
*   **性能不降反升**：在提升安全性的同时，任务性能评分（GS）提升了 **6.85%**，证明了逻辑引导有助于模型更好地理解场景。
*   **动态推理的必要性**：在处理“反应性参与者”（如突然切入的车辆）导致的失败时，GuardAD 显著优于依赖静态规则的 SafeAuto。
*   **鲁棒性增强**：在面对对抗攻击（如 CoA, ADvLM）时，GuardAD 依然能有效降低事故率。

### 7. 优点
*   **模型无关性**：作为后处理插件，无需重新训练或微调底层的 MLLM，具有极强的通用性。
*   **时序逻辑建模**：引入马尔可夫逻辑归纳，解决了自动驾驶中关键的“动态风险预测”问题。
*   **神经符号结合**：结合了神经网络的感知能力和符号逻辑的可解释性/严谨性。
*   **闭环与实车验证**：实验不仅停留在离线指标，还通过仿真和物理实验证明了实际应用潜力。

### 8. 不足与局限
*   **感知依赖性**：GuardAD 的效果高度依赖于底层感知栈（实体检测、谓词提取）的准确性，感知错误会导致逻辑推理失效。
*   **计算开销**：当检测到违规时，需要进行二次推理（重新请求 MLLM），这在实时性要求极高的自动驾驶场景中可能存在延迟风险。
*   **覆盖范围**：目前的谓词和规则集虽然涵盖了常见风险，但仍难以完全覆盖所有“长尾”场景或复杂的驾驶意图。

（完）
