---
title: Visual Text Compression as Measure Transport
title_zh: 视觉文本压缩作为测度传输
authors: "Lv Tang, Tianyi Zheng, Yang Liu, Bo Li, Xingyu Li"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.06708v1"
tags: ["query:evlm"]
score: 9.0
evidence: 用于高效长上下文处理的视觉文本压缩
tldr: 本研究针对视觉文本压缩（VTC）在长文本处理中效率高但性能不稳定的问题，提出了一种基于测度传输（Measure Transport）的理论框架。通过将文本和视觉标记视为概率测度，量化了视觉编码过程中的信息损失（精度与覆盖成本）。基于此，研究开发了无标签路由准则和自适应注视机制，在显著减少Token消耗的同时，提升了模型在多项NLP任务上的表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06708v1/fig-001.webp\", \"caption\": \"Figure 3: TE decision plane at 4B scale. Each of 24 benchmarks is placed at its (VCR(x), ISR(x)) coordinates and coloured by the oracle preference. The dashed curve is the TE(x) = τ contour, a hyperbola because TE(x) = ISR(x) · VCR(x). The two shaded regions are the select-visual and select-text assignments of the rule TE(x) ≥ τ , and decision errors are marked with an ×. For oracle labeling, the visual arm is the better of VLM and VLM+Fov, matching the upper-bound definition in Tab. 2. The single scalar τ is fixed across all benchmarks. The rule is correct on 17/24 datasets (70.8%) at Qwen3-4B, raises the macro-average score from 45.9 to 47.4 (+1.5 points, +3.3% relative), and uses 10.3% fewer average input tokens than the LLM-only baseline model.\", \"page\": 6, \"index\": 1, \"width\": 412, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-002.webp\", \"caption\": \"Figure 11: MultiFieldQA example 2. The three panels show the rendered page, the normalized patch-cost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 43, \"index\": 2, \"width\": 815, \"height\": 1178}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-003.webp\", \"caption\": \"Table 12: Per-family theory-data validation. Predicted regimes are derived from the dominant term of C(x) in Eq. 8. Pred. sign denotes the expected LLM/VLM gap direction. “+” means LLM wins, “−” means VLM is non-degraded or better, and “N/A” means no fixed prediction for the mixed regime. The ∆ columns report observed family-average gaps at Qwen3-4B, Qwen3-8B, and Qwen3-32B. Positive values mean LLM beats VLM. Family ∆ values are computed from full-precision LLM and VLM scores and rounded to two decimals for display. Recomputing ∆ from the 1dp scores in Tab. 13 can give values differing by up to 0.06 across the 18 cells of this table due to per-dataset rounding accumulation.\", \"page\": 25, \"index\": 3, \"width\": 827, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-004.webp\", \"caption\": \"Figure 6: QASPER example 3. The three panels show the rendered page, the normalized patchcost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 38, \"index\": 4, \"width\": 815, \"height\": 1177}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-005.webp\", \"caption\": \"Table 13: Per-dataset performance at the 4B, 8B, and 32B scales for the three inference strategies considered in Sec. 4: text-only LLM, text-rendered-as-image VLM, and VLM with localized inverse transport (Foveation, FOV). Within each dataset and scale, the best score among the three methods is boldfaced. Macro rows aggregate over S2/S3 (the 4B visual-friendly partition held fixed across all three scale columns; per-scale partitions are reported separately in App. J) and over all 24 datasets.\", \"page\": 26, \"index\": 5, \"width\": 828, \"height\": 814}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-006.webp\", \"caption\": \"Figure 7: HotpotQA example 1. The three panels show the rendered page, the normalized patchcost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 39, \"index\": 6, \"width\": 815, \"height\": 960}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-007.webp\", \"caption\": \"Table 8: Per-tier VLM/LLM accuracies and fitted (α, β) across model scales. “Low/Med/High” are the three tier values of W for the α probe or L for the β probe. The gap g is max(0, 1 − VLMavg/LLMavg). Each tier aggregates 160 trials per model for the α probe and 120 for the β probe. The fitted parameter is a no-intercept least-squares fit of g on the tier value, with R2 measuring the fraction of g2 explained by the linear fit.\", \"page\": 22, \"index\": 7, \"width\": 828, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-008.webp\", \"caption\": \"Figure 10: MultiFieldQA example 1. The three panels show the rendered page, the normalized patch-cost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 42, \"index\": 8, \"width\": 815, \"height\": 1178}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-009.webp\", \"caption\": \"Figure 1: Left: the text-only baseline. Right: our VTC framework. Given x, we first compute labelfree probes (W,L,TRR,VCR, γ), combine them into the transport-efficiency score TE(x), and route the instance under the rule TE(x) ≥ τ (Sec. 3.2). If the visual path is selected, x is rendered to an image, encoded by the ViT push-forward map, and read by a VLM augmented with foveation that re-encodes high-Cq patches (Sec. 3.3). Across the 24-benchmark suite, the label-free rule matches the per-dataset oracle on 17/24 datasets (70.8%), raises the macro-average score from 45.9 to 47.4 (+1.5 points, +3.3% relative), and uses 10.3% fewer average input tokens than pure-LLM.\", \"page\": 3, \"index\": 9, \"width\": 826, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-010.webp\", \"caption\": \"Table 19: QASPER foveation examples corresponding to Figs. 4–6. The QASPER evaluation pipeline records F1 only, so predicted text is omitted. Long strings are truncated.\", \"page\": 35, \"index\": 10, \"width\": 826, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-011.webp\", \"caption\": \"Table 22: Per-dataset foveation trigger rate at three scales together with the resulting per-dataset ∆Fov from Tab. 18. The trigger rate is the fraction of samples for which the foveation trigger of Eq. 16 fires. The macro-average row averages over all 24 datasets at each scale.\", \"page\": 46, \"index\": 11, \"width\": 828, \"height\": 756}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-012.webp\", \"caption\": \"Figure 13: Per-dataset relationship between the foveation trigger rate and the foveation gain ∆Fov at each backbone scale. Each point is one benchmark, colored by task family. Datasets with near-zero trigger rate cluster around ∆Fov = 0 at every scale. Datasets with high trigger rate carry most of the scale-dependent positive and negative ∆Fov changes, including the largest 32B short-answer-QA gains and the largest 8B short-answer-QA losses.\", \"page\": 45, \"index\": 12, \"width\": 821, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-013.webp\", \"caption\": \"Table 14: Decision-rule accuracy versus threshold τ across model scales. The 4B column reports the standard TE rule used for the main result. The 32B column uses the bounded-benefit variant defined in App. F.2. Bolded rows mark per-scale diagnostic peaks. The sweep shows that τ = 1.28 is stable for the main 4B setting, while larger-scale behavior is diagnostic of scale-dependent probe regimes.\", \"page\": 28, \"index\": 13, \"width\": 828, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-014.webp\", \"caption\": \"Table 25: Macro-average scores at the Qwen3-4B / InternVL3.5-4B scale. “LLM” is text-only Qwen3-4B. “Qwen-VL” is Qwen3-VL-4B, “+Fov” is Qwen-VL with foveation, and “InternVL” is InternVL3.5-4B baseline. ∆Int is InternVL minus Qwen-VL. The S2/S3 split is the 4B visualfriendly partition (10 datasets visual-wins, 14 datasets text-wins, See Tab. 13).\", \"page\": 50, \"index\": 14, \"width\": 764, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-015.webp\", \"caption\": \"Table 2: Main results at the Qwen3-4B scale. Scores are macro-averages of per-dataset metrics (Tab. 5 in App. C). Tokens are the mean input-token count per sample. Oracle-routed picks the better of LLM and the best visual arm post hoc and gives an upper bound (UB).\", \"page\": 8, \"index\": 15, \"width\": 827, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-016.webp\", \"caption\": \"Figure 8: HotpotQA example 2. The three panels show the rendered page, the normalized patchcost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 40, \"index\": 16, \"width\": 815, \"height\": 1177}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-017.webp\", \"caption\": \"Figure 4: QASPER example 1. The three panels show the rendered page, the normalized patchcost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 36, \"index\": 17, \"width\": 815, \"height\": 1177}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-018.webp\", \"caption\": \"Table 27: Macro-average scores at the Qwen3-32B / InternVL3.5-38B scale. Both models share the same Qwen3-32B LLM backbone, so visual-path differences primarily reflect the InternViT-vsQwen-VL encoder swap. The S2/S3 split is the 32B visual-friendly partition: S2 at 32B contains 11 datasets where the visual arm (max of VLM and FOV) wins LLM under the bolding rule of Tab. 13 at 32B. Compared to 8B, race, multifieldqa, and qmsum enter S2, while drop remains outside S2 as the 32B LLM closes the precision gap. All scores are in percent.\", \"page\": 51, \"index\": 18, \"width\": 764, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-019.webp\", \"caption\": \"Table 26: Macro-average scores at the Qwen3-8B / InternVL3.5-8B scale. The S2/S3 split is the 8B visual-friendly partition: S2 at 8B contains 8 datasets where the visual path wins under the bolding rule of Tab. 13 at 8B. This is the 4B set minus drop and race, since both flip to S3 at 8B as the 8B LLM closes the gap. All scores are in percent.\", \"page\": 51, \"index\": 19, \"width\": 764, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-020.webp\", \"caption\": \"Table 18: Per-dataset foveation gain at three scales. Each scale shows two columns: the baseline VLM score and the FOV minus VLM gap in absolute points. Positive ∆Fov means foveation improves the visual path on that dataset. The All row reports paired averages over all 24 datasets at each scale after applying the trigger-zero convention in Sec. G.5.\", \"page\": 32, \"index\": 20, \"width\": 827, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-021.webp\", \"caption\": \"Figure 12: MultiFieldQA example 3. The three panels show the rendered page, the normalized patch-cost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 44, \"index\": 21, \"width\": 815, \"height\": 1178}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-022.webp\", \"caption\": \"Figure 9: HotpotQA example 3. The three panels show the rendered page, the normalized patchcost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 41, \"index\": 22, \"width\": 815, \"height\": 1105}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-023.webp\", \"caption\": \"Figure 5: QASPER example 2. The three panels show the rendered page, the normalized patchcost overlay, and the selected crop box used by foveation. Hot patches mark question-keyword concentration after redundancy is discounted, not literal answer locations.\", \"page\": 37, \"index\": 23, \"width\": 815, \"height\": 1177}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-024.webp\", \"caption\": \"Table 28: Per-dataset cross-architecture gain ∆Int = InternVL−Qwen-VL at three scales. Positive values mean InternVL outperforms Qwen-VL on that dataset. The 32B column compares Qwen3VL-32B against InternVL3.5-38B (both share the Qwen3-32B LLM). Tab. 25, Tab. 26, and Tab. 27 report the macro and family-level aggregates from the same data. The win/loss/tie counts cited in this section use a tie threshold of |∆Int| ≤ 0.5. Per-dataset ∆Int values are rounded to one decimal from full-precision differences. Their 24-row average can differ from the macro ∆Int in Tabs. 25–27 by up to ±0.1 when the full-precision macro lies near a 1dp display boundary. This affects the 32B row of the macro tables: the 24-row average of the per-dataset values shown here is +0.83, which rounds to +0.8, while subtracting the 1dp macros in Tab. 27 gives +0.9.\", \"page\": 52, \"index\": 24, \"width\": 540, \"height\": 756}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-025.webp\", \"caption\": \"Figure 2: The label-free proxy C(x) tracks the labelled operational gap ∆(x) on 24 NLP benchmarks with the Qwen3-4B backbone (LLM and VLM). (a) Mean ∆(x) = stext − svis within each C(x) tertile, with 8 datasets per bin and error bars of ±1 standard error. ∆ is reported in the native task metric, all on the same 0 to 100 scale. The mean ∆ increases monotonically from −6.9 in the low-C tertile to +10.1 in the high-C tertile, showing that the label-free proxy orders the C(x) tertiles consistently with the labelled operational gap. (b) Per-dataset decomposition of C(x) into its intra-patch component ∆intra(x) = α ·W (x) (Orange) and inter-patch component ∆inter(x) = β · L(x) (1− TRR(x)) (Green), matching Eq. 8. The composition varies systematically with task structure. ∆inter is largest on long-document summarization such as CNN/DM [15], XSum [32], GovReport [17], and BillSum [20], and on several high-coverage reasoning or classification inputs such as LogiQA [25] and BoolQ [10]. ∆intra remains substantial on precision-sensitive tasks such as QuALITY [34], RACE-H [21], ContractNLI [19], and IMDB [30]. This empirical separation is consistent with the factorization T# = Tinter ◦ Tintra in Eq. 6.\", \"page\": 5, \"index\": 25, \"width\": 819, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2605.06708v1/fig-026.webp\", \"caption\": \"Table 24: Runtime and peak-memory measurements on six representative benchmarks at the 4B scale. Latency is reported as seconds per sample. Peak memory is measured as GPU memory in GB. The routed strategy uses the label-free routing decision and reports the measured latency and memory of the selected path. The macro-average row is the unweighted average over the six datasets.\", \"page\": 48, \"index\": 26, \"width\": 828, \"height\": 250}]"
motivation: 视觉文本压缩虽然能大幅减少Token数量，但其带来的信息损失难以量化，导致下游任务性能表现不稳定。
method: 将VTC建模为测度传输问题，通过计算ViT编码器产生的精度成本和覆盖成本来评估信息损失，并据此实现路径路由与局部高分辨率重编码。
result: "在24个NLP数据集上，该方法在减少10.3% Token消耗的同时，将平均任务得分提升了3.3%，且路由准确率达到70.8%。"
conclusion: 测度传输理论为理解和优化视觉文本压缩提供了有效工具，实现了效率与性能的更佳平衡。
---

## 摘要
视觉文本压缩 (VTC) 通过将文本渲染为图像并使用视觉语言模型重新编码，有望实现高效的长上下文处理，其生成的解码器 token 通常比子词分词少 3 到 20 倍。然而，token 的节省并不能预见性地转化为下游效用：在某些任务上，视觉路径的表现与文本路径相当甚至更优，而在其他任务上则会崩溃，且压缩率本身无法预测会出现哪种情况。因此，缺失的关键量并非另一种效率总结，而是对视觉编码引起的任务相关信息损失的一种原则性度量。我们通过用测度传输的语言来形式化 VTC 解决了这一问题。通过将文本和视觉 token 视为经验概率测度，我们证明了 ViT patch 编码器诱导了一个前推映射，其传输成本可分解为来自 patch 内聚合的精度成本和来自跨 patch 碎裂化的覆盖成本。这两项都可以通过无需下游标签的探测器进行估算。这种形式化带来了两个操作层面的结果：一个无需下游标签的路由准则，用于为给定输入或基准实例选择是否使用视觉路径；以及一种基于传输信息的中央凹化机制，以更高分辨率重新编码高成本区域。在 Qwen3-4B 上的 24 个 NLP 数据集中，我们的无标签规则在 17/24 个数据集 (70.8%) 上达到了每个数据集的最优表现，且相对于纯大语言模型，在平均 token 减少 10.3% 的情况下，将平均任务得分提高了 3.3%。

## Abstract
Visual text compression (VTC) promises efficient long-context processing by rendering text into an image and re-encoding it with a vision-language model, often producing $3$--$20\times$ fewer decoder tokens than subword tokenization. Yet token savings do not translate predictably into downstream utility: on some tasks the visual path matches or exceeds the text path, on others it collapses, and the compression ratio itself does not predict which regime will occur. The missing quantity is therefore not another summary of efficiency, but a principled measure of task-relevant information loss induced by visual encoding. We address this problem by formulating VTC in the language of measure transport. Treating text and visual tokens as empirical probability measures, we show that the ViT patch encoder induces a push-forward map whose transport cost decomposes into a precision cost from within-patch aggregation and a coverage cost from cross-patch fragmentation. Both terms are estimable from downstream-label-free probes. This formulation yields two operational consequences: a downstream-label-free routing criterion that selects whether to use the visual path for a given input or benchmark instance, and a transport-informed foveation mechanism that re-encodes high-cost regions at higher resolution. Across $24$ NLP datasets at Qwen3-4B, our label-free rule matches the per-dataset oracle on $17/24$ datasets ($70.8\%$), and improves the average task score by $+3.3\%$ with $-10.3\%$ average tokens relative to a pure-LLM.

---

## 论文详细总结（自动生成）

### 视觉文本压缩作为测度传输 (Visual Text Compression as Measure Transport) 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
视觉文本压缩（VTC）是一种通过将文本渲染为图像并利用视觉语言模型（VLM）重新编码来处理长上下文的技术。虽然 VTC 能显著减少 Token 消耗（通常为 3-20 倍），但其在下游任务中的表现极不稳定：在某些任务上能超越纯文本路径，而在另一些任务上则性能崩溃。
**核心问题**在于：压缩率本身无法预测性能损失。论文认为，缺失的是一种能够量化“任务相关信息损失”的原则性度量。研究动机是建立一套理论框架，在无需下游任务标签的情况下，预测何时该使用视觉路径，并修复视觉编码过程中的局部损失。

#### 2. 方法论：核心思想与关键技术
论文将 VTC 形式化为**测度传输（Measure Transport）**问题：
*   **核心思想**：将文本单元和视觉 Token 视为经验概率测度。ViT（Vision Transformer）编码器被建模为一个“前推映射”（Push-forward map），它将精细的文本测度转换为粗糙的视觉测度。
*   **传输成本分解**：总传输成本 $C(x)$ 被分解为两个可估算的项：
    1.  **精度成本（Precision Cost, $\Delta_{intra}$）**：由 Patch 内聚合引起，导致细粒度的词汇区别（如“2023”与“2024”）被平滑。
    2.  **覆盖成本（Coverage Cost, $\Delta_{inter}$）**：由跨 Patch 碎裂化引起，影响需要整合跨位置或跨页面证据的任务。
*   **关键公式**：$C(x) = \alpha \cdot W(x) + \beta \cdot L(x) \cdot (1 - TRR(x))$。其中 $W$ 是精度特征，$L$ 是覆盖特征，$TRR$ 是文本冗余率。
*   **操作化应用**：
    *   **传输效率（TE）路由**：结合信息生存率（ISR）和视觉压缩率（VCR），通过公式 $TE(x) = ISR(x) \cdot VCR(x)$ 决定是否走视觉路径。
    *   **注视机制（Foveation）**：识别高成本 Patch，并以更高分辨率对其进行局部重编码（逆传输过程）。

#### 3. 实验设计
*   **数据集**：涵盖 24 个 NLP 基准测试，包括多项选择阅读理解（RACE-H, QuALITY）、二分类（IMDB, BoolQ）、多跳问答（HotpotQA, MuSiQue）、摘要生成（CNN/DM, GovReport）等。
*   **对比方法**：
    1.  **LLM**：纯文本路径（基准）。
    2.  **VLM**：纯视觉路径（直接渲染编码）。
    3.  **VLM + Foveation**：带局部修复的视觉路径。
    4.  **Decision-routed（本文方法）**：基于 TE 分数自动选择路径。
    5.  **Oracle-routed**：事后选择最优路径的上限参考。
*   **模型架构**：主要使用 Qwen3 系列（4B, 8B, 32B）的 Instruct 版本，确保 LLM 和 VLM 路径的参数量匹配。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA L40S GPU** 上运行。
*   **算力细节**：论文未明确说明总训练时长或 GPU 确切数量，但提供了详细的推理延迟（Latency）和显存占用（Peak Memory）数据。例如，在 4B 模型上，路由策略相比纯 LLM 减少了约 36% 的平均延迟。

#### 5. 实验数量与充分性
*   **实验规模**：在 24 个数据集上进行了详尽测试，并跨越了 4B、8B、32B 三个模型尺度。
*   **充分性**：实验设计非常全面，包含了：
    *   **消融实验**：验证精度和覆盖成本项的有效性。
    *   **敏感性分析**：测试渲染字体大小、路由阈值 $\tau$ 的影响。
    *   **跨架构验证**：在 InternVL3.5 上重复实验，证明了结论的普适性。
*   **客观性**：参数 $\alpha, \beta, \gamma$ 是通过独立的合成探测器（Synthetic Probes）估算的，不依赖下游任务标签，确保了评估的公平性。

#### 6. 主要结论与发现
*   **路由有效性**：在 4B 尺度下，无标签路由规则在 17/24 个数据集上匹配了 Oracle 选择。
*   **性能提升**：相比纯 LLM，路由策略在**减少 10.3% Token** 的同时，将**平均任务得分提升了 3.3%**（+1.5 分）。
*   **任务分化**：视觉路径在多跳问答和分类任务上表现优异，但在需要极高词汇精度的任务（如 Cloze）和极长文档摘要上表现较差。
*   **注视机制作用**：Foveation 能有效修复局部信息损失，尤其在证据集中的问答任务中提升显著。

#### 7. 优点
*   **理论严谨**：首次将视觉文本压缩与测度传输理论联系起来，为 VTC 的不稳定性提供了数学解释。
*   **无需标签（Label-free）**：路由和修复机制均不依赖下游任务的真实标签，具有极强的实用性。
*   **效率与精度兼顾**：通过动态路由，在保持（甚至提升）精度的前提下实现了显著的计算节省。

#### 8. 不足与局限
*   **天花板效应**：对于文本路径已接近性能天花板且压缩率极高的任务（如 VTCBench 的内存子任务），该模型可能会过度倾向于视觉路径。
*   **尺度依赖性**：探测器参数在不同模型尺度（如 8B 与 32B）下表现不一，导致 Foveation 在某些尺度下效果受限。
*   **渲染固定**：目前采用统一的渲染配置，未针对特定任务优化渲染参数（如字体、排版）。

（完）
