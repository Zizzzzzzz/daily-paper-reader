---
title: Joint Architecture-Token-Bitwidth Multi-Axis Optimization of Vision Transformers for Semiconductor IC Packaging
title_zh: 面向半导体集成电路封装的 Vision Transformer 架构-Token-位宽联合多轴优化
authors: "Phat Nguyen, Xue Geng, Kaixin Xu, Wang Zhe, Xulei Yang, Ngai-Man Cheung"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01742v1"
tags: ["query:evlm"]
score: 9.0
evidence: 架构与Token压缩的联合优化
tldr: 针对视觉Transformer（ViT）在半导体封装检测等资源受限工业场景中部署难的问题，本文提出了一种多轴联合优化框架。该框架整合了神经架构搜索（AutoFormer）、令牌合并（ToMe）和混合精度推理（fp16）三种互补技术。通过在ImageNet-1K和实际3D X射线缺陷分类数据集上的验证，该方法在保持精度的同时，实现了吞吐量、参数量和能耗的显著优化，为工业级ViT部署提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01742v1/fig-001.webp\", \"caption\": \"Fig. 1: 3D X-ray semiconductor defect. The sub-figures show the appearances of different categories of defects.\", \"page\": 4, \"index\": 1, \"width\": 525, \"height\": 1247}]"
motivation: 现有的ViT优化研究多集中于单一维度，难以满足半导体制造等工业场景对计算、内存和能耗的严苛要求。
method: 提出一个整合了架构搜索、令牌合并和混合精度推理的联合优化框架，从模型结构、数据处理和计算精度三个维度协同压缩。
result: 在半导体缺陷检测任务中，该框架实现了超过10倍的吞吐量提升，并同步减少了10倍以上的参数量、计算量和能耗。
conclusion: 本研究证明了多轴联合优化在提升ViT工业部署效率方面的巨大潜力，是半导体制造领域资源受限视觉任务的重要进展。
---

## 摘要
Vision Transformer (ViT) 在视觉识别领域取得了卓越性能，但在资源受限的工业环境中的部署仍然受到限制。主要挑战在于其高昂的计算成本、内存需求和能耗。尽管神经架构搜索 (NAS)、Token 压缩和低精度推理等单一效率技术已得到广泛研究，但大多数先前的工作仅针对单一优化轴，在保持准确性的同时限制了整体部署收益。在本文中，我们提出了首批整体框架之一，该框架联合优化了三个互补轴：架构、Token 和位宽。具体而言，该框架通过神经架构搜索 (AutoFormer) 识别紧凑的主干网络，通过 Token 合并 (ToMe) 减少信息处理，并通过 fp16 混合精度推理加速单次操作的执行。以 DeiT-B/16 为基准，我们首先分析了在激进压缩下 ImageNet-1K 上的准确率与效率权衡。然后，我们将选定的配置应用于用于集成电路芯片封装检测的真实内部 3D X 射线半导体缺陷分类数据集。结果表明，所提出的多轴框架在吞吐量方面实现了 10 倍以上的提升，同时在参数量、FLOPs 和能耗方面降低了 10 倍以上，并保持了下游工业任务所需的准确率。据我们所知，这是最早在 ViT 中联合优化架构、Token 和位宽维度的研究之一，也是首个针对半导体制造量身定制的、以部署为中心的资源高效型研究。

## Abstract
Vision Transformers (ViTs) have achieved strong performance in visual recognition, yet their deployment in resource-constrained industrial environments remains limited. Some main challenges are their high computational cost, memory requirement, and energy consumption. While individual efficiency techniques such as neural architecture search (NAS), token compression, and low-precision inference have been extensively studied, most prior work targets only a single optimization axis, limiting overall deployment gains while preserving accuracy. In this paper, we present one of the first holistic frameworks that jointly optimizes three complementary axes: architecture, token, and bit-width. Specifically, the framework identifies compact backbones via Neural Architecture Search (AutoFormer), reduces information processing via token merging (ToMe), and accelerates per-operation execution via fp16 mixed-precision inference. Starting from a DeiT-B/16 baseline, we first analyze accuracy-efficiency trade-offs on ImageNet-1K under aggressive compression. Then, we apply the selected configurations to a real-world in-house 3D X-ray semiconductor defect classification dataset for IC chip packaging inspection. Results show that the proposed multi-axis framework achieves more than 10 times improvement in throughput along with over 10 times reductions in parameter count, FLOPs, and energy consumption, while maintaining the required accuracy on the downstream industrial task. To the best of our knowledge, this is among the earliest works to jointly optimize architecture, token, and bit-width dimensions in ViTs and the first such resource-efficient, deployment-focused study tailored to semiconductor manufacturing.

---

## 论文详细总结（自动生成）

这是一份关于论文《Joint Architecture-Token-Bitwidth Multi-Axis Optimization of Vision Transformers for Semiconductor IC Packaging》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于解决 **Vision Transformer (ViT) 在资源受限的工业环境（特别是半导体集成电路封装检测）中部署困难** 的挑战。
*   **背景**：ViT 虽然性能强劲，但其高昂的计算成本、内存占用和能效消耗限制了其在实时工业检测中的应用。
*   **动机**：现有的优化研究（如架构搜索、Token 压缩、量化）大多孤立地针对单一维度进行，未能充分利用不同优化手段之间的互补性。本研究旨在提出一个**多轴联合优化框架**，通过协同优化架构、Token 和位宽，实现极致的部署效率。

### 2. 方法论：核心思想与关键技术
该框架通过三个互补的“轴”进行联合优化：
*   **架构轴（Architecture）**：采用 **AutoFormer**（一种 One-shot 神经架构搜索框架）。通过训练一个共享权重的超网，直接识别出紧凑的子网（如 AutoFormer-Small/Tiny），避免了从头训练每个候选模型的成本。
*   **Token 轴（Token）**：引入 **ToMe (Token Merging)** 技术。在推理过程中逐步合并相似的视觉 Token，从而减少后续 Transformer 层的序列长度，降低计算复杂度。
*   **位宽轴（Bit-width）**：利用 **fp16 混合精度推理**。通过 TensorRT 将模型转换为半精度格式，利用硬件加速器提升单次操作的执行速度。
*   **流程**：首先在 ImageNet 上筛选出具有良好“准确率-效率”权衡的配置，然后将这些配置迁移至特定的工业任务中进行微调。

### 3. 实验设计
*   **数据集**：
    1.  **ImageNet-1K**：作为基准测试，用于分析不同压缩配置下的性能权衡。
    2.  **内部 3D X-ray 半导体缺陷数据集**：包含 93 个 3D 体积数据（80 个训练，13 个测试），涵盖 Pillar、Solder、Void、Pad 等 5 类缺陷。
*   **Benchmark（基准模型）**：以 **DeiT-B/16** 作为原始基准。
*   **对比方法**：对比了 AutoFormer-S（小）和 AutoFormer-T（极小）在不同 ToMe 合并比例（r=10, r=15）下的表现。

### 4. 资源与算力
*   **硬件**：推理测试在 **NVIDIA RTX 6000 Ada Generation GPU** 上进行。
*   **软件栈**：使用 PyTorch 训练，导出为 ONNX（opset 17），并使用 **TensorRT 10** 生成 FP16 和 FP32 推理引擎。
*   **训练细节**：在 ImageNet-1K 上重训练 100 个 epoch，使用 AdamW 优化器，batch size 为 1024。在工业数据集上进行微调以恢复精度。

### 5. 实验数量与充分性
*   **实验规模**：论文在 ImageNet 上测试了约 7 组主要配置，并在工业数据集上验证了 5 组选定配置。
*   **充分性评价**：实验设计较为合理，涵盖了从通用基准到特定工业场景的迁移验证。通过对比吞吐量 (FPS)、能耗 (J)、参数量和 GFLOPs 等多个硬件感知指标，客观地展示了多轴优化的叠加效应。
*   **局限性**：工业数据集规模较小（仅 93 个样本），虽然在小样本上达到了 100% 精度，但其统计显著性和泛化性仍有待在大规模工业数据上进一步验证。

### 6. 主要结论与发现
*   **协同效应显著**：架构压缩与 Token 压缩具有互补性，ToMe 在已经压缩过的 AutoFormer 骨干网上依然能显著提升效率。
*   **极致性能提升**：在半导体缺陷检测任务中，最优变体（AutoF-Tr=15）相比 DeiT-B/16 基准：
    *   **吞吐量**：从 13.1 FPS 提升至 161.1 FPS（**12.3倍加速**）。
    *   **能耗**：降低了约 **13倍**。
    *   **参数量**：减少了 **15.1倍**。
    *   **计算量 (FLOPs)**：减少了 **24.1倍**。
*   **精度保持**：通过下游微调，压缩模型能够完全恢复并保持工业任务所需的分类精度。

### 7. 优点（亮点）
*   **多轴协同**：是首批将 NAS、Token 压缩和混合精度联合应用于 ViT 部署的研究之一。
*   **部署导向**：实验不仅关注理论 FLOPs，还实测了 FPS 和能耗，对工业落地具有极强的参考价值。
*   **行业针对性**：首次将此类高效 ViT 优化应用于半导体 3D X-ray 检测这一垂直领域。

### 8. 不足与局限
*   **量化深度不足**：目前仅探讨了 fp16 混合精度，未涉及更具挑战性的 INT8 或 INT4 量化，而后者在边缘设备上通常能带来更大收益。
*   **数据集规模限制**：工业数据集样本量极少（总共 93 个 3D 样本），可能存在过拟合风险，且难以评估模型在复杂工业环境下的鲁棒性。
*   **自动化程度**：虽然使用了 NAS，但 Token 压缩比例和位宽选择仍带有一定的人工启发式筛选，未实现完全端到端的自动化搜索。

（完）
