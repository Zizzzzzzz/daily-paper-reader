---
title: Neuromorphic Monocular Depth Estimation with Uncertainty Modeling
title_zh: 具有不确定性建模的神经形态单目深度估计
authors: "Viktor Bergkvist, Felix Rydell, Per-Erik Forssén, David Gustafsson, Johan Rideg"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10675v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 基于事件流的单目深度估计
tldr: 本研究针对事件相机的高动态范围和低延迟特性，提出了一种结合不确定性建模的单目深度估计方法。通过U-Net架构，对比了体素网格、CSTR和TORE等六种事件表示方式，并引入高斯、对数正态及证据学习框架来预测像素级深度分布。实验证明，该方法在合成和真实数据集上均表现良好，能够有效识别深度估计中的可靠区域，为神经形态视觉应用提供了更稳健的深度感知方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10675v1/fig-001.webp\", \"caption\": \"Fig. 3. Comparison of depth predictions for six combinations of uncertainty modeling approaches and event representations. Orange indicates closer regions, while purple denotes greater depth. The predicted depth maps are visually similar across methods; however, E2Depth produces smoother results, likely due to the use of a multi-scale gradient matching loss.\", \"page\": 8, \"index\": 1, \"width\": 802, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.10675v1/fig-002.webp\", \"caption\": \"TABLE I EVALUATION OF THE UNCERTAINTY MODELS AND EVENT REPRESENTATIONS ON THE TEST SEQUENCE IN TERMS OF ABSOLUTE RELATIVE ERROR, ROOT MEAN SQUARED ERROR, AREA UNDER THE SPARSIFICATION ERROR. (M) CONSIDERS ONLY PIXELS THAT GENERATED EVENTS WITHIN THE CONSIDERED TIME WINDOW, APPROXIMATELY 10 % OF ALL PIXELS. (C) CONSIDERS THE PIXELS WITH LOWEST PREDICTED UNCERTAINTY, SELECTED TO MATCH THE PROPORTION OF EVENT-TRIGGERED PIXELS IN (M). THE BEST VALUES (APART FROM E2DEPTH) FOR EACH METRIC ARE HIGHLIGHTED IN BOLD. THE TEST DATASET IS MVSEC INDOOR FLYING 1.\", \"page\": 7, \"index\": 2, \"width\": 800, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2605.10675v1/fig-003.webp\", \"caption\": \"Fig. 2. Sparsification curves for some combinations of uncertainty models and event representations tested on MVSEC indoor flying 1.\", \"page\": 7, \"index\": 3, \"width\": 814, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2605.10675v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the network architecture used for predicting depth and uncertainty. The blue arrows denote skip connections.\", \"page\": 4, \"index\": 4, \"width\": 806, \"height\": 318}]"
motivation: 旨在利用事件相机的高时间分辨率和高动态范围优势，解决单目深度估计中缺乏可靠性度量的问题。
method: 采用U-Net模型对比了六种不同的事件表示法，并集成高斯、对数正态和证据学习框架进行不确定性量化。
result: 实验发现10个时间箱的对数正态模型和5个时间箱的证据学习模型在各项指标上表现最佳，能准确指示可靠深度像素。
conclusion: 证明了不确定性估计可以成功整合进事件驱动的单目深度估计任务中，显著提升了系统对深度预测质量的感知能力。
---

## 摘要
事件相机相比传统的基于帧的传感器具有显著优势，包括微秒级的时间分辨率、高动态范围和低带宽。在本文中，我们利用深度神经网络从单目事件流中预测逐像素深度分布。我们使用高斯、对数正态和证据学习框架来估计不确定性。我们比较了六种事件表示方法：具有 1、5、10 和 20 个时间仓的时空体素网格、紧凑时空表示 (CSTR) 以及时间排序近期事件 (TORE) 卷。我们的基于 U-Net 的模型在合成数据上进行训练，然后在真实序列上进行微调。我们使用绝对相对误差、均方根误差和稀疏化误差曲线下面积来评估性能。定量结果表明，这些表示方法的表现相似，而 10 仓对数正态和 5 仓证据学习在各项指标上表现最佳。我们的实验证明，不确定性估计可以成功集成到基于事件的单目深度估计中，并用于指示具有可靠深度的像素。

## Abstract
Event cameras offer distinct advantages over conventional frame-based sensors, including microsecond-level temporal resolution, high dynamic range, and low bandwidth. In this paper, we predict per-pixel depth distributions from monocular event streams using deep neural networks. We estimate uncertainty using Gaussian, log-normal, and evidential learning frameworks. We compare six event representations: spatio-temporal voxel grids with 1, 5, 10, and 20 temporal bins, the Compact Spatio-Temporal Representation (CSTR), and Time-Ordered Recent Event (TORE) volumes. Our U-Net-based models are trained on synthetic data and then fine-tuned on real sequences. We evaluate performance using absolute relative error, root mean squared error, and the area under the sparsification error. Quantitative results show that the representations perform similarly, while 10 bin log-normal and 5 bin evidential learning perform best across metrics. Our experiments demonstrate that uncertainty estimation can be successfully integrated into event-based monocular depth estimation, and be used to indicate pixels with reliable depth.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neuromorphic Monocular Depth Estimation with Uncertainty Modeling》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：传统的基于帧的相机在高速运动（运动模糊）和极端光照（动态范围有限）环境下表现不佳。事件相机（Event Cameras）凭借微秒级响应、高动态范围（HDR）和低带宽需求，成为机器人导航和无人机感知的理想选择。
*   **核心问题**：现有的基于事件的单目深度估计研究大多只关注预测深度值，而忽略了预测的**可靠性（不确定性）**。
*   **研究动机**：在自动驾驶或无人机避障等安全敏感应用中，了解深度预测何时不可靠至关重要。本研究旨在探索如何从异步事件流中同时预测深度及其不确定性分布。

### 2. 论文提出的方法论
*   **核心思想**：将单目深度估计任务建模为一个概率分布预测问题，而非简单的回归问题。通过神经网络输出分布参数，从而量化每个像素的预测置信度。
*   **关键技术细节**：
    *   **网络架构**：采用基于 **U-Net** 的循环编码器-解码器结构。编码器包含三层下采样卷积和 **ConvLSTM** 模块，以捕获事件流的时序相干性。
    *   **事件表示（6种对比）**：
        1.  **时空体素网格（Voxel Grids）**：分别测试了 1, 5, 10, 20 个时间仓（bins）。
        2.  **紧凑时空表示（CSTR）**：通过归一化事件计数和平均时间戳压缩信息。
        3.  **时间排序近期事件（TORE）**：存储每个像素最近 K 个事件的时间信息。
    *   **不确定性模型**：
        1.  **高斯分布（Gaussian）**：预测均值 $\mu$ 和标准差 $\sigma$。
        2.  **对数正态分布（Log-normal）**：考虑到深度值始终为正的特性，预测对数空间的分布。
        3.  **证据深度学习（Evidential Deep Learning, EDL）**：预测 Normal-Inverse-Gamma 分布的四个参数，旨在区分**偶然不确定性**（数据噪声）和**认知不确定性**（模型知识不足）。
*   **损失函数**：主要使用负对数似然（NLL）损失进行训练，EDL 模型额外增加了正则化项以惩罚预测误差较大的高置信度区域。

### 3. 实验设计
*   **数据集与场景**：
    *   **BlinkVision（合成数据）**：包含 24 个室内序列，提供像素级完美的深度真值，用于模型预训练。
    *   **MVSEC（真实数据）**：使用其中的室内飞行序列（Indoor Flying），利用 LiDAR 获取的真值进行微调和最终评估。
*   **Benchmark（基准方法）**：
    *   **E2Depth**：一种经典的基于事件的监督学习深度估计模型（不带不确定性）。
    *   **灰度图模型**：使用相同架构但在传统灰度图像上训练的模型，用于对比事件相机的优势。
*   **评估指标**：
    *   **精度指标**：绝对相对误差（AbsRel）、均方根误差（RMSE）。
    *   **不确定性指标**：稀疏化误差曲线下面积（AUSE），用于衡量预测的不确定性与实际误差的相关性。

### 4. 资源与算力
*   **硬件环境**：工作站配备了 **Intel Core Ultra 9 285K CPU**，**96 GB RAM**，以及 **NVIDIA RTX 5090 GPU**。
*   **训练细节**：
    *   预训练：200 个 epoch，学习率 $10^{-4}$。
    *   微调：学习率降至 $10^{-5}$，加入权重衰减。
    *   Batch Size：20。
    *   输入处理：将 50ms 的事件窗口转换为对应表示，图像下采样至 $480 \times 270$。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了 6 种事件表示与 3 种不确定性模型的多种组合（共计约 10 组主要配置）。
*   **充分性评估**：
    *   **客观性**：通过在合成数据预训练、真实数据微调的流程，验证了模型的泛化能力。
    *   **公平性**：所有模型使用相同的 U-Net 骨干网络，确保了对比的变量仅为事件表示法或概率模型。
    *   **局限性**：实验主要集中在室内场景，缺乏室外高速或更复杂动态场景的验证。

### 6. 主要结论与发现
*   **最佳配置**：**10 仓对数正态模型（10 bin Log-normal）**和 **5 仓证据学习模型（5 bin EDL）**在综合指标上表现最优。
*   **不确定性的价值**：实验证明预测的不确定性可以有效识别“可靠像素”。通过只保留不确定性最低的 10% 像素，RMSE 误差可降低约 4.8 倍。
*   **表示法影响**：不同的事件表示法对结果有影响，但差异相对较小，表明 U-Net 架构对多种输入格式具有一定的鲁棒性。
*   **与基准对比**：虽然 E2Depth 在纯深度精度（RMSE）上仍领先（约 21%），但本研究提出的模型提供了关键的可靠性度量，且在特定子集上精度极高。

### 7. 优点（亮点）
*   **首创性**：据作者称，这是首个在基于事件的单目深度估计中引入不确定性建模的工作。
*   **多维度对比**：系统地比较了当前主流的多种事件表示法（Voxel, CSTR, TORE），为后续研究提供了参考。
*   **实用性**：引入的 AUSE 指标和稀疏化分析直观地展示了不确定性在实际过滤错误预测中的应用潜力。

### 8. 不足与局限
*   **视觉平滑度**：由于缺乏 E2Depth 中使用的多尺度梯度匹配损失，预测的深度图在边缘处显得过于平滑，缺乏锐度。
*   **场景覆盖**：仅限于室内环境，未探讨事件相机在强光、高动态室外场景下的极端表现。
*   **精度权衡**：为了获得不确定性估计，模型在整体深度预测精度上相比最先进的回归模型（如 E2Depth）有所下降。
*   **算力需求**：虽然使用了高性能的 RTX 5090，但未详细讨论模型在嵌入式端（如无人机载板）的推理延迟。

（完）
