---
title: "CoVSpec: Efficient Device-Edge Co-Inference for Vision-Language Models via Speculative Decoding"
title_zh: CoVSpec：通过投机解码实现视觉-语言模型的高效端-边协同推理
authors: "Yuanyuan Jia, Shunpu Tang, Qianqian Yang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02218v1"
tags: ["query:evlm"]
score: 9.0
evidence: 通过投机解码实现高效视觉语言模型架构与推理
tldr: 针对视觉语言模型（VLM）在移动端部署受限的问题，本文提出CoVSpec框架，通过端云协同的投机解码实现高效推理。该框架引入了无须训练的视觉Token剪枝、自适应草稿策略以及并行分支机制，在保证模型精度的前提下，显著提升了推理吞吐量并大幅降低了通信开销，为移动端多模态应用提供了高效支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02218v1/fig-001.webp\", \"caption\": \"Fig. 1: Illustration of the proposed CoVSpec, a communication-aware device-edge collaborative speculative decoding framework for VLM inference.\", \"page\": 3, \"index\": 1, \"width\": 954, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2605.02218v1/fig-002.webp\", \"caption\": \"TABLE I: Performance comparison on VQAv2, MMMU, and MMBench at SNR = 10 dB. Spd., Acc., Comm., and Cost Red. denote the speedup ratio, accuracy, communication cost, and cost reduction percentage, respectively. The best and second-best results are highlighted in bold and underlined, respectively.\", \"page\": 5, \"index\": 2, \"width\": 1079, \"height\": 188}]"
motivation: 现有的投机解码在VLM端云协同推理中面临视觉Token计算量巨大和通信开销过高的效率瓶颈。
method: 提出了一种结合视觉Token剪枝、自适应草稿长度调整及并行解耦验证纠错机制的协同推理框架。
result: "实验表明CoVSpec在保持精度的同时，吞吐量提升高达2.21倍，且通信开销较基准方案降低了96%以上。"
conclusion: CoVSpec为大规模视觉语言模型在资源受限的移动设备上的高效部署提供了一种极具竞争力的端云协同方案。
---

## 摘要
视觉-语言模型 (VLMs) 在多模态感知和推理方面展现了强大的能力。然而，由于巨大的计算和内存需求，在移动设备上部署大型 VLM 仍然面临挑战。一种实际的替代方案是端-边协同推理，即移动设备上的轻量级草稿 VLM 通过投机解码与边缘服务器上的大型目标 VLM 进行协作。然而，由于过度的视觉 Token 计算和高昂的通信开销，直接将投机解码扩展到 VLM 会导致严重的效率低下。为了应对这些挑战，我们提出了 CoVSpec，这是一个用于 VLM 推理的高效协作投机解码框架。具体而言，我们首先开发了一个无需训练的视觉 Token 削减框架，通过综合考虑查询相关性、Token 活跃度和低秩依赖性，剪枝移动设备上的冗余视觉 Token。此外，我们设计了一种自适应草稿策略，动态调整验证频率和草稿长度。此外，我们引入了一种具有解耦验证-纠错功能的并行分支机制，以提高目标端验证期间草稿端的利用率，并减少与纠错相关的传输开销。在多个基准测试上的实验表明，CoVSpec 的吞吐量比仅目标推理高出多达 2.21 倍，且与基准方案相比，在不损失任务准确性的情况下，减少了 96% 以上的通信开销。

## Abstract
Vision-language models (VLMs) have demonstrated strong capabilities in multimodal perception and reasoning. However, deploying large VLMs on mobile devices remains challenging due to their substantial computational and memory demands. A practical alternative is device-edge co-inference, where a lightweight draft VLM on the mobile device collaborates with a larger target VLM on the edge server via speculative decoding. Nevertheless, directly extending speculative decoding to VLMs suffers from severe inefficiency due to excessive visual-token computation and high communication overhead. To address these challenges, we propose CoVSpec, an efficient collaborative speculative decoding framework for VLM inference. Specifically, we first develop a training-free visual token reduction framework that prunes redundant visual tokens on the mobile device by jointly considering query relevance, token activity, and low-rank dependency. Moreover, we design an adaptive drafting strategy that dynamically adjusts both the verification frequency and the draft length. In addition, we introduce a parallel branching mechanism with decoupled verification-correction to improve draft-side utilization during target-side verification and reduce correction-related transmission overhead. Experiments on multiple benchmarks show that CoVSpec achieves up to 2.21x higher throughput than target-only inference and reduces communication overhead by more than 96% compared with baselines, without compromising task accuracy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **CoVSpec** 的高效端-边（Device-Edge）协同推理框架，旨在解决视觉-语言模型（VLM）在移动设备上部署时的计算与通信瓶颈。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：VLM 具有强大的多模态推理能力，但其巨大的参数量和计算需求使得在移动端直接部署大模型（Target Model）不切实际。
*   **核心挑战**：
    1.  **计算过载**：VLM 包含大量视觉 Token，在资源受限的移动端进行草稿生成（Drafting）时计算压力巨大。
    2.  **通信瓶颈**：传统的投机解码（Speculative Decoding）在端-边协作时需要频繁传输 Token 和 Logits，在无线网络环境下会导致高延迟。
*   **研究动机**：通过优化视觉 Token 处理和改进投机解码流程，实现高效、低延迟且低通信成本的端-边协同 VLM 推理。

### 2. 方法论
CoVSpec 的核心思想是**“端侧轻量草稿、边侧全量验证”**，具体包含以下关键技术：
*   **视觉 Token 削减（Visual Token Selection）**：
    *   **查询相关性评分**：利用 spaCy 提取查询关键词，计算视觉 Token 与关键词的余弦相似度。
    *   **Token 活跃度评分**：测量视觉 Token 在模型深层的表示变化幅度，变化大的被视为更重要。
    *   **低秩冗余消除**：对预选 Token 进行奇异值分解（SVD），保留能量最高的 Token，进一步压缩视觉上下文。
*   **自适应草稿策略（Adaptive Drafting）**：
    *   **余量门控（Margin-based Gating）**：计算 Top-1 和 Top-2 概率的差值，若差值够大（置信度高）则跳过边缘验证，直接本地提交。
    *   **动态长度调整**：根据历史接受率和当前信道状况（SNR、带宽），动态缩放草稿长度（$k$ 值）。
*   **并行分支与解耦验证-纠错（Parallel Branching & DVC）**：
    *   **并行分支**：在边缘端验证期间，移动端并行预测多个可能的后续分支，减少等待时间。
    *   **解耦纠错**：将纠错采样从边缘端移至移动端。边缘端仅在拒绝时返回目标 Logits，移动端利用本地 Logits 完成采样，大幅减少上行链路的 Logits 传输。

### 3. 实验设计
*   **数据集/场景**：VQAv2（视觉问答）、MMMU（多模态理解）、MMBench（综合能力评测）。
*   **Benchmark（基准）**：
    *   **Edge-only**：仅在边缘服务器运行大模型。
    *   **Device-only**：仅在移动端运行小模型。
    *   **Vanilla SD**：标准的端-边投机解码。
    *   **U-HLM**：一种现有的不确定性感知混合推理方法。
*   **模型配置**：草稿模型为 InternVL2.5-4B，目标模型为 InternVL2.5-78B。

### 4. 资源与算力
*   **移动端（Device）**：配备 M5 芯片的 MacBook Pro。
*   **边缘端（Edge Server）**：2 台 NVIDIA RTX 4090 GPU（采用 INT8 量化）。
*   **网络参数**：带宽 $B=5$ MHz，信噪比 $SNR=10$ dB。
*   **训练时长**：文中未提及训练时长，因为该方法强调 **Training-free（无需训练）**，属于推理侧优化。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上各随机采样 50 个样本，生成长度设为 1024 Token。
*   **消融实验**：非常充分。作者逐一验证了视觉削减、门控机制、自适应长度、并行分支和解耦纠错对性能的影响。
*   **客观性**：实验考虑了真实的无线信道模型和 API 调用成本，对比了速度、精度、通信量和经济成本四个维度，评价较为全面。

### 6. 主要结论与发现
*   **性能提升**：CoVSpec 在 VQAv2 上实现了 **2.21 倍** 的加速（相比仅边缘推理）。
*   **通信优化**：相比传统投机解码，通信开销降低了 **96% 以上**（从数百 MB 降至约 20 MB）。
*   **成本降低**：通过减少边缘端大模型的调用频率，推理成本降低了约 **30%**。
*   **精度保持**：尽管端侧大幅削减了视觉 Token（从 768 减至 64），但由于边缘端仍使用全量 Token 验证，最终准确率几乎没有损失。

### 7. 优点
*   **即插即用**：无需对模型进行重新训练或微调，易于部署。
*   **通信感知**：通过解耦纠错和自适应长度，极大地适应了无线通信环境的波动。
*   **多准则 Token 剪枝**：结合了语义相关性和模型内部活跃度，比单纯的随机剪枝更科学。

### 8. 不足与局限
*   **样本覆盖面**：每个数据集仅测试 50 个样本，样本量相对较小，可能无法完全代表极端长尾场景。
*   **模型依赖**：实验仅基于 InternVL 系列模型，虽然具有代表性，但未验证在其他架构（如 Qwen-VL 或 LLaVA）上的泛化表现。
*   **硬件假设**：移动端使用了性能较强的 M5 芯片，对于更低端的嵌入式设备，视觉 Token 削减带来的收益与开销平衡点可能需要重新评估。

（完）
