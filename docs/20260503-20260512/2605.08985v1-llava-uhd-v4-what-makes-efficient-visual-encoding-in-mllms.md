---
title: "LLaVA-UHD v4: What Makes Efficient Visual Encoding in MLLMs?"
title_zh: LLaVA-UHD v4：多模态大语言模型（MLLM）中高效视觉编码的关键是什么？
authors: "Kechen Fang, Yihua Qin, Chongyi Wang, Wenshuo Ma, Tianyu Yu, Yuan Yao"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08985v1"
tags: ["query:evlm"]
score: 10.0
evidence: 多模态大模型中的视觉标记压缩与高效编码
tldr: "针对多模态大模型在处理高分辨率图像时视觉编码计算开销大的问题，本文提出了LLaVA-UHD v4。该研究通过实验证明基于切片的编码优于全局编码，并引入了ViT内部早期压缩技术以在浅层减少标记数量。LLaVA-UHD v4在显著降低55.8%视觉编码计算量的同时，在文档理解、OCR和通用VQA等任务上保持或超越了基准性能，为高效高分辨率MLLM设计提供了新方向。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08985v1/fig-001.webp\", \"caption\": \"Figure 2: Average performance and computational cost. Left: average accuracy across training data scales, comparing LLaVA-UHD v4 and the post-ViT baseline. Right: FLOPs comparison between the two systems.\", \"page\": 7, \"index\": 1, \"width\": 724, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2605.08985v1/fig-002.webp\", \"caption\": \"Table 1: Comparison of encoding strategies. We compare the two encoding strategies under different compression rates and data scales using SigLIP 2 as the ViT backbone. GE denotes global encoding and SE denotes slice-based encoding.\", \"page\": 3, \"index\": 2, \"width\": 827, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2605.08985v1/fig-003.webp\", \"caption\": \"Figure 3: Benchmark trends across training data scales. We compare Post-ViT and our method on eight benchmarks across different training data scales.\", \"page\": 8, \"index\": 3, \"width\": 805, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.08985v1/fig-004.webp\", \"caption\": \"Figure 1: Comparison of high-resolution MLLM encoding paradigms. (a) Previous works feed the full image into the ViT under global encoding and reduce visual tokens only at the post-ViT connector. (b) Our work, LLaVA-UHD v4, adopts slice-based encoding and introduces an intra-ViT compression module D that reduces token count early in the vision encoder. D performs local window attention followed by pixel unshuffle and MLP-based fusion, enabling later layers to operate on fewer tokens. Compared to (a), this design substantially lowers ViT-internal compute, supports more aggressive compression ratios, and incurs nearly no performance loss.\", \"page\": 5, \"index\": 4, \"width\": 826, \"height\": 511}]"
motivation: 现有的全局编码和ViT后压缩策略在处理高分辨率输入时存在巨大的计算瓶颈和冗余。
method: 结合基于切片的编码策略以保留局部细节，并引入ViT内部早期压缩技术在浅层减少视觉标记。
result: "在多个基准测试中，LLaVA-UHD v4将视觉编码的FLOPs降低了55.8%，且性能与基准模型持平甚至更优。"
conclusion: 视觉编码效率可以在不牺牲下游任务性能的情况下大幅提升，切片编码与内部压缩是高效高分辨率MLLM的有效设计方向。
---

## 摘要
视觉编码是多模态大语言模型（MLLM）中的主要计算瓶颈，尤其是在处理高分辨率图像输入时。现有的主流做法通常采用全局编码，随后进行 ViT 后压缩。全局编码会产生海量的 token 序列，而 ViT 后压缩在进行任何 token 缩减之前，就已经产生了 ViT 完整的二次方注意力计算开销。在这项工作中，我们从编码策略和视觉 token 压缩两个维度重新审视了这一惯例。首先，受控实验表明，基于切片的编码在各项基准测试中均优于全局编码，这表明通过切片视图保留局部细节比应用全局注意力进行细粒度感知更有益。其次，我们引入了 ViT 内部早期压缩，它在 ViT 的浅层减少 token，在保持下游性能的同时大幅降低了视觉编码的 FLOPs。通过将 ViT 内部压缩集成到基于切片的编码框架中，我们提出了 LLaVA-UHD v4，这是一种专为高分辨率输入量身定制的高效且计算可控的视觉编码方案。在涵盖文档理解、OCR 和通用 VQA 的多种基准测试中，LLaVA-UHD v4 将视觉编码的 FLOPs 降低了 55.8%，同时达到甚至超过了基准性能。这些结果表明，在不牺牲下游性能的情况下，可以大幅提高视觉编码效率，为高效高分辨率 MLLM 提供了一个实用的设计方向。所有模型权重和代码都将公开发布，以支持进一步的研究。

## Abstract
Visual encoding constitutes a major computational bottleneck in Multimodal Large Language Models (MLLMs), especially for high-resolution image inputs. The prevailing practice typically adopts global encoding followed by post-ViT compression. Global encoding produces massive token sequences, while post-ViT compression incurs the full quadratic attention cost of the ViT before any token reduction takes place. In this work, we revisit this convention along two dimensions: the encoding strategy and visual token compression. First, controlled experiments show that slice-based encoding outperforms global encoding across benchmarks, suggesting that preserving local details through sliced views can be more beneficial than applying global attention for fine-grained perception. Second, we introduce intra-ViT early compression, which reduces tokens in shallow ViT layers and substantially lowers visual-encoding FLOPs while preserving downstream performance. By integrating intra-ViT compression into the slice-based encoding framework, we present LLaVA-UHD v4, an efficient and compute-controllable visual encoding scheme tailored for high-resolution inputs. Across a diverse set of benchmarks covering document understanding, OCR, and general VQA, LLaVA-UHD v4 reduces visual-encoding FLOPs by 55.8% while matching or even surpassing baseline performance. These results suggest that visual-encoding efficiency can be substantially improved without sacrificing downstream performance, providing a practical design direction for efficient high-resolution MLLMs. All model weights and code will be publicly released to support further research.

---

## 论文详细总结（自动生成）

以下是对论文《LLaVA-UHD v4: What Makes Efficient Visual Encoding in MLLMs?》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于多模态大语言模型（MLLM）在处理**高分辨率图像**时的计算效率瓶颈。
*   **现状与痛点**：主流模型（如 Qwen2-VL）倾向于使用“全局编码 + 后置压缩”方案。这意味着图像在被压缩前，必须经过 Vision Transformer (ViT) 完整的二次方复杂度注意力计算，导致视觉编码阶段消耗了大量算力。
*   **核心问题**：如何在高分辨率输入下实现更高效的视觉编码？作者重新审视了“编码策略”和“Token 压缩”两个关键维度，挑战了“全局编码优于切片编码”的传统认知。

### 2. 方法论：核心思想与关键技术
LLaVA-UHD v4 提出了一个结合**切片编码**与**ViT 内部早期压缩**的架构：
*   **基于切片的编码 (Slice-based Encoding, SE)**：将高分辨率图像分解为一个低分辨率缩略图和多个局部切片，分别独立编码。实验证明这种方式比将整图强行缩放后全局编码（GE）更能保留细粒度细节。
*   **ViT 内部早期压缩模块 (Intra-ViT Compressor $D$)**：
    *   **插入位置**：在 ViT 的浅层（实验确定为第 6 层后）插入压缩模块，使后续的大部分 ViT 层仅在 1/4 的 Token 数量上运行。
    *   **模块结构**：包含一个 **2×2 窗口注意力 (Window Attention)** 块（用于局部上下文增强）和一个基于 **Pixel Unshuffle 的 MLP**（用于空间降采样和通道融合）。
    *   **参数重用初始化 (Parameter-Reuse Initialization)**：这是关键技术。为了不破坏预训练 ViT 的特征流形，压缩模块 $D$ 的权重直接从其前一个 ViT 层复制并进行特定变换（如 Block-Diagonal 初始化），实现“热启动”，避免了从头训练导致的性能坍塌。

### 3. 实验设计
*   **数据集与场景**：涵盖了从 4M 到 64M 不同规模的内部训练数据，以及 LLaVA-OneVision 的开源数据集。
*   **Benchmark**：在 8 个主流基准上进行评估，包括：
    *   **通用 VQA**：MMBench (EN/CN), MMStar。
    *   **知识与推理**：MMMU, MathVista, AI2D, HallusionBench。
    *   **细粒度感知**：OCRBench。
*   **对比方法**：
    *   编码策略对比：全局编码 (GE) vs. 切片编码 (SE)。
    *   连接器对比：Query-based Resampler vs. MLP-based Spatial Merging。
    *   压缩方案对比：后置压缩 (Post-ViT) vs. 内部压缩 (Intra-ViT) 及其各种消融变体（均值池化、交叉注意力等）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及总训练时长。
*   **训练细节**：提到了采用四阶段训练流程（对齐、知识注入、交错训练、指令微调），并给出了不同阶段的 Batch Size（从 9 到 64 不等）和学习率设置。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了极其详尽的受控实验。
    *   **编码策略实验**：在不同数据规模（4M/8M）和压缩率（4x/16x）下验证了 SE 的优越性。
    *   **消融实验**：针对压缩模块的插入深度 $k$（测试了 3, 6, 9, 15 层）、初始化方式（随机 vs. 重用）、模块结构（窗口注意力 vs. 交叉注意力）进行了多组对比。
*   **充分性与客观性**：实验覆盖了从 400 万到 6400 万样本的扩展性分析，证明了该方法在不同数据量级下的稳定性。通过固定 LLM（Qwen3-8B）和 ViT（SigLIP 2）基座，确保了对比的公平性。

### 6. 主要结论与发现
*   **切片编码胜出**：在相同 Token 预算下，切片编码在 OCR 等细粒度任务上显著优于全局编码。
*   **内部压缩高效且无损**：LLaVA-UHD v4 在 ViT 内部进行 4x 压缩，将视觉编码阶段的 **FLOPs 降低了 55.75%**，而下游任务性能几乎无损（平均偏差仅 -0.29 分）。
*   **初始化是关键**：简单的内部降采样会破坏特征分布，而“参数重用初始化”能让模型在微调初期就处于预训练流形附近，是实现无损压缩的核心。
*   **插入深度存在帕累托最优**：第 6 层是效率与性能的最佳平衡点；过早压缩（如第 3 层）会严重损害性能。

### 7. 优点与亮点
*   **打破直觉**：系统性地证明了切片编码在现代 MLLM 中的优势，纠正了社区对全局编码的盲目追求。
*   **架构创新**：提出的参数重用初始化方法为“如何在不破坏预训练模型的前提下修改其内部结构”提供了极具参考价值的范式。
*   **实用性强**：在显著降低计算开销的同时保持了极高的性能，非常适合需要处理超高分辨率图像的工业级应用。

### 8. 不足与局限
*   **固定压缩模式**：目前的压缩是静态且均匀的，无法根据图像内容的复杂度（如空白背景 vs. 密集文字）动态调整 Token 分配。
*   **超参数依赖**：最佳插入深度 $k=6$ 主要是针对 SigLIP 2 架构得出的，对于其他不同深度或结构的视觉编码器（如 InternViT），可能需要重新搜索。
*   **上下文割裂**：切片编码虽然提升了局部细节，但本质上割裂了跨切片的全局空间关系，主要依赖低分辨率缩略图来弥补，这在极少数需要超远距离空间关联的任务中可能存在风险。

（完）
