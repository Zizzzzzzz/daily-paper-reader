---
title: "SplitZip: Ultra Fast Lossless KV Compression for Disaggregated LLM Serving"
title_zh: SplitZip：面向解耦式大语言模型推理的极速无损 KV 缓存压缩
authors: "Yipin Guo, Siddharth Joshi"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01708v1"
tags: ["query:evlm"]
score: 9.0
evidence: 解耦式LLM推理的无损KV压缩
tldr: 针对大模型推理中预填充与解码分离架构下的KV缓存传输瓶颈，本文提出SplitZip，一种专为GPU设计的超快速无损压缩算法。它利用浮点数指数部分的冗余，通过固定长度编码和稀疏逃逸流机制，在保持无损的同时实现了极高的编解码吞吐量，显著降低了长文本推理的延迟并提升了系统吞吐量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01708v1/fig-001.webp\", \"caption\": \"Table 2: Comparison of encoding/decoding throughput and compression ratio of various compression methods. While maintaining a respectable compression ratio, SplitZip demonstrates a significant advantage in codec throughput.\", \"page\": 6, \"index\": 1, \"width\": 566, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2605.01708v1/fig-002.webp\", \"caption\": \"Figure 3: KV-cache transfer time across sequence-length and batch-size sweeps using Mooncake[22].\", \"page\": 7, \"index\": 2, \"width\": 819, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2605.01708v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of SplitZip for lossless KV-cache compression. SplitZip exploits the redundancy of the BF16 exponent field while keeping sign and mantissa exact. It encodes the most frequent exponent values with fixed 4-bit codes and stores rare values in a small escape buffer with their positions and raw exponents. This design enables GPU-friendly parallel decoding and reconstructs the original KV cache bit-exactly.\", \"page\": 3, \"index\": 3, \"width\": 794, \"height\": 396}]"
motivation: 在分离式LLM推理架构中，跨节点传输海量KV缓存已成为制约长文本处理性能的主要瓶颈，而现有无损压缩算法难以满足在线推理的极高速度要求。
method: SplitZip利用KV激活值中指数部分的冗余，采用离线校准的Top-16指数码本进行固定长度编码，并结合稀疏逃逸流处理罕见值，实现了高度并行的GPU加速编解码。
result: 在BF16张量上，SplitZip实现了613.3 GB/s的压缩和2181.8 GB/s的解压吞吐量，使KV缓存传输速度提升达1.32倍，首字延迟（TTFT）降低达1.30倍。
conclusion: SplitZip证明了通过针对GPU优化的轻量级无损压缩，可以有效缓解分布式LLM推理中的通信瓶颈，是提升大规模模型服务效率的关键技术。
---

## 摘要
当代大语言模型（LLM）推理系统采用了预填充-解码解耦（prefill-decode disaggregation）架构，以更好地平衡计算密集型的预填充阶段与内存密集型的解码阶段。在这种设计下，预填充工作节点生成的 KV 缓存必须在令牌生成开始前传输到解码工作节点。由于这些节点位于不同的物理系统中，该传输过程成为了大规模 LLM 推理的显著瓶颈。对于通常需要长输入的长文本和智能体（agentic）工作负载，这一瓶颈尤为突出。现有的无损编解码器并不适用于此场景，因为它们主要针对离线权重压缩、依赖 CPU 端，或使用变长编码（虽然解压快但压缩慢，不适合在线使用）。SplitZip 是一种面向 KV 缓存传输的 GPU 友好型无损压缩器。它利用了 KV 激活值中浮点指数位的冗余，使用定长编码对最频繁出现的指数值进行编码，并将（位置，值）对以及稀有指数值编码到逃逸流（escape stream）中。通过离线校准的前 16 位指数码本实现高效在线编码，而规则的密集路径和稀疏逃逸修正使得压缩和解压在 GPU 上均能高效运行。在真实的 BF16 激活张量上，SplitZip 实现了 613.3 GB/s 的压缩吞吐量和 2181.8 GB/s 的解压吞吐量，在延迟敏感的编解码路径上显著优于先前的无损压缩器。端到端传输实验表明，BF16 KV 缓存传输速度提升高达 1.32 倍，首字延迟（TTFT）加速 1.30 倍，请求吞吐量提升 1.23 倍。

## Abstract
Contemporary systems serving large language models (LLMs) have adopted prefill-decode disaggregation to better load-balance between the compute-bound prefill phase and the memory-bound decode phase. Under this design, prefill workers generate a KV cache that must be transferred to decode workers before token generation can begin. With these workers residing on different physical systems, this transfer becomes a significant bottleneck to serving LLMs at scale. This bottleneck gets exacerbated for long-input and agentic workloads, which typically require long inputs. Existing lossless codecs are not well suited to this setting as they primarily target offline weight compression, rely on CPU-side, or use variable-length coding that decompresses fast but compresses too slowly for online use. SplitZip is a GPU-friendly lossless compressor for KV-cache transfer. It exploits redundancy in floating-point exponents of KV activations, encoding the most frequent exponent values with fixed-length codes, and encoding (position, value) pairs and value of rare exponents in an escape stream. An offline calibrated top-16 exponent codebook enables online encoding, while the regular dense path and sparse escape correction make both encoding and decoding efficient on GPUs. On real BF16 activation tensors, SplitZip achieves 613.3 GB/s compression throughput and 2181.8 GB/s decompression throughput, substantially outperforming prior lossless compressors on the latency-critical codec path. End-to-end transfer experiments show up to 1.32$\times$ speedup for BF16 KV-cache transfer, 1.30$\times$ speedup for TTFT and 1.23$\times$ increase on Request Throughput.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SplitZip** 的 GPU 原生无损压缩算法，旨在解决大语言模型（LLM）在“预填充-解码解耦”（Prefill-Decode Disaggregation）架构下的通信瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在现代 LLM 服务中，为了优化性能，通常将预填充（计算密集型）和解码（内存密集型）阶段分离在不同的 GPU 节点上。这导致预填充生成的 **KV 缓存（KV Cache）** 必须通过网络传输到解码节点。
*   **研究背景**：随着上下文长度（Context Length）的增加，KV 缓存的体积呈爆炸式增长，网络传输延迟成为了系统吞吐量和首字延迟（TTFT）的主要瓶颈。
*   **现有局限**：现有的无损压缩方案（如 Huffman 编码）虽然压缩率高，但由于采用变长编码，在 GPU 上难以并行化，导致编解码速度太慢，无法满足在线推理的实时性要求。

### 2. 论文提出的方法论
*   **核心思想**：利用 BF16 浮点数中**指数位（Exponent）**的高度冗余性。研究发现，KV 缓存中前 16 个最频繁出现的指数值覆盖率超过 99%。
*   **关键技术细节**：
    *   **定长编码（Fixed-length Coding）**：将 8 位的指数部分压缩为 4 位固定长度的编码（对应 Top-16 频率值），每两个编码打包进一个字节。
    *   **逃逸机制（Escape Stream）**：对于不在 Top-16 范围内的稀有指数值，将其原始值和位置记录在额外的“逃逸流”中。
    *   **符号与尾数保留**：符号位和尾数位（共 8 位）保持不变，确保完全无损。
    *   **GPU 友好设计**：由于采用定长编码，算法不涉及复杂的位偏移计算，能够完美适配 GPU 的大规模并行处理能力。
    *   **离线校准**：通过预先在代表性数据集上统计指数分布，生成静态码本，避免了在线统计的开销。

### 3. 实验设计
*   **数据集/场景**：使用了 WikiText-2、HumanEval、GSM8K、MMLU 和 PTB 等多样化数据集。
*   **模型覆盖**：涵盖了 Qwen3 (30B/32B/80B)、Llama-3.1 (70B)、Llama-3 (8B) 和 Phi-2 等主流模型。
*   **对比方法（Benchmark）**：
    *   通用 GPU 压缩库：nvCOMP (LZ4, Cascaded, Bitcomp)。
    *   学术界先进方案：ZipNN、DFloat11、Falcon、ZipServ。
*   **测试维度**：编解码吞吐量（GB/s）、压缩比、端到端传输时间、TTFT 加速比、请求吞吐量。

### 4. 资源与算力
*   **硬件环境**：实验使用了配备 **NVIDIA H200 GPU** 和双路 Intel Xeon Platinum 8468 CPU 的服务器。
*   **网络环境**：采用了 Mellanox ConnectX-7 网卡，支持 RoCE 4×200G 高速互联。
*   **软件框架**：集成到了 SGLang 和 Mooncake（KV 缓存传输引擎）中进行端到端评估。
*   **训练/校准时长**：文中未明确说明校准的具体时长，但强调校准是“一次性且轻量级”的。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括不同 Batch Size（1 到 256）和序列长度（128 到 128K）的组合。
*   **消融实验**：非常充分。对比了 Top-8 vs Top-16、不同校准粒度（张量/Token/通道级）、静态 vs 动态码本、以及逃逸位置记录方式的差异。
*   **客观性**：通过在多个模型家族和不同任务数据集上验证，证明了指数分布的稳定性，实验结果具有较强的说服力和普适性。

### 6. 论文的主要结论与发现
*   **极高的吞吐量**：SplitZip 实现了 **613.3 GB/s** 的压缩吞吐量和 **2181.8 GB/s** 的解压吞吐量，远超所有对比方法（如比 nvCOMP Bitcomp 快 1.8 倍，比 Cascaded 解压快 14 倍）。
*   **端到端收益**：在长文本场景下，KV 传输时间缩短了约 **30%**，TTFT 加速高达 **1.30x**，系统请求吞吐量提升 **1.23x**。
*   **无损保证**：通过位对位（Bit-exact）对比，确认压缩前后模型输出的 Logits 完全一致。

### 7. 优点
*   **速度极快**：其编解码速度足以“隐藏”在网络传输时间之内，使压缩过程对用户几乎透明。
*   **实现简单**：基于查找表和位移操作，易于集成到现有的推理框架（如 vLLM, SGLang）。
*   **泛化性强**：离线校准的码本在不同模型和任务间表现稳定，无需针对每个请求重新计算。

### 8. 不足与局限
*   **压缩率上限**：作为无损压缩，其压缩比受限于指数位的冗余度（BF16 下理论极限接近 1.33x），相比有损压缩（如 4-bit 量化）其体积缩减有限。
*   **短文本收益小**：在短序列场景下，由于内核启动开销和网络带宽未饱和，压缩带来的收益可能被编解码耗时抵消。
*   **格式依赖**：该方法高度依赖于浮点数（BF16/FP8）的指数结构，对于已经过深度压缩或非浮点格式的数据可能无效。

（完）
