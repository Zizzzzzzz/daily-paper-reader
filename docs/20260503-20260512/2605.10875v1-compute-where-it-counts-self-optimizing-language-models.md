---
title: "Compute Where it Counts: Self Optimizing Language Models"
title_zh: 算在关键处：自优化语言模型
authors: "Yash Akhauri, Mohamed S. Abdelfattah"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10875v1"
tags: ["query:evlm"]
score: 9.0
evidence: 用于高效LLM推理的Token级注意力稀疏化和结构化激活
tldr: "针对LLM推理中每Token计算量固定导致的效率低下问题，本文提出自优化语言模型（SOL）。该方法在冻结的LLM基础上引入轻量级策略网络，根据Token难度动态调整注意力稀疏度、MLP剪枝和量化精度。通过组相对策略优化（GRPO）训练，SOL在保持相同计算预算的同时，显著提升了模型性能，在MMLU任务上比静态分配策略高出7.3%，为推理效率优化提供了新方向。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10875v1/fig-001.webp\", \"caption\": \"Figure 2. Quality–efficiency trade-offs for jointly-controlled SOL policies with increasingly fine-grained action sets: (Left) SOL-J-2L-T16 (8 joint actions), (Middle) SOL-J-3L-T16 (27 joint actions), and (Right) SOL-J-FL-T16 (1560 joint actions).\", \"page\": 6, \"index\": 1, \"width\": 875, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-002.webp\", \"caption\": \"Figure 3. Effect of episode length T (KV-refresh period) on the quality–efficiency frontier for a joint controller with a shared action space: (Left) SOL-J-FL-T4, (Middle) SOL-J-FL-T16, and (Right) SOL-J-FL-T64.\", \"page\": 6, \"index\": 2, \"width\": 873, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-003.webp\", \"caption\": \"Figure 1. (Top Left) The policy is a small autoregressive transformer that uses the LLM’s own hidden state to decide how much compute to allocate per token. (Bottom Left) At training time, the policy samples multiple counterfactual compute schedules (trajectories) and learns to optimize quality under a requested budget target. (Right) At inference, the controller incurs a minimal, constant overhead (proportional to the control horizon, e.g., 16 decode steps), enabling fine-grained control over per-step efficiency.\", \"page\": 2, \"index\": 3, \"width\": 1010, \"height\": 474}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-004.webp\", \"caption\": \"Figure 4. Budget adherence of SOL when provided a range of requested targets. The plots compare (Left) requested vs. realized token-attention keep-rate κ, (Middle) requested vs. realized structured MLP keep-rate ρ, and (Right) requested vs. realized normalized quantization ratio η.\", \"page\": 7, \"index\": 4, \"width\": 888, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-005.webp\", \"caption\": \"Figure 11. Per-axis optimization with two-level action sets. Each subplot trains three single-axis controllers (SOL-Q/P/C-2L-T16). For each two-level action set (x-axis), the violin shows the distribution of normalized perplexity over 30 randomly sampled schedules. Lower is better.\", \"page\": 14, \"index\": 5, \"width\": 971, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-006.webp\", \"caption\": \"Figure 10. Hand-crafted budget-allocation heuristics (entropy- and drift-based) do not consistently improve over the fixed baseline, while the learned SOL policy achieves the best quality–efficiency trade-off.\", \"page\": 14, \"index\": 6, \"width\": 979, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-007.webp\", \"caption\": \"Figure 9. Comparison against 500-sample random schedule search. For selected Pareto operating points in SOL-J-2L-T16, SOL-J-3L-T16, and SOL-J-FL-T16, violins show the distribution of normalized perplexity across 500 randomly sampled compute schedules at similar net keep-rates; markers denote the fixed baseline, the best-of-500 random schedule, and the learned policy. Lower is better.\", \"page\": 13, \"index\": 7, \"width\": 971, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-008.webp\", \"caption\": \"Table 1. Paired comparison between SOL and fixed allocation over matched budget sweeps. SOL achieves significantly lower perplexity across all configurations.\", \"page\": 17, \"index\": 8, \"width\": 817, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-009.webp\", \"caption\": \"Figure 8. For each model variant (SOL-J-2L-T16, SOL-J-3L-T16, SOL-J-FL-T16), we sample random counterfactual compute schedules at Pareto operating points and visualize the resulting distribution as a 2D KDE over (net keep-rate, perplexity). We overlay the fixed (static) allocation baseline and the learned policy.\", \"page\": 12, \"index\": 9, \"width\": 971, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2605.10875v1/fig-010.webp\", \"caption\": \"Figure 7. Axis-dependent KV-pollution: mean direct ∆ perplexity on the corrupted steps vs. p90 peak ∆ perplexity in the subsequent dense tail.\", \"page\": 12, \"index\": 10, \"width\": 904, \"height\": 331}]"
motivation: 传统LLM推理对所有Token采用统一计算预算，忽略了Token难度的差异，导致简单步骤计算过度而复杂步骤计算不足。
method: 引入轻量级策略网络动态控制注意力、MLP和量化的稀疏度与精度，并采用组相对策略优化（GRPO）进行训练。
result: "SOL在多个模型和计算场景下均优于静态分配策略，在相同预算下将MMLU准确率提升了高达7.3%，实现了更优的质量-效率平衡。"
conclusion: 动态预算分配是提升LLM推理效率的一个重要且互补的维度，能显著优化模型在受限资源下的性能表现。
---

## 摘要
高效大语言模型（LLM）推理研究主要集中在降低每个解码步骤的成本（例如，使用量化、剪枝或稀疏注意力），通常对每个生成的 token 应用统一的计算预算。在实践中，token 的难度差异很大，因此静态压缩可能会在简单步骤上过度计算，而在困难步骤上计算不足。我们研究了自回归解码的动态预算分配：在单个模型内部学习为每个 token 投入多少计算量。自优化语言模型（SOL）将冻结的 LLM 与轻量级策略网络配对，该网络读取 LLM 的隐藏状态，并在每个解码步骤选择离散的效率动作。这些动作可以联合控制 (i) token 级注意力稀疏性，(ii) MLP 中的结构化激活剪枝，以及 (iii) 激活量化位宽，同时保持基础模型权重不变。我们在教师强制（teacher-forced）回合中通过群体相对策略优化（group-relative policy optimization）来训练该策略：token 序列是固定的，而我们采样多个计算调度（即仅针对相同 token 路径改变效率动作的“反事实”调度），并在相同的监督下比较它们的似然度。我们的奖励函数在语言模型质量与软惩罚之间进行权衡，后者鼓励回合平均预算使用量符合预设目标。在不同的模型变体和计算方案中，SOL 在相同预算下的质量优于静态分配和强随机调度搜索，为推理效率优化提供了一个互补的维度。SOL 在所有实验中都发现了更好的质量-效率帕累托前沿（Pareto-front），并且在 MMLU 准确率上比统一预算分配策略提高了多达 7.3%。

## Abstract
Efficient LLM inference research has largely focused on reducing the cost of each decoding step (e.g., using quantization, pruning, or sparse attention), typically applying a uniform computation budget to every generated token. In practice, token difficulty varies widely, so static compression can over-compute on easy steps and under-compute on hard ones. We study dynamic budget allocation for autoregressive decoding: learning how much computation to spend per token from within a single model.   Self-Optimizing Language Models (SOL) pair a frozen LLM with a lightweight policy network that reads the LLM hidden state and selects a discrete efficiency action at each decode step. Actions can jointly control (i) token-level attention sparsity, (ii) structured activation pruning in the MLP, and (iii) activation quantization bit-width, while leaving the base model weights unchanged. We train the policy with group-relative policy optimization on teacher-forced episodes: the token sequence is fixed, while we sample multiple compute schedules (i.e., "counterfactual" schedules that vary only the efficiency actions for the same token path) and compare their likelihoods under the same supervision. Our reward trades off language-model quality against soft penalties that encourage episode-average budget usage to match a requested target. Across model variants and compute regimes, SOL improves quality at matched budget over static allocation and strong random schedule search, offering a complementary axis for inference-efficiency optimization. SOL discovers a better quality-efficiency pareto-front across all our experiments and improves MMLU accuracy by up to 7.3% over uniform budget allocation strategies.

---

## 论文详细总结（自动生成）

这是一份关于论文《Compute Where it Counts: Self Optimizing Language Models》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）推理效率优化中的一个关键痛点：**计算资源的“平均主义”分配**。
*   **现状**：现有的量化、剪枝和稀疏注意力技术通常对每个生成的 token 应用固定的压缩率。
*   **问题**：Token 的生成难度差异巨大。简单的 token（如惯用语中的下一个词）不需要全量计算，而复杂的 token（涉及长程依赖或逻辑推理）则需要更多资源。固定预算会导致“简单步骤浪费计算，困难步骤计算不足”。
*   **核心目标**：实现**动态预算分配**，即在推理过程中实时决定每个 token 应该消耗多少计算资源，从而在不牺牲质量的前提下最大化效率。

### 2. 方法论：自优化语言模型（SOL）
SOL 的核心思想是引入一个轻量级的“控制器”来动态管理冻结的 LLM 的计算行为。
*   **核心架构**：将一个冻结的 LLM 与一个轻量级策略网络（Policy Network）配对。策略网络读取 LLM 的隐藏状态、当前进度和预算目标，输出离散的效率动作。
*   **联合控制空间**：策略网络同时控制三个维度的效率开关：
    1.  **Token 级注意力稀疏度**（使用 Quest 方法）：决定关注多少历史 KV 缓存。
    2.  **结构化 MLP 激活剪枝**（使用 TEAL 方法）：动态关闭部分 MLP 通道。
    3.  **激活量化位宽**（使用 ZeroQuant 方法）：在 4 到 16 位之间动态调整精度。
*   **关键技术细节**：
    *   **KV 污染管理**：识别到过度压缩会污染 KV 缓存并导致延迟的性能下降，因此引入了“控制视野（Horizon）”和周期性的“KV 刷新”机制。
    *   **训练算法**：采用**组相对策略优化（GRPO）**。在“教师强制”模式下，针对同一 token 序列采样多个不同的计算调度（反事实轨迹），通过比较它们的似然度（Likelihood）和预算达标情况来更新策略。
    *   **奖励函数**：$R = \text{Task\_Reward} - \text{Budget\_Penalty}$。任务奖励是 token 的对数似然，预算惩罚则强制模型在整个回合（Episode）内平均计算量接近用户设定的目标。

### 3. 实验设计
*   **基础模型**：Llama-3.2-1B、Llama-3.2-3B、Llama-3.1-8B-Instruct 以及 DeepSeek-R1-Distill-Llama-8B。
*   **数据集/场景**：
    *   **语言建模**：Wikitext、Dolma（用于训练和困惑度评估）。
    *   **下游任务**：ARC-Easy、PIQA、Winogrande（常识推理）；MMLU（多任务理解）；GSM8K（数学推理，用于长文本生成测试）。
*   **Benchmark 与对比方法**：
    *   **静态分配（Fixed）**：在整个推理过程中使用统一的压缩率。
    *   **强随机搜索**：采样数百个随机计算调度，取其中的帕累托最优。
    *   **手工启发式**：基于信息熵（Entropy）或隐藏状态漂移（Drift）的动态分配规则。
    *   **标准基准**：Quest、ZeroQuant、TEAL 的原始静态版本。

### 4. 资源与算力
论文明确列出了控制器训练的成本（使用 **H100 GPU**）：
*   **Llama-3.2-1B**：约 4 GPU 小时。
*   **Llama-3.2-3B**：约 7 GPU 小时。
*   **Llama-3.1-8B-Instruct**：约 20 GPU 小时。
*   **控制器参数量**：以 8B 模型为例，控制器仅 8.56M 参数，推理开销相对于基础模型几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验和对比实验，包括：
    *   不同动作空间粒度（8 种到 1560 种动作组合）。
    *   不同控制视野（T=4, 16, 64）。
    *   跨模型规模的扩展性验证（1B 到 8B）。
    *   预算遵循度（Budget Adherence）测试。
*   **充分性与公平性**：实验设计较为客观。通过“反事实调度”对比，排除了 LLM 采样随机性对结果的影响；对比了强随机搜索和手工启发式方法，证明了学习到的策略具有非平凡的优越性。

### 6. 主要结论与发现
*   **帕累托前沿提升**：在所有测试的模型和预算范围内，SOL 始终优于静态分配策略。
*   **下游任务增益**：在相同计算预算下，SOL 将 MMLU 准确率提升了高达 **7.3%**。
*   **预算可控性**：策略网络能够根据输入的预算目标（Target）精准调整行为，实现推理时的实时效率控制。
*   **KV 污染洞察**：实验证明 token 稀疏化对 KV 缓存的污染最严重，且这种负面影响具有延迟性，验证了非短视（Non-myopic）策略训练的必要性。

### 7. 优点
*   **正交性**：SOL 提供了一个与现有压缩技术正交的优化维度（时间维度/Token 维度），可以与任何现有的静态压缩方法结合。
*   **低成本**：无需微调庞大的基础模型，仅需训练极小的控制器，且训练时间极短。
*   **灵活性**：一个训练好的策略可以覆盖从极度压缩到全量计算的整个区间，无需为不同预算重复训练。

### 8. 不足与局限
*   **硬件实测延迟**：论文主要使用“保留率（Keep-rate）”作为计算预算的代理指标，虽然在理论上与计算量成正比，但实际的端到端推理加速（Latency）取决于底层算子（Kernel）对稀疏和混合精度的支持程度。
*   **层间分配缺失**：目前 SOL 主要在 Token 维度做分配，所有层共享相同的压缩决策。论文提到层间分配（Per-layer allocation）会极大增加动作空间，尚未在此文中深入探讨。
*   **长文本挑战**：虽然测试了 T=64 的视野，但在极长文本（如 100k+ token）下的 KV 污染累积效应和策略稳定性仍需进一步验证。

（完）
