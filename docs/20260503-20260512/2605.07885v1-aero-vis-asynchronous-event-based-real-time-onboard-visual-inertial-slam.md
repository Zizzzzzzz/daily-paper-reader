---
title: "AERO-VIS: Asynchronous Event-based Real-time Onboard Visual-Inertial SLAM"
title_zh: AERO-VIS：异步基于事件的实时机载视觉惯性 SLAM
authors: "Yannick Burkhardt, Sebastián Barbas Laina, Simon Boche, Leonard Freißmuth, Stefan Leutenegger"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07885v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 基于事件相机的异步事件驱动实时视觉惯性SLAM系统
tldr: 本研究针对事件相机在处理高动态范围和运动模糊方面的优势，提出了AERO-VIS，一种异步立体事件惯性SLAM系统。该系统集成了数据驱动且性能优化的关键点检测器，通过异步处理事件流，动态适应下游运行需求，确保了低延迟和实时性。这是首个在机载计算资源下实现闭环无人机控制和大规模状态估计的纯事件惯性SLAM系统，显著提升了在挑战性环境下的定位精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07885v1/fig-001.webp\", \"caption\": \"Fig. 1: Estimated trajectories by AERO-VIS when employed onboard a UAV.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 678}, {\"url\": \"assets/figures/arxiv/2605.07885v1/fig-002.webp\", \"caption\": \"TABLE II: Root mean square (RMS) of absolute trajectory error (ATE) [cm]\", \"page\": 6, \"index\": 2, \"width\": 1052, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2605.07885v1/fig-003.webp\", \"caption\": \"Fig. 4: Estimated trajectory during a 2 km (20 min) urban walk, processed online on an NVIDIA Jetson Orin NX. The initial pose was manually aligned with the map.\", \"page\": 7, \"index\": 3, \"width\": 514, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2605.07885v1/fig-004.webp\", \"caption\": \"Fig. 3: Two samples of the boxes 6dof -sequence of the ECD dataset: we visualize one channel-pair with positive (red) and negative (blue) polarity for MCTS∆t (fixed time windows) and MCTSNe (fixed event counts), as well as the matches predicted by SuperEvent [1], SuperEvent+ and SuperLitE. Green matches have a reprojection error of less than 5 pixels using ground truth pose measurements, while yellow matches are outliers.\", \"page\": 5, \"index\": 4, \"width\": 502, \"height\": 520}]"
motivation: 现有的事件视觉里程计多采用固定频率处理，限制了系统的延迟表现和吞吐量，无法充分发挥事件相机的高时间分辨率优势。
method: 提出一种结合数据驱动关键点检测器的异步立体事件惯性SLAM框架，能够根据计算需求动态调整处理节奏。
result: 在无人机机载平台上实现了极高的定位精度，并成功演示了首个基于纯事件惯性SLAM的闭环飞行控制。
conclusion: AERO-VIS证明了异步事件处理机制在提升机载实时SLAM系统鲁棒性与低延迟性能方面的巨大潜力。
---

## 摘要
事件相机对高动态范围和运动模糊的鲁棒性具有在挑战性环境中改进视觉里程计系统的潜力。尽管它们的高时间分辨率不需要同步处理，但大多数基于事件的里程计方法仍以固定速率运行，这虽然简化了系统设计，却限制了延迟和吞吐量。在这项工作中，我们提出了 AERO-VIS，这是一个双目事件惯性 SLAM 系统，集成了一个数据驱动、鲁棒且性能优化的关键点检测器。通过异步处理事件流，该系统能够动态适应下游的运行需求，确保了低延迟和实时性能。在无人机（UAV）上部署 AERO-VIS 时，我们在机载基于事件的 SLAM 中实现了前所未有的精度。这些独特的特性使我们能够展示首个纯基于事件的惯性 SLAM 系统，该系统在仅依赖机载计算的情况下，实现了无人机闭环控制和大规模状态估计。实验视频和源代码可在 ethz-mrl.github.io/AERO-VIS 获取。

## Abstract
The robustness of event cameras to high dynamic range and motion blur holds the potential to improve visual odometry systems in challenging environments. Although their high temporal resolution does not require synchronous processing, most event-based odometry methods still run at fixed rates, which simplifies system design but restricts latency and throughput. In this work, we present AERO-VIS, a stereo event-inertial SLAM system with an integrated, data-driven, robust, and performance-optimized keypoint detector. By processing the event stream asynchronously, the system dynamically adapts to downstream runtime demands, ensuring low-latency and real-time performance. When deploying AERO-VIS on a UAV, we achieve unprecedented accuracy in onboard event-based SLAM. These unique characteristics enable us to present the first purely event-based inertial SLAM system that demonstrates closed-loop UAV control and large-scale state estimation while relying solely on onboard compute. A video of the experiments and the source code are available at ethz-mrl.github.io/AERO-VIS.

---

## 论文详细总结（自动生成）

这是一份关于论文《AERO-VIS: Asynchronous Event-based Real-time Onboard Visual-Inertial SLAM》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在计算资源受限的无人机（UAV）机载平台上，利用事件相机（Event Camera）实现高精度、低延迟、且能支持闭环控制的实时视觉惯性 SLAM。
*   **研究背景**：传统帧相机在高速运动或高动态范围（HDR）环境下易失效。事件相机虽具备高动态范围和无运动模糊优势，但现有算法多采用固定频率的同步处理模式，未能充分利用事件流的异步特性，且往往依赖高性能桌面级 GPU，难以在嵌入式平台上兼顾精度与实时性。

### 2. 方法论：核心思想与关键技术
AERO-VIS 扩展了 OKVIS2 框架，其核心思想是将数据驱动的关键点检测与异步处理架构相结合：
*   **恒定事件计数的 MCTS 表达**：改进了多通道时间表面（MCTS）表征。不同于以往使用固定时间窗口（$\Delta t$），该方法采用**固定事件数量（$N_e$）**来构建时间表面。这使得系统能自适应不同强度的运动，在高速运动时缩短窗口，低速时延长窗口，从而保持特征外观的一致性。
*   **SuperLitE 网络架构**：针对嵌入式端优化的轻量化关键点检测与描述网络。通过减少编码器层数、降低描述符维度（从 256 维降至 64 维）以及使用 8 位整数重化，大幅提升了推理速度。
*   **异步系统设计**：
    *   **解耦预处理**：事件预处理（更新 MCTS 缓存）在独立的高频线程运行。
    *   **动态触发前端**：前端不再以固定频率运行，而是在完成上一轮处理后立即抓取最新的 MCTS 缓存进行推理。这种“尽力而为”的异步模式最小化了感知到估计的延迟。
*   **后端优化**：继承了 OKVIS2 的因子图优化和回环检测机制，并针对事件特征进行了适配。

### 3. 实验设计与 Benchmark
*   **数据集**：
    *   **公开数据集**：ECD (Event Camera Dataset)、EDS (Event-aided Direct Sparse Odometry)、rpg-stereo、TUM-VIE（包含极具挑战性的抖动序列）、VECtor（大规模室内序列）。
    *   **真实世界实验**：使用搭载 NVIDIA Jetson Orin NX 的定制无人机进行闭环控制实验（“8”字飞行、HDR 旋转、剧烈手持抖动）以及 2 公里的城市大规模行走实验。
*   **对比方法**：
    *   **SOTA 基准**：ESVO2（直接法双目事件惯性里程计）、SDEVO（深度学习双目事件里程计）。
    *   **消融/变体对比**：SuperEvent（原始模型）、SuperEvent+（改进 MCTS 的模型）、OKVIS2（原始帧相机版本）。

### 4. 资源与算力
*   **训练阶段**：文中提到采用了 SuperEvent 的训练策略，但未详细列出训练所用的具体 GPU 型号和时长。
*   **推理/运行阶段**：
    *   **嵌入式端**：NVIDIA Jetson Orin NX。SuperLitE 在该平台上推理仅需 **2.5 ms**（相比基准提升了 90% 的速度）。
    *   **桌面端**：Intel i5-13600 + NVIDIA RTX 4070（用于离线高精度模式对比）。
    *   **优化技术**：使用了 TensorRT 编译、CUDA Graph 捕获以及 16-bit 浮点/8-bit 整数重化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从微观关键点匹配精度（AUC 评估）到宏观 SLAM 轨迹误差（ATE 评估）的多维度测试。
*   **充分性**：实验设计非常充分。不仅在多个标准公开数据集上进行了定量对比，还通过无人机闭环控制验证了系统的实用性。
*   **客观性**：作者在相同硬件（Jetson Orin NX）和相同分辨率下重新运行了对比方法（ESVO2, SDEVO），确保了对比的公平性。同时，通过 2 公里的长距离实验验证了系统的长期稳定性。

### 6. 主要结论与发现
*   **精度领先**：AERO-VIS 在几乎所有公开数据集上的表现均优于现有的 SOTA 事件 SLAM 系统，尤其是在剧烈运动场景下。
*   **实时性突破**：这是首个能在嵌入式计算资源下，仅靠事件相机实现无人机闭环控制的系统。
*   **鲁棒性验证**：在 HDR 环境和剧烈抖动（3 m/s, 270°/s）下，AERO-VIS 的漂移量比传统帧相机系统（OKVIS2）降低了约 90%。
*   **异步优势**：异步架构能有效利用计算资源，在处理负载变化时保持系统不崩溃且延迟最低。

### 7. 优点与亮点
*   **首创性**：首次实现了纯事件惯性 SLAM 的机载闭环控制。
*   **算法优化**：提出的 $N_e$ 恒定 MCTS 解决了事件数据随运动速度变化而产生的外观畸变问题。
*   **工程落地**：系统设计考虑了实际部署中的带宽限制（空间下采样）和计算瓶颈（轻量化网络与异步线程）。

### 8. 不足与局限
*   **精度上限**：在光照良好的标准环境下，其精度仍略逊于基于高性能帧相机的 OKVIS2（受限于事件相机的空间分辨率和振动引起的噪声）。
*   **回环检测瓶颈**：在大规模实验中，随着关键帧增加，回环检测和后端优化逐渐成为计算瓶颈，导致处理频率下降（降至 5Hz 以下）。
*   **纯旋转限制**：在纯旋转运动中，由于缺乏平移，回环检测的启发式校验可能会失效，导致漂移无法及时校正。

（完）
