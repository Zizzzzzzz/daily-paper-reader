---
title: "Telegraph English: Semantic Prompt Compression via Structured Symbolic Rewriting"
title_zh: 电报英语：通过结构化符号重写的语义提示词压缩
authors: "Mikhail L. Arbuzov, Sisong Bei, Ziwei Dong, Dmitri Kalaev, Alexey A. Shvets"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04426v1"
tags: ["query:evlm"]
score: 9.0
evidence: 通过符号重写的语义提示词压缩
tldr: "本研究针对大模型提示词冗余问题，提出了一种名为Telegraph English (TE) 的语义压缩协议。不同于传统的Token删除法，TE通过结构化符号重写，将自然语言转化为由约40个逻辑符号组成的原子事实行。该方法不仅能根据信息密度自适应调整压缩比，还将压缩与语义分块合二为一。实验表明，在50%的压缩率下，TE在LongBench-v2基准上表现优异，尤其在小模型上显著提升了细节处理能力，为高效长文本处理提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04426v1/fig-001.webp\", \"caption\": \"Table 1: Core relational and logical operators in the TE symbol vocabulary. The full vocabulary contains roughly 40 symbols organised by function (causal, logical, comparative, modal).\", \"page\": 3, \"index\": 1, \"width\": 556, \"height\": 235}]"
motivation: 传统的提示词压缩方法主要依赖Token删除，容易损失关键语义且压缩比例固定，难以平衡效率与准确性。
method: 引入Telegraph English协议，利用约40个逻辑符号将输入重写为结构化的原子事实行，实现语义重写与自适应压缩。
result: "在50%压缩率下，TE在GPT-4.1上保留了99.1%的准确率，且在各项任务中全面超越了LLMLingua-2。"
conclusion: 通过显式的关系结构重写，TE不仅提升了压缩效率，还通过结构化表示补偿了小模型在处理复杂细节时的能力不足。
---

## 摘要
我们引入了“电报英语”（Telegraph English, TE），这是一种提示词压缩协议，可将自然语言重写为一种符号丰富、形式结构化的方言。与 LLMLingua-2 等通过训练分类器以固定比例删除低重要性 Token 的 Token 删除方法不同，TE 执行完整的语义重写：它将输入分解为原子事实行，用约 40 个逻辑和关系符号替换冗长的短语，并使压缩率能够适应每个文档的信息密度。行结构规则的一个结果是，压缩和语义分块（semantic chunking）变成了同一种操作——每一行输出都是一个独立可寻址的事实，因此压缩后的表示同时也是一个语义索引。我们在来自 LongBench-v2 的 4,081 个问答对上评估了 TE，涵盖了五种 OpenAI 模型和两个难度级别。在 Token 减少约 50% 的情况下，TE 在 GPT-4.1 上的关键事实准确率保持在 99.1%，并在所有测试的模型和任务中，在相同压缩率下均优于 LLMLingua-2。在较小的模型上，这种差距进一步扩大（在细节任务中高达 11 个百分点），表明显式的关系结构弥补了模型能力的不足。我们发布了语法规范、压缩提示词、基准数据和参考实现。

## Abstract
We introduce Telegraph English (TE), a prompt-compression protocol that rewrites natural language into a symbol-rich, formally-structured dialect. Where token-deletion methods such as LLMLingua-2 train a classifier to delete low-importance tokens at a fixed ratio, TE performs a full semantic rewrite: it decomposes the input into atomic fact lines, substitutes verbose phrases with $\sim$40 logical and relational symbols, and lets the compression ratio adapt to each document's information density. A consequence of the line-structure rule is that compression and semantic chunking become the same operation -- each output line is an independently addressable fact, so the compressed representation is simultaneously a semantic index. We evaluate TE on 4{,}081 question-answer pairs from LongBench-v2 across five OpenAI models and two difficulty levels. At roughly 50\% token reduction, TE preserves 99.1\% accuracy on key facts with GPT-4.1 and outperforms LLMLingua-2 at matched compression ratios on every model and task tested. The gap widens on smaller models -- up to 11 percentage points on fine-detail tasks -- suggesting that explicit relational structure compensates for limited model capacity. We release the grammar specification, compression prompt, benchmark data, and reference implementation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Telegraph English: Semantic Prompt Compression via Structured Symbolic Rewriting》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在 RAG、多智能体协作和长文本推理中的广泛应用，提示词（Prompt）的长度直接决定了推理成本和延迟。现有的提示词压缩方法主要分为两类：**抽取式**（如 LLMLingua-2，通过删除不重要的 Token）和**生成式**（如摘要化）。
论文指出，现有的 Token 删除方法存在三大缺陷：
*   **逻辑断裂**：删除 Token 可能切断指代关系或逻辑连接词，导致模型幻觉。
*   **缺乏结构**：输出是原文本的“残缺版”，无法被索引或动态更新。
*   **非自适应**：通常采用固定压缩比，忽略了文本本身的信息密度差异。
**研究动机**：提出一种名为“电报英语”（Telegraph English, TE）的协议，通过将自然语言重写为结构化的符号化方言，实现更高保真度、更具结构化的压缩。

### 2. 论文提出的方法论
核心思想是将自然语言执行一次**完整的语义重写**，而非简单的 Token 过滤。
*   **核心原则**：
    1.  **保真度优先**：除非信息可推导，否则不得丢弃。
    2.  **原子行结构**：每一行仅包含一个独立的事实、步骤或事件。
    3.  **符号化表达**：使用约 40 个预定义的逻辑和关系符号（如 `→` 表示因果，`∴` 表示结论，`∧` 表示逻辑与）。
*   **关键技术细节**：
    *   **标签系统**：使用 `PAST:`、`FUTURE:`、`CONF=0.87` 等标签来压缩冗长的时态和模态描述。
    *   **压缩即分块（Compression as Chunking）**：由于遵循“一行一事实”的规则，压缩后的文本天然就是语义分块后的索引，可直接用于 RAG 检索。
    *   **自验证机制**：在压缩提示词中嵌入 12 点质量检查清单，要求压缩器（LLM）在输出前进行自我审计。
*   **流程**：采用六步推理序列（概念识别 → 权利提取 → 关系映射 → 冗余消除 → 数值验证 → 引用核对），在单次推理中完成。

### 3. 实验设计
*   **数据集**：基于 **LongBench-v2**，筛选出 339 个文档，切分为 4,081 个评估单元。
*   **场景/任务**：
    1.  **Key Facts**：考察核心概念和主要论点的保留情况。
    2.  **Fine Facts**：对抗性设计的精细细节测试（如精确数值、边界条件）。
*   **对比方法（Benchmark）**：**LLMLingua-2**（目前最强的抽取式基线），分别设置 50% 和 33% 的保留率。
*   **评估模型**：使用了 GPT-4.1, GPT-4o, GPT-4o-mini, GPT-4.1-nano 等 5 种模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号或数量）。
*   **实现方式**：压缩过程主要依赖 OpenAI 的 **o4-mini** 模型通过 API 完成。由于该方法是基于 Prompt Engineering 的语义重写，不涉及大规模的基础模型预训练，因此其算力消耗主要体现在 API 调用成本和推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 4,081 组关键事实 QA 对和 801 组精细细节 QA 对的测试，覆盖了单文档、多文档及对话历史等多种长文本场景。
*   **充分性与客观性**：
    *   实验涵盖了从超大模型到纳米级小模型的完整梯度，验证了方法的普适性。
    *   引入了“修改后答案”机制，防止模型通过简单的字符串匹配得分，保证了评估的客观性。
    *   对比了不同压缩率下的表现，实验设计较为严谨。

### 6. 论文的主要结论与发现
*   **性能超越基线**：在约 50% 的 Token 削减下，TE 在所有模型和任务上均优于 LLMLingua-2。GPT-4.1 在阅读 TE 压缩文本时的准确率高达 99.1%。
*   **小模型效应**：模型规模越小，TE 的优势越明显。在精细细节任务中，TE 比 LLMLingua-2 高出多达 **11 个百分点**。这表明显式的逻辑结构能有效补偿小模型推理能力的不足。
*   **自适应性**：TE 的压缩率随信息密度变化（0.13 到 1.57 不等），对于高密度文本（如数据表）会自动降低压缩强度以保真。
*   **成本优势**：在多步智能体流水线中，由于 TE 格式可以持久化存在，其累计节省的 Token 成本远高于仅作用于输入的 Token 删除法。

### 7. 优点
*   **结构化程度高**：压缩结果即是语义索引，方便后续的检索、更新和动态管理。
*   **逻辑严密**：通过显式符号保留了因果和逻辑链条，减少了下游模型的幻觉风险。
*   **多功能合一**：将压缩、分块、索引三个步骤合并为一个重写过程，简化了 RAG 架构。
*   **可读性与可审计性**：相比破碎的 Token 片段，TE 格式对人类和模型而言都更易于快速审计。

### 8. 不足与局限
*   **压缩成本与延迟**：TE 需要调用 LLM 进行重写，这比 LLMLingua-2 这种轻量级分类器过滤要慢且贵，不适合处理即用即弃的临时输入。
*   **语言限制**：目前仅针对英语设计，符号语法在其他语种（如合成语或表意文字）上的适用性尚未验证。
*   **评估偏差**：评估过程依赖 OpenAI 的闭源模型，可能存在模型偏好或数据污染风险。
*   **动态管理缺乏实证**：文中提到的“动态上下文管理”更多是架构上的论证，缺乏大规模的长期对话实验数据支持。

（完）
