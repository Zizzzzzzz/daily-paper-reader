---
title: "Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models"
title_zh: 败中求进：面向视觉-语言-动作模型的自适应失败启发式学习
authors: "Meng Zheng, Samhita Marri, Anwesa Choudhuri, Benjamin Planche, Zhongpai Gao, Van Nguyen Nguyen, Terrence Chen, Girish Chowdhary, Ziyan Wu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08434v1"
tags: ["query:evlm"]
score: 9.0
evidence: 视觉-语言-动作模型的自适应失败告知学习
tldr: 针对视觉-语言-动作（VLA）模型在行为克隆中因缺乏纠错信号而导致的鲁棒性不足问题，本文提出了自适应失败告知学习（AFIL）框架。该框架通过在线生成失败轨迹作为负向引导，利用共享骨干网络的双动作生成器同时学习成功与失败行为。在推理阶段，AFIL 根据扩散步骤自适应地引导模型避开易错区域，显著提升了机器人在复杂任务中的成功率和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08434v1/fig-001.webp\", \"caption\": \"Figure 3: Visualization of in-domain (left) and out-of-domain (right) manipulation task setup. Row 1: exterior camera image, row 2: wrist camera image. See supplementary for more visualizations.\", \"page\": 8, \"index\": 1, \"width\": 738, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.08434v1/fig-002.webp\", \"caption\": \"Figure 2: Dual Action Generator (DAG)-VLA with Adaptive Failure-Informed Learning (AFIL).\", \"page\": 5, \"index\": 2, \"width\": 824, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.08434v1/fig-003.webp\", \"caption\": \"Table 4: Performance comparison on out-of-domain manipulation tasks. Success rates (%) reported over 25 configs × 3 runs for each task.\", \"page\": 9, \"index\": 3, \"width\": 764, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.08434v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed Adaptive Failure-Informed Learning (AFIL) pipeline. AFIL uses online-generated failure trajectories as adaptive negative guidance, steering action generation away from failure-prone regions and toward more reliable success modes.\", \"page\": 2, \"index\": 4, \"width\": 782, \"height\": 293}]"
motivation: 现有的 VLA 模型主要依赖成功示例进行行为克隆，导致其在面对微小执行错误时容易因缺乏纠错能力而产生不可恢复的失败。
method: 提出 AFIL 框架，通过在线生成失败样本并训练共享权重的双动作生成器，在采样过程中利用失败分布对动作生成进行自适应负向引导。
result: 在多种机器人操纵任务（包括长程和跨域场景）中，AFIL 显著提高了任务成功率，并展现出比现有 VLA 基准模型更强的鲁棒性。
conclusion: AFIL 证明了利用失败经验作为负向反馈能有效增强 VLA 模型的纠错能力和执行可靠性，为构建更健壮的机器人策略提供了高效且通用的方案。
---

## 摘要
视觉-语言-动作（VLA）模型为可扩展的机器人操纵提供了一种极具前景的范式，但其对仅包含成功案例的行为克隆的依赖使其显得十分脆弱；由于缺乏纠正性训练信号，微小的执行误差会迅速累积成不可恢复的分布外失败。为了解决这一局限性，我们提出了自适应失败启发式学习（AFIL），这是一个端到端框架，利用失败轨迹作为基于扩散（diffusion）和流（flow）的 VLA 策略的自适应负向引导。AFIL 使用预训练的 VLA 在线生成失败展开（rollouts），避免了对手工设计失败模式或人机协同恢复的需求。随后，它在共享通用视觉-语言主干网络的同时，联合训练用于成功和失败行为的双动作生成器（DAG），从而以有限的参数开销实现高效的失败感知策略学习。在采样过程中，失败生成器自适应地引导动作生成远离易出错区域，并向更可靠的成功模式靠拢，引导强度由每个扩散步骤中成功与失败分布之间的距离决定。在涵盖短时程和长时程设置的域内及跨域机器人操纵任务上的实验表明，AFIL 相比现有的 VLA 基准模型，一致地提高了任务成功率和鲁棒性，证明了其有效性、高效性和通用性。

## Abstract
Vision-language-action (VLA) models provide a promising paradigm for scalable robotic manipulation, yet their reliance on success-only behavioral cloning leaves them brittle; lacking corrective training signals, minor execution errors rapidly compound into unrecoverable, out-of-distribution failures. To address this limitation, we propose Adaptive Failure-Informed Learning (AFIL), an end-to-end framework that leverages failure trajectories as adaptive negative guidance for diffusion- and flow-based VLA policies. AFIL uses a pretrained VLA to generate failure rollouts online, avoiding the need for handcrafted failure-mode design or human-in-the-loop recovery. It then jointly trains Dual Action Generators (DAGs) for successful and failed behaviors while sharing a common vision-language backbone, enabling efficient failure-aware policy learning with limited parameter overhead. During sampling, the failure generator adaptively steers action generation away from failure-prone regions and toward more reliable success modes, with guidance strength determined by the per-diffusion-step distance between success and failure distributions. Experiments across in-domain and out-of-domain robotic manipulation tasks, covering both short- and long-horizon settings, show that AFIL consistently improves task success rates and robustness over existing VLA baselines, demonstrating its effectiveness, efficiency, and generality.

---

## 论文详细总结（自动生成）

这是一份关于论文《Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 OpenVLA, $\pi_0$ 等）主要依赖**仅包含成功示例**的行为克隆（Behavioral Cloning）进行训练。这种方法的局限性在于：
*   **缺乏纠错能力**：机器人从未见过“失败”或“错误”的状态，因此在执行中一旦出现微小偏差，误差会迅速累积，导致模型进入分布外（OOD）状态而无法恢复。
*   **数据利用率低**：现有的失败尝试通常被直接丢弃，而这些数据其实包含了关于“哪些动作不该做”的宝贵信息。
*   **现有方案的局限**：以往利用失败数据的方法往往需要手工设计失败模式（如“抓取不稳”），或者依赖昂贵的人类实时干预，难以扩展。

### 2. 核心方法论：自适应失败启发式学习（AFIL）
论文提出了 **AFIL 框架**，其核心思想是将失败轨迹转化为“负向引导信号”，在生成动作时主动避开易错区域。

*   **双动作生成器（DAG）架构**：
    *   模型共享一个预训练的视觉-语言模型（VLM）骨干网络。
    *   后端设计了两个轻量级的动作生成头：**成功生成器 ($G_{succ}$)** 和 **失败生成器 ($G_{fail}$)**。
    *   $G_{succ}$ 学习成功轨迹，$G_{fail}$ 学习在线生成的失败轨迹。
*   **在线失败数据生成**：
    *   利用预训练 VLA 在任务中直接运行（Rollout），自动收集自然发生的失败案例，无需人工标注。
*   **自适应负向引导采样（Adaptive Guidance）**：
    *   在推理阶段（扩散或流匹配的去噪过程中），最终的动作分值（Score）由成功分值减去失败分值得到。
    *   **关键公式逻辑**：$\epsilon^* = \epsilon_{succ} - \hat{\lambda}_\eta (\epsilon_{fail} - \epsilon_{succ})$。
    *   **自适应权重 $\hat{\lambda}_\eta$**：基于成功与失败分布之间的**余弦相似度**动态调整。当两者方向差异大（即失败信号明确）时，增强引导强度；当两者方向一致（如任务初期正常的接近动作）时，减弱引导，避免误伤有效动作。

### 3. 实验设计
*   **实验场景**：
    *   **ManiSkill 仿真环境**：涵盖短时程（如叠方块、提插销）和长时程（如清理桌面、水果分类）任务。
    *   **LIBERO 基准测试**：包括 LIBERO-Spatial, Object, Goal 和 Long 四个子集，用于评估泛化能力。
*   **对比方法（Baselines）**：
    *   **Success-Only**：仅用成功数据训练的基准模型（基于 $\pi_{0.5}$）。
    *   **Success + Failure Correction**：加入纠错数据但无负向引导。
    *   **Static-FIL**：使用固定权重的失败引导。
    *   **SOTA 模型**：包括 Diffusion Policy, Octo, OpenVLA, CoT-VLA 和 $\pi_{0.5}$。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   但文中提到，由于采用了共享骨干网络和轻量级双头设计，该方法在参数开销上是“有限的”（Limited parameter overhead），且推理时的余弦相似度计算具有较高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ManiSkill 中，每个任务评估 50 种配置 $\times$ 3 次运行 = 150 次 Rollouts。
    *   在 LIBERO 中，遵循标准协议，每个子集包含 10 个任务，总计进行 500 次 Rollouts。
    *   进行了**消融实验**，验证了失败纠错数据、静态引导与自适应引导各自的贡献，并测试了不同引导缩放因子 $\alpha$ 的影响。
*   **充分性评价**：实验设计较为充分，涵盖了域内（In-domain）和跨域（OOD）场景，且在长程任务上的提升尤为显著，证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：AFIL 在所有任务中均优于基准模型。在 ManiSkill 长程任务中，成功率比静态引导提升了约 7%-12%。
*   **泛化能力**：在面对未见过的物体几何形状、颜色和杂乱环境（OOD 实验）时，AFIL 表现出极强的适应性，成功率远超 Success-only 模型。
*   **自适应的重要性**：实验证明，固定权重的负向引导在任务初期可能干扰正常动作，而自适应机制能精准地在“即将失败”的关头介入。

### 7. 优点与亮点
*   **无需人工干预**：通过在线 Rollout 自动获取失败数据，解决了失败数据获取难的问题。
*   **模型无关性**：该框架可无缝集成到任何基于扩散（Diffusion）或流匹配（Flow-matching）的 VLA 模型中。
*   **高效性**：利用负向提示（Negative Prompting）的思想，在不增加大规模参数的前提下，显著提升了策略的纠错能力。

### 8. 不足与局限
*   **算力细节缺失**：未披露具体的训练资源消耗，难以评估其在有限算力环境下的部署成本。
*   **失败定义的依赖**：虽然不需要手工设计失败模式，但仍需要一个基本的“任务失败”判定机制（如仿真器自带的检测）来筛选数据。
*   **现实世界验证**：实验全部在仿真环境（ManiSkill/LIBERO）中完成，尚未在真实的物理机器人上验证其在复杂物理交互（如摩擦力、光照剧变）下的表现。

（完）
