---
title: Neuromorphic visual attention for Sign-language recognition on SpiNNaker
title_zh: 基于 SpiNNaker 的神经形态视觉注意力手语识别
authors: "Sarka Liskova, Olha Vedmedenko, Mazdak Fatahi, Matej Hoffmann, P. Michael Furlong, Giulia D Angelo"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06005v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: SpiNNaker上的类脑传感和脉冲神经网络
tldr: "本研究针对手语识别在实时部署中面临的高延迟和高功耗挑战，提出了一种基于SpiNNaker神经形态平台的端到端架构。该系统结合了用于提取感兴趣区域的脉冲视觉注意力机制与紧凑型脉冲神经网络（SNN）。通过在Sign Language MNIST和ASL-DVS数据集上的实验，该方案在保持竞争力的识别准确率（硬件端83.1%）的同时，实现了极低的功耗（0.565 mW）和延迟（3 ms），显著优于现有基准方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06005v1/fig-001.webp\", \"caption\": \"Fig. 1. Neuromorphic Sign-language recognition architecture: Event streams are processed through the spiking visual attention (sVA) model to extract salient regions (ROIs), which are then downsampled and fed into the recognition network (sR) deployed on SpiNNaker. The network architecture comprises convolutional (L1, 256 neurons, 4 channels, 5× 5 kernel), hidden (L2, 512 neurons), and output (L3, 24 output neurons) layers.\", \"page\": 1, \"index\": 1, \"width\": 542, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2605.06005v1/fig-002.webp\", \"caption\": \"TABLE VII SUMMARY OF RECENT WORK IN DVS GESTURE RECOGNITION. ABBREVIATIONS: TERNARIZED CONVOLUTION NETWORK (TCN), HIDDEN MARKOV MODEL (HMM), ARTIFICIAL NEURAL NETWORK (ANN), SPIKING NEURAL NETWORK (SNN), LIQUID STATE MACHINE (LSM), DEEP NEURAL NETWORK (DNN), EVENT-BASED GATED RECURRENT UNIT (EGRU).\", \"page\": 7, \"index\": 2, \"width\": 1149, \"height\": 513}, {\"url\": \"assets/figures/arxiv/2605.06005v1/fig-003.webp\", \"caption\": \"Fig. 3. Training and validation accuracy curves for SL-MNIST dataset (35 ms and 100 ms samples) and for the ASL-DVS dataset (35 ms samples).\", \"page\": 4, \"index\": 3, \"width\": 516, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2605.06005v1/fig-004.webp\", \"caption\": \"Fig. 4. Comparison of per-class test accuracy of the sR model evaluated in simulation and deployed on SpiNNaker for the SL-MNIST and the ASLDVS dataset. The horizontal axis indicates the ASL fingerspelling classes, while the vertical axis reports classification accuracy (%).\", \"page\": 5, \"index\": 4, \"width\": 508, \"height\": 248}]"
motivation: 旨在解决传统手语识别方法因高延迟和高功耗而难以在实时边缘设备上部署的问题。
method: 开发了一种集成脉冲视觉注意力机制和紧凑型SNN的端到端神经形态架构，并将其部署在SpiNNaker硬件平台上。
result: "在神经形态硬件上实现了83.1%的准确率，且功耗仅为0.565 mW，处理延迟缩短至3 ms。"
conclusion: 该系统证明了结合视觉注意力机制的神经形态计算在实现高效、低功耗边缘端手语识别方面的卓越性能。
---

## 摘要
近年来，手语识别在分类准确率方面取得了显著进展；然而，大多数现有方法的延迟和功耗要求限制了它们在实时部署中的适用性。神经形态传感与处理提供了一种基于稀疏、事件驱动计算的替代范式，支持低延迟和高能效的感知。在这项工作中，我们介绍了一种用于美式手语（ASL）指拼识别的端到端神经形态架构，该架构集成了一个用于在线感兴趣区域提取的脉冲视觉注意力机制，以及一个部署在 SpiNNaker 神经形态平台上的紧凑型脉冲神经网络。我们在两个数据集上对所提出的系统进行了基准测试：合成生成的事件化 Sign Language MNIST 数据集和原生录制的 ASL-DVS 数据集，同时对手语识别及相关工作进行了全面概述。这项工作在仿真中表现出极具竞争力的性能（92.27%），在神经形态硬件部署中表现出相当的性能（83.1%），同时在所有基准测试方法中实现了最高能效的架构（0.565 mW）和低延迟（3 ms）。尽管设计紧凑，该系统仍证明了任务相关的视觉注意力应用在边缘部署中的适用性。

## Abstract
Sign-language recognition has achieved substantial gains in classification accuracy in recent years; however, the latency and power requirements of most existing methods limit their suitability for real-time deployment. Neuromorphic sensing and processing offer an alternative paradigm based on sparse, event-driven computation that supports low-latency and energy-efficient perception. In this work, we introduce an end-to-end neuromorphic architecture for American Sign Language (ASL) fingerspelling recognition that integrates a spiking visual attention mechanism for online region-of-interest extraction with a compact spiking neural network deployed on the SpiNNaker neuromorphic platform. We benchmark the proposed system against two datasets: a synthetically generated event-based version of the Sign Language MNIST dataset and a natively recorded ASL-DVS dataset, whilst providing a comprehensive overview of Sign-language recognition and related work. This work yields competitive performance in simulation (92.27%) and comparable performance on neuromorphic hardware deployment (83.1%), while achieving the most energy-efficient architecture (0.565 mW) and low latency (3 ms) across all benchmarked approaches. Despite its compact design, the system demonstrates the suitability of task-dependent visual attention applications for edge deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neuromorphic visual attention for Sign-language recognition on SpiNNaker》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
手语识别（Sign-language recognition）在交互式应用中对**实时性**和**能效**有极高要求。传统的基于深度学习（ANN）和RGB相机的方案虽然准确率高，但存在高延迟（通常超过100-200ms）、高功耗以及在光照变化或运动模糊下鲁棒性差的问题。
论文的核心动机是利用**神经形态计算（Neuromorphic Computing）**的异步、稀疏和事件驱动特性，结合生物启发式的**视觉注意力机制**，构建一个超低延迟、超低功耗的端到端美式手语（ASL）指拼识别系统，使其能够部署在机器人或可穿戴设备等边缘端。

### 2. 方法论：核心思想与关键技术
论文提出了一种集成“注意力提取”与“分类识别”的端到端脉冲神经网络（SNN）架构：
*   **脉冲视觉注意力模型 (sVA)：** 
    *   **核心思想：** 模拟视觉皮层中的边缘所有权细胞（Border Ownership Cells），通过过滤背景噪声，自动定位并提取包含手部的感兴趣区域（ROI）。
    *   **技术细节：** 使用冯·米塞斯（von Mises）滤波器组进行多方向特征池化，识别显著性区域（Proto-objects）。这种方法无需学习，计算开销极低。
*   **脉冲识别模型 (sR)：**
    *   **架构：** 采用紧凑的卷积脉冲神经网络。输入为48×48的事件流，包含一层卷积层（4通道，5×5核）、一层隐藏层（512个LIF神经元）和一层输出层（24个神经元，对应24个字母）。
    *   **硬件适配：** 针对 SpiNNaker 平台的神经元、突触和路由资源限制进行了专门的轻量化设计。
*   **算法流程：** 事件相机捕捉数据 -> sVA 提取 ROI -> 空间下采样 -> sR 模型在 SpiNNaker 硬件上进行实时推理。

### 3. 实验设计与基准测试
*   **数据集：**
    1.  **Sign Language MNIST (SL-MNIST)：** 将传统的静态灰度图通过随机游走像素偏移（Random-walk）转换为合成事件流。
    2.  **ASL-DVS：** 目前唯一的原生录制手语指拼事件数据集，包含5名受试者在真实办公环境下的10万个样本。
*   **基准测试 (Benchmark)：**
    *   对比了基于 GPU 的图卷积网络（G-CNN, RG-CNN）。
    *   对比了部署在 Intel Loihi 神经形态芯片上的 SNN 模型（如 VGGNet, MLP）。
    *   对比了其他基于 DVS 的手势识别系统（如 IBM DVSGesture 上的各种实现）。

### 4. 资源与算力
*   **训练阶段：** 在 MacBook (Apple M4 Pro 芯片) 上使用 `snnTorch` 框架进行仿真训练。论文未明确给出具体的训练时长，但提到了使用了 AdamW 优化器和余弦退火调度。
*   **部署阶段：** 使用 **SpiNN-3 开发板**（包含4颗 SpiNNaker 芯片，每颗芯片有18个 ARM968E-S 核心）。
*   **算力特点：** 强调了硬件的低功耗特性，而非大规模并行算力。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了仿真测试（Mac）和硬件实测（SpiNNaker）。针对两个数据集分别进行了 35ms 和 100ms 时间窗口的对比实验。
*   **充分性评价：** 实验设计较为充分。作者不仅测试了整体准确率，还给出了逐类（Per-class）的准确率分析，并详细测量了动态功耗和推理延迟。通过跨受试者（Cross-subject）验证确保了实验的客观性。

### 6. 主要结论与发现
*   **极高性能功耗比：** 在 ASL-DVS 数据集上，硬件部署的准确率为 **83.1%**（仿真为 92.27%）。
*   **超低延迟：** 推理延迟仅为 **3 ms**，远低于人类感知的 100ms 阈值。
*   **极低功耗：** 动态功耗仅为 **0.565 mW**，比 Loihi 平台上的同类模型功耗降低了约 86 倍。
*   **注意力机制的必要性：** 实验证明 sVA 能有效在复杂背景中定位手部，显著优于简单的中心裁剪（Center-crop）基准。

### 7. 优点（亮点）
*   **端到端神经形态化：** 从感知（DVS）到注意力过滤再到分类识别，全链路采用脉冲机制，充分发挥了硬件的异步优势。
*   **硬件约束下的优化：** 不同于许多只在仿真中运行的论文，本项目在严格的 SpiNNaker 硬件资源限制下完成了模型压缩与部署。
*   **开源贡献：** 提供了合成的事件化 SL-MNIST 数据集和完整的代码库，利于社区复现。

### 8. 不足与局限
*   **仿真与硬件的性能差距：** 硬件部署准确率（83.1%）明显低于仿真（92.27%），主要原因是 SpiNNaker 的定点数权重转换带来的量化误差。
*   **任务范围有限：** 目前仅针对静态手势（指拼），尚未扩展到包含动态动作的连续手语识别。
*   **注意力机制的计算平台：** 虽然 sVA 理论上兼容 SpiNNaker，但在本实验中 sVA 部分仍运行在通用处理器上，未实现完全的单芯片集成。
*   **类别覆盖：** 排除掉了 J 和 Z 两个具有动态属性的字母，识别范围尚不完整。

（完）
