---
title: FPGA-Based Hardware Architecture for Contrast Maximization in Event-Based Vision
title_zh: 基于 FPGA 的事件视觉对比度最大化硬件架构
authors: "Michal Filipkowski, Marcin Kowalczyk, Tomasz Kryjak"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09581v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 用于事件相机视觉传感器和系统的FPGA硬件架构
tldr: 本研究针对基于事件视觉中的对比度最大化（CM）算法，提出了一种基于FPGA的硬件架构。该架构利用FPGA的并行性和流水线特性，实现了事件扭曲、对比度计算和迭代优化的硬件加速。作为首个CM算法硬件加速方案，它在处理速度上比CPU和GPU快200倍以上，为高帧率、低功耗的嵌入式实时运动估计提供了高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09581v1/fig-001.webp\", \"caption\": \"Fig. 3. Hardware architecture diagram of an application for object tracking using CM\", \"page\": 8, \"index\": 1, \"width\": 514, \"height\": 376}]"
motivation: 旨在解决事件相机运动参数估计中CM算法计算量大、难以在嵌入式设备上实时运行的问题。
method: 设计了一种深度流水线的FPGA架构，包含事件扭曲、对比度计算及迭代优化模块，并采用了硬件感知的优化方法。
result: 实验表明该架构的运动参数估计速度比传统CPU和GPU实现快200倍以上，且具有极高的能效比。
conclusion: 该硬件架构为高速、低功耗嵌入式系统中的实时运动估计奠定了坚实基础，并成功应用于目标跟踪任务。
---

## 摘要
本文提出了一种在现场可编程逻辑门阵列（FPGA）资源中实现对比度最大化（CM）算法的硬件架构，用于事件视觉系统。CM 通过最大化从异步事件流重建的扭曲事件图像（IWE）的对比度来估计运动参数。事件视觉传感器产生具有高时间分辨率和低空间冗余的稀疏数据，这使其非常适合硬件处理。利用 FPGA 的确定性、大规模并行结构，设计了一种深度流水线架构，能够进行高吞吐量、高能效的处理，适用于实时嵌入式应用。本文详细介绍了负责事件扭曲、对比度计算和迭代优化的硬件模块，讨论了关键的实现决策，并展示了设计中使用的硬件感知优化方法。实验结果表明，与基于 CPU 和 GPU 的实现相比，该架构在速度和效率上有显著提升，运动参数估计的执行速度快了 200 倍以上。据我们所知，这是首个能够加速 CM 算法计算的硬件架构。其性能从处理速度、能效和硬件资源利用率等方面进行了评估。所提出的设计通过一个基于事件的目标跟踪应用进行了验证。结果证实，该架构为高速、低功耗嵌入式系统中的实时运动估计提供了坚实的基础。

## Abstract
This paper presents a hardware architecture that implements the Contrast Maximization (CM) algorithm in Field-Programmable Gate Array (FPGA) resources for event-based vision systems. CM estimates motion parameters by maximizing the contrast of an Image of Warped Events (IWE) reconstructed from asynchronous event streams. Event-based vision sensors generate sparse data with high temporal resolution and low spatial redundancy, which makes them well suited for hardware processing. The deterministic, massively parallel structure of the FPGA is leveraged to design a deeply pipelined architecture capable of high-throughput, energy-efficient processing suitable for real-time embedded applications. This paper details the hardware modules responsible for event warping, contrast computation, and iterative optimization, discusses key implementation decisions, and presents the hardware-aware optimization method used in the design. Experimental results demonstrate a substantial speed and efficiency improvement over CPU- and GPU-based implementations, with motion parameter estimation executing over 200 times faster. To the best of our knowledge, this is the first hardware architecture enabling acceleration of CM algorithm computations. Its performance is evaluated in terms of processing speed, energy efficiency, and hardware resource utilization. The proposed design is validated using an event-based object tracking application. The results confirm that the architecture provides a solid foundation for real-time motion estimation in high-speed, low-power embedded systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《FPGA-Based Hardware Architecture for Contrast Maximization in Event-Based Vision》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：事件相机（DVS）以其高时间分辨率、低延迟和高动态范围在机器人和自动驾驶领域备受关注。对比度最大化（CM）算法是处理事件数据的核心方法，用于估计运动参数、深度和光流。
*   **核心问题**：CM 算法涉及复杂的迭代优化过程，计算量巨大。现有的基于 CPU 或 GPU 的实现往往难以满足嵌入式系统对实时性、低功耗和高能效的严苛要求。
*   **研究动机**：利用 FPGA 的并行性和深度流水线特性，设计首个专门针对 CM 算法的硬件加速架构，以实现高速、低功耗的实时运动估计。

### 2. 论文提出的方法论
*   **核心思想**：将 CM 算法的各个阶段（事件预处理、扭曲、双线性投票、梯度计算、参数更新）映射到 FPGA 的硬件逻辑中，通过并行处理和流水线设计消除计算瓶颈。
*   **关键技术细节**：
    *   **事件扭曲（Warping）**：根据当前的运动参数 $(v_x, v_y)$，将每个事件的时间戳与参考时间的差值作为位移补偿，计算出扭曲后的空间坐标。
    *   **双线性投票（Bilinear Voting）**：为了提高精度，将扭曲后的事件按距离分配给相邻的 4 个像素。硬件上通过将内存划分为 12 个 BRAM 库（对应 IWE 图像及其在 x、y 方向的导数图像），实现了单时钟周期内 4 个像素值的同步写入。
    *   **冲突处理机制**：设计了一个基于寄存器的微型缓冲区，存储最近 3 次写入的地址和值，解决了流水线中“读-改-写”操作可能导致的内存写入冲突。
    *   **梯度下降优化**：在硬件中实现梯度上升循环，通过 100 次迭代不断更新运动参数，最终输出最优的 ROI（感兴趣区域）位置。

### 3. 实验设计
*   **实验平台**：硬件实现基于 **Xilinx Kria KV260** Vision AI 入门套件（搭载 Zynq UltraScale+ MPSoC）。
*   **数据集/场景**：使用了标准的 **DAVIS 240C 数据集**进行验证，具体应用场景为基于 CM 的目标跟踪。
*   **Benchmark 与对比方法**：
    *   **对比对象**：Python 参考模型（PyTorch 实现）、Intel Core i5-11300H CPU、Nvidia GeForce RTX 3050 Ti GPU。
    *   **评估指标**：处理时间（ms）、时钟频率、资源利用率、功耗。

### 4. 资源与算力
*   **FPGA 资源占用**：
    *   LUTs: 3,655 (3.12%)，Registers: 7,857 (3.35%)。
    *   **BRAM**: 25.5 (17.7%)，**DSPs**: 151 (12.1%)。
*   **功耗**：片上总功耗仅为 **0.813 W**。
*   **算力表现**：系统运行频率为 **210 MHz**。处理一批 5,000 个事件（100 次迭代）仅需 **0.92 ms**。
*   **对比设备规格**：CPU (i5-11300H, 3.10 GHz)，GPU (RTX 3050 Ti)。

### 5. 实验数量与充分性
*   **实验规模**：论文通过对特定批次事件的处理时间进行了直接对比，并推导了处理时间公式 $C_{batch}$ 以预测不同参数下的性能。
*   **充分性评价**：实验在功能验证（与 PyTorch 结果一致性）和性能对比上是充分的。然而，由于这是该领域的首个 FPGA 架构，缺乏与其他硬件加速器（如 ASIC 或其他 FPGA 方案）的横向对比。实验主要集中在 64x64 的 ROI 尺寸上，对于更大分辨率的扩展性实验相对较少。

### 6. 论文的主要结论与发现
*   **性能飞跃**：FPGA 架构的处理速度比 CPU 快约 **200 倍**，比 GPU 快约 **450 倍**（注：GPU 在处理此类稀疏、小规模迭代任务时因数据传输开销表现不佳）。
*   **高能效比**：在维持极高处理速度的同时，功耗控制在 1W 以下，证明了 FPGA 在嵌入式事件视觉任务中的绝对优势。
*   **实时性**：该架构能够轻松满足高速视觉应用的需求，为实时目标跟踪提供了坚实的硬件基础。

### 7. 优点
*   **首创性**：填补了 CM 算法缺乏专用硬件加速架构的空白。
*   **高效的内存管理**：通过多库并行访问和冲突缓冲机制，巧妙解决了双线性投票在硬件实现中的吞吐量难题。
*   **硬件感知优化**：不仅是算法的移植，还针对 FPGA 特性优化了迭代流程和资源分配。

### 8. 不足与局限
*   **分辨率限制**：受限于片上 BRAM 容量，目前主要支持较小的 ROI（如 64x64 或 128x128），处理全分辨率图像（如 240x180 以上）需要依赖外部存储器，这可能会引入延迟。
*   **运动模型单一**：目前仅支持 2D 恒速平移模型，尚未扩展到更复杂的旋转、缩放或 3D 运动模型。
*   **优化算法简单**：采用的是基础的梯度上升法，对于复杂的非凸损失函数，可能容易陷入局部最优，未来可引入更先进的优化策略。

（完）
