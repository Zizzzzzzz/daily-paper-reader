---
title: Structured Redundancy Modeling for Efficient Visual Token Pruning in High-Resolution MLLMs
title_zh: 面向高分辨率多模态大语言模型的高效视觉标记剪枝的结构化冗余建模
authors: "Jouwon Song, Woohyeong Kim, Kyeongbo Kong"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23046v1"
tags: ["query:evlm"]
score: 9.0
evidence: 高分辨率MLLM的视觉Token剪枝
tldr: 高分辨率多模态大模型因视觉token爆炸导致严重延迟，现有迭代剪枝方法选择开销剧增。本文提出SFPruner，通过语义引导的脊杠杆和排序定向掩码，在单次前向传播中实现冗余感知选择。在Qwen2.5-VL上，选择时间从112.4ms降至2.5ms，在激进压缩下保持竞争力，实现了理论缩减向实际加速的转化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23046v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1095, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1179, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1035, \"height\": 593, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23046v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1195, \"height\": 666, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 644, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1191, \"height\": 592, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 787, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1213, \"height\": 212, \"label\": \"Table\"}]"
motivation: 现有视觉token剪枝方法依赖迭代子集构造，token数量增加时选择开销严重，阻碍了理论FLOPs降低转化为实际加速。
method: 提出SFPruner，通过语义引导的脊杠杆抑制冗余协方差方向，并结合排序定向掩码的并行张量操作消除残差重叠，实现单前向传播冗余感知选择。
result: 在Qwen2.5-VL上，512 token选择时间从112.4ms降至2.5ms，并在激进压缩下保持与SOTA竞争的性能。
conclusion: 结构化的非迭代剪枝框架成功将理论token缩减转化为实际推理加速，同时保持高性能。
---

## 摘要
近期的高分辨率多模态大语言模型（MLLMs）为每个输入生成数千个视觉标记，导致视觉标记爆炸，带来严重的延迟瓶颈。尽管标记剪枝可以缓解这一问题，但目前最先进的子集优化方法通常依赖迭代子集构建来同时捕捉视觉多样性和指令相关性。随着视觉标记数量的增长，这种顺序依赖性引入了显著的选择开销，严重限制了理论FLOPs减少转化为实际墙钟加速。为解决此限制，我们提出单向前剪枝器（SFPruner），一种视觉标记剪枝的结构化重构，将冗余控制直接嵌入得分空间，无需迭代组合优化。我们的非迭代框架通过两种互补机制在单次前向传播中实现冗余感知的重要性选择。首先，为在协方差层面减弱冗余，我们引入语义引导的岭杠杆方案。通过整合指令相关性和视觉显著性，该机制抑制主导协方差方向并缓解表示偏差。其次，基于排名的方向掩码通过非对称相似性竞争解决残余重叠，其中得分较高的标记通过并行张量操作显式抑制冗余的较低得分候选项。大量评估表明，我们的方法保持稳定的选择成本，将标记选择过程减少多达110毫秒，在Qwen2.5-VL的512个标记下从112.4毫秒降至仅2.5毫秒。这种结构效率成功将理论标记减少转化为实际的推理加速，同时在激进压缩下保持与最先进技术极具竞争力的性能。

## Abstract
Recent high-resolution Multimodal Large Language Models (MLLMs) generate thousands of visual tokens per input, leading to a visual token explosion that introduces severe latency bottlenecks. While token pruning mitigates this issue, state-of-the-art subset-optimization methods typically rely on iterative subset construction to jointly capture visual diversity and instruction relevance. As visual token counts scale, this sequential dependency introduces significant selection overhead, severely limiting the translation of theoretical FLOPs reductions into actual wall-clock speedups. To address this limitation, we propose Single-Forward Pruner (SFPruner), a structural reformulation of visual token pruning that embeds redundancy control directly into the scoring space, bypassing the need for iterative combinatorial optimization. Our non-iterative framework achieves redundancy-aware importance selection in a single forward pass through two complementary mechanisms. First, to attenuate redundancy at the covariance level, we introduce a semantics-guided ridge leverage scheme. By integrating instruction relevance and visual saliency, this mechanism suppresses dominant covariance directions and mitigates representation bias. Second, ranking-based directional masking resolves residual overlap through asymmetric similarity competition, where higher-scoring tokens explicitly suppress redundant lower-scoring alternatives via parallel tensor operations. Extensive evaluations demonstrate that our approach maintains stable selection costs, reducing the token selection process by up to 110 ms, from 112.4 ms to just 2.5 ms at 512 tokens in Qwen2.5-VL. This structural efficiency successfully translates theoretical token reductions into tangible inference speedups while preserving highly competitive performance against state-of-the-art techniques under aggressive compression.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题背景**：高分辨率多模态大语言模型（MLLMs）如LLaVA-NeXT、Qwen-VL等，采用动态分辨率机制为每张输入图像生成数千个视觉token（例如5×576=2880个token）。由于自注意力的计算复杂度与token数量的平方成正比，这带来了严重的延迟和内存瓶颈。
- **现有方法局限**：
  - **启发式方法**（如Top-K、关注局部注意力）速度快，但无法充分消除空间冗余，在激进剪枝下性能大幅下降。
  - **子集优化方法**（如基于DPP、MWIS、多样性最大化）能更好地兼顾重要性和多样性，但依赖迭代子集构造（NP-hard问题需贪心搜索），随着token数量增加，选择开销急剧上升，导致理论FLOPs减少无法转化为实际加速（例如CDPruner在512 token时需112.4ms选择时间）。
- **核心动机**：提出一种非迭代的结构化冗余建模方法，将冗余控制直接嵌入评分过程，在单次前向传播中完成冗余感知的token选择，从而消除迭代依赖，实现理论加速向实际加速的转化。

## 2. 论文提出的方法论

### 核心思想
将视觉token剪枝从迭代子集优化重构为结构化的评分空间问题，通过两个互补机制在单次前向传播中同时处理协方差层面和成对层面的冗余。

### 关键技术细节

#### 步骤一：语义引导（Semantic Guidance）——基础重要性评分
- **文本相关性**：计算每个视觉token与投影文本嵌入的余弦相似度，经温度缩放归一化得到 \( S_{\text{rel},i} \)。
- **视觉显著性**：从视觉编码器的自注意力中提取CLS token到各视觉token的注意力权重 \( S_{\text{attn},i} \)（若无CLS则使用空间token聚合）。
- **语义引导融合**：线性加权 \( S_{\text{guide},i} = \alpha S_{\text{rel},i} + (1-\alpha)S_{\text{attn},i} \)，并进行Min-Max归一化得到 \( \tilde{S}_{\text{guide}} \)。

#### 步骤二：语义引导的岭杠杆评分（SG-RLS）——协方差级冗余减弱
- 定义特征协方差矩阵 \( C = V^\top V \in \mathbb{R}^{D \times D} \)。
- 计算每个token的岭杠杆得分：\( \ell_i = v_i (C + \lambda I_D)^{-1} v_i^\top \)。
- 岭正则化抑制主导协方差方向，使结构独特的token获得更高得分。
- 结合语义引导得到最终协方差级得分：\( S_{\text{SG-RLS},i} = \ell_i \cdot \tilde{S}_{\text{guide},i} \)。这相当于软与门操作，同时要求结构独特性和语义重要性。
- **计算优化**：利用Woodbury矩阵恒等式将 \( D \times D \) 协方差矩阵与 \( N \times N \) 克矩阵等价转换，在 \( N > D \) 时选择较小维度求逆，避免 \( O(N^3) \) 复杂度。

#### 步骤三：基于排名的方向掩码（Ranking-Based Directional Masking）——成对冗余解决
- 计算token间余弦相似度矩阵 \( C_{\text{sim}} = V V^\top \)。
- 构建方向掩码矩阵 \( M_{ij} = \mathbb{I}(S_{\text{SG-RLS},j} > S_{\text{SG-RLS},i}) \)，实现非对称竞争：高得分token可抑制低得分token，反之不行。
- 对每个token i，计算抑制惩罚 \( P_i = 1 - \max_{j} (M_{ij} \cdot C_{\text{sim},ij}) \)。
- 最终得分 \( S_{\text{final},i} = S_{\text{SG-RLS},i} \cdot P_i \)。
- 所有操作均为并行张量运算（矩阵乘法、逐元素比较、行归约），最后通过一次Top-K选择最终子集。

### 算法流程（文字说明）
1. 输入视觉token嵌入矩阵V和文本嵌入q。
2. 计算语义引导得分 \( S_{\text{guide}} \)。
3. 计算岭杠杆得分 \( \ell \) 并融合得到 \( S_{\text{SG-RLS}} \)。
4. 计算余弦相似度矩阵和方向掩码，得到方向抑制惩罚 \( P \)。
5. 计算最终得分 \( S_{\text{final}} = S_{\text{SG-RLS}} \cdot P \)。
6. 执行单次Top-K选择得到保留token子集。

## 3. 实验设计

### 数据集/场景
- **图像理解基准**：VQAv2、GQA、TextVQA、MME、ScienceQA (SQA)、POPE、MMBench、MMBench-CN、MMVet。
- **视频理解基准**：VideoMME、MLVU、LongVideoBench、MMVBench。

### Benchmark设置
- **模型架构**：
  - **LLaVA-NeXT-7B**：多块高分辨率设定（每张图最多5个patch，每patch 576 token），测试保留每patch 128/64/32 token（总640/320/160 token）。
  - **Qwen2.5-VL-7B**：单序列密集编码（全图连续token序列），测试保留40%和20% token（平均512和256 token）。
  - **LLaVA-Video-7B**：多帧视频处理（每帧保留512或256 token）。
- **极端可扩展性测试**：Qwen2.5-VL上输入9216 token（30%保留约2770 token），测量峰值GPU内存、剪枝时间、端到端推理延迟。

### 对比方法
- **启发式方法**：PruMerge+、VisionZip、SparseVLM、VisPruner。
- **子集优化方法**：DivPrune、CDPruner、D2Pruner。
- **其他**：VisionSelector（基于学习的方法）。

## 4. 资源与算力

- **GPU型号**：单张NVIDIA RTX 4090 GPU。
- **训练时长**：论文未明确说明模型训练时长（SFPruner本身无需额外训练，仅进行推理时剪枝）。
- **内存消耗**：极端测试中峰值内存约18.4 GB（与对比方法相当），低于未剪枝的完整序列（19.7 GB）。

## 5. 实验数量与充分性

- **实验组数**：共进行5大类实验：
  1. LLaVA-NeXT-7B上3种保留预算（128/64/32 per patch） × 9个数据集 = 27个性能比较。
  2. Qwen2.5-VL上2种保留比例（40%/20%） × 8个数据集 = 16个性能比较。
  3. LLaVA-Video上2种framewise保留（512/256） × 4个视频基准 = 8个性能比较。
  4. 极端可扩展性测试（1组完整对比）。
  5. 消融实验（4种配置对比，含延迟和性能）。
- **充分性评估**：
  - 覆盖了多架构（3种MLLM）、多分辨率设置（多patch、单序列、多帧）、多种压缩率（从22%到5.5%保留）。
  - 消融实验验证了SG-RLS和方向掩码各自的贡献。
  - 实验设计客观：所有方法在同一基准下公平比较，报告了绝对准确率和相对保留性能，以及选择延迟。
- **可能不足**：未在更大模型（如13B/72B）上验证，也未在更广泛视频数据集上测试；消融实验仅在LLaVA-NeXT-7B一个预算下进行。

## 6. 论文的主要结论与发现

- **非迭代剪枝实现结构化效率**：SFPruner通过协方差级冗余减弱和并行方向掩码，在单次前向传播中完成冗余感知选择，选择时间稳定（如Qwen2.5-VL上始终2.5ms），不随保留token数变化。
- **高性能保持**：
  - LLaVA-NeXT-7B：640 token保留时达到100.0%相对性能；320 token时98.2%；160 token时96.4%。
  - Qwen2.5-VL：40%保留时99.0%相对性能；20%保留时96.5%，均接近或超过优化方法。
  - 视频任务：512 token/frame时94.9%相对性能，为最高。
- **实现实际加速**：极端测试（9,216 token）中，SFPruner将预填充延迟从1813ms降至1114ms，总推理时间从6101秒降至3601秒；而优化方法因剪枝开销大，加速有限。
- **理论FLOPs减少成功转化为实际加速**：关键在于消除了迭代依赖，使剪枝开销保持恒定且极小。

## 7. 优点

- **方法创新性**：首次将岭杠杆得分引入视觉token剪枝，并结合语义引导和方向掩码，实现结构化非迭代剪枝。
- **效率极高**：选择延迟仅几毫秒，接近最简启发式方法，但性能接近优化方法。
- **通用性强**：在三种不同MLLM架构（多patch、单序列、多帧）上均有效，无需重新训练。
- **可扩展性好**：在9,216 token极端输入下仍保持高效，峰内存未显著增加。
- **分析深入**：通过微架构分析（图3）揭示了迭代方法的瓶颈，并通过消融实验验证各模块贡献。

## 8. 不足与局限

- **实验覆盖有限**：
  - 未在更大规模模型（如13B、72B）上测试，也未在更多样化任务（如医学图像、遥感）上验证。
  - 视频任务仅使用LLaVA-Video-7B，未在更强视频模型（如Video-LLava 13B）上对比。
  - 消融实验仅在单一保留预算（320 token）下进行，未分析不同预算下模块贡献变化。
- **超参数敏感性**：方法包含超参数α（语义融合权重）和λ（岭正则化常数），论文仅提及在补充材料中讨论，但未在主文中给出详细敏感性分析。
- **依赖视觉编码器注意力显著性**：当视觉编码器没有CLS token时，使用替代聚合可能不鲁棒；且视觉显著性本身可能受噪声影响。
- **应用限制**：方法仅适用于需要保留token数量的场景（预先设定K），若需动态调整token数则需额外策略。

（完）
