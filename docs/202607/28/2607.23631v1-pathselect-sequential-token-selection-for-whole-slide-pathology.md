---
title: "PathSelect: Sequential Token Selection for Whole Slide Pathology"
title_zh: PathSelect：面向全切片病理学的顺序令牌选择
authors: "Jingzhi Chen, Landi He, Zehong Chen, Peihang Wu, Lijian Xu"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23631v1"
tags: ["query:evlm"]
score: 9.0
evidence: 面向全切片病理图像的序列化Token选择（剪枝）
tldr: "全切片病理图像（WSI）序列极长，现有空间采样或训练无关剪枝方法易丢失微弱但关键信号。本文提出PathSelect，将令牌剪枝重构为序列选择过程，使模型自主学习最优路由。采用可微Soft Top-K算子与对角线注意力去噪器，训练后仅依赖确定性Hard Top-K。在SlideBench（TCGA）上平均仅44.86个令牌即达74.00%准确率，空间压缩36.6倍，显著优于采样方法，且即插即用、推理无额外延迟。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23631v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1709, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1645, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 1209, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1592, \"height\": 1551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1685, \"height\": 645, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23631v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1711, \"height\": 777, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23631v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 450, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23631v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 514, \"label\": \"Table\"}]"
motivation: 现有WSI视觉语言模型因序列过长计算瓶颈大，静态启发式剪枝易丢失微弱诊断信号，需自适应可学习方法。
method: 提出PathSelect，集成于预训练SlideChat，冻结编码器和LLM；通过可微Soft Top-K门控和去噪器实现连续梯度训练，推理时用Hard Top-K自适应选择少量关键令牌。
result: "SlideBench（TCGA）上平均44.86令牌达74.00%准确率，较基线压缩36.6倍，优于所有采样方法。"
conclusion: PathSelect实现自适应高效WSI令牌选择，大幅加速并保持精度，为病理VLM提供即插即用最优路由方案。
---

## 摘要
千兆像素全切片图像（WSI）因极长的序列长度给视觉语言模型（VLM）带来了根本性的计算瓶颈。现有方法主要依赖空间采样或无训练剪枝，这有可能稀释微弱但信息量丰富的信号，导致因病理线索的空间弥散性而丢失关键诊断证据。我们将WSI令牌剪枝重新构思为顺序选择过程，使模型能够自主学习最优路由策略，而非依赖静态启发式方法。本文提出一种解耦路由框架，作为主动插件集成到完全预训练的SlideChat基础模型中，同时保持切片编码器和大型语言模型冻结。为在训练期间为非可微剪枝操作提供连续梯度，我们引入了PathSelect。PathSelect采用保持方差的噪声门控，通过可微的Soft Top-K算子调制每个补丁的信息流，并配以对角注意力去噪器，在不引起语义泄漏的情况下恢复扰动后的表示。在推理时，PathSelect模块完全分离。仅依靠训练后的评分器，确定性Hard Top-K算子执行自适应、数据依赖的轨迹终止，以极低的顺序令牌选择延迟显著加速下游生成处理。在K=128的最大约束下，平均仅使用44.86个令牌，我们的框架在SlideBench（TCGA）上实现了74.00%的整体准确率，相对于未压缩基线平均实现了约36.6倍的空间令牌缩减，同时持续优于基于采样的对应方法。

## Abstract
Gigapixel Whole-Slide Images (WSIs) present a fundamental computational bottleneck for vision-language models (VLMs) due to extreme sequence lengths. Existing approaches predominantly rely on spatial sampling or training-free pruning, which risk diluting weak but informative signals, leading to the loss of critical diagnostic evidence due to the spatially diffuse nature of pathological cues. We reformulate WSI token pruning as a sequential selection process, enabling the model to autonomously learn an optimal routing strategy rather than relying on static heuristics. We herein propose a decoupled routing framework integrated as an active plugin into the fully pre-trained SlideChat base model, leaving both the slide encoder and large language model frozen. To provide continuous gradients for the non-differentiable pruning operation during training, we introduce PathSelect. PathSelect employs a variance-preserving noise gate to modulate each patch's information flow via a differentiable Soft Top-K operator, paired with a diagonal-attention Denoiser that recovers the perturbed representations without semantic leakage. At inference, the PathSelect module is entirely detached. Relying solely on the trained Scorer, a deterministic Hard Top-K operator executes adaptive, data-dependent trajectory termination, significantly accelerating downstream generative processing with exceptionally low sequential token selection latency. Driven by an empirical average of only 44.86 tokens under a maximum constraint of K = 128, our framework achieves 74.00% overall accuracy on SlideBench (TCGA), representing an approximate 36.6x spatial token reduction relative to the uncompressed baseline average while consistently outperforming sampling-based counterparts.

---

## 论文详细总结（自动生成）

# 论文PathSelect：面向全切片病理学的顺序令牌选择 详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：全切片图像（WSI）分辨率极高，生成超过10^5个补丁，输入视觉语言模型（VLM）时导致序列长度爆炸，自注意力计算复杂度呈二次增长，难以端到端处理。
- **现有方法的不足**：主流方案采用空间采样或训练无关剪枝（如注意力幅度、视觉相似性），但这些启发式方法假设压缩就是去除背景噪声，忽略了病理数据的极端稀疏性——关键诊断证据（如微转移灶）可能仅占切片1%以下，容易导致微弱但关键信号被稀释或丢失。
- **核心含义**：本文重新定义WSI令牌剪枝为**顺序选择过程**，模型自学习最优路由策略，而非依赖静态启发式规则，从而保留稀疏病理特征并大幅压缩序列长度。

## 2. 方法论

### 核心思想
- **解耦路由框架**：将PathSelect作为主动插件插入完全预训练的SlideChat（视觉编码器和LLM均冻结），仅在训练时使用可微分机制，推理时完全分离，实现低延迟自适应选择。
- **关键技术**：
  - **条件指针解码器**：将令牌选择建模为自回归顺序决策过程。使用交叉注意力计算候选令牌得分，动态更新被选令牌的屏蔽掩码，防止重复选择。引入虚拟停止令牌s_stop实现数据依赖的轨迹终止。
  - **可微分选择（PathSelect模块）**：通过Soft Top-K算子将离散argmax转化为连续扰动。**方差保持噪声门控**：根据累积存活加权概率，对未选中的背景令牌注入各向同性高斯噪声，保留关键信息的同时提供梯度。**对角注意力去噪器**：使用对角注意力掩码限制令牌间信息聚合，防止语义泄漏（未选中令牌通过全局注意力绕过噪声门）。
  - **联合优化与长度惩罚**：损失函数为语言建模损失 + λ·(K/N)，鼓励极端稀疏化但避免过度压缩（K为动态选择的令牌数，N为总补丁数）。
- **推理阶段**：移除噪声门控和去噪器，仅靠训练后的评分器执行确定性Hard Top-K，极低延迟。

## 3. 实验设计

- **数据集与基准**：
  - 主要训练和评估：**SlideBench (TCGA)**，报告整体准确率及三个子任务：显微镜（微观形态）、诊断（组织分型）、临床（临床推理）。
  - 零样本泛化：**SlideBench (BCNB)** 和 **WSI-VQA\***（外部VQA数据集）。
- **对比方法**：
  - 上界：SlideChat（未压缩）
  - 随机基线
  - 通用VLM：GPT-4o、LLaVA-Med、Quilt-LLaVA、MedDr
  - 所有基线在计算资源匹配条件下比较。

## 4. 资源与算力

- 使用**单张NVIDIA A6000 GPU**（48GB显存），单阶段训练协议。
- 仅训练PathSelect模块（评分器、噪声门控、去噪器）和线性投影桥，**全部预训练参数冻结**。
- 论文未明确说明训练时长、迭代次数或总参数量，但强调为“轻量级单GPU训练”。

## 5. 实验数量与充分性

- **主实验**：Table 1 在SlideBench (TCGA)上报告整体及子任务准确率，并在两个零样本数据集上验证。
- **消融实验**（Table 2/3）：
  - 去噪器注意力机制（全局 vs 对角）→ 验证语义泄漏风险。
  - 评分器聚合策略（4种变体）→ 确认完整公式最优。
  - 长度惩罚系数扫描（λ从0到6）及目标预算K扫描（64/128/256）→ 分析稀疏性与性能权衡。
- **效率分析**：Figure 3对比令牌数/FLOPs；Figure 4展示不同令牌数下准确率稳定性；Figure 5 UMAP可视化选定令牌的拓扑分布。
- **充分性评估**：实验覆盖了方法核心组件、超参数、效率、泛化和可视化分析，相对全面。但在消融实验中未与同类型可学习方法（如Loc-Path）直接对比，略有不足。对比基线包括通用和专用模型，公平性良好。

## 6. 主要结论与发现

- **核心性能**：在最大约束K=128下，**平均仅用44.86个令牌**，达到SlideBench (TCGA) 74.00%整体准确率，相较于未压缩SlideChat（平均1641.87令牌）实现**36.6倍空间压缩**，且**临床推理子项准确率（74.49%）甚至超过上界（71.43%）**，表明噪声冗余被有效过滤。
- **零样本泛化**：在BCNB上56.39%，WSI-VQA*上60.76%，与上界竞争。
- **自适应特性**：准确率在不同动态保留令牌数下波动极小（Figure 4），验证了策略对诊断难度的适应性。
- **路由机制有效性**：UMAP显示选中令牌位于特征空间边缘（异常值），对应关键病理区域；未被选中的集中分布在中心（冗余正常组织）。

## 7. 优点

- **方法创新性**：首次将WSI令牌剪枝建模为**可微分顺序选择**，通过Soft Top-K+噪声门控+对角注意力去噪器解决非可微难题，无需辅助损失或额外标注。
- **高效性与通用性**：即插即用，不修改预训练模型，单GPU可训练；推理时几乎零延迟附加；适用于任何基于视觉编码器+LLM的病理VLM。
- **性能突出**：在极端压缩下保持甚至提升诊断精度，尤其在临床推理任务上超过未压缩模型，证明适当剪枝可去噪提效。
- **分析充分**：提供详细消融、效率、可视化分析，验证各组件必要性。

## 8. 不足与局限

- **实验覆盖范围**：主要基于TCGA单一癌症数据集，未在多癌种、多染色、不同扫描仪数据集上充分验证泛化性；零样本仅两个外部集，规模有限。
- **对比局限性**：未与近期其他可学习WSI剪枝方法（如Loc-Path、TokenPaste等）直接比较。
- **上界假设**：最大K=128虽然适应大多数情况，但极复杂切片可能仍需更多令牌，实验未探索K>256场景。
- **训练敏感性**：长度惩罚系数λ的选择和温度τ可能影响性能，论文未详细分析不同设置下的稳定性。
- **推理延迟**：虽然宣称极低，但未给出具体推理时间对比（如msec/样本）。
- **去噪器风险**：对角注意力虽防止语义泄漏，但可能限制去噪器对全局上下文的利用，在某些场景下可能不够鲁棒。

（完）
