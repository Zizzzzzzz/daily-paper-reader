---
title: A First Exploration of Neuromorphic OT-CFM for Multi-Speaker VSR
title_zh: 面向多说话人VSR的神经形态OT-CFM初步探索
authors: "Lin Chen, Jingping Fang, Hairui Liu, Chenyang Xu, Junhao Chen, Xiaorui Li, Weidong Cai, Xiaoming Chen"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31225v2"
tags: ["query:ev-cam"]
score: 9.0
evidence: 神经形态事件流用于视觉语音识别
tldr: "多说话人视觉语音识别（VSR）因快速头部运动、遮挡和细微唇部动作而困难，传统RGB方法帧率低且存在运动模糊。为此提出LipsFlow，将RGB视频转换为高时间分辨率事件流，利用ByteTrack和TalkNet将场景分割为单说话人片段，并通过最优传输条件流匹配（OT-CFM）在语义潜空间中强制确定性直线轨迹生成，仅需两步ODE推理。结合双级语义监督（BERT权重绑定和句子级先验）消除同音词歧义。在基准上实现22.3%词错误率和240ms延迟，建立了鲁棒高效的基于事件的VSR范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31225v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 422}, {\"url\": \"assets/figures/arxiv/2606.31225v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 716}, {\"url\": \"assets/figures/arxiv/2606.31225v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1238, \"height\": 677}, {\"url\": \"assets/figures/arxiv/2606.31225v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1255, \"height\": 501}, {\"url\": \"assets/figures/arxiv/2606.31225v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1260, \"height\": 554}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31225v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1276, \"height\": 566}, {\"url\": \"assets/tables/arxiv/2606.31225v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1011, \"height\": 389}, {\"url\": \"assets/tables/arxiv/2606.31225v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1215, \"height\": 305}, {\"url\": \"assets/tables/arxiv/2606.31225v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1023, \"height\": 282}, {\"url\": \"assets/tables/arxiv/2606.31225v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 797, \"height\": 339}]"
motivation: 现有RGB方法受限于低帧率和运动模糊，难以处理多说话人场景中的快速运动和遮挡。
method: 将RGB视频转为事件流，用ByteTrack和TalkNet分割单说话人，用OT-CFM高效建模并双级语义监督消歧。
result: "在基准上SOTA，WER 22.3%且延迟仅240ms。"
conclusion: LipsFlow为事件驱动VSR提供鲁棒高效的范式，验证了神经形态与流匹配结合的有效性。
---

## 摘要
复杂多说话人场景中的视觉语音识别（VSR）任务严重受到快速头部运动、遮挡和细微唇部发音的阻碍。基于RGB的传统方法由于帧率低和运动模糊而难以应对。为了解决这些问题，我们提出了LipsFlow，一种受神经形态启发的VSR框架，将RGB视频转换为高时间分辨率的事件流。对于多说话人场景，我们采用ByteTrack跟踪和TalkNet主动说话人检测，将场景按时间分割为单说话人片段，从而实现针对每个说话人的聚焦分析。通过可学习的事件表示显式捕获微秒级别的发音动态，LipsFlow对视觉退化具有内在的鲁棒性。为了高效建模这些密集的事件特征并适应说话人特定的发音模式，我们引入了最优传输条件流匹配（OT-CFM）。它在语义潜在空间中强制生成确定性的直线轨迹，将推理延迟缩短至仅两个常微分方程（ODE）步长。此外，我们设计了双重层级语义监督机制，结合词元级BERT权重绑定和句子级先验，以解决同音词歧义。在竞争性基准测试上，LipsFlow在240毫秒延迟下实现了22.3%的词错误率（WER），为基于事件的VSR建立了高度鲁棒且高效的范式。

## Abstract
Visual Speech Recognition (VSR) tasks in complex multi-speaker scenarios are severely hindered by rapid head motions, occlusions, and subtle lip articulations. Traditional RGB-based methods struggle here due to low rates and motion blur of frames. To overcome these, we propose LipsFlow, a neuromorphic-inspired VSR framework that converts RGB videos into high-temporal-resolution event streams. For multi-speaker, we employ ByteTrack tracking and TalkNet active speaker detection to temporally segment scenes into single-speaker clips, enabling focused per-speaker analysis. By explicitly capturing microsecond-level articulatory dynamics via learnable event-based representations, LipsFlow achieves inherent robustness against visual degradation. To efficiently model these dense event-based features and adapt to speaker-specific articulatory patterns, we introduce Optimal Transport Conditional Flow Matching (OT-CFM). It enforces deterministic, straight-line trajectory generation in a semantic latent space, slashing inference latency to just two Ordinary Differential Equation (ODE) steps. Furthermore, we design a Dual-Level Semantic Supervision mechanism combining token-level BERT weight tying and sentence-level priors to resolve homophene ambiguities. Validated on competitive benchmarks, LipsFlow achieves a state-of-the-art WER of 22.3\% at 240 ms latency, establishing a highly robust and efficient paradigm for event-based VSR.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）
- **研究动机**：多说话人场景下的视觉语音识别（VSR）面临快速头部运动、遮挡、细微唇部动作等挑战。传统基于RGB的帧式摄像头（25–30 FPS）受限于低帧率和运动模糊，丢失了如爆破音（/p/, /b/）等毫秒级的瞬态视觉线索，导致识别精度严重下降。
- **背景**：现有RGB方法（如Auto-AVSR、VATLM）依赖大规模自监督预训练，但物理感知瓶颈无法克服；神经形态事件相机虽能提供微秒级时间分辨率，但现有事件VSR方法（如MSTP、SNN-Lip）在表示学习和生成效率上存在瓶颈（聚合事件丢失细粒度发音信息、自回归/扩散模型推理缓慢）。
- **整体目标**：提出LipsFlow，首次将神经形态事件流与最优传输条件流匹配（OT-CFM）结合，在复杂多说话人场景下实现高效、鲁棒的VSR。

## 2. 方法论：核心思想、关键技术细节与算法流程
### 2.1 核心思想
- 将RGB视频转为高时间分辨率的事件流（微秒级），捕获瞬态发音动态；利用OT-CFM在语义潜空间学习确定性直线轨迹，实现仅两步ODE的高效生成；通过双级语义监督解决同音词歧义。

### 2.2 关键技术细节
1. **多说话人前端处理**：
   - 使用ByteTrack（卡尔曼滤波+IoU关联）进行身份跟踪，保持遮挡下的身份连续性。
   - 采用TalkNet-ASD进行主动说话人检测，将身份与音频段对齐。
   - 通过RetinaFace+Dlib检测68个面部关键点，提取唇部ROI（30%上下文填充），并用仿射变换消除头部抖动，归一化至128×64。
   - 三级标注协议（静默、非语音口部运动、活跃语音），通过同步音频校准时间边界。

2. **可学习事件表示**：
   - 分层帧插值：使用轻量级RIFE分阶段（2×→2×→5×）将25 FPS提升至500 FPS，避免大位移伪影。
   - 自适应事件生成：利用可微分量化器，通过轻量CNN回归局部阈值图Θ，触发事件条件为|Δlog I| > θ(x)，训练时采用soft relaxation保持可微性。
   - 事件编码：混合3D-2D编码器（3D Stem + 2D ResNet）聚合局部运动，输出特征序列F_event ∈ R^(T'×512)（125 FPS），再经12层Conformer建模全局时间依赖，得到上下文视觉特征V ∈ R^(T'×512)。

3. **说话人条件OT-CFM**：
   - 长度预测与对齐：混合长度预测器（回归+分类）估计语义token数量N，单调对齐模块将125 Hz视觉流投影至N个token槽，得到对齐视觉条件C ∈ R^(N×512)。CTC损失作为辅助对齐约束。
   - 说话人嵌入：TDNN + 时间统计池提取全局说话人嵌入s ∈ R^256，使用InfoNCE对比损失强制身份-视觉解耦。
   - 最优传输流匹配：生成目标语义x_1（BERT编码），从高斯先验x_0～N(0,1)通过OT路径学习直线概率流，训练目标为回归位移向量场v_θ。条件注入：CFM-Former（16层）通过交叉注意力和自适应层归一化（AdaLN）分别融合视觉条件C和说话人s。推理时采用2步欧拉求解器（Δt=0.5），直接恢复x_1 ∈ R^(N×512)。

4. **先验引导语义解码器**：
   - 跨模态权重绑定：将恢复的x_1经投影ϕ(·)映射至BERT嵌入空间W，通过温度缩放余弦相似度计算词汇概率分布ŷ（公式3）。
   - 双级语义监督：
     - 词元级：交叉熵损失L_CE。
     - 句子级：使用冻结的Sentence-BERT编码器，通过软加权token嵌入（ŷ加权求和）生成连续序列，最小化与真实句子嵌入的L2距离（公式4），确保端到端可微。
   - 自适应终止：训练时随机附加[PAD]token，模型学习生成终止信号，推理时截断至第一个终止token。

### 2.3 训练流程
- 两阶段课程：前10轮冻结视觉前端（lr=5e-4）训练生成模块，后20轮全模型端到端微调（lr=1e-4）。
- 联合损失：L = L_FM（流匹配）+ λ_1 L_CE + λ_2 L_sem + λ_3 L_CTC + λ_4 L_speaker。

## 3. 实验设计
### 3.1 数据集
- **DVS-Lip**：首个大规模真实事件流VSR语料库，包含细微唇部微动和挑战性词汇，直接使用其提供的事件流。
- **AVA**：非受控自然多说话人场景，通过视频帧模拟事件流。进一步划分为三个子集：
  - Rapid Motion（快速运动）
  - Low Light（低光）
  - Severe Occlusion（严重遮挡）

### 3.2 对比方法
- **基于RGB视频的方法**：Auto-AVSR、VATLM、RAVEn、SynthVSR、CMA-VSR、LipGen、Llama-AVSR。
- **基于事件的方法**：MSTP（ResNet-18）、SNN-Lip（脉冲神经网络）。
- 所有方法均按官方协议复现。

### 3.3 评估指标
- WER（词错误率）、VER（视位错误率）、RTF（实时因子）、Latency（推理延迟）、Params（参数量）。

## 4. 资源与算力
- **GPU**：4× NVIDIA A100 (80GB)
- **框架**：PyTorch Distributed Data Parallel，BF16混合精度 + TF32加速
- **全局batch size**：64（每GPU 32序列）
- **训练时长**：约30小时，总计约45k次迭代（10轮冻结 + 20轮微调）
- **优化器**：AdamW (β1=0.9, β2=0.995)，梯度裁剪1.0，余弦退火学习率调度（2000 warm-up steps）

## 5. 实验数量与充分性
### 5.1 主要实验
- **主结果表**（Table 1）：在DVS-Lip上与10种方法对比WER/VER/RTF/Latency/Params。
- **鲁棒性测试**（Table 2）：在AVA的三个退化场景（快速运动、低光、严重遮挡）上对比6种方法，记录WER及相对退化幅度。
- **消融实验**（Tables 3-5）：
  - 表3：架构消融（视觉仅、朴素融合、神经形态分支、扩散变体、完整LipsFlow）。
  - 表4：推理步数（NFE）与速度-精度权衡（1/2/4/10步 vs Diffision 50步）。
  - 表5：损失配置（CE-only、OT-CFM、去噪对比、完整损失）、训练策略（单阶段/两阶段/数据增强）影响。
- 所有实验均使用官方train/test分割，确保公平对比。

### 5.2 充分性评价
- **完整性**：覆盖了精度、效率、鲁棒性、消融四个维度，实验设计系统。
- **客观性**：对比方法涵盖主流RGB和事件基线，且标注了“遵循官方协议”。
- **局限性**：未在不同帧率/分辨率下进行额外敏感性分析；AVA事件流由模拟生成，可能低估真实事件相机噪声影响。整体实验充分但可扩展。

## 6. 主要结论与发现
- **性能突破**：LipsFlow在DVS-Lip上达到22.3% WER，优于所有RGB方法（包括Llama-AVSR的23.9%）和事件方法（MSTP的30.5%），且VER为19.8%，RTF仅0.18，延迟240ms。
- **鲁棒性优势**：在AVA的低光、快速运动场景中退化极小（分别仅↑0.6%和↑1.8%），严重遮挡下WER 26.5%，远超RGB方法（如SynthVSR 38.6%，Llama-AVSR 35.1%）。
- **效率优势**：OT-CFM仅需2步ODE推理，相比扩散模型（50步）加速25倍，同时参数量仅45.8M（扩散变体185.6M）。
- **消融验证**：分层插值、自适应阈值、双级语义监督、两阶段训练均带来显著增益；说话人对比损失将说话人验证准确率提升至93.8%。

## 7. 优点
- **方法创新性**：首次将神经形态事件流与最优传输流匹配结合，突破RGB物理瓶颈和扩散模型推理瓶颈。
- **多说话人处理实用化**：完整的流水线（ByteTrack跟踪+TalkNet检测+ROI稳定+三级标注）解决实际场景中身份丢失、噪声干扰问题。
- **双级语义监督**：软加权句子级损失保持端到端可微，有效消除同音词歧义。
- **推理效率与精度平衡**：2步ODE即可达到SOTA精度，RTF 0.18满足实时需求。
- **鲁棒性验证**：在三个退化场景下系统评估，证实事件流对运动模糊和低光的内在抗性。

## 8. 不足与局限
- **数据依赖**：训练需大量RGB视频并模拟事件流（V2E），模拟误差可能影响实际场景泛化；DVS-Lip事件流为真实数据但规模有限。
- **遮挡适应不足**：严重遮挡下WER达26.5%，虽优于其他方法，但仍有较大提升空间，可能缺乏自适应遮挡处理机制。
- **长序列生成**：固定长度N的预测依赖长度预测器，极端变长场景可能引入截断或填充错误。
- **计算资源**：4×A100训练30小时，相对资源需求较高，未评估在更低端硬件上的可行性和量化部署难度。
- **音频信息利用**：方法完全依赖视觉，未与音频融合；虽然强调纯视觉，但在严重降质场景下多模态融合可能进一步改善。

（完）
