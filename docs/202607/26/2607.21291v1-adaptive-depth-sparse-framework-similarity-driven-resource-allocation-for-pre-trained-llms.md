---
title: "Adaptive Depth Sparse Framework: Similarity-Driven Resource Allocation for Pre-Trained LLMs"
title_zh: 自适应深度稀疏框架：面向预训练大语言模型的相似性驱动资源分配
authors: "Yidu Wu, Xiang Wang, Kejie Zhao, Zhangchi Wang, Qinghai Guo, Xiaoying Tang"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21291v1"
tags: ["query:evlm"]
score: 8.0
evidence: 通过标记保留分配实现LLM深度稀疏推理加速
tldr: 大语言模型推理成本高昂，现有加速方法需微调或重新训练，适应性差。AdaDSF利用层输入输出余弦相似度表征层贡献，据此分配逐层token保留率，无需全量重训即可转换预训练模型。轻量路由器基于保留比选取重要token，并引入特征对齐目标匹配稀疏与密集模型的中间和最终表示，保持表征质量。在GPT-NeoX和Qwen2.5的语言建模与常识推理任务中，大幅降低FLOPs同时性能接近密集模型，优于MoD、D-LLM等强基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21291v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1168, \"height\": 678}, {\"url\": \"assets/figures/arxiv/2607.21291v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 950, \"height\": 565}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21291v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1148, \"height\": 429}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1236, \"height\": 509}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 663}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 223}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 468, \"height\": 140}]"
motivation: 现有LLM加速方法依赖任务特定微调或从零训练，增加适配成本且限制了跨任务可用性。
method: AdaDSF利用层输入输出余弦相似度分配token保留率，轻量路由器选择每层重要token，并采用特征对齐目标匹配中间和最终表示。
result: 在GPT-NeoX和Qwen2.5的语言建模和常识推理任务上，大幅减少FLOPs且性能接近密集模型，优于MoD、D-LLM等。
conclusion: AdaDSF将预训练LLM转为深度稀疏模型，无需全量重训即可实现高效推理，保持跨任务泛化能力。
---

## 摘要
大语言模型（LLMs）展现了强大的生成与推理能力，但Transformer架构带来了高昂的推理成本。现有加速方法通常依赖特定任务的微调或从头训练，增加了适配成本并限制了跨任务可用性。我们提出了一种自适应深度稀疏框架（AdaDSF），可在无需完全重训练的情况下将现成的预训练LLMs转化为深度稀疏模型。我们的关键洞察是：各层对表示变换的贡献不均，这一特征可通过层输入与输出隐藏状态之间的余弦相似度来刻画。基于此，AdaDSF从相似性统计中分配逐层的令牌保留比率，使用轻量级路由器在每层选择信息丰富的令牌，并引入特征保持对齐目标以匹配稀疏模型与稠密模型之间的中间及最终表示。在GPT-NeoX和Qwen2.5上针对语言建模与常识推理任务的实验表明，AdaDSF在显著减少推理FLOPs的同时，保持了接近稠密模型的性能。在可比稀疏度下，与MoD、D-LLM和DLO等强基线相比，AdaDSF始终产生更小的精度下降。

## Abstract
Large language models (LLMs) achieve strong generation and reasoning performance, but the Transformer architecture incurs high inference cost. Existing acceleration methods often rely on task-specific fine-tuning or training from scratch, increasing adaptation cost and limiting cross-task usability. We present an Adaptive Depth Sparse Framework (AdaDSF) that converts off-the-shelf pre-trained LLMs into depth-sparse models without full retraining. Our key insight is that layers contribute unequally to representation transformation, characterized by the cosine similarity between layer input and output hidden states. Based on this, AdaDSF assigns layer-wise token retention ratios from similarity statistics, uses a lightweight router to select informative tokens at each layer, and introduces a feature-preserving alignment objective to match intermediate and final representations between sparse and dense models. On GPT-NeoX and Qwen2.5 over language modeling and commonsense reasoning, AdaDSF substantially reduces inference FLOPs while preserving performance close to dense counterparts. Under comparable sparsity, AdaDSF consistently yields smaller accuracy degradation than strong baselines including MoD, D-LLM, and DLO.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：大语言模型（LLMs）基于Transformer架构，其推理成本随深度线性增长、随序列长度二次增长，导致部署成本高昂。现有加速方法（如量化、蒸馏、轻量注意力等）往往依赖任务特定微调或从零训练，增加适配成本且限制跨任务可用性。
- **核心动机**：如何在不进行完全重训练的前提下，将现成的预训练稠密LLM高效转化为深度稀疏模型，从而在保持性能的同时显著降低推理FLOPs。
- **整体含义**：本文提出的AdaDSF框架通过数据驱动的相似性度量分配计算资源，实现了通用、轻量、无需架构修改的推理加速，为资源受限场景下的LLM部署提供了实用路径。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **关键洞察**：不同Transformer层对表示变换的贡献不均，可通过层输入与输出隐藏状态之间的余弦相似度量化。相似度越低说明该层变换作用越强，应分配更多计算（保留更多token）；反之则保留更少token。
- **整体流程**：基于相似度统计分配逐层token保留比率 → 轻量级MLP路由器动态选择每层信息丰富的token → 特征保持对齐目标训练以匹配稀疏与稠密模型的中间和最终表示。

### 关键技术细节
1. **相似性驱动的层保留比率分配**：
   - 对第i层，计算余弦相似度 \(s_i = \text{CosSim}(x_{\text{in}}^{(i)}, x_{\text{out}}^{(i)})\)。
   - 通过温度归一化加权、偏差缩放、Sigmoid映射、全局预算校正四个步骤得到最终保留比率 \(r_i\)，满足总平均保留率为 \(t\)。
2. **轻量级路由器与稀疏模块**：
   - 每个稀疏层包含一个MLP路由器和一个Transformer层。路由器为每个token预测重要性分数，根据保留比率 \(r_i\) 选择Top-K个token（\(K = \lfloor r_i \cdot s \rfloor\)），其余token通过残差跳过全计算。
3. **特征保持对齐训练**：
   - 隐藏状态对齐损失：最小化稀疏与稠密模型对应层输出经Softmax后的差距。
   - 输出分布对齐损失（KL散度）：匹配最终logits分布。
   - 总损失：\(L = L_{\text{align}} + \frac{1}{L}\sum_{l} L_{\text{hid}}^{(l)}\)。

## 3. 实验设计

- **模型与数据集**：
  - GPT-NeoX-130M：在Wikitext103上训练（模拟预训练场景）。
  - Qwen2.5-0.5B、Qwen2.5-1.5B：在指令微调数据集（GenQA, InfinityInstruct, OpenHermes2.5）上训练。
- **评估任务**：
  - 语言建模：Wikitext103测试集困惑度（PPL）。
  - 常识推理：ARC-Challenge、ARC-Easy、HellaSwag、PIQA、WinoGrande、OpenBookQA六个基准，报告准确率平均分及与稠密基线的差值。
- **对比方法**：
  - MoD（Mixture-of-Depths）、D-LLM、DLO（Dynamic Layer Operation）。D-LLM因不适用于指令微调场景，仅在GPT-NeoX上对比。
- **稀疏度设置**：token保留比率分别为90%、80%、70%（对应平均保留率t）。

## 4. 资源与算力

- 论文中**未明确说明**所使用的GPU型号、数量及训练时长。仅提及实验在GPT-NeoX-130M和Qwen2.5（0.5B/1.5B）上进行，但未提供具体硬件配置与计算时间。这是论文信息的一个缺失。

## 5. 实验数量与充分性

- **主要实验组**：
  - 两组主实验：Qwen2.5系列（两个模型，六个推理任务，三个保留比率） + GPT-NeoX-130M（Wikitext103，三个保留比率）。
  - 消融实验：相似性驱动分配策略的影响（改变温度τ）、中间层对齐损失的影响。
  - 对比了三个基线方法（MoD, D-LLM, DLO）。
- **充分性评估**：
  - **优点**：覆盖了不同模型规模（130M/0.5B/1.5B）、不同训练范式（预训练/指令微调）、多个推理任务；消融实验验证了核心设计（相似性分配与对齐损失）的必要性；与当前主流方法的公平对比（相同保留比率下比较FLOPs和精度）。
  - **局限**：实验仅局限于1.5B及以下的模型，未扩展到7B+；未讨论路由器的额外计算开销对实际延迟的影响（仅报告FLOPs缩减）；未进行跨模型架构（如Llama系列）的验证；未讨论训练成本（如对齐训练所需的额外计算量）。

## 6. 主要结论与发现

- AdaDSF能够将预训练稠密LLM转换为深度稀疏模型，在显著降低FLOPs的同时保持接近稠密模型的性能，且优于MoD、D-LLM、DLO等基线。
- 在Qwen2.5-0.5B上，80%保留率时平均推理准确率仅下降2.6%（MoD下降7.3%，DLO下降3.4%），FLOPs降至0.785×。
- 在GPT-NeoX-130M上，80%保留率时困惑度18.9（稠密17.9），而MoD为21.6，D-LLM高达2019.0，DLO为19.6，AdaDSF表现最佳。
- 相似性驱动的分配策略比均匀或启发式分配更有效；中间层对齐损失对保持性能至关重要。

## 7. 优点

- **方法创新性**：首次提出基于层输入输出余弦相似度来分配token保留比率，是一种数据驱动、无需人工设定的计算分配策略。
- **轻量高效**：仅需在每层添加一个MLP路由器，无需修改Transformer内部结构，可即插即用于现有预训练模型。
- **对齐训练增强**：同时对齐中间隐藏状态和最终输出，有效缓解token跳过导致的表示偏移。
- **实验公平性**：在相同保留比率下对比FLOPs与精度，消融实验设计清晰，结论可信度高。
- **实用性**：无需全量重训，仅需少量校准数据进行相似度计算和轻量训练，适合实际部署场景。

## 8. 不足与局限

- **模型规模有限**：实验最大1.5B参数，未验证在7B+规模上的表现，可扩展性待验证。
- **无硬件延迟测量**：仅报告FLOPs缩减，缺乏实际推理速度（如 tokens/s）和内存占用数据，忽略了路由器额外的计算开销。
- **训练成本未报告**：对齐训练需要多少额外数据、计算资源，以及是否可接受未提及。
- **基线覆盖不全**：缺少与剪枝（如结构化剪枝）或量化方法的对比；D-LLM在指令微调场景不适用，导致部分对比缺失。
- **假设局限**：基于余弦相似度表征层变换程度可能不够鲁棒（如对于相同变换但方向相反的表示，相似度可能很低但实际变换合理）。
- **过激活风险**：全局预算校正假设所有层的保留率可独立调整，但忽略了层间依赖（如前一层的跳跃会影响后一层输入分布）。

（完）
