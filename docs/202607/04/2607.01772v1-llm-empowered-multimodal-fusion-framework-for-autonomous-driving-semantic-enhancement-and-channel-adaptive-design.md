---
title: "LLM-Empowered Multimodal Fusion Framework for Autonomous Driving: Semantic Enhancement and Channel-Adaptive Design"
title_zh: LLM赋能的自动驾驶多模态融合框架：语义增强与信道自适应设计
authors: "Wen Wang, Yaping Sun, Yejun He, Hao Chen, Zhiyong Chen, Xiaodong Xu, Nan Ma, Shuguang Cui"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01772v1"
tags: ["query:mm-trf"]
score: 9.0
evidence: 基于LLM的多模态融合用于自动驾驶
tldr: "针对自动驾驶中视觉-雷达融合质量受遮挡、天气和信道噪声影响而动态变化的问题，提出LM-SCIP框架，将大语言模型作为核心推理中枢，结合通道自适应语义模块(CASM)将链路指标映射为通道提示，动态门控外部雷达特征，并利用LoRA微调LLM与异构混合专家(H-MoE)协同推理。在nuScenes上切换雷达输入时，定位RMSE相比纯视觉降低40.0%；在VIRAT上达到0.214m定位RMSE和0.179m minFDE。该工作将静态数据融合重构为信道感知语义推理，实现了低信噪比下的视觉主导鲁棒回退与高信噪比下的协同增强。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01772v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 653, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01772v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1117, \"height\": 800, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01772v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1248, \"height\": 753, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01772v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 731, \"height\": 444, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01772v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1880, \"height\": 334, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01772v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 647, \"height\": 424, \"label\": \"Table\"}]"
motivation: 现有视觉-雷达融合方法忽略输入质量动态变化，在遮挡、恶劣天气或信道噪声下性能退化严重。
method: 提出LM-SCIP，以LLM为核心融合局部视觉与质量可变的雷达流；通过CASM将信道指标转化为通道提示，动态门控雷达特征；LoRA微调LLM与H-MoE协同推理。
result: "在nuScenes上定位RMSE降低40.0%，VIRAT上定位RMSE达0.214m，minFDE为0.179m(k=1)。"
conclusion: LM-SCIP实现了低信噪比下的鲁棒视觉回退与高信噪比下的深度融合，为动态质量多模态融合提供了新范式。
---

## 摘要
视觉-雷达融合是鲁棒自动驾驶的核心，结合了密集的视觉语义和雷达的精确距离与速度测量。然而，真实的融合质量从根本上受到动态变化的输入质量的挑战，这些变化来源于遮挡、恶劣天气和信道噪声。为解决此问题，我们将问题从静态数据融合重新定义为信道感知的语义推理，并提出一个以大型语言模型为中心的语义层信道感知集成感知（LM-SCIP）框架。该框架将大型语言模型（LLM）作为核心推理中枢，融合本地视觉流和用于覆盖感知盲区的质量变化的外部雷达流。具体地，LM-SCIP将分层雷达-视觉编码器与信道自适应语义模块（CASM）耦合，该模块将链路指示器映射为"信道提示"以动态门控外部雷达特征。一个参数高效的LoRA微调LLM，结合异构专家混合（H-MoE），在本地视觉线索和信道条件的雷达上下文之间进行仲裁。最后，一个解耦的多任务解码器输出定位、轨迹预测和图像重建。在nuScenes和VIRAT上的实验验证了我们的方法。在nuScenes上，在受控雷达输入切换下，LM-SCIP相对于仅视觉基线将定位RMSE降低了40.0%。在VIRAT上，模型达到了0.214m的定位RMSE和0.179m的minFDE（k=1）。这些结果表明，所提出的LM-SCIP能够在低信噪比下实现鲁棒的视觉主导回退，在高信噪比下实现协同融合。

## Abstract
Vision-radar fusion is central to robust autonomous driving, combining dense visual semantics with precise range and velocity measurements from radar. However, real-world fusion quality is fundamentally challenged by dynamically varying input quality, stemming from occlusion, adverse weather, and channel noise. To address this, we re-frame the problem from static data fusion to channel-aware semantic reasoning and propose a Large Language Model-centric Semantic-layer Channel-aware Integrated Perception (LM-SCIP) framework. It places a Large Language Model (LLM) as a central reasoning core to fuse a local visual stream with a quality-varying external radar stream used to cover perception-blind spots. Concretely, LM-SCIP couples a hierarchical radar-vision encoder with a Channel-Adaptive Semantic Module (CASM) that maps link indicators into a "Channel Prompt" to dynamically gate external radar features. A parameter-efficient, LoRA-tuned LLM, in conjunction with a heterogeneous Mixture-of-Experts (H-MoE), then arbitrates between local visual cues and the channel-conditioned radar context. Finally, a decoupled multi-task decoder outputs localization, trajectory forecasting, and image reconstruction. Experiments on nuScenes and VIRAT validate our approach. On nuScenes, under a controlled toggle of radar input, LM-SCIP reduces localization RMSE by 40.0% versus a vision-only baseline. On VIRAT, the model attains a 0.214m localization RMSE and 0.179m minFDE (k=1). These results reveal that the proposed LM-SCIP enables a robust vision-dominant fallback at low SNR and synergistic fusion at high SNR.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：自动驾驶场景中，视觉-雷达融合的感知质量会因遮挡、恶劣天气、无线信道噪声等动态变化而严重退化。传统融合方法假设输入质量稳定，采用固定融合策略，缺乏对实时链路质量的感知与自适应能力。
- **研究背景**：单车辆感知易受遮挡影响，而协同感知（V2X）引入的外部数据流会经历时变的信号噪声比（SNR）干扰或丢包。现有方法（如BEV Transformer家族、UniAD等）在稳定输入下表现良好，但无法动态调整对外部雷达特征的信任度。
- **整体含义**：本文提出将 **静态数据融合提升为信道感知的语义推理**，利用大语言模型（LLM）作为核心推理中枢，结合信道状态信息动态融合本地视觉与质量可变的外部雷达数据，实现鲁棒的自适应协同感知。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：以LLM为中心，构建 **语义层信道感知集成感知框架（LM-SCIP）**，将链路质量（SNR、调制方式）作为一等上下文注入推理过程，动态权衡视觉与雷达特征的贡献。
- **关键技术细节**：
  - **多模态语义编码器（MSE）**：分别用层级ViT编码图像、复杂CNN编码雷达I/Q波形，通过轻量双向交叉注意力对齐生成统一token序列 \( S_{\text{multi}} \)。
  - **信道自适应语义模块（CASM）**：将链路指标（SNR、调制指数）通过MLP和嵌入表编码为多token“信道提示” \( P_{\text{ch}} \)；利用全局门控 \( g(P_{\text{ch}}) \) 对融合序列进行元素级缩放，实现特征级自适应门控：\( H_{\text{enc}} = \text{LN}( S_{\text{multi}} \odot (1 + g(P_{\text{ch}})) ) \)。
  - **LLM-异构MoE核心**：token级top-k路由器将 \( H_{\text{enc}} \) 分配给异构专家（MLP专家用于定位/重建，时序Transformer专家用于轨迹预测）；输出与信道提示拼接后送入LoRA微调的GPT-2进行全局语义仲裁，生成 \( h_{\text{LLM}} \)。
  - **解耦多任务解码器**：ViT-U分支重建图像，自注意力池化+两个MLP头分别回归位置和未来轨迹，避免密集-稀疏梯度干扰。
  - **训练目标**：加权多任务损失 \( \mathcal{L}_{\text{total}} = \lambda_{\text{rec}}\mathcal{L}_{\text{rec}} + \lambda_{\text{loc}}\mathcal{L}_{\text{loc}} + \lambda_{\text{traj}}\mathcal{L}_{\text{traj}} + \lambda_{\text{vel}}\mathcal{L}_{\text{vel}} + \lambda_{\text{bal}}\mathcal{L}_{\text{bal}} \)，包含感知损失、平滑L1损失、速度正则和负载平衡项。

#### 3. 实验设计
- **数据集与场景**：
  - **nuScenes**：用于受控融合增益研究，固定训练设置，切换雷达可用性，评估定位RMSE相对纯视觉的降低百分比。
  - **VIRAT**：构建四个测试场景：理想（25 dB SNR，清晰图像）、中等遮挡（10-25%随机擦除）、雨夜（模糊）、低SNR+遮挡（5 dB + 中等遮挡）。自动流水线（YOLOv10+SAM）生成实例，合成雷达I/Q波形。
- **基准与对比方法**：
  - **消融基线**：仅视觉、w/o CASM、w/o H-MoE、仅雷达。
  - **SOTA对比**：在VIRAT上对比轨迹预测方法（Multiverse、GraphST、SimAug）；在nuScenes上对比融合方法（CenterFusion、CRAFT）。
- **评估指标**：定位RMSE（米）、轨迹minFDE/ADE（米）、重建PSNR（dB），以及任务级QoS准确率（阈值：定位0.5m、ADE 0.5m、PSNR 20dB）。

#### 4. 资源与算力
- **文中未明确说明**：论文未提及使用的GPU型号、数量、训练时长等算力资源。仅在方法中提到使用LoRA微调GPT-2（参数高效），属于轻量级部署友好设计。

#### 5. 实验数量与充分性
- **实验组数**：
  - 消融实验：4个场景（理想、低SNR、中等遮挡、雨夜）× 4个变体（全模型、仅视觉、w/o CASM、w/o H-MoE）= 16组指标（表1）。
  - 信道适应性实验：SNR从-5 dB到25 dB，展示了定位/轨迹/重建的趋势（图4）。
  - SOTA对比：VIRAT上4种方法（4个minFDE值），nuScenes上3种方法的相对增益对比（表2）。
  - 另外有QoS准确率分析（文本中给出，未列表）。
- **充分性评估**：
  - **优点**：覆盖了不同退化场景（视觉退化、信道退化、组合退化），消融设计完整（去CASM、去H-MoE），验证了各组件的必要性；使用公开数据集和标准指标。
  - **不足**：缺少与更多近期融合方法的全面对比（如只比了CRAFT，未比CRN、MVFusion）；仅在一个合成波形设置下评估（缺乏真实V2X信道数据）；未进行真实路测或在ImageNet等更大规模视觉任务上的泛化实验。

#### 6. 主要结论与发现
- **多模态融合增益显著**：全模型相比纯视觉在理想场景下定位RMSE降低42.3%（0.214 vs 0.371 m）。
- **信道感知至关重要**：低SNR下移除CASM导致RMSE上升12.6%（0.274 vs 0.244 m），ADE上升14.5%。
- **任务专业化必要**：移除异构MoE导致定位RMSE剧增至13.08 m（理想场景），无法同时支持重建与稀疏预测。
- **CASM实现两阶段自适应**：低SNR时视觉主导回退，高SNR时协同融合（RMSE从0.245降至0.214 m）。
- **SOTA对比**：在VIRAT上轨迹预测minFDE=0.179m，与SimAug持平；在nuScenes上相对于纯视觉的定位改善达-40.0%，超过CRAFT（-32.3%）。

#### 7. 优点
- **创新性**：首次将LLM作为核心推理中枢用于协同多模态融合，并引入信道提示显式建模链路质量，打破了固定融合范式。
- **鲁棒性**：CASM机制使模型能在低SNR下自动降权不可靠雷达特征，保持视觉主导稳定性。
- **参数效率**：采用LoRA微调GPT-2，无需从头训练大模型；异构MoE实现任务特化，避免多任务干扰。
- **多任务统一**：同时输出定位、轨迹预测和图像重建，且通过解耦解码器稳定联合训练。
- **实验设计较全面**：涵盖三个维度（消融、信道扫描、SOTA对比），且引入任务级QoS指标更具实际参考价值。

#### 8. 不足与局限
- **实验局限**：
  - 仅在合成雷达波形和模拟信道噪声下评估，缺乏真实V2X链路数据和硬件部署验证。
  - 缺少与更多近期方法（如CRN、MVFusion、UniAD）的直接横向对比。
  - 遮挡场景仅使用随机擦除和合成模糊，未采用现实遮挡（如建筑物或车辆）。
- **应用限制**：
  - 假设RSU-A和RSU-B之间的时空校准已预完成，实际部署中误差可能引入对齐偏差。
  - 需要外部节点持续传输雷达数据，依赖V2X通信可靠性，本文未考虑丢包或延迟问题。
  - 合成雷达I/Q波形基于LFM-SIMO模型，可能无法完全模拟真实雷达回波的复杂性。
- **算力与资源未说明**：缺少训练成本（GPU时间、能耗）报告，不利于复现和实际部署评估。

（完）
