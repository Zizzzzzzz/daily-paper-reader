---
title: Robust KV Cache Management for LLM Serving under Output Token Length Uncertainty
title_zh: 面向输出令牌长度不确定性的LLM服务鲁棒KV缓存管理
authors: "Jiaming Cheng, Duong The Do, Duong Tung Nguyen"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16892v1"
tags: ["query:evlm"]
score: 9.0
evidence: 面向LLM服务的KV缓存管理
tldr: "LLM服务中输出长度不确定导致KV缓存预留不足或过量，引发高预置开销或内存浪费。本文提出鲁棒缓存管理框架，联合优化GPU并行、缓存预留、路由和前缀缓存，采用Wasserstein分布鲁棒优化建模不确定性，并设计块坐标下降求解。在BurstGPT等生产负载上，相比固定分位数基线成本降低56%，同时维持低P99延迟和SLO达标率。该框架无需手动调参，自动适应不同成本场景，显著提升服务效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16892v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 1121, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 921, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 375, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 920, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 709, \"height\": 518, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16892v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 921, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 911, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 795, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 842, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1136, \"height\": 227, \"label\": \"Table\"}]"
motivation: 应对输出长度不确定性造成的KV缓存预留与预置风险之间的权衡，优化服务质量与系统成本。
method: 提出分布鲁棒优化与块坐标下降算法，联合优化GPU并行配置、预留分位数、请求路由和前缀缓存，满足延迟SLO约束。
result: "在BurstGPT等生产迹线下，成本降低56%，P99延迟和SLO违规率与基线相当，适应多种工作负载。"
conclusion: 揭示自适应分位数结构，平衡内存效率与预置风险，无需人工调参，为LLM部署提供鲁棒方案。
---

## 摘要
KV缓存内存是部署在GPU集群上的现代LLM服务系统中的主要瓶颈。一个基本挑战是，KV缓存必须在请求到达时预留，而输出令牌长度直到生成完成前未知。预留不足会触发抢占——强制终止和重新计算请求，带来显著开销——而过度预留则浪费内存并降低吞吐量。这就在内存效率与抢占风险之间形成了一个核心权衡。我们提出了一个用于LLM服务的鲁棒KV缓存管理框架，该框架联合优化GPU并行配置、每请求类别的KV缓存预留、跨异构服务组的请求路由以及共享提示的前缀缓存。该框架整合了延迟SLO约束，并捕捉了内存分配、吞吐量和排队延迟之间的交互。为应对输出令牌长度不确定性和工作负载分布变化，我们开发了一个Wasserstein分布鲁棒优化（DRO）公式，以及针对所得到的混合整数问题的可扩展块坐标下降算法。我们的分析揭示了一个关键的分位数结构，该结构能自动调整预留分位数以适应不同的抢占和内存成本模式，无需手动调参。在生产级LLM工作负载（包括BurstGPT、Azure和ShareGPT追踪数据）上的轨迹驱动评估表明，与固定分位数预留基线相比，该方案在多种运行模式下实现了高达56%的成本降低，同时保持了具有竞争力的P99延迟、有效吞吐量和SLO违规率。

## Abstract
KV cache memory is a primary bottleneck in modern LLM serving systems deployed on GPU clusters. A fundamental challenge is that the KV cache must be reserved upon request arrival, while the output token length remains unknown until generation completes. Under-reservation triggers preemption -- forcing termination and recomputation of requests and incurring significant overhead -- whereas over-reservation wastes memory and reduces throughput. This creates a central trade-off between memory efficiency and preemption risk. We present a robust KV cache management framework for LLM serving that jointly optimizes GPU parallelism configuration, KV cache reservation per request class, request routing across heterogeneous serving groups, and prefix caching for shared prompts. The framework incorporates latency SLO constraints and captures the interaction between memory allocation, throughput, and queueing delay. To address output token length uncertainty and workload distribution shift, we develop a Wasserstein distributionally robust optimization (DRO) formulation together with a scalable block coordinate descent algorithm for the resulting mixed-integer problem. Our analysis reveals a critical fractile structure that automatically adapts reservation quantiles to different preemption and memory cost regimes without manual tuning. Trace-driven evaluation on production LLM workloads, including BurstGPT, Azure, and ShareGPT traces, demonstrates up to 56\% lower cost than fixed-quantile reservation baselines while maintaining competitive P99 latency, goodput, and SLO violation rates across diverse operating regimes.

---

## 论文详细总结（自动生成）

# 鲁棒KV缓存管理：面向输出令牌长度不确定性的LLM服务优化

## 1. 论文的核心问题与整体含义

- **研究动机**：在GPU集群上部署LLM推理服务时，KV缓存内存是核心瓶颈。KV缓存在请求到达时就必须预留，但输出令牌长度直到生成完成才知晓。预留不足会触发抢占（termination & recomputation，开销极大），预留过多则浪费内存、降低吞吐量。这构成了内存效率与抢占风险之间的根本权衡。
- **整体含义**：当前主流系统（如vLLM基于PagedAttention、Orca连续批处理）主要关注运行时执行机制，但预留决策（per-class reservation）和路由策略仍依赖操作员手动设定固定分位数（如P90、P95、P99），无法适应工作负载非平稳性、异构性和成本结构变化。作者提出一个统一的决策策略框架，联合优化GPU并行配置、缓存预留、路由和前缀缓存，在输出长度不确定性下提供鲁棒性保证。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将KV缓存预留问题建模为分布鲁棒优化（DRO）问题，使用Wasserstein模糊集应对输出长度分布偏移；同时联合优化四个决策变量：配置部署 \(z_k\)（每组GPU的配置数量）、预留量 \(r_i\)（每类请求的KV缓存令牌数）、路由比例 \(\pi_{i,k}\)、前缀缓存指示 \(\delta_{i,k}\)。
- **关键技术细节**：
  - **成本结构**：预置成本 \(c_p \cdot (L_{in}+\xi_i - r_i)^+\)，浪费成本 \(c_w \cdot (r_i - L_{in} - \xi_i)^+\)，成本比 \(\rho = c_p/c_w\) 决定最优预留分位数。
  - **DRO公式**：对每类请求，定义Wasserstein模糊集 \(P_i^\varepsilon = \{P: W_1(P, \hat{P}_i^N) \le \varepsilon\}\)，优化目标为最坏情况下的期望成本最小化，并加入GPU预算约束、路由一致性、内存容量约束（Little's law + 安全因子）、SLO约束（M/G/1排队）。
  - **可计算转化**：利用Kantorovich对偶，将无限维DRO转化为有限线性规划，引入辅助变量 \(\gamma_i\)（运输成本惩罚）和 \(\theta_{i,n}\)。
  - **关键分位数结构**（Proposition IV.2）：最优预留满足 \(r_i^* = L_{in} + F_i^{-1}(\rho/(\rho+1))\)，其中 \(F_i\) 是输出长度在worst-case分布下的CDF。此分位数自动调整：预置成本高时保守、浪费成本高时激进，无需手动调参。
  - **算法**：BCD-DRO，块坐标下降，交替优化连续变量（路由、预留）和整数变量（前缀缓存、配置选择），收敛到块最优解。复杂度：离线性规划可扩展至15类请求、12种配置、2000样本，约257秒。
- **公式与算法流程**（文字说明）：
  - 整体混合整数双线性规划（MIBLP）经对偶转化为有限LP。
  - 算法初始化后循环：Block 1（交替两个LP，优化π与r,γ,θ），Block 2（δ闭式更新），Block 3（枚举有限配置集选最优）。直到配置和缓存决策不再变化。

## 3. 实验设计

- **数据集**：三个生产级LLM推理轨迹数据集：
  - **BurstGPT**：140万请求，61天Azure OpenAI服务，输出长度均值125，P99=1586 tokens。
  - **Azure LLM 2024**：4400万请求，7天，含Code和Conversation两类。
  - **ShareGPT**：36.8万助手响应，均值265 tokens。
- **Benchmark场景**：模拟8×A100-80GB GPU集群，服务70B模型，四种并行配置（TP=2/4 × PP=1/2）。成本参数默认 \(\rho=10\)（交互聊天场景），并跨 \(\rho\in\{2,5,10,20,50,100\}\) 进行评估。
- **对比方法**：
  - 固定分位数：Max, Mean, P90, P95, P99。
  - 长度预测：LP+1σ, LP+2σ。
  - 样本平均近似（SAA，ε=0）。
  - 消融变体：统一路由（uniform π）、无DRO（SAA）、无前缀缓存（δ=0）。
- **评价指标**：总成本（预置+浪费+GPU+SLO+拒绝）、P99延迟、有效吞吐量（Goodput）、SLO违规率。

## 4. 资源与算力

- 论文未明确说明实验所需具体GPU型号、训练时长或框架细节。仅指出模拟8×A100-80GB GPU集群，并使用70B参数模型。算法离线优化阶段在CPU上运行（约257秒求解大规模实例），在线决策为O(1)每请求。未提及其他训练或推理计算资源消耗。

## 5. 实验数量与充分性

- **实验数量**：相对充分，包括：
  - 主对比实验：跨6个成本比ρ，对比DRO与6种基线（Table II, IV）。
  - 算法可扩展性测试：不同问题规模下MIBLP vs BCD-DRO求解时间（Table III）。
  - 敏感性分析：负载缩放（图3a）、Wasserstein半径ε（图3b）、次要权重（cslo, crej, κ）（图4）。
  - 分布偏移鲁棒性：静态vs滚动时域（图5a），不同ρ和偏移幅度下DRO优势（图5b）。
  - 消融实验：去除路由、DRO、前缀缓存，评估成本、P99、Goodput、SLO违规（Table V）。
  - 预置成本-延迟权衡：全ρ范围（Table IV）。
- **充分性与公平性**：
  - 使用了三个真实轨迹，覆盖不同工作负载类型和规模。
  - 对比了多种baseline，包括当前系统默认（P95）、常用（P90）、长度预测等。
  - 但实验仅在8-GPU模拟场景，未扩展至更大集群或真实部署；未提及随机种子数、重复次数（除Table V标明5种子均值±std）。部分结果（如图2a、Table II）未显示误差线，可能缺乏重复性验证。
  - 消融实验中，路由的缺失导致成本飙升（93k vs 13k），说明路由是“可行性约束”而非调优参数，结论合理。

## 6. 论文的主要结论与发现

1. **无单一固定分位数最优**：每个固定分位数只在匹配其临界分位数 \(\rho/(\rho+1)\) 的成本比下最优；DRO自动适应任意成本结构。
2. **关键分位数结构**：DRO的预留分位数贴合理论公式 \(q^* = \rho/(\rho+1)\)，消除了手动调参。
3. **算法可扩展**：BCD-DRO在15×12×2000规模下约257秒，可用于周期性重优化。
4. **鲁棒性收益**：Wasserstein半径ε带来<1.3%成本开销，但在分布偏移下显著优于SAA；滚动时域适应进一步改善。
5. **消融表明路由、DRO、前缀缓存均为关键**，缺一不可。
6. **跨成本比稳定性**：DRO成本始终保持在最优基线的小常数因子内，而其他基线在某些ρ下成本呈数量级上升。

## 7. 优点

- **理论贡献**：将KV缓存预留问题精确建模为分布鲁棒优化，推导出关键分位数结构，赋予自适应能力，具有强理论支撑。
- **系统视角**：联合优化四个决策（配置、预留、路由、前缀缓存），是首个在不确定性下提供鲁棒保证的统一框架。
- **实用算法**：BCD-DRO将混合整数问题分解为可解子问题，支持周期性重优化，工业部署可行。
- **实验全面**：覆盖多个真实轨迹、多种成本比、消融、敏感性分析，充分验证方法有效性和鲁棒性。
- **与现有系统兼容**：框架输出可直接被现有运行时（vLLM、Orca等）执行，无需修改数据平面。

## 8. 不足与局限

- **实验资源未披露**：未说明GPU型号数量（仅模拟8 A100）、训练/推理时间、代码是否开源，可复现性存疑。
- **模拟尺度有限**：仅8 GPU集群，未测试百卡或跨数据中心场景；假设M/G/1排队模型，可能对复杂调度（如连续批处理、chunked prefill）的近似精度不足。
- **延迟模型简化**：SLO约束基于期望响应时间，未考虑尾部延迟的精确建模；排队模型中的\(C_{s,k}\)仅从训练样本计算并固定，可能随分布变化。
- **参数敏感性未充分覆盖**：虽然评估了ε和ρ，但成本参数（cp, cw, cslo, crej, κ）的交叉敏感性仅部分展示（图4仅单个变量变化），且默认值合理性缺乏论证。
- **训练-测试时间偏移**：仅展示单次偏移因子（2.5×）下的消融，未系统评估多种偏移类型（如尾部形状变化、突发性）的影响。
- **未与更先进预测方法对比**：如基于prompt的length prediction模型、在线学习策略（如bandit、Bayesian optimization）。
- **仅针对KV缓存，未考虑其他内存瓶颈**：如模型权重、激活内存、中间结果等，实际系统中可能存在复合瓶颈。
- **结论的应用边界**：适用于批处理、连续批处理等典型场景，可能不适用于实时流式推理或边缘部署。

（完）
