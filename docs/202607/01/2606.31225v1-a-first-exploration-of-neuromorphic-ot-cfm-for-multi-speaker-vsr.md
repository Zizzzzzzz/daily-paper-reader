---
title: A First Exploration of Neuromorphic OT-CFM for Multi-Speaker VSR
title_zh: 基于神经形态OT-CFM的多说话人视觉语音识别的初步探索
authors: "Lin Chen, Jingping Fang, Hairui Liu, Chenyang Xu, Xiaorui Li, Weidong Cai, Xiaoming Chen"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31225v1"
tags: ["query:ev-cam"]
score: 9.0
evidence: 受神经形态启发的框架，将RGB视频转换为事件流用于视觉语音识别
tldr: "针对多说话人视觉语音识别中快速头部运动、遮挡和嘴唇细微变化导致的性能下降问题，提出LipsFlow框架，将RGB视频转换为高时间分辨率事件流，结合ByteTrack和TalkNet进行说话人分割，并引入最优传输条件流匹配（OT-CFM）在语义潜空间中生成确定性直线轨迹，仅需两步ODE推理。同时设计双级语义监督机制解决同音词歧义。在基准上取得22.3%词错误率，延迟240毫秒，建立鲁棒高效的基于事件的VSR范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31225v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31225v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31225v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1238, \"height\": 677, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31225v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1255, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31225v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1260, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31225v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1276, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31225v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1011, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31225v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1215, \"height\": 305, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31225v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1023, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31225v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 797, \"height\": 339, \"label\": \"Table\"}]"
motivation: 传统RGB方法因帧率低和运动模糊，难以应对复杂多说话人VSR中的快速头部运动、遮挡和细微嘴唇变化。
method: 将RGB视频转为事件流，用ByteTrack和TalkNet分割单说话人片段，采用可学习事件表示捕获微秒级发音动态，并引入OT-CFM实现两步ODE推理及双级语义监督。
result: "在基准上达到SOTA：词错误率22.3%，延迟240毫秒。"
conclusion: LipsFlow证明了神经形态事件驱动方法在高效鲁棒VSR中的潜力，为多说话人场景提供新方案。
---

## 摘要
复杂多说话人场景中的视觉语音识别任务受到快速头部运动、遮挡和细微唇部动作的严重阻碍。传统的基于RGB的方法由于帧率低和运动模糊而难以应对这些问题。为克服这些挑战，我们提出LipsFlow，一种受神经形态启发的VSR框架，将RGB视频转换为高时间分辨率的事件流。针对多说话人场景，我们采用ByteTrack跟踪和TalkNet主动说话人检测，将场景按时间分割为单说话人片段，实现聚焦于每个说话人的分析。通过学习基于事件的可表示，LipsFlow显式捕捉微秒级的发音动态，从而对视觉退化具有内在鲁棒性。为高效建模这些密集的事件特征并适应说话人特定的发音模式，我们引入最优传输条件流匹配（OT-CFM）。它在语义潜在空间中强制生成确定性的直线轨迹生成，将推理延迟缩减至仅两步常微分方程求解。此外，我们设计了双层语义监督机制，结合词级BERT权重绑定和句级先验，以解决同音词歧义。在竞争性基准测试中，LipsFlow在240毫秒延迟下实现了22.3%的词错误率，为基于事件的VSR建立了高度鲁棒且高效的范式。

## Abstract
Visual Speech Recognition (VSR) tasks in complex multi-speaker scenarios are severely hindered by rapid head motions, occlusions, and subtle lip articulations. Traditional RGB-based methods struggle here due to low rates and motion blur of frames. To overcome these, we propose LipsFlow, a neuromorphic-inspired VSR framework that converts RGB videos into high-temporal-resolution event streams. For multi-speaker, we employ ByteTrack tracking and TalkNet active speaker detection to temporally segment scenes into single-speaker clips, enabling focused per-speaker analysis. By explicitly capturing microsecond-level articulatory dynamics via learnable event-based representations, LipsFlow achieves inherent robustness against visual degradation. To efficiently model these dense event-based features and adapt to speaker-specific articulatory patterns, we introduce Optimal Transport Conditional Flow Matching (OT-CFM). It enforces deterministic, straight-line trajectory generation in a semantic latent space, slashing inference latency to just two Ordinary Differential Equation (ODE) steps. Furthermore, we design a Dual-Level Semantic Supervision mechanism combining token-level BERT weight tying and sentence-level priors to resolve homophene ambiguities. Validated on competitive benchmarks, LipsFlow achieves a state-of-the-art WER of 22.3\% at 240 ms latency, establishing a highly robust and efficient paradigm for event-based VSR.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多说话人视觉语音识别（VSR）面临快速头部运动、遮挡和细微嘴唇变化等挑战，传统RGB摄像头因帧率低（25–30 FPS）和运动模糊，会丢失关键瞬态发音动态（例如/p/和/b/等爆破音在毫秒级内的嘴唇闭合），导致性能严重退化。
- **研究背景**：现有VSR方法大多假设单说话人场景，难以处理复杂多说话人环境；少数基于事件流的方法（如MSTP、SNN-Lip）在表示学习和生成效率上存在瓶颈，无法有效解耦发音动态与非语音口部噪声（如咀嚼、打哈欠），且自回归或扩散模型推理延迟大。
- **整体含义**：本文首次将神经形态事件感知与最优传输条件流匹配（OT-CFM）相结合，提出端到端框架LipsFlow，旨在通过高时间分辨率事件流捕获微秒级发音运动，并利用确定性直线轨迹生成实现高质量、低延迟的多说话人VSR。

### 2. 论文提出的方法论
- **核心思想**：将RGB视频转换为高帧率事件流，利用事件流对运动细节的敏感优势，通过OT-CFM在预训练语义潜在空间中学习从高斯噪声到目标语义的连续直线概率流，仅需两次常微分方程（ODE）求解即可完成推理。
- **关键技术细节**：
  - **分层帧插值与事件生成**：采用RIFE进行2×→2×→5×分层插值将25 FPS提升至500 FPS，再利用可微分量化器生成事件流，并引入自适应阈值网络（轻量CNN回归阈值图）以增强发音微运动。
  - **事件编码与上下文建模**：使用3D-2D混合编码器（3D Stem + 2D ResNet）和12层Conformer编码器提取全局时空特征。
  - **说话人身份分离**：通过TDNN和时序统计池化提取说话人嵌入，并使用InfoNCE对比损失强制解耦，过滤背景噪声和无关身份信息。
  - **长度预测与对齐**：混合长度预测器（回归+分类头）估计目标序列长度，通过单调对齐模块将高频事件特征（125 Hz）对齐到预测的N个token槽，并利用CTC辅助损失训练。
  - **OT-CFM解码器**：采用16层CFM-Former，通过交叉注意力注入视觉条件，通过自适应层归一化（AdaLN）注入说话人嵌入；训练目标为回归直线位移场 \( \|v_\theta(x_t, c, s) - (x_1 - x_0)\|^2 \)；推理时使用2步欧拉求解器（\(\Delta t = 0.5\)）。
  - **先验引导语义解码器**：恢复的语义表示通过可学习投影映射到预训练BERT嵌入空间，使用温度缩放余弦相似度计算词概率；添加自终止标记（[PAD]）实现变长序列。
  - **双层语义监督**：词级交叉熵损失（\(L_{CE}\)）+ 句级语义一致性损失（\(L_{sem}\)，通过Sentence-BERT编码软嵌入的均值池化特征计算余弦距离）。总损失：\(L = L_{FM} + L_{CE} + L_{sem} + L_{CTC} + L_{speaker}\)。
- **多阶段数据处理流水线**：
  - 使用ByteTrack进行身份保持跟踪，结合卡尔曼滤波和IoU关联处理遮挡和姿态变化。
  - 使用RetinaFace检测人脸，Dlib回归68个关键点，截取嘴唇ROI（索引48-67，加20%边框填充），通过仿射变换稳定ROI，消除刚性头部抖动。
  - 三分类标注（静默、非语音口动、主动说话），利用同步音频校准主动说话时间边界；使用TalkNet-ASD将跟踪的身份与音频段关联，剔除低置信度对。

### 3. 实验设计
- **数据集与场景**：
  - **DVS-Lip**：首个大规模真实事件流VSR语料库，包含微妙唇部微运动及挑战性词汇，直接使用其原生事件流。
  - **AVA**：无约束自然场景的多说话人数据，通过本文数据处理流水线将RGB帧转换为事件流，并划分为三个子集：Rapid Motion、Low Light、Severe Occlusion。
- **基准与对比方法**：
  - RGB视频方法：Auto-AVSR、VATLM、RAVEn、SynthVSR、CMAVSR、LipGen、Llama-AVSR。
  - 事件流方法：MSTP（ResNet-18）、SNN-Lip（SNN）。
- **评价指标**：词错误率（WER）、视素错误率（VER）、推理延迟（ms）、实时因子（RTF）、模型参数量（M）以及说话人验证准确率（SpkVA）。

### 4. 资源与算力
- 明确提及：训练使用4张NVIDIA A100（80GB）GPU，采用PyTorch分布式数据并行。
- 训练策略：两阶段课程学习——前10个epoch冻结视觉前端（约15k步，学习率5×10⁻⁴），后20个epoch端到端微调（约30k步，学习率1×10⁻⁴）。总迭代约45k步，训练时长约30小时。
- 推理方面：2步欧拉求解器实现240ms延迟，RTF=0.18。

### 5. 实验数量与充分性
- **主要基准对比**：在DVS-Lip上与9种方法对比，报告WER、VER、RTF、延迟、参数量（表1）。
- **鲁棒性测试**：在AVA的三个复杂子集（快速运动、低照度、严重遮挡）上比较了5种方法，显示相对退化幅度（表2）。
- **消融实验**：
  - 架构变体：视觉仅、朴素融合、神经形态分支、扩散变体、完整LipsFlow（表3）。
  - 推理动力学：不同NFE（1,2,4,10步）的WER与延迟权衡，并与扩散变体比较（表4）。
  - 损失配置与训练策略：逐步添加CE、OT-CFM、说话人对比、完整损失；单阶段与两阶段训练；数据增强影响（表5）。
- **充分性评价**：实验覆盖了主要基准、多个退化场景、全面的消融组件，对比方法涵盖RGB和事件域多种主流范式。结果客观呈现了各方法的性能，消融实验有效验证了各模块贡献。但仅在两个数据集上验证，未在更多真实多说话人场景（如LRS3等传统VSR数据集）上评估，泛化性有待进一步验证。

### 6. 论文的主要结论与发现
- LipsFlow在DVS-Lip上实现SOTA：WER 22.3%、VER 19.8%，超过RGB方法（包括大型语言模型辅助的Llama-AVSR）和所有事件基线。
- 在AVA复杂场景下，LipsFlow对快速运动（WER +1.8%）、低照度（+0.6%）、严重遮挡（+4.2%）的性能退化远小于RGB方法（退化11.2%~23.7%），验证了事件表示的环境鲁棒性。
- OT-CFM是核心：相比扩散模型（50步，6000ms），仅用2步（240ms）即达到更优WER，RTF低至0.18，实现25倍加速。
- 双层语义监督有效解决同音词歧义，说话人对比学习使SpkVA达到93.8%。

### 7. 优点
- **新颖性**：首次将OT-CFM引入VSR领域，并针对多说话人场景设计端到端事件流框架，在表示和生成两方面均有创新。
- **高效性**：2步ODE推理实现实时性（240ms），优于扩散模型的同时保持高精度。
- **鲁棒性**：事件表示天然规避运动模糊和低帧率问题，对环境退化（快速运动、低照度、遮挡）表现出显著优势。
- **完整性**：从传感器模拟（RIFE+自适应阈值）到多说话人跟踪（ByteTrack+TalkNet）、身份解耦、长度预测、语义监督，形成完整流水线。
- **消融充分**：验证了各组件（事件分支、融合策略、OT-CFM路径、损失项、两阶段训练、数据增强）的实际贡献。

### 8. 不足与局限
- **实验覆盖有限**：仅在DVS-Lip和AVA两个数据集上进行评估，缺乏在更大规模标准VSR数据集（如LRS2、LRS3、MPIIFaceGaze等）上的验证，也未见与真实事件相机数据的对比（DVS-Lip是模拟/真实混合？AVA是模拟事件流）。
- **模拟事件流的依赖**：训练使用模拟事件流（从RGB视频生成），模拟质量直接影响性能。真实事件相机在实际部署中可能面临噪声、分辨率等差异，鲁棒性未验证。
- **计算资源需求**：虽然推理快，但训练仍需4×A100和30小时，对资源要求较高；模型参数45.8M，在边缘设备上可能仍有挑战。
- **说话人数上限**：框架依赖检测和跟踪模块，高度遮挡或极快速运动场景下跟踪可能失败；同时处理说话人数量受限于GPU显存和计算资源。
- **自终止机制**：引入[PAD]标记实现变长，但未讨论长度预测误差对整体WER的具体影响，也未与显式长度校准方法对比。
- **双语/多语泛化**：当前主要面向英语（BERT、Sentence-BERT），对其他语言（如中文、日语等复杂口型运动）的适应性未知。

（完）
