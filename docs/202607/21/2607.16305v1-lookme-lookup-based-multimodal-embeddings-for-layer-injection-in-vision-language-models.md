---
title: "LookME: Lookup-Based Multimodal Embeddings for Layer Injection in Vision-Language Models"
title_zh: "LookME: 基于查找的多模态嵌入用于视觉语言模型中的层注入"
authors: "Zeyu Xu, Xingzhong Hou, Pengkai Guo, Siling Lin, Xiao Xu, Menghua Zhai, Haoyu Chen, Yunke Zhang, Fei Huang"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.16305v1"
tags: ["query:evlm"]
score: 8.0
evidence: 提出基于查找的多模态嵌入用于高效VLM层注入，降低内存和延迟
tldr: 视觉语言模型扩展MoE存在内存与延迟权衡。现有逐层嵌入架构仅支持文本查找，无法处理多模态连续嵌入。本文提出LookME框架，实现多模态嵌入的查找增强与分区存储按需加载，通过层级粗到精两阶段查找（场景级到原语级），并结合稀疏注入策略自适应优先关键嵌入、促进邻层重用。在多个视觉基准上优于文本PLE方法，验证了查找式多模态嵌入增强的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16305v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 856, \"height\": 268, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16305v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1780, \"height\": 821, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16305v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1631, \"height\": 780, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16305v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 885, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16305v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 886, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16305v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16305v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 884, \"height\": 209, \"label\": \"Table\"}]"
motivation: 现有PLE方法仅支持文本嵌入查找，无法有效增强视觉语言模型中的多模态嵌入，且大规模模型部署受限于资源约束。
method: 提出LookME框架，采用层级两阶段查找（场景级→原语级）检索多模态嵌入，并设计稀疏注入策略优先关键嵌入、支持跨层重用表。
result: 在多个视觉基准上，LookME优于文本PLE方法，证明了查找式多模态嵌入增强在视觉语言任务中的有效性。
conclusion: LookME首次实现多模态嵌入的查找增强，兼顾效率与性能，为资源受限环境下的VLM部署提供新思路。
---

## 摘要
视觉语言模型（VLM）在多模态理解方面取得了显著进展。然而，扩展密集或稀疏的混合专家（MoE）模型以提高性能，由于全加载的高内存使用与按需加载的高延迟之间的权衡，限制了在资源受限环境中的部署。最近，逐层嵌入（PLE）架构通过使用存储在ROM中的大型外部嵌入表扩展模型，并执行轻量级查找来检索相关嵌入以增强令牌表示，从而解决了这一问题。然而，现有的PLE风格方法主要针对文本嵌入设计，因为基于ID的检索方便，这限制了它们在VLM中的有效性，因为多模态嵌入包含更丰富的视觉任务信息。在本文中，我们提出了LookME，这是第一个能够在VLM中实现基于查找的多模态嵌入增强，同时支持分区存储和按需加载的框架。为了高效地从大规模嵌入表中查找任意的连续多模态嵌入，我们提出了一种层次化的两级查找方法，采用从粗到细的策略，从场景级到场景内基元级进行查找。此外，我们将查找方法与稀疏注入策略相结合，该策略自适应地优先处理关键嵌入，而非层内大量的多模态嵌入，并促进相邻层之间的嵌入表重用，从而改善效率、模型大小和性能之间的权衡。在多个视觉基准上的实验表明，LookME优于纯文本的PLE风格方法，验证了基于查找的多模态嵌入增强的有效性。

## Abstract
Vision-Language Models (VLMs) have achieved strong progress in multimodal understanding. However, scaling dense or sparse Mixture-of-Experts (MoE) models to improve performance limits deployment in resource-constrained environments due to the trade-off between high memory usage from full loading and increased latency from on-demand loading. Recently, the Per-Layer Embedding (PLE) architecture addresses this by scaling models with large external embedding tables stored in ROM and performing lightweight lookup to retrieve relevant embeddings to enhance token representations. Nevertheless, existing PLE-style methods are primarily designed for text embeddings due to the convenience of ID-based retrieval, limiting their effectiveness in VLMs where multimodal embeddings contain richer information for visual tasks. In this paper, we propose LookME, the first framework that enables lookup-based enhancement for multimodal embeddings in VLMs while supporting partitioned storage and on-demand loading. To efficiently lookup arbitrary continuous multimodal embeddings from large-scale embedding tables, we propose a hierarchical two-level lookup method employing a coarse-to-fine strategy that performs lookups from the scene-level to the intra-scene primitive-level. Furthermore, we integrate the lookup method with a sparse injection strategy, which adaptively prioritizes critical embeddings over voluminous multimodal embeddings within layers, and facilitates embedding table reuse across neighboring layers, improving the trade-off among efficiency, model size, and performance. Experiments on multiple visual benchmarks show that LookME outperforms text-only PLE-style methods, validating the effectiveness of lookup-based multimodal embedding enhancement.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：视觉语言模型（VLM）在多模态理解任务上表现优异，但扩展模型规模（如密集模型或混合专家模型MoE）会带来高内存使用或高延迟，不利于在资源受限的边缘设备上部署。近期提出的逐层嵌入（Per-Layer Embedding, PLE）架构通过在ROM中存储大型外部嵌入表，利用轻量级查找来增强token表示，实现了参数稀疏激活。然而，现有PLE方法主要针对文本嵌入设计（文本有离散ID便于索引），无法有效增强VLM中占主导地位的多模态嵌入（如视觉token和跨模态嵌入），导致性能提升有限。

- **核心问题**：如何将PLE的查找增强机制从文本扩展到连续的多模态嵌入（图像、跨模态），同时保持分区存储和按需加载的优势，以提升VLM在视觉任务上的表现并兼顾效率。

- **整体含义**：本文提出LookME，首次实现了VLM中多模态嵌入的基于查找的增强，为在资源受限环境下部署高性能VLM提供了新途径。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将PLE框架扩展至多模态，设计层次化两级查找方法处理连续嵌入，并引入稀疏注入策略以平衡效率、模型大小和性能。

- **关键技术细节**：
  - **总体架构**：在VLM解码器中间层插入LookME层，每条LookME层包含文本、图像、跨模态三个并行的查找通路，从ROM存储的外部嵌入表中检索增强嵌入，然后通过稀疏注入融合到主干隐藏状态。公式表示为：  
    \(H_{\text{out}} = \text{DecoderLayer}(H_{\text{in}}) + S(R_{\text{text}} + R_{\text{img}} + R_{\text{cross}})\)，其中S为稀疏注入函数。
  - **多模态嵌入查找**：
    - **文本通路**：使用离散token ID直接索引文本嵌入表（O(1)查找），仅作用于文本位置。
    - **图像通路**：针对连续图像嵌入，提出层次化两级查找：
      - **两级嵌入表**：将图像嵌入表重塑为二维结构 \(E_{\text{img}} \in \mathbb{R}^{N_s \times N_p \times d_e}\)（场景×场景原语），以粗到细策略实现平衡查找。
      - **第一级（场景检索）**：通过余弦相似度将图像投影后的嵌入路由到最近的场景。
      - **第二级（场景原语检索）**：在选定场景内，用共享MLP计算原语相关性分数，取前\(\tilde{T}\)个原语，通过加权组合（softmax+tanh）生成最终检索嵌入。
      - **多尺度检索**：从视觉编码器多个中间层提取特征，独立进行两级查找，再通过token级软门控融合不同尺度的结果。
    - **跨模态通路**：使用解码器当前层的隐藏状态作为查询，同样采用层次化两级查找，应用于所有位置（文本、图像、任意模态）。
  - **稀疏注入**：
    - **层内稀疏激活（SKA）**：基于自注意力贡献\(\Delta_i\)，用Gumbel-Sigmoid门控为每个token/通路生成二元掩码，仅对高贡献token执行查找和注入。
    - **层间稀疏激活**：仅将少数解码层转换为LookME层（如[2/10/18]），非LookME层通过轻量级瓶颈传播（嵌入空间投影）继承最近LookME层的检索嵌入，避免重复检索。

## 3. 实验设计：数据集、基准、对比方法

- **训练数据**：来自开源图像-文本数据集，共60M样本（约570亿视觉token、140亿文本token），覆盖代表性视觉任务以保持分布均衡。
- **基准评测**：在11个基准上零样本评估，涵盖6个任务类别：
  - STEM：MMMU、MathVision。
  - 通用VQA：MMBench-EN、MMStar。
  - 幻觉：HallusionBench。
  - 文档理解：InfoVQA、OCRBench。
  - 视觉感知：HRBench8K、CV-Bench-3D、CCBench。
  - 多图像：BLINK。
- **对比方法**：
  - 原本基线：Qwen2.5VL-3B。
  - 继续预训练基线：Qwen2.5VL-3B + CPT（在同一训练数据上进一步预训练，隔离数据增益）。
  - 文本PLE方法：Engram、MeKi（使用相同超参数设置）。
- **实现细节**：骨干网络为Qwen2.5VL-3B，全参数微调LookME及所有基线。

## 4. 资源与算力

- 论文未明确说明所使用的GPU型号、数量及训练时长。只提到训练数据规模（60M样本，570亿视觉token，140亿文本token）和全参数微调，未提供算力细节。这一点是信息缺失。

## 5. 实验数量与充分性

- **实验组数**：
  - 主对比实验：在11个基准上比较4种方法（共44个数据点）。
  - 消融实验：
    - 多模态查找通路消融（3种变体×4个基准=12个数据点）。
    - 层间稀疏激活（NetProp）消融（3种变体×4个基准=12个数据点）。
    - 嵌入维度消融（3种维度×4个基准=12个数据点）。
    - 注入层位置消融（3种配置×4个基准=12个数据点）。
  - 总计约92个实验点（加上可能还有其他未列出的补充实验）。
- **充分性与客观性**：
  - 基准覆盖全面（多种视觉任务），消融实验覆盖核心设计点（通路、稀疏策略、超参数），较充分。
  - 对比方法包括基线、继续预训练基线、两个最新文本PLE方法，公平地在统一数据集上训练，使用相同超参数，比较合理。
  - 不足之处：未与多模态MoE方法（如DeepSeek-VL2）直接对比，也未报告推理时GPU内存和延迟的实际测量值，以验证“资源受限”场景的优势。

## 6. 论文的主要结论与发现

- LookME在11个基准上全面超越基线和文本PLE方法，尤其在需要细粒度多模态对齐的任务（如视觉感知：CV-Bench-3D +3.9，HRBench8K +3.1）上增益显著。
- 多模态查找通路（文本+图像+跨模态）共同带来最佳性能，仅文本通路增益有限，验证了多模态查找的必要性。
- 稀疏注入中的“头尾传播”策略（Head-Tail NetProp）在大多数基准上优于无传播或全传播，表明跨层有效传播需要控制传播距离。
- 嵌入维度512D最佳，注入层选择浅层（2/10/18）优于深层次，说明早期层对检索嵌入更敏感。

## 7. 优点

- **创新性**：首次将PLE查找机制从文本扩展到多模态嵌入，解决了VLM中主导视觉信息无法被增强的空白。
- **技术巧思**：针对连续嵌入的层次化两级查找设计（场景+原语），有效避免平坦大表的路由不平衡问题；多尺度图像检索和跨模态统一机制增强了灵活性。
- **稀疏注入**：SKA基于自注意力信号动态选择关键token激活，避免冗余计算；层间传播复用检索结果，减少重复查找开销。
- **实用性**：支持ROM分区存储和按需加载，有利于边缘设备部署。
- **实验充分**：消融实验覆盖三个关键通路、稀疏策略、超参数，逻辑清晰。

## 8. 不足与局限

- **算力信息缺失**：未报告训练所需GPU型号、数量、时间，也无法量化推理时相比基线的额外开销（延迟和峰值内存），削弱了“资源受限”的论证力度。
- **对比不完整**：未与多模态MoE模型（如MoE-VLM）或其它参数高效方法（如Adapter、LoRA）比较，也未报告在同等参数预算下与纯MoE的性能/效率权衡。
- **域外偏差**：训练数据仅来自开源数据集（可能偏向英文和常见场景），在低资源语言或专业领域（如医学图像）上的泛化性未知。
- **注入层策略**：实验中默认注入层为[2/10/18]（3层），但模型有36层（从Qwen2.5VL-3B推理），外层共33层解码层，仅3层增强。若增加LookME层数，性能可能进一步提升但会带来更多参数，文中未探索这种权衡。
- **鲁棒性**：两级查找依赖场景路由，若出现新场景分布外数据，路由质量可能下降；SKA门控依赖于自注意力，当令牌被掩盖或注意力分散时可能失效。这些问题未在实验中专门探讨。

（完）
