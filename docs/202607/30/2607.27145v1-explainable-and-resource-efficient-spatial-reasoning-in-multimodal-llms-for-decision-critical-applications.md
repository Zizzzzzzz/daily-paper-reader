---
title: Explainable and Resource-Efficient Spatial Reasoning in Multimodal LLMs for Decision-Critical Applications
title_zh: 面向决策关键应用的多模态大语言模型中可解释且资源高效的空间推理
authors: "Piyush Jain, Kousik Dasgupta, Rajarshi Roy, Subarna Tripathi"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27145v1"
tags: ["query:evlm"]
score: 9.0
evidence: 多模态大语言模型中的资源高效空间推理
tldr: "多模态大模型在决策关键任务中空间推理不透明且易产生幻觉，已有LDP方法因粗粒度分层无法解决同平面物体间关系。ByDeWay-V2通过集成显式空间关系谓词与深度线索，利用YOLO-World-L检测器计算几何关系并注入提示，无需训练。在BLINK空间子集上相比LDP提升46% F1，VSR上从随机水平恢复至0.53 F1，支持40-token预算下CPU运行，适用于资源受限实时决策。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27145v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1255, \"height\": 1080, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27145v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1078, \"height\": 727, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27145v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1177, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27145v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1061, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27145v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1097, \"height\": 375, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27145v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 948, \"height\": 436, \"label\": \"Table\"}]"
motivation: 现有MLLM空间推理存在幻觉且缺乏细粒度物体间关系理解，LDP的粗深度分层无法区分同平面投影与拓扑关系。
method: 在LDP基础上，用YOLO-World-L检测物体，计算成对几何关系并转为结构化空间谓词，与深度线索共同注入MLLM提示。
result: "BLINK空间子集上相对LDP提升46% F1；VSR上BLIP-Base从近随机恢复至0.53 F1；轻量配置仅需40 token预算。"
conclusion: 该训练无关框架以极低资源开销提升了MLLM空间推理的可解释性与准确性，适合实时决策支持场景。
---

## 摘要
随着多模态大语言模型（MLLMs）越来越多地部署于机器人、具身AI及安全监控等决策关键型管线，其空间判断的不透明性限制了操作者的信任与可审计性。MLLMs展现出强大的推理能力，但在细粒度空间理解和对象幻觉方面仍面临挑战。先前工作ByDeWay引入了基于分层深度的提示（LDP），这是一种免训练框架，通过利用单目深度估计构建提示来缓解幻觉。然而，粗粒度的深度分层在解决同一几何平面内对象间的空间关系（如投影关系“左”、“上”及拓扑关系“内部”、“接触”）方面存在不足。我们提出ByDeWay-V2，该框架将显式的空间关系上下文与深度线索相结合，以人类可读的谓词形式表达，作为下游决策支持的可审计证据。利用开放词汇目标检测器（YOLO-World-L），我们的框架计算检测对象间的成对几何关系，并将其作为结构化空间谓词注入MLLM提示，无需任何训练即可桥接3D场景深度与2D空间语义。我们在Visual Spatial Reasoning（VSR）和BLINK基准上，针对多个MLLM评估ByDeWay-V2，并通过POPE评估幻觉归因。在BLINK空间子集上，Qwen2.5-VL的ByDeWay-V2相比LDP实现了46%的相对F1提升，并将BLIP-Base在VSR上的空间推理从近乎随机性能恢复至具有竞争力的F1分数0.53。我们最轻量级的配置在CPU上严格限制40个token上下文预算下运行，展示了该框架适用于资源受限的实时决策支持场景。

## Abstract
As Multimodal Large Language Models (MLLMs) are increasingly deployed in decision-critical pipelines such as robotics, embodied AI, and safety monitoring, the opacity of their spatial judgments limits operator trust and auditability. MLLMs demonstrate strong reasoning but often struggle with fine-grained spatial understanding and object hallucination. Prior work, ByDeWay, introduced Layered-Depth-Based Prompting (LDP), a training-free framework that mitigates hallucinations by structuring prompts using monocular depth estimation. However, coarse depth layering falls short in resolving object-to-object spatial relationships within the same geometric plane, such as projective ("left of", "above") and topological ("inside", "touching") relations. We propose ByDeWay-V2, which integrates explicit spatial relational context alongside depth cues, expressed as human-readable predicates that serve as auditable evidence for downstream decision support. Using an open-vocabulary object detector (YOLO-World-L), our framework computes pairwise geometric relations between detected objects and injects them as structured spatial predicates into the MLLM prompt, bridging 3D scene depth and 2D spatial semantics without any training. We evaluate ByDeWay-V2 on the Visual Spatial Reasoning (VSR) and BLINK benchmarks across multiple MLLMs, with hallucination grounding assessed via POPE. On the BLINK spatial subset, ByDeWay-V2 achieves a 46 percent relative F1 improvement over LDP for Qwen2.5-VL, and recovers BLIP-Base's spatial reasoning on VSR from near-random performance to a competitive F1 of 0.53. Our lightest configuration operates under a strict 40-token context budget on CPU, showing the framework's suitability for resource-constrained, real-time decision-support settings.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态大语言模型（MLLMs）在决策关键型应用（如机器人、具身AI、安全监控）中，其空间推理能力不足，且输出不透明，缺乏可审计性。具体表现为：细粒度空间理解薄弱，容易产生对象幻觉（hallucination），尤其是同一深度平面内的物体间关系（如左/右、上/下、内部、接触等）难以准确判断。
- **背景**：先前工作 ByDeWay 提出了基于分层深度的提示（LDP），利用单目深度估计将场景分层并生成深度相关描述，有效缓解了部分幻觉。但 LDP 的粗粒度分层无法解决同平面物体间的几何关系（投影关系、拓扑关系等），导致空间推理性能在复杂查询上依然较差（例如 BLIP-Base 在 VSR 上的 F1 仅为 0.05，Qwen2.5-VL 在 BLINK 空间子集上 F1 仅 0.50）。
- **整体含义**：本文旨在提升 MLLM 空间推理的可解释性与准确性，同时保持资源高效（无需训练，可 CPU 运行），为决策关键场景提供可审计的证据。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：在 LDP 深度分层提示的基础上，额外加入显式的空间关系谓词（spatial predicates），这些谓词由开放词汇目标检测器 YOLO-World-L 检测对象，并基于成对边界框的几何特征计算得到，以自然语言形式注入 MLLM 提示。
- **关键技术细节**：
  - **Stage 1: LDP**：使用 DepthAnything V2 生成深度图，按百分位阈值（30th, 70th）划分为三层（最近、中间、最远），每层用 KOSMOS-2 生成自然语言描述，得到深度上下文串 \( C_{LDP} \)。
  - **Stage 2: SpatialAnalyser 模块**：
    - 开放词汇目标检测：YOLO-World-L 在任务查询关键词及 COCO 类别上检测，得到边界框和标签。
    - 成对几何关系计算：对每一对有序对象 \((i, j)\)，计算中心坐标、垂直/水平关系（above/below, left/right）、包含/接触关系（通过 IoU 或重叠比阈值 \(\theta_{in}=0.70\), \(\theta_{touch}=0.10\)）、接近程度（归一化中心距离阈值 \(\tau_{near}=0.30\)）。优先级：包含 > 接触 > 垂直/水平 > 接近。
    - 自然语言序列化：生成如 “The cup is left of laptop.” 的句子，拼接成空间上下文串 \( C_{Spatial} \)。
  - **混合提示构建**：最终提示 \( P = C_{LDP} \parallel C_{Spatial} \parallel Q \)，其中 Q 是用户查询。整个生成过程见算法 1。
- **算法流程**（文字说明）：输入图像和用户查询 → 深度层生成深度描述 → YOLO-World-L 检测对象 → 计算所有有序对几何关系 → 序列化为空间谓词 → 将深度描述、空间谓词、查询拼接为最终提示 → 输入 MLLM 进行推理。

## 3. 实验设计

- **数据集与基准**：
  - **VSR**（Visual Spatial Reasoning）：10,119 张图片-描述对，包含七种空间关系（投影、拓扑、邻接、方向、接近、方向、未分配），二分类（true/false）。
  - **POPE**（Polling-based Object Probing Evaluation）：对象存在性幻觉测试，使用 150 样本的简化版（来自 ByDeWay）。
  - **BLINK**：空间推理子集，测试对象相对位置（左/右/上/下）。
- **评估模型**：
  - Qwen2.5-VL-7B（7B 参数，SOTA MLLM）
  - BLIP-Base（约 250M 参数，轻量级 VLP）
  - ViLT-B/32（严格 40 token 输入限制的 transformer）
- **对比方法**：
  - LDP（V1）：仅使用深度层描述。
  - LDP+Spatial（V2）：深度层描述 + 空间谓词（ViLT 因 token 限制仅用 Spatial-Only）。

## 4. 资源与算力

- 原文未明确说明训练时长（因为是训练-free 框架）。推理资源：
  - Qwen2.5-VL：单张 NVIDIA A100 (40 GB)。
  - ViLT：CPU。
  - YOLO-World-L 检测器置信度阈值 0.20，最多 20 个空间关系对。
- 未提及 GPU 数量、训练时长等（因为无需任何参数更新）。

## 5. 实验数量与充分性

- 实验覆盖三大数据集（VSR、POPE、BLINK），三个不同规模/架构的模型（大、中、小），足够多样化。
- 每个模型在两种提示配置下比较（V1 vs V2），对于 ViLT 还额外评估了仅空间谓词版本。
- VSR 上额外做了按空间关系类别的细粒度分析（表4），展示不同类别上的 F1 增益，有助于理解方法对各关系的效果。
- 实验设计较充分，但缺少以下方面：
  - 与端到端训练方法的对比（本文强调训练-free，但未与如 SpatialVLM 等训练方法做公平比较）。
  - 未进行消融实验（例如仅使用 LDP、仅使用空间谓词、两者联合的完整消融）。
  - 未评估 YOLO-World 检测器召回率、精度对最终结果的影响。
  - 未在更多模型（如 LLaVA、GPT-4V 等）上验证泛化性。
- 总体而言，实验性价比高，但严格性可提升。

## 6. 论文的主要结论与发现

- 显式空间关系谓词的注入可以显著提升 MLLM 的空间推理能力，特别是对缺乏空间训练的小模型（BLIP-Base 在 VSR 上从 0.05 提高到 0.53 F1）。
- 在 BLINK 空间子集上，Qwen2.5-VL 获得 46% 相对 F1 提升（0.496 → 0.727），证明直接相关的谓词显著帮助模型回答。
- 所有模型在 POPE 上的幻觉指标均改善（F1 提升），说明空间上下文还能抑制对象存在幻觉。
- ViLT 的 40 token 严格预算下，空间谓词比深度描述更高效（Spatial-Only 优于 LDP）。
- 方法完全无需训练，模型无关，适用于资源受限的实时决策支持场景。

## 7. 优点

- **可解释性**：输出的空间谓词是显式、人类可读的证据，便于审计和决策追溯。
- **训练-free**：无需重新训练或微调任何 MLLM，直接即插即用。
- **资源高效**：轻量级配置（ViLT + 40 token）可在 CPU 上运行，适合实时嵌入式场景。
- **模型无关**：可搭配任意 MLLM 使用（已验证三种不同架构）。
- **融合深度与2D几何**：同时利用深度结构信息和物体间几何关系，互补性强。

## 8. 不足与局限

- **实验覆盖不够充分**：
  - 仅对比了 LDP 基线，未与当前其他先进的幻觉缓解方法（如指令微调、对比解码等）进行比较。
  - 未在更多 MLLM（如 LLaVA、Gemini、GPT-4V）上验证。
  - 缺乏完整的消融实验（如去除深度部分只用空间谓词、改变谓词优先级等）。
- **检测器依赖**：依赖 YOLO-World-L 的检测质量，检测失败或错误时可能误导 MLLM。论文未分析检测器误差对最终结果的影响。
- **提示设计可能引入偏差**：作者为 Qwen2.5-VL 添加了“Vision-First”指令，可能削弱了空间谓词的作用（导致 recall 下降）。这种设计选择可能不适用于所有模型。
- **空间关系类别有限**：仅覆盖水平/垂直、包含/接触、接近等几种，未处理更复杂的如“在…后面”、“穿过”等。
- **资源评估缺失**：未报告推理时间、内存占用等实际成本，仅给出了 token 预算说明。
- **部分结果有矛盾**：Qwen2.5-VL 在 VSR 上 V2 的 F1 下降（0.783→0.718），作者归因于“Vision-First”指令，但提示了方法在不同指令策略下的敏感性。这种 trade-off 需要进一步分析。
- **数据集规模有限**：POPE 只用了 150 样本的小版本，可能不够统计显著。

（完）
