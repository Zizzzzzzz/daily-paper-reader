---
title: Attention-Free and Lightweight Token Reduction for Efficient Vision-Language Models
title_zh: 面向高效视觉语言模型的无注意力轻量级令牌缩减方法
authors: "Xuanyi Hao, Zuoyuan Zhang, Zhibo Wang, Xiaoyi Pang, Jiahui Hu, Jiacheng Du, Shuguo Zhuo"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13500v1"
tags: ["query:evlm"]
score: 9.0
evidence: 提出一种免注意力的token缩减框架以高效推理VLM
tldr: 视觉语言模型(VLM)处理大量视觉令牌导致计算开销大，难以部署在边缘设备。现有令牌缩减方法依赖注意力图或计算密集的成对相似性比较，缺乏可扩展性。本文提出一种免注意力、轻量级的令牌缩减框架，作为即插即用模块用于VLM。通过信息论视角的熵准则保留重要令牌，并通过变换一致性信号进行步长选择保持多样性。实验表明，该方法在压缩下保持性能，实现准确性与效率的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13500v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1830, \"height\": 1116, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 800, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 889, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 932, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 896, \"height\": 587, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13500v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1883, \"height\": 1169, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13500v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 938, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13500v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 894, \"height\": 164, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13500v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 926, \"height\": 573, \"label\": \"Table\"}]"
motivation: 现有令牌缩减方法依赖注意力图或密集相似性计算，难以在资源受限边缘设备上实现高效VLM推理。
method: 提出免注意力重要性估计（熵准则）和轻量级多样性选择（变换一致性信号+步长采样），形成即插即用令牌缩减模块。
result: 在多个VLM基准上，该方法在激进压缩下仍保持竞争性能，实现有利的准确率-效率折衷。
conclusion: 该框架可实现高效VLM推理，适用于边缘部署，且兼容现代加速框架。
---

## 摘要
视觉语言模型在多模态理解中取得了强劲性能，但由于处理大量视觉令牌的计算开销巨大，在资源受限的边缘设备上部署仍具挑战性。令牌缩减是加速视觉语言模型推理的一个有前景的方向，但现有方法要么依赖于与现代加速框架不兼容的注意力图，要么依赖于计算密集型的成对相似性比较，这损害了可扩展性并抵消了部署中的实际收益。本文提出了一种无注意力和轻量级的令牌缩减框架，作为视觉语言模型的即插即用模块，该框架保留重要且多样化的令牌以生成紧凑的视觉表示。首先，为了实现无注意力重要性估计，我们采用信息论视角，利用一种新的基于熵的准则量化令牌信息，保留那些具有更具表达性和更少退化的特征表示的令牌。其次，为了以轻量级方式确保多样化的视觉覆盖，我们引入一种变换诱导的一致性信号，其中相似的令牌产生相似的信号，从而按此信号排序会将相似的令牌彼此靠近，并能够通过步长选择生成多样化的令牌集。在多个视觉语言模型基准上的大量实验表明，我们的框架实现了良好的精度-效率权衡，在激进压缩下仍保持有竞争力的性能。

## Abstract
Vision-Language Models (VLMs) have achieved strong performance in multimodal understanding, yet remain challenging to deploy on resource-constrained edge devices due to the substantial computational overhead of processing numerous visual tokens. Token reduction is a promising direction for accelerating VLMs inference, but existing approaches either rely on attention maps that are incompatible with modern acceleration frameworks or depend on computationally intensive pairwise similarity comparisons, which undermine scalability and negate their practical benefits in deployment. In this paper, we propose an attention-free and lightweight token reduction framework as a plug-and-play module for VLMs, which preserves both important and diverse tokens to produce a compact visual representation. First, to enable attention-free importance estimation, we adopt an information-theoretic perspective and quantify token information using a novel entropy-based criterion, retaining those with more expressive and less degenerate feature representations. Second, to ensure diverse visual coverage in a lightweight manner, we introduce a transformation-induced consistency signal where similar tokens yield similar signals, such that sorting by this signal places similar tokens close to each other and enables stride-based selection to produce a diverse token set. Extensive experiments across multiple VLMs benchmarks demonstrate that our framework achieves a favorable accuracy-efficiency trade-off, maintaining competitive performance under aggressive compression.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉语言模型（VLMs）在处理多模态任务时，视觉编码器生成大量视觉令牌（如每张图像数百甚至数千个），这些令牌作为语言模型输入导致序列长度激增，进而引发自注意力机制的二次计算复杂度，严重制约了模型在资源受限边缘设备上的部署。
- **现有方法的局限**：
  - 依赖注意力图的方法（如SparseVLMs、PyramidDrop、MustDrop）与现代加速框架（如FlashAttention）不兼容，因为后者避免显式生成注意力矩阵。
  - 基于特征相似性的方法（如SAINT、DyMU）需要进行计算密集的成对令牌比较（通常二次复杂度），削弱了令牌缩减的实际加速收益。
- **研究意义**：迫切需要一种既无注意力依赖、又计算轻量、且兼容现有加速框架的视觉令牌缩减方法，以实现高效的VLM边缘部署。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
提出一种即插即用（plug-and-play）的无注意力轻量级令牌缩减框架（ALTR），在视觉编码器与LLM之间执行单次令牌选择，同时兼顾 **重要性** 和 **多样性**。

### 关键技术细节

#### （1）重要性感知令牌选择（Stage I）：基于Rényi熵
- **动机**：信息丰富的令牌通常具有更均匀、更不退化的特征激活分布。
- **操作**：
  - 对每个视觉令牌 \( x_i \in \mathbb{R}^D \)，沿通道维度归一化特征绝对值：\( p_{i,j} = |x_{i,j}| / \sum_k |x_{i,k}| \)，形成离散概率分布。
  - 计算二阶Rényi熵：\( H_2(x_i) = -\log \sum_{j=1}^D p_{i,j}^2 \)。
  - 熵越高表示激活越均匀（信息更丰富），保留Top-K1个高熵令牌组成 \( X_{\text{imp}} \)。

#### （2）多样性感知令牌选择（Stage II）：基于变换一致性信号
- **动机**：避免重要性选择后剩余令牌中语义相似造成冗余，通过轻量级步长采样保证覆盖。
- **操作**：
  - 从视觉编码器最后一层MLP获取令牌的变换前表示 \( x'_i \) 和变换后表示 \( x''_i \)（无额外计算开销）。
  - 计算余弦相似度 \( s_i = \cos(x'_i, x''_i) \)，该标量值反映令牌对变换的敏感性；语义相似的令牌具有相近的 \( s_i \)。
  - 将剩余令牌按 \( s_i \) 升序排序，然后进行步长采样（stride-based sampling）以均匀覆盖，得到 \( X_{\text{div}} \)（K2个令牌）。

#### （3）整体流程
- 总预算 K = K1 + K2，由超参数 λ 控制：K1 = λK, K2 = (1-λ)K。
- 最终令牌集 \( X^* = X_{\text{imp}} \cup X_{\text{div}} \)。
- **计算复杂度**：O(N D + N log N)，远低于 VisionZip（O(N²D)）和 VisPruner（O(N³D)）。

## 3. 实验设计

### 使用的数据集/场景
- **主实验**：LLaVA-v1.5-7B 在四个基准上评估：
  - MME（总分）、TextVQA（准确率）、ScienceQA（准确率）、GQA（准确率）。
- **泛化实验**：
  - LLaVA-NeXT（高分辨率）在 GQA 上测试（每个图像分割为5个子图，令牌数更多）。
  - Qwen2.5-VL-7B 在 TextVQA 上测试。

### 基准模型（Baselines）
- **Inner-LM 方法**：SparseVLMs (ICML'25)、PyramidDrop (CVPR'25)、MustDrop (arXiv)。
- **Before-LM 方法**：VisionZip (CVPR'25)、VisPruner (ICCV'25)。

### 令牌预算设置
- LLaVA-v1.5：保留 192、128、64 个令牌（对应缩减 66.7%、77.8%、88.9%），原始 576 个。
- LLaVA-NeXT：保留 960、640、320 个令牌（对应缩减 66.7%、77.8%、88.9%）。
- Qwen2.5-VL：保留约 450、300、150 个令牌。

## 4. 资源与算力

- **文中未明确说明模型训练的算力资源**，仅提及推理效率实验在单张 NVIDIA RTX 5880 GPU 上进行，同时边缘部署测试在 NVIDIA Jetson AGX Orin 上执行。
- 由于ALTR是训练无关（training-free）的方法，**无需模型重新训练或微调**，因此算力消耗主要体现在推理阶段。文中未报告训练耗时或总GPU小时数。

## 5. 实验数量与充分性

- **主实验**：在 LLaVA-v1.5-7B 上对3种令牌预算×4个数据集共计12组结果；包含与5种基线方法的对比。
- **泛化实验**：在 LLaVA-NeXT（GQA）和 Qwen2.5-VL（TextVQA）上各3种预算，共6组对比。
- **效率分析**：计算复杂度分析（表格）+ FLOPs + CUDA时间对比（3种方法）。
- **兼容性分析**：在 Qwen2.5-VL 上比较标准注意力与FlashAttention-2的性能差异。
- **边缘部署实验（Testbed Evaluation）**：在 Jetson AGX Orin 上测试OOM边界，比较5种方法在不同令牌预算下的最大无OOM生成长度。
- **消融实验**：在 LLaVA-v1.5-7B 128令牌预算下，移除重要性模块、移除多样性模块，共3组变体对比，并附带可视化。
- **超参数分析**：λ 在0~1之间变化，测试不同预算下的敏感度。

**充分性评估**：实验覆盖了准确率、效率、兼容性、实际部署等多个维度，且比较了多个主流方法，消融与超参数分析齐全，实验设计较为客观公平。但缺乏在多模态对话或视频任务上的验证，泛化性测试仅涉及两个变体模型。

## 6. 主要结论与发现

- ALTR在所有测试预算下均取得 **最优或接近最优** 的准确率保持（在LLaVA-v1.5上192令牌时保留98.86%性能；64令牌时保留94.52%），优于所有基线。
- 在LLaVA-NeXT高分辨率场景中同样表现最佳，尤其极端压缩下（320令牌）显著优于其他方法（如比PyramidDrop高近10%）。
- 在Qwen2.5-VL上，极低令牌预算（150令牌）时比VisionZip高5.48%，展示了强大泛化能力。
- **效率优势**：仅需4.13M FLOPs和0.579ms CUDA时间，远低于VisionZip和VisPruner。
- **兼容性好**：与FlashAttention-2无缝集成，降低视觉编码器延迟约5倍，预填延迟降低约3倍。
- **边缘部署可行**：在Jetson AGX Orin上可将最大无OOM生成长度从768扩展至1536+令牌，且缩减阶段延迟最低（约70ms）。
- 消融实验证实两大组件缺一不可：移除重要性模块平均性能下降约5%；移除多样性模块下降约0.83%。

## 7. 优点

- **方法设计创新**：首次将二阶Rényi熵用于视觉令牌重要性估计，无需注意力图；利用MLP变换一致性作为轻量级多样性信号，避免成对比较。
- **训练无关**：即插即用，无需任何微调或重新训练，易于部署。
- **计算效率极高**：线性复杂度，实际开销极低（<1ms），同时兼容FlashAttention等现代加速框架。
- **实验全面**：从准确率、效率、兼容性、边缘部署四个维度验证，覆盖多种主流VLM架构和基准。
- **可视化与消融完备**：提供令牌选择可视化，直观展示各组件作用。

## 8. 不足与局限

- **实验覆盖范围**：仅评测了图像理解任务（VQA、科学QA、场景图QA），未涉及视频理解、图像生成或多模态对话；模型仅测试了LLaVA系列和Qwen2.5-VL，缺少对InternVL、Gemini等模型的验证。
- **多样性信号依赖最后一层MLP**：该方法要求视觉编码器包含MLP层，对于不使用标准ViT的视觉编码器（如SigLIP部分变体）可能需要适配。
- **超参数λ的敏感性**：虽然文中显示在较宽范围内性能稳定，但最优λ仍存在微小波动，且所有方法采用固定λ不够公平（可能存在baseline调优不充分）。
- **缺乏与更多近期方法的对比**：未与DyMU、TAMP、SwiftVLM等方法比较，可能遗漏更强基线。
- **假设缺陷**：熵准则假设激活均匀性对应信息丰富性，但某些关键细粒度令牌（如小物体）可能具有低熵且仍然重要，方法可能偏好背景区域？文中未深入讨论这种偏差风险。
- **边缘部署实验限制**：仅使用单块16GB预算，未探讨不同内存预算下的表现；且只测试了长响应生成，未涵盖推理延迟指标。

（完）
