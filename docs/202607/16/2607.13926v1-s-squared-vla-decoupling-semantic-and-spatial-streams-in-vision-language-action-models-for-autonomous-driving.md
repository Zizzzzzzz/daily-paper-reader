---
title: "S-squared-VLA: Decoupling Semantic and Spatial Streams in Vision-Language-Action Models for Autonomous Driving"
title_zh: S-squared-VLA：面向自动驾驶的视觉-语言-动作模型中语义流与空间流的解耦
authors: "Jianguo Yu, Rukang Wang, Duanfeng Chu, Chen Wang, Renju Feng, Liping Lu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13926v1"
tags: ["query:evlm"]
score: 8.0
evidence: 面向自动驾驶的VLA模型，解耦语义和空间流
tldr: "大型视觉-语言模型在自动驾驶规划中面临语义与物理空间的鸿沟，标准视觉-语言-动作模型因耦合语义与空间流导致空间表征严重退化。本文提出S-squared-VLA，显式解耦语义流与空间流：语义流通过层级桥接提取多尺度特征用于意图推理，空间流绕过自回归语言瓶颈直接保留视觉编码器的未压缩空间特征，并通过辅助感知监督增强几何先验。双流规划适配器利用级联注意力融合高层语义与精细空间约束。在NAVSIM闭环基准上，该方法达到PDMS 87.1，碰撞率仅1.6%，刷新纯监督微调下VLA模型的最优性能，有效缓解了空间表征崩溃问题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 912, \"height\": 1020, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13926v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1877, \"height\": 869, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13926v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 920, \"height\": 740, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13926v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 567, \"height\": 506, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13926v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1537, \"height\": 542, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13926v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1626, \"height\": 226, \"label\": \"Table\"}]"
motivation: 标准VLA模型耦合语义与空间流导致空间表征崩溃，细粒度几何先验不可逆退化，无法生成精确控制动作。
method: 提出S-squared-VLA，解耦语义和空间流：语义流层级桥接VLM特征，空间流独立保留未压缩特征并辅以感知监督，双流规划适配器级联融合。
result: "在NAVSIM上PDMS达87.1，No Collision率98.4%，超越所有基线方法，成为纯SFT下VLA模型SOTA。"
conclusion: 通过解耦语义与空间流避免了空间表征崩溃，实现了精确闭环驾驶规划，展示了VLA模型在自动驾驶中的潜力。
---

## 摘要
视觉-语言模型（VLM）在自动驾驶的高层推理方面展现出巨大潜力，但其从根本上难以生成精确的低层控制动作。这一局限性源于离散语言令牌与连续轨迹规划之间的固有失配所导致的语义-物理鸿沟。尽管视觉-语言-动作（VLA）架构试图通过将感知与控制统一为单一策略来弥合这一鸿沟，但这种耦合也带来了新的瓶颈。标准VLA会出现严重的空间表征坍塌，不可逆地降低了安全边界感知导航所必需的细粒度空间与几何先验。为解决这一局限性，我们提出了S-squared-VLA，该模型显式解耦了视觉-语言-动作模型中的语义流与空间流。语义流利用分层桥接提取多尺度VLM特征以实现鲁棒的意图推理。同时，独立的空间流绕过了自回归语言瓶颈，直接保留来自视觉编码器的未压缩空间特征。通过集成辅助感知监督，该流显式地赋予模型丰富的空间与几何先验。最后，一个双流规划适配器通过级联注意力机制融合高层语义意图与精确空间约束。在NAVSIM闭环基准上的评估表明，S-squared-VLA在纯监督微调（SFT）设置下达到了87.1的预测驾驶员模型得分（PDMS），创下了VLA模型的新纪录。通过缓解传统VLM的空间表征坍塌，我们的框架显著优于基线，在所有评估方法中实现了最高的无碰撞率（NC）98.4。

## Abstract
Vision-Language Models (VLMs) have demonstrated remarkable potential for high-level reasoning in autonomous driving, yet they fundamentally struggle to generate precise, low-level control actions. This limitation is rooted in a semantic-physical gap caused by the inherent mismatch between discrete language tokens and continuous trajectory planning. While Vision-Language-Action (VLA) architectures attempt to bridge this gap by unifying perception and control into a single policy, this entanglement creates a new bottleneck. Standard VLAs experience a severe spatial representation collapse, which irreversibly degrades the fine-grained spatial and geometric priors essential for safe, boundary-aware navigation. To address this limitation, we propose the S-squared-VLA, which explicitly decouples the semantic and spatial streams in Vision-Language-Action models. The semantic stream leverages hierarchical bridging to extract multi-scale VLM features for robust intent reasoning. In parallel, an independent spatial stream bypasses the autoregressive language bottleneck, directly preserving uncompressed spatial features from the visual encoder. By integrating auxiliary perception supervision, this stream explicitly equips the model with rich spatial and geometric priors. Finally, a dual-stream planning adapter fuses high-level semantic intent with precise spatial constraints via cascaded attention mechanisms. Evaluations on the NAVSIM closed-loop benchmark show that S-squared-VLA achieves a Predictive Driver Model Score (PDMS) of 87.1, establishing a new state-of-the-art for VLA models under a purely supervised fine-tuning (SFT) setting. By mitigating the spatial representation collapse of traditional VLMs, our framework significantly outperforms baselines, achieving the highest No Collision (NC) rate of 98.4 among all evaluated methods.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：自动驾驶中，端到端（E2E）模型缺乏语义可解释性和高层推理能力；视觉-语言模型（VLM）能提供自然语言理解和链式推理，但其输出是离散的（如“左转”），无法直接输出连续的轨迹控制信号，存在“语义-物理鸿沟”。
- **现有局限**：将VLM扩展至低层控制时，语言令牌化和深层抽象会逐步丢失细粒度空间与几何信息，导致**空间表征坍塌**（spatial representation collapse）。标准视觉-语言-动作（VLA）模型虽然试图统一感知、推理和控制，但其单流设计将语义推理与执行耦合，跨模态对齐在高度压缩的嵌入上进行，造成**语义优先于几何的偏差**，生成的轨迹可能不安全或几何不一致。
- **核心目标**：提出一种解耦语义流与空间流的新架构，在保留高层语义推理能力的同时，显式保持并注入空间/几何先验，以生成安全、符合边界约束的轨迹。

## 2. 方法论：核心思想、关键技术细节
### 总体架构
- **双流设计**：输入包括前视图像、导航指令、自车运动历史、以及可学习的视觉查询和动作查询。经过视觉编码后，显式分为**语义流**和**空间流**。
- **语义流**：使用 InternVL3-2B 作为视觉-语言骨干。将文本令牌、图像令牌和 64 个可学习动作查询注入骨干，从选定层（第3、8、13、18、23、24层）稀疏采样多尺度特征。最终输出密集语义特征和动作中心特征，并与自车状态嵌入拼接形成状态记忆。
- **空间流**：直接访问视觉编码器（ViT）的未压缩特征，引入64个可学习视觉查询，聚合细粒度几何细节。通过图块级位置编码和Transformer编码器处理后，进一步通过两个辅助感知头（地图头、智能体头）注入显式空间/几何先验。
  - **地图头**：将1D序列重塑为2D网格，通过上采样和卷积生成局部BEV语义地图（32m前向×64m横向），使用加权交叉熵损失训练。
  - **智能体头**：基于DETR范式，使用30个可学习查询，Transformer解码器输出目标2D包围框和类别，使用匈牙利匹配和L1+BCE损失。
- **双流规划适配器**：由N个堆叠块组成，每个块依次进行：
  1. **语义与状态对齐**：以规划令牌为查询，与多尺度语义特征和状态记忆做交叉注意力，并用可学习门控融合自注意力结果。
  2. **视觉空间精炼**：将上一步结果与来自空间流的密集几何特征做交叉注意力，强制规划令牌遵守物理约束。
  3. 最后通过MLP解码为未来轨迹（M=8个路点），优化目标包含L1轨迹回归和平滑性约束（加速度、加加速度的SmoothL1）。
- **多任务联合损失**：`L_total = λ_plan * L_plan + λ_agent * L_agent + λ_map * L_map`，超参数分别为1.0、0.1、0.5。

## 3. 实验设计
- **数据集/场景**：使用 **NAVSIM 基准**（闭环仿真），强调动态环境中的交互。主要指标为**PDMS**（预测驾驶员模型得分），由五个子指标构成：无碰撞率（NC）、可行驶区域合规率（DAC）、碰撞时间（TTC）、舒适度（Comf）、自车进度（EP）。
- **对比方法**：
  - **E2E 方法**：TransFuser、UniAD、PARA-Drive、DRAMA、ARTEMIS、DiffusionDrive。
  - **VLM/VLA 方法**：InternVL3-2B、ImagiDrive、AutoVLA、ReCogDrive。
  - 所有对比均在纯监督微调（SFT）设置下进行，排除了强化学习后训练变体以确保公平。
- **评价方式**：定量比较四个维度的结果；定性可视化轨迹对比（与Transfuser、InternVL3-2B、ReCogDrive）；消融实验逐步验证组件贡献。

## 4. 资源与算力
- **硬件**：4块 NVIDIA A100 GPU。
- **训练设置**：三阶段训练——阶段1在ReCogDrive VQA数据集上SFT 3个epoch；阶段2冻结VLM，使用LoRA训练意图提取和辅助感知模块4个epoch；阶段3冻结感知和意图组件，仅优化视觉精炼模块4个epoch。总共约11个epoch（不含预训练）。
- **其他**：超参数表给出学习率1e-4、批大小16、LoRA rank/alpha=8/16等。

## 5. 实验数量与充分性
- **定量实验**：在NAVSIM上报告了所有11个对比方法的完整指标（PDMS及五个子项），进行了**两组主要对比**（E2E组、VLM/VLA组）。
- **定性实验**：展示了3个典型场景的轨迹可视化，与三个基线做对比。
- **消融实验**：逐步添加语义特征、空间流、辅助感知任务，共4个配置，完整报告了所有子指标和PDMS。
- **充分性评估**：对比基线均为近期顶会/期刊工作（CVPR、NeurIPS、ICLR等），且专门排除了RL后处理以保证公平；消融设计清晰，每一步的贡献量化和归因合理。但未提供跨数据集泛化实验（如nuScenes）、多视角或多模态输入对比，也未在真实世界数据上验证。总体充分，但局限于单一闭环仿真基准。

## 6. 主要结论与发现
- **核心结论**：通过解耦语义流与空间流，S²-VLA 在NAVSIM上达到 **PDMS 87.1**，**NC 98.4%**，均超越所有其他VLM/VLA方法，成为纯SFT设置下的新SOTA。
- **具体贡献**：
  - 语义流中的多尺度特征提升Ego Progress和TTC，证明高层语义对长程规划有益。
  - 空间流的加入显著提升DAC（94.0→94.5），证明保持未压缩几何特征对道路保持至关重要。
  - 辅助感知任务（地图+智能体）使NC提升0.3、EP提升1.0，说明显式空间先验对安全性至关重要。
- **定性分析**：S²-VLA生成的轨迹在转向、交叉口等复杂场景下更贴近专家轨迹，克服了VLM的量化误差和E2E模型的边界违规。
- **根本原因**：绕过语言令牌的量化瓶颈，保留连续空间特征，是生成安全物理可行轨迹的关键。

## 7. 优点
- **架构创新**：首次在VLA中显式解耦语义与空间流，避免耦合带来的空间表征坍塌，设计符合规划任务的“意图先确定、几何再验证”的自然分解。
- **有效的多级特征利用**：语义流稀疏采样多层特征，双向（语义+动作查询）提取；空间流通过视觉查询和辅助任务保持细粒度信息。
- **双流规划适配器**：级联的语义对齐+空间精炼，门控融合机制允许自适应平衡语义与空间约束。
- **实验公平性**：严格控制纯SFT设置，排除RL后训练影响；消融实验逐步累加，量化每个组件的贡献。
- **单目视觉可达高性能**：仅用前视相机达到甚至超过LiDAR融合方法（如ARTEMIS 87.0 vs 87.1），显示双流架构对深度歧义的补偿能力。
- **代码/方法透明**：提供了详细的超参数、训练阶段设置和损失函数定义。

## 8. 不足与局限
- **计算开销**：双流并行的密集提取与融合带来额外计算成本，文章承认该局限，并计划未来开发更稀疏的表示机制。
- **训练范式单一**：未探索闭环强化学习后训练（已指明是与双流正交的方向，作为未来工作）。
- **实验覆盖有限**：
  - 仅在NAVSIM单一仿真基准上评估，未在开放性真实世界数据集（如nuScenes、Waymo）或复杂长尾场景（夜间、雨雪、遮挡严重）中验证。
  - 未对比多视图输入或多模态融合（如激光雷达+相机）组合下的性能。
  - 消融实验只测试了4种配置，未系统研究不同稀疏采样层数、动作查询数量等超参数的影响。
- **偏差风险**：NAVSIM可能引入与训练数据分布相关的隐含偏差；纯SFT方法若训练数据含有偏行为，模型可能放大偏差。未讨论公平性或对抗鲁棒性。
- **应用限制**：依赖高质量BEV地图和智能体标注进行辅助监督，获取成本高；实时推理效率未明确报告，仅说明使用4×A100训练，未提推理速度与部署要求。

（完）
