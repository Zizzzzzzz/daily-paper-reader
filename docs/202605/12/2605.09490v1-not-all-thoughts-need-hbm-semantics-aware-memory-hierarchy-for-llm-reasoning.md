---
title: "Not All Thoughts Need HBM: Semantics-Aware Memory Hierarchy for LLM Reasoning"
title_zh: 并非所有“思考”都需要 HBM：面向大语言模型推理的语义感知内存层级结构
authors: "Aojie Yuan, Tianqi Shen, Dajun Zhang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09490v1"
tags: ["query:evlm"]
score: 9.0
evidence: 大语言模型中 KV 缓存管理的语义感知内存层级
tldr: "针对推理型大语言模型在生成长思维链时KV缓存过度占用GPU显存的问题，本文提出了一种语义感知的内存分级方案。该方法通过累积注意力评分将Token划分为HBM、DDR、压缩和剔除四个层级，将低重要性Token卸载至CPU内存而非直接删除，并在计算前预取回GPU。这种“零近似误差”卸载技术在减少50%显存占用的同时，能保持极高的推理准确率，解决了传统剔除算法导致性能崩溃的难题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09490v1/fig-001.webp\", \"caption\": \"Table 6: Cross-scale validation on GSM8K (n = 50). The hierarchy preserves accuracy across 7B– 32B while eviction catastrophically fails at every scale. 7B/14B use fp16; 32B uses int4 quantization. 95% Clopper-Pearson CIs in brackets.\", \"page\": 11, \"index\": 1, \"width\": 682, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-002.webp\", \"caption\": \"Figure 9: Token tier distribution during generation. The hierarchy dynamically assigns tokens to tiers as the chain grows, with T0 (HBM) capacity remaining bounded.\", \"page\": 21, \"index\": 2, \"width\": 566, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-003.webp\", \"caption\": \"Figure 11: Latency breakdown per sample (averaged). Transfer overhead (GPU↔CPU) constitutes only 5–7% of total inference time.\", \"page\": 22, \"index\": 3, \"width\": 567, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-004.webp\", \"caption\": \"Figure 10: End-to-end accuracy and throughput comparison. Hierarchy configurations (green) achieve comparable accuracy to the full baseline (blue) with modest throughput reduction. Eviction (red) collapses in accuracy.\", \"page\": 22, \"index\": 4, \"width\": 816, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-005.webp\", \"caption\": \"Table 10: End-to-end system prototype results on GSM8K (n = 50, int8 model, RTX 5080). Hierarchy configurations match or exceed full-cache accuracy while keeping only 30–50% of KV cache tokens in HBM. 95% CIs in brackets.\", \"page\": 14, \"index\": 5, \"width\": 660, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-006.webp\", \"caption\": \"Figure 7: Latency breakdown for hierarchy configurations. Transfer overhead (GPU↔CPU offload + prefetch) constitutes only 5–7% of total inference time; model computation dominates.\", \"page\": 14, \"index\": 6, \"width\": 527, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-007.webp\", \"caption\": \"Figure 1: Attention importance distribution of reasoning tokens (DeepSeek-R1-Distill-Qwen-7B, GSM8K, n = 50). (a) Histogram showing long-tail distribution on log scale. (b) CDF confirming that 80% of tokens have near-zero importance. (c) Importance concentration: top-20% tokens capture 56.5% of total importance.\", \"page\": 3, \"index\": 7, \"width\": 817, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-008.webp\", \"caption\": \"Figure 2: Streaming eviction accuracy vs. KV-cache budget. Attention-based scoring outperforms random, but both exhibit a cliff effect below 70% budget. At 50% budget, accuracy drops to 0%.\", \"page\": 3, \"index\": 8, \"width\": 480, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-009.webp\", \"caption\": \"Figure 5: PCIe transfer latency across all 28 KV layers (RTX 5080, PCIe Gen5 x16, fp16 KV cache). Transfer cost scales linearly with token count. At the 64-token eviction interval, latency is <2 ms.\", \"page\": 13, \"index\": 9, \"width\": 522, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-010.webp\", \"caption\": \"Figure 6: PCIe bandwidth utilization. Throughput saturates above 64 tokens at ∼22 GB/s (GPU→CPU) and ∼15 GB/s (CPU→GPU), 24–35% of the theoretical 63 GB/s peak.\", \"page\": 13, \"index\": 10, \"width\": 521, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-011.webp\", \"caption\": \"Table 3: Memory hierarchy vs. pure eviction on GSM8K (DeepSeek-R1-Distill-Qwen-7B, n = 50). 95% Clopper-Pearson CIs in brackets. At the same eviction ratio, the hierarchy approach (offload to DDR) vastly outperforms pure eviction (permanent removal). Quantization of offloaded entries degrades accuracy.\", \"page\": 8, \"index\": 11, \"width\": 762, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-012.webp\", \"caption\": \"Table 2: H2O eviction baseline vs. our memory hierarchy on GSM8K (n = 50). Our hierarchy at 5% eviction outperforms H2O at every budget level. 95% CIs in brackets.\", \"page\": 8, \"index\": 12, \"width\": 506, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-013.webp\", \"caption\": \"Table 14: Landscape of KV-cache optimization. Our approach is the first to combine reasoning-aware scoring with tiered offloading.\", \"page\": 17, \"index\": 13, \"width\": 606, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-014.webp\", \"caption\": \"Figure 4: Eviction ratio sweep. The sweet spot is 3–5% eviction. 8-bit quantization of offloaded tokens degrades accuracy significantly.\", \"page\": 9, \"index\": 14, \"width\": 480, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-015.webp\", \"caption\": \"Figure 3: Memory hierarchy vs. pure eviction. At equivalent eviction ratios, the hierarchy (which offloads to DDR rather than discarding) substantially outperforms pure eviction. Pure eviction collapses at ≥30% eviction.\", \"page\": 9, \"index\": 15, \"width\": 602, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2605.09490v1/fig-016.webp\", \"caption\": \"Figure 8: Distribution of rank correlations between attention-based and gradient-based importance scores across 50 GSM8K samples. The near-zero mean Spearman ρ (−0.13) indicates the two methods rank tokens in fundamentally different orders.\", \"page\": 16, \"index\": 16, \"width\": 612, \"height\": 218}]"
motivation: 传统的KV缓存剔除策略会严重破坏推理型LLM的逻辑链条，导致模型准确率在缓存减半时近乎归零。
method: 引入语义感知内存层级，利用累积注意力评分将Token动态调度至CPU内存，并通过预取机制实现无损的注意力计算。
result: "在14B规模模型上，该方法在节省一半显存的情况下实现了与全缓存基准相当的准确率，且仅引入5-7%的传输开销。"
conclusion: 推理准确性主要取决于永久剔除的Token比例而非HBM中的留存量，通过内存分级可有效缓解大模型推理的显存瓶颈。
---

## 摘要
推理型大语言模型（LLM）会产生数千个思维链（CoT）token，其 KV 缓存必须驻留在稀缺的 GPU HBM 中。目前主流的应对方案——永久剔除低重要性 token——对推理任务而言是灾难性的：当移除一半缓存时，准确率会骤降至 0-2.5%。我们提出了一个不同的问题：是否每个 token 都必须驻留在 HBM 中，或者其中一些可以存放在别处？我们引入了一种语义感知的内存层级结构，利用累积注意力评分将 token 分为四个层级：HBM、DDR、压缩和剔除。低重要性的 token 被移动到 CPU 内存而非直接销毁；在每个注意力步骤之前，它们会以全精度预取回 GPU，其贡献的项与从未离开过 GPU 时完全一致。我们将其形式化为“零近似误差卸载”，并得出核心发现：准确率仅取决于永久丢弃的 token 数量（剔除率），而与保留在 HBM 中的数量无关。通过在 HBM 占比和剔除率上进行的 3x3 受控实验，我们在三种模型规模（7B-32B）和四个基准测试上证实了这一点。在仅有 3% 剔除率的情况下，该层级结构在 GSM8K 上保留了 91% 的全缓存准确率，在 MATH-500 (n=200) 上保留了 71%；在 14B 规模下，它在 HBM 占用减半的同时，达到了与未压缩基准相当的水平（90% 对 86%）。在相同预算下，对当前最先进（SOTA）的剔除方法 R-KV 进行的直接复现仅获得了 0-32% 的准确率。具有真实 GPU-CPU 数据传输的系统原型表明，这种性能保留的代价很小——仅 5-7% 的传输开销——且扩展性分析预测在生产批处理大小下可节省 2-48 GB 的 HBM。

## Abstract
Reasoning LLMs produce thousands of chain-of-thought tokens whose KV cache must reside in scarce GPU HBM. The dominant response -- permanently evicting low-importance tokens -- is catastrophic for reasoning: accuracy collapses to 0-2.5% when half the cache is removed. We ask a different question: must every token live in HBM, or can some live elsewhere? We introduce a semantics-aware memory hierarchy that sorts tokens into four tiers -- HBM, DDR, compressed, and evicted -- using cumulative attention scoring. Low-importance tokens are moved to CPU memory rather than destroyed; before each attention step they are prefetched back at full precision, contributing exactly the same terms as if they had never left the GPU. We formalize this as zero-approximation-error offloading and derive our central finding: accuracy depends solely on how many tokens are permanently discarded (the eviction ratio), not on how many remain in HBM. A controlled 3x3 grid over HBM and eviction ratios confirms this across three model scales (7B-32B) and four benchmarks. With only 3% eviction, the hierarchy retains 91% of full-cache accuracy on GSM8K and 71% on MATH-500 (n=200); at 14B scale it matches the uncompressed baseline (90% vs. 86%) while halving HBM occupancy. A head-to-head reproduction of R-KV -- the current SOTA eviction method -- on our setup achieves only 0-32% at comparable budgets. A system prototype with real GPU-CPU data movement shows that the price of this preservation is modest -- 5-7% transfer overhead -- and scaling analysis projects 2-48 GB HBM savings at production batch sizes.

---

## 论文详细总结（自动生成）

### 论文总结：Not All Thoughts Need HBM: Semantics-Aware Memory Hierarchy for LLM Reasoning

#### 1. 核心问题与整体含义（研究动机和背景）
推理型大语言模型（如 OpenAI o1, DeepSeek-R1）在解决复杂问题时会生成极长的“思维链”（Chain-of-Thought, CoT），导致其 **KV Cache（键值缓存）** 迅速膨胀，严重挤占昂贵且稀缺的 GPU 显存（HBM）。
*   **研究动机**：现有的 KV Cache 管理方法（如 H2O、StreamingLLM）主要采用“永久剔除”策略，即直接删除不重要的 Token。然而，推理任务具有高度的非平稳性，早期的计算结果可能在数百个 Token 后被重新引用。
*   **核心发现**：论文指出推理任务存在“悬崖效应”——当剔除 50% 的缓存时，模型准确率会从正常水平骤降至近乎 0%。这说明推理 Token “虽冷但不可丢弃”。

#### 2. 方法论：语义感知内存层级
论文提出了一种四层内存管理体系，旨在将 HBM 容量与推理质量解耦：
*   **核心思想**：不再做“保留或丢弃”的二选一，而是根据 Token 的重要性将其动态调度到不同的存储介质中。
*   **四层架构**：
    1.  **T0 (HBM)**：存放高重要性 Token、Prompt 和滑动窗口内的近期 Token，确保极速访问。
    2.  **T1 (DDR)**：存放中等重要性 Token，以全精度卸载至 CPU 内存。在注意力计算前通过 PCIe 预取回 GPU。
    3.  **T2 (Compressed)**：存放低重要性 Token，尝试以低精度（如 8-bit）存储在 CPU。
    4.  **T3 (Evicted)**：仅永久剔除极少数（如 3%-5%）重要性最低的尾部 Token。
*   **关键技术细节**：
    *   **累积注意力评分**：通过跨层、跨头的累积注意力权重来衡量 Token 的全局重要性，而非仅看局部窗口。
    *   **零近似误差卸载（Zero-Approximation-Error Offloading）**：由于 T1 层的 Token 在计算前被完整取回，它们对注意力结果的贡献与一直留在 HBM 中完全一致。
    *   **异步差异化预取**：利用 PCIe 带宽，仅传输层级变动的部分，将传输开销控制在极低水平。

#### 3. 实验设计
*   **数据集/场景**：涵盖了数学推理（GSM8K, MATH-500, MATH Level-5）和科学推理（ARC-Challenge）。
*   **模型规模**：DeepSeek-R1-Distill-Qwen 系列（7B, 14B, 32B）。
*   **对比基准（Benchmarks）**：
    *   **Full Cache**：无压缩的全量缓存（性能上限）。
    *   **Streaming Eviction**：基于滑动窗口的剔除。
    *   **H2O**：基于 Heavy-Hitter 的剔除算法。
    *   **R-KV**：当前 SOTA 的推理感知剔除方法（论文进行了 1:1 复现对比）。

#### 4. 资源与算力
论文在多种硬件环境下进行了验证：
*   **算法实验**：使用 NVIDIA RTX 6000 Ada (48GB) 和 A100 (40GB)。
*   **系统原型**：在 NVIDIA RTX 5080 (16GB, PCIe Gen5) 上测试了真实的 GPU-CPU 数据移动开销。
*   **量化支持**：部分实验使用了 int4 (NF4) 和 int8 量化。
*   **训练时长**：该方法属于推理优化，不涉及模型重训。

#### 5. 实验数量与充分性
实验设计非常详尽且具有说服力：
*   **3x3 矩阵实验**：交叉验证了三种 HBM 占比（30%, 50%, 70%）与三种剔除率（3%, 5%, 10%），证明了准确率仅与剔除率相关。
*   **跨规模验证**：从 7B 到 32B 模型均表现出一致的规律。
*   **消融实验**：对比了 4 种不同的重要性评分方法（注意力、梯度、冗余度等）以及窗口大小、管理间隔等超参数。
*   **统计显著性**：使用了 Fisher 精确检验（Fisher’s exact test）并标注了 95% 置信区间，确保结论的客观性。

#### 6. 主要结论与发现
*   **HBM 独立性**：推理准确率取决于“永久丢弃了多少”，而非“HBM 里留了多少”。只要不彻底删除，存放在 DDR 中并预取回来的 Token 不会造成精度损失。
*   **性能表现**：在仅剔除 3% Token 的情况下，该方法在 GSM8K 上保留了 91% 的准确率；在 14B 模型上，仅用 50% 的 HBM 即可达到全量缓存的性能。
*   **系统开销**：真实的 PCIe 传输仅增加 5%-7% 的推理延迟，但在生产环境下可节省 2-48 GB 的显存。

#### 7. 优点
*   **理论严谨**：通过“零近似误差”公式形式化地解释了为何卸载优于剔除。
*   **实用性强**：不改变模型权重，不依赖复杂的重训，且能显著降低长文本推理的硬件门槛。
*   **系统级优化**：不仅有算法模拟，还提供了真实硬件上的原型验证，证明了 PCIe 带宽足以支撑这种层级调度。

#### 8. 不足与局限
*   **量化层级（T2）失效**：实验发现推理 Token 对精度极度敏感，8-bit 量化会导致准确率大幅下降，这意味着目前的压缩层级尚不成熟。
*   **架构覆盖**：主要基于 Qwen 架构的 DeepSeek 模型，尚未在 Llama 或 Mistral 等其他主流架构上进行大规模验证。
*   **批处理复杂性**：虽然预测了多 Batch 下的节省量，但在极高并发下，PCIe 总线可能成为新的瓶颈，需更复杂的调度策略。

（完）
