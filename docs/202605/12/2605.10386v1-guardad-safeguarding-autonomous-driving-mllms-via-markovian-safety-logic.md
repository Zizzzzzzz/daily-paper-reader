---
title: "GuardAD: Safeguarding Autonomous Driving MLLMs via Markovian Safety Logic"
title_zh: GuardAD：通过马尔可夫安全逻辑保护自动驾驶多模态大语言模型
authors: "Tianyuan Zhang, Peng Yue, Zihao Peng, Jiangfan Liu, Zonghao Ying, Jiakai Wang, Tianlin Li, Jian Yang, Yaodong Yang, Aishan Liu, Xianglong Liu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10386v1"
tags: ["query:mm-trf"]
score: 9.0
evidence: 保障自动驾驶多模态大模型在交通交互中的安全
tldr: 针对多模态大模型（MLLM）在自动驾驶中面临的动态安全威胁，本文提出GuardAD框架。该方法将安全建模为演化的马尔可夫逻辑状态，通过神经符号逻辑形式化和高阶逻辑归纳，实现对潜在危险的持续推理。GuardAD不仅能识别风险，还能在不修改原模型的情况下引导动作修正，显著降低了事故率并提升了驾驶性能，在仿真和实车测试中均表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10386v1/fig-001.webp\", \"caption\": \"Figure 1: Illustration of GUARDAD, a safeguard that captures the dynamic nature of autonomous driving and mitigates accident-prone decisions.\", \"page\": 1, \"index\": 1, \"width\": 450, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.10386v1/fig-002.webp\", \"caption\": \"Table 2: Main results on the DriveLM benchmark for different AD-MLLMs, comparing the vanilla model with safeguard methods (RF, CM, SA, GA). We report the standard deviation (σ) and the change (∆). The GA (ours) columns are shaded in gray. Improvements are highlighted in red.\", \"page\": 7, \"index\": 2, \"width\": 946, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2605.10386v1/fig-003.webp\", \"caption\": \"Figure 2: Framework of GUARDAD. GUARDAD is a model-agnostic safeguard that formulates AD safety as an evolving Markovian logical state. It constructs the safety state via Neuro-Symbolic Logic Formalization, induces a temporally consistent state through n-th order Markovian Logic Induction, and then performs Logic-Driven Action Revision to correct unsafe actions without modifying the AD-MLLMs.\", \"page\": 4, \"index\": 3, \"width\": 926, \"height\": 458}]"
motivation: 现有的自动驾驶大模型安全机制多依赖静态约束，缺乏对动态交通环境中演化交互的时序安全推理能力。
method: 提出一种模型无关的防护框架，利用神经符号逻辑形式化和马尔可夫逻辑归纳推理潜在危险，并通过逻辑驱动的动作修正机制优化决策。
result: "实验显示GuardAD将事故率降低了32.07%，同时任务性能提升了6.85%，并在闭环仿真和实车研究中验证了其有效性。"
conclusion: GuardAD通过引入动态逻辑推理，为增强自动驾驶大模型的安全性和鲁棒性提供了一种高效且通用的神经符号解决方案。
---

## 摘要
多模态大语言模型（MLLMs）正越来越多地集成到自动驾驶（AD）系统中；然而，它们在面对各种安全威胁时仍然十分脆弱，特别是在事故多发场景下。最近的保护机制通过引入逻辑约束展现出了潜力，但大多数机制依赖于静态公式，缺乏对不断演变的交通交互进行基于时间的安全性推理，导致其在动态驾驶环境中的鲁棒性有限。为了解决这些局限性，我们提出了 GuardAD，这是一种与模型无关的保护机制，它将自动驾驶安全性建模为一种演化的马尔可夫逻辑状态。GuardAD 引入了神经符号逻辑形式化（Neuro-Symbolic Logic Formalization），该方法表示了针对异构交通参与者的安全谓词，并通过 n 阶马尔可夫逻辑归纳（n-th order Markovian Logic Induction）对其进行持续推导。这种设计使得能够推断出超出单步观测范围的突发和潜在危险。GuardAD 并非简单地否决不安全动作，而是执行逻辑驱动的动作修正（Logic-Driven Action Revision），其中推断出的安全状态在不修改底层 MLLM 的情况下主动引导动作优化。在多个基准测试和自动驾驶 MLLM 上的广泛实验表明，GuardAD 显著降低了事故率（-32.07%），同时略微提升了任务性能（+6.85%）。此外，闭环仿真评估以及物理世界车辆研究进一步验证了 GuardAD 的有效性和潜力。

## Abstract
Multimodal large language models (MLLMs) are increasingly integrated into autonomous driving (AD) systems; however, they remain vulnerable to diverse safety threats, particularly in accident-prone scenarios. Recent safeguard mechanisms have shown promise by incorporating logical constraints, yet most rely on static formulations that lack temporally grounded safety reasoning over evolving traffic interactions, resulting in limited robustness in dynamic driving environments. To address these limitations, we propose GuardAD, a model-agnostic safeguard that formulates AD safety as an evolving Markovian logical state. GuardAD introduces Neuro-Symbolic Logic Formalization, which represents safety predicates over heterogeneous traffic participants and continuously induces them via n-th order Markovian Logic Induction. This design enables the inference of emerging and latent hazards beyond single-step observations. Rather than simply vetoing unsafe actions, GuardAD performs Logic-Driven Action Revision, where inferred safety states actively guide action refinement without modifying the underlying MLLM. Extensive experiments on multiple benchmarks and AD-MLLMs demonstrate that GuardAD substantially reduces accident rates (-32.07%) while slightly improving task performance (+6.85%). Moreover, closed-loop simulation evaluations, together with physical-world vehicle studies, further validate the effectiveness and potential of GuardAD.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GuardAD** 的新型自动驾驶安全框架，旨在通过马尔可夫逻辑推理增强多模态大语言模型（MLLMs）在动态交通环境中的安全性。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：多模态大模型（AD-MLLMs）在自动驾驶的感知、预测和规划中展现出强大潜力，但在面对事故多发场景时仍存在安全隐患。
*   **核心问题**：现有的逻辑驱动保护机制大多依赖于**静态约束**（如“红灯停”），缺乏对动态演化的交通交互进行**时序安全推理**的能力。这种局限性导致模型难以识别那些由多个时间步累积而成的潜在危险（如突然横穿的行人或快速接近的车辆）。

### 2. 方法论：GuardAD 框架
GuardAD 是一种模型无关（Model-agnostic）的保护机制，其核心思想是将自动驾驶安全建模为**演化的马尔可夫逻辑状态**。
*   **神经符号逻辑形式化（Neuro-Symbolic Logic Formalization）**：
    *   将神经感知的输出（如车辆、行人位置）提升为符号化的**安全谓词**。
    *   谓词分为四类：动作谓词、环境谓词、目标存在谓词和目标运动谓词。
    *   通过 Horn 子句形式的规则模板，将当前观测转化为瞬时逻辑状态 $Z_t$。
*   **n 阶马尔可夫逻辑归纳（n-th order Markovian Logic Induction）**：
    *   利用 n 阶马尔可夫逻辑网络（nMLN）聚合过去 $n$ 个时间步的逻辑证据。
    *   通过时序规则推断出当前帧无法直接观测到的潜在风险，生成时序一致的安全状态 $\hat{Z}_t$。
*   **逻辑驱动的动作修正（Logic-Driven Action Revision）**：
    *   **违规检测**：检查 AD-MLLM 提出的原始动作是否违反了 $\hat{Z}_t$ 中的约束。
    *   **提示引导修正**：若违规，不直接否决，而是将约束转化为“辅助提示”（Auxiliary Prompt）重新输入模型，引导其自主修正动作，无需修改模型参数。

### 3. 实验设计
*   **数据集与基准（Benchmarks）**：
    *   **VRU-Accident**：专注于事故多发场景，评估模型在极端情况下的安全性。
    *   **DriveLM**：评估模型在通用驾驶任务中的表现。
    *   **闭环仿真**：在 CARLA 0.9.15 环境下进行 Bench2ADVLM 测试。
    *   **物理世界研究**：人类在环（Human-in-the-loop）的实车实验。
*   **对比方法**：
    *   **SafeAuto (SA)**：针对自动驾驶的静态逻辑保护基准。
    *   **RoboFactory (RF)**、**Code-as-Monitor (CM)**：具身智能领域的代表性保护机制。
*   **评估指标**：GPT/Qwen 分数（任务性能）、GPT/Qwen 事故率（安全性）。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **128 核 Intel Xeon 8358 CPU (2.60 GHz)**、**1 TB RAM** 以及 **8 × NVIDIA A800 (80 GB) GPUs** 的服务器上运行。
*   **训练/推理时长**：论文未明确给出总训练时长（因为该方法主要是推理侧的保护），但在效率分析中提到，GuardAD 在 DriveLM 上的平均推理延迟约为 1.77 秒，在 Dolphins 上约为 3.54 秒，仅比原始模型略有增加。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三种主流 AD-MLLMs（DriveLM, Dolphins, EM-VLM4AD）。
    *   每组实验重复 **10 次**取平均值，以确保结果的统计显著性。
    *   进行了详尽的**消融实验**，包括不同模块的作用、不同 Prompt 生成器（GPT-4o, Qwen-3 等）的影响，以及超参数 $n$（马尔可夫阶数）和 $k$（观测历史长度）的敏感性分析。
*   **客观性**：通过引入对抗攻击（CoA, ADvLM, CAD）测试了框架的防御鲁棒性，并结合了仿真与实车两种环境，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **安全性显著提升**：在 VRU-Accident 基准上，GuardAD 将平均事故率降低了 **32.07%**。
*   **性能不降反升**：在减少事故的同时，任务性能（GPT Score）提升了 **6.85%**，证明了逻辑引导有助于模型更好地理解场景。
*   **闭环与实车验证**：在 CARLA 仿真中路线成功率提升了 **45.60%**；在实车实验中，GuardAD 显著缩小了 AI 与人类驾驶员在应对突发状况时的表现差距。

### 7. 优点与亮点
*   **动态推理能力**：突破了传统静态规则的限制，能够识别具有时序特征的复杂危险。
*   **模型无关性**：作为一种“插件式”保护层，无需重新训练底层大模型，具有极强的通用性。
*   **最小干预原则**：仅在检测到违规时触发修正，保持了模型在安全情况下的原始决策能力。

### 8. 不足与局限
*   **感知依赖性**：系统的有效性高度依赖于前端感知堆栈对实体和谓词提取的准确性，感知错误可能导致保护失效。
*   **计算开销**：虽然延迟在可接受范围内，但动作修正步骤需要二次调用大模型，这在实时性要求极高的自动驾驶场景中仍是一个挑战。
*   **谓词覆盖范围**：目前的谓词集合虽然涵盖了常见场景，但可能仍无法完全覆盖所有开放世界的驾驶意图或复杂的长尾错误。

（完）
