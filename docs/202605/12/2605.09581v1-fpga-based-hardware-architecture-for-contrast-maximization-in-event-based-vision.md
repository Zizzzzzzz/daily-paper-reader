---
title: FPGA-Based Hardware Architecture for Contrast Maximization in Event-Based Vision
title_zh: 基于 FPGA 的事件视觉对比度最大化硬件架构
authors: "Michal Filipkowski, Marcin Kowalczyk, Tomasz Kryjak"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09581v1"
tags: ["query:ev-cam"]
score: 10.0
evidence: 用于事件视觉系统的FPGA硬件
tldr: 本研究针对基于事件视觉中的对比度最大化（CM）算法，提出了一种基于FPGA的硬件架构。利用FPGA的并行性和流水线结构，设计了包含事件扭曲、对比度计算和迭代优化的专用模块。该架构显著提升了运动参数估计的处理速度和能效，是首个实现CM算法硬件加速的方案，为高速、低功耗嵌入式实时运动估计奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09581v1/fig-001.webp\", \"caption\": \"Fig. 3. Hardware architecture diagram of an application for object tracking using CM\", \"page\": 8, \"index\": 1, \"width\": 514, \"height\": 376}]"
motivation: 旨在解决事件视觉中对比度最大化算法在通用处理器上计算开销大、难以满足实时嵌入式应用需求的问题。
method: 利用FPGA的并行处理能力，设计了一种深度流水线的硬件架构，实现了事件扭曲、对比度计算及迭代优化的硬件模块。
result: 实验表明该架构在运动参数估计速度上比CPU和GPU实现快200倍以上，且具有极高的能效比。
conclusion: 该设计成功验证了FPGA在加速事件视觉算法方面的潜力，为实时、低功耗的嵌入式运动估计提供了高效的硬件解决方案。
---

## 摘要
本文提出了一种在现场可编程门阵列（FPGA）资源中实现对比度最大化（CM）算法的硬件架构，用于事件视觉系统。CM 通过最大化从异步事件流重建的扭曲事件图像（IWE）的对比度来估计运动参数。事件视觉传感器产生具有高时间分辨率和低空间冗余的稀疏数据，这使其非常适合硬件处理。利用 FPGA 的确定性、大规模并行结构，设计了一种深度流水线架构，能够进行适用于实时嵌入式应用的高吞吐量、高能效处理。本文详细介绍了负责事件扭曲、对比度计算和迭代优化的硬件模块，讨论了关键的实现决策，并展示了设计中使用的硬件感知优化方法。实验结果表明，与基于 CPU 和 GPU 的实现相比，该架构在速度和效率上有显著提升，运动参数估计的执行速度提高了 200 倍以上。据我们所知，这是首个能够加速 CM 算法计算的硬件架构。其性能从处理速度、能效和硬件资源利用率等方面进行了评估。所提出的设计通过一个基于事件的目标跟踪应用进行了验证。结果证实，该架构为高速、低功耗嵌入式系统中的实时运动估计提供了坚实的基础。

## Abstract
This paper presents a hardware architecture that implements the Contrast Maximization (CM) algorithm in Field-Programmable Gate Array (FPGA) resources for event-based vision systems. CM estimates motion parameters by maximizing the contrast of an Image of Warped Events (IWE) reconstructed from asynchronous event streams. Event-based vision sensors generate sparse data with high temporal resolution and low spatial redundancy, which makes them well suited for hardware processing. The deterministic, massively parallel structure of the FPGA is leveraged to design a deeply pipelined architecture capable of high-throughput, energy-efficient processing suitable for real-time embedded applications. This paper details the hardware modules responsible for event warping, contrast computation, and iterative optimization, discusses key implementation decisions, and presents the hardware-aware optimization method used in the design. Experimental results demonstrate a substantial speed and efficiency improvement over CPU- and GPU-based implementations, with motion parameter estimation executing over 200 times faster. To the best of our knowledge, this is the first hardware architecture enabling acceleration of CM algorithm computations. Its performance is evaluated in terms of processing speed, energy efficiency, and hardware resource utilization. The proposed design is validated using an event-based object tracking application. The results confirm that the architecture provides a solid foundation for real-time motion estimation in high-speed, low-power embedded systems.

---

## 论文详细总结（自动生成）

这篇论文由波兰 AGH 科技大学的研究团队撰写，详细介绍了一种在 FPGA 上实现事件视觉对比度最大化（CM）算法的硬件架构。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：事件相机（DVS）具有高时间分辨率和低延迟，但其产生的异步事件流需要复杂的算法（如对比度最大化 CM）来估计运动参数。CM 算法涉及迭代优化，计算复杂度极高，在传统的 CPU 或 GPU 上难以实现低功耗、超高速的实时处理。
*   **研究动机**：利用 FPGA 的大规模并行性和确定性流水线结构，克服通用处理器在处理稀疏、高频事件数据时的性能瓶颈，为嵌入式视觉系统（如无人机、自动驾驶）提供高效的运动估计方案。

### 2. 核心方法论
*   **核心思想**：将 CM 算法的全流程（事件预处理、扭曲、对比度计算、梯度下降优化）映射到 FPGA 硬件电路中，通过深度流水线化实现高吞吐量。
*   **关键技术细节**：
    *   **事件扭曲（Event Warping）**：根据当前的运动参数 $(v_x, v_y)$，将每个事件的时间戳偏移映射到参考时间点，计算其在图像平面上的新坐标。
    *   **双线性投票（Bilinear Voting）**：为了提高精度，将扭曲后的事件按距离分配给相邻的 4 个像素。硬件上通过 12 个独立的 BRAM 存储体（Bank）实现并行写入，解决了内存访问冲突。
    *   **对比度与梯度计算**：计算扭曲事件图像（IWE）的方差作为目标函数。利用链式法则推导梯度，通过硬件模块同步计算 IWE 及其对速度的偏导数图像。
    *   **迭代优化循环**：采用梯度上升法更新运动参数。设计了专门的逻辑在每次迭代后自动重置 BRAM，确保流水线连续运行。
    *   **ROI 更新**：以目标跟踪为应用案例，根据估计的运动参数动态更新感兴趣区域（ROI）。

### 3. 实验设计
*   **数据集/场景**：使用了标准的 **DAVIS 240C 数据集**（由 Mueggler 等人提供），包含真实的事件相机数据。
*   **Benchmark（基准）**：
    *   **软件实现**：基于 PyTorch 的 Python 参考模型。
    *   **硬件对比**：Intel Core i5-11300H CPU (3.10 GHz) 和 Nvidia GeForce RTX 3050 Ti GPU。
    *   **文献对比**：与现有的实时 CPU 实现（如 Kim 等人的工作）进行了处理耗时的对比。

### 4. 资源与算力
*   **硬件平台**：**Kria KV260 Vision AI Starter Kit**（搭载 Zynq UltraScale+ MPSoC）。
*   **算力与资源占用**：
    *   **时钟频率**：210 MHz。
    *   **逻辑资源**：LUT 占用 3.12% (3655)，寄存器占用 3.35% (7857)。
    *   **专用资源**：DSP 占用 12.1% (151)，BRAM 占用 17.7% (25.5个单元)。
    *   **功耗**：片上总功耗仅为 **0.813 W**。
*   **训练/运行耗时**：处理一批 5000 个事件（100 次迭代）仅需 **0.92 ms**。

### 5. 实验数量与充分性
*   **实验规模**：论文通过目标跟踪应用验证了架构的有效性，对比了 FPGA、CPU 和 GPU 在相同参数（5000 事件、100 次迭代、64x64 ROI）下的执行时间。
*   **充分性评价**：实验在功能验证和性能对比上较为充分，客观展示了硬件加速的量级优势。但由于是首个 FPGA 实现，缺乏与其他硬件加速器（如 ASIC 或其他 FPGA 方案）的横向对比。

### 6. 主要结论与发现
*   **性能飞跃**：FPGA 实现的处理速度比 CPU 快约 **200 倍**，比 GPU 快约 **450 倍**（注：GPU 在处理此类小规模、高迭代任务时因数据传输开销表现不佳）。
*   **实时性**：该架构能够在 1 毫秒内完成一次完整的运动参数估计，完全满足极高速视觉任务的需求。
*   **低功耗优势**：在维持高性能的同时，功耗控制在 1W 以下，极具嵌入式应用前景。

### 7. 优点（亮点）
*   **首创性**：据作者所述，这是**首个**实现 CM 算法硬件加速的 FPGA 架构。
*   **高效的内存管理**：通过多 Bank 划分和寄存器缓冲技术，巧妙解决了双线性投票中的写冲突问题，实现了单时钟周期的并行累加。
*   **硬件感知优化**：算法设计充分考虑了 FPGA 的特性，如利用 BRAM 的快速重置逻辑和深度流水线。

### 8. 不足与局限
*   **分辨率限制**：受限于片上 BRAM 容量，目前支持的 ROI 较小（如 64x64）。若要支持全分辨率（如 240x180 或更高），需要更高端的 FPGA 或引入外部 DDR 存储，这会增加延迟。
*   **运动模型单一**：目前仅实现了 2D 恒速平移模型，尚未扩展到更复杂的旋转、缩放或 6 自由度运动模型。
*   **优化算法简单**：采用的是基础的梯度上升法，对于复杂的非凸优化问题，可能容易陷入局部最优。

（完）
