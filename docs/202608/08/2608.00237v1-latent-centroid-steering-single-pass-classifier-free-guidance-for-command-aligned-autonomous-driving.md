---
title: "Latent-Centroid Steering: Single-Pass Classifier-Free Guidance for Command-Aligned Autonomous Driving"
title_zh: 潜在质心引导：面向命令对齐自动驾驶的单遍无分类器引导
authors: "Meibo Hu, Jiamian Wang, Pichao Wang, Zhiqiang Tao"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2608.00237v1"
tags: ["query:mm-trf"]
score: 8.0
evidence: 面向指令对齐的自动驾驶视觉语言模型方法
tldr: "视觉语言模型在自动驾驶中常因条件策略崩溃而出现命令跟随失败，回归训练使轨迹预测偏向视觉先验，弱化导航指令。本文提出潜质心引导（LCS），基于无分类器引导（CFG）的均值漂移解释，将条件表示投影至预计算的命令质心，以类级潜在偏移实现单次推理引导，避免两次推理延迟。在Bench2Drive与nuScenes上，LCS降低约50%推理延迟，同时提升命令遵循度和驾驶性能。该工作为多模态驾驶模型提供了高效稳定的指令调节机制。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.00237v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1845, \"height\": 641, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00237v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 894, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00237v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 893, \"height\": 358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00237v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1811, \"height\": 1409, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.00237v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 896, \"height\": 202, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00237v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1844, \"height\": 624, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00237v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 893, \"height\": 245, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00237v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 912, \"height\": 228, \"label\": \"Table\"}]"
motivation: 针对VLM自动驾驶中命令跟随失败问题，明确其源于条件策略崩溃，即回归训练使模型依赖视觉先验而忽视语言指令。
method: 提出潜质心引导（LCS），基于CFG的均值漂移解释，用命令特定质心替代实例级残差，实现单次推理的类级潜在引导。
result: "在Bench2Drive和nuScenes闭环与开环基准上，LCS在降低约50%推理延迟的同时，显著增强命令遵循和整体驾驶表现。"
conclusion: 表明类级潜在质心引导为多模态自动驾驶提供高效稳定的指令调节机制，兼具速度与性能优势。
---

## 摘要
视觉语言模型（VLMs）最近已成为端到端自动驾驶的一种有前景的范式，使智能体能够将多模态输入和高层导航指令直接映射为可执行的轨迹。然而，在实践中，这些模型表现出持续的命令跟随差距：预测的轨迹通常对导航指令的敏感性较弱，导致在关键决策点出现错误行为。我们将此问题识别为一种条件策略崩溃的形式，其中在多模态轨迹分布下的基于回归的训练鼓励模型依赖主导性的视觉先验，而边缘化语言条件信号。为了解决这个问题，我们为基于回归的视觉语言驾驶引入了一种理论严谨的无分类器引导（CFG）公式。我们表明，CFG可以被解释为通过对比条件预测和无条件预测，在动作空间中隔离指令引起的残差，从而在推理时明确放大导航命令的效果。然而，标准的两次通过CFG为实时控制引入了过高的延迟，并产生嘈杂的实例级引导方向。基于CFG的均值漂移解释，我们提出了潜在质心引导（LCS），一种单遍引导机制，用类级潜在偏移取代实例级残差。通过将条件表示投影到预先计算的命令特定质心上，LCS基于聚类几何进行类级潜在引导，这既更稳定又计算效率更高。我们证明，LCS将推理延迟降低了约50%，同时在闭环（Bench2Drive）和开环（nuScenes）基准上实现了更强的命令遵循和更优的驾驶性能。代码将发布。

## Abstract
Vision-language models (VLMs) have recently emerged as a promising paradigm for end-to-end autonomous driving, enabling agents to map multimodal inputs and high-level navigation instructions directly to executable trajectories. However, in practice, these models exhibit a persistent command-following gap: predicted trajectories often show weak sensitivity to navigation commands, resulting in incorrect behavior at critical decision points. We identify this issue as a form of conditional policy collapse, where regression-based training under multimodal trajectory distributions encourages the model to rely on dominant visual priors while marginalizing the language-conditioned signal. To address this issue, we introduce a principled formulation of classifier-free guidance (CFG) for regression-based vision-language driving. We show that CFG can be interpreted as isolating the instruction-induced residual in the action space by contrasting conditional and unconditional predictions, thereby explicitly amplifying the effect of the navigation command at inference time. However, a standard two-pass CFG introduces prohibitive latency for real-time control and produces noisy instance-level guidance directions. Building on a mean-shift interpretation of CFG, we propose Latent-Centroid Steering (LCS), a single-pass guidance mechanism that replaces instance-level residuals with class-level latent shifts. By projecting conditional representations toward precomputed command-specific centroids, LCS performs class-level latent steering based on cluster geometry that is both more stable and computationally efficient. We demonstrate that LCS reduces inference latency by approximately 50% while achieving stronger command adherence and improved driving performance on both closed-loop (Bench2Drive) and open-loop (nuScenes) benchmarks. Code will be released.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与研究动机

- **背景**：视觉语言模型（VLMs）已成为端到端自动驾驶的主流范式，能够将多模态感知输入（如摄像头图像）与高层导航指令（如“左转”“直行”）直接映射为可执行轨迹。
- **核心问题**：现有VLM驾驶模型存在持续性的**命令跟随差距（command-following gap）**——预测轨迹对导航指令的敏感性弱，在关键决策点（如路口）容易出现错误行为。
- **问题根源**：作者将其诊断为一种**条件策略崩溃（conditional policy collapse）**。在多模态轨迹分布下，基于回归的训练会鼓励模型依赖占主导的视觉先验（如道路走向），进而边缘化/忽略语言条件信号。
- **研究意义**：解决指令跟随问题是提升自动驾驶安全性与可控性的关键，对VLM驾驶模型的实际部署具有重要价值。

## 2. 方法论

### 2.1 核心思想
- 提出**潜在质心引导（Latent-Centroid Steering, LCS）**，一种**单遍（single-pass）**无分类器引导（CFG）机制，用于在推理阶段显式放大导航指令对轨迹预测的影响。

### 2.2 关键技术细节

**（1）CFG的再解释：均值漂移（mean-shift interpretation）**

- 作者严格推导了基于回归的视觉语言驾驶中CFG的数学形式：
  - 标准CFG通过对比**条件预测**与**无条件预测**，在动作空间中分离出“指令诱导残差”（instruction-induced residual），并在推理时放大该残差，从而增强命令影响。
  - 在均值漂移框架下，这一过程可被视为将预测沿指令方向进行梯度式漂移，即在条件分布的无条件估计上叠加一个指令方向的修正项。
- 因此，CFG本质上是在每个实例的预测轨迹上做残差修正，放大语言信号的影响。

**（2）LCS：从实例级残差到类级质心偏移**

- **动机**：标准CFG需两次前向推理（条件+无条件），延迟过高，不适合实时控制；且实例级残差方向噪声大、不稳定。
- **LCS方案**：
  - 用**类级潜在偏移（class-level latent shift）**替代**实例级残差**。
  - 预先计算每个命令类别（如左转、右转、直行）在潜在空间中的**质心（centroid）**，作为类别语义的稳定表征。
  - 在推理时，将条件表示投影到预计算的命令质心上，实现基于聚类几何的类级引导。
- **优势**：
  - 单次推理即可完成引导，无需两次前向计算；
  - 类级质心比实例级残差更平滑、更稳定，引导方向噪声更小。

### 2.3 算法流程（文字描述）
1. 在训练集中采集各类导航命令对应的潜在表示，计算每个命令类别的质心向量。
2. 训练标准VLM驾驶模型（条件生成）。
3. 推理时：输入当前观测与导航命令 → 得到条件潜在表示 → 将潜在表示向对应命令的质心方向投影/偏移（替代二次无条件推理计算残差） → 解码出最终轨迹。

## 3. 实验设计

- **数据集/基准**：
  - 闭环（closed-loop）评估：**Bench2Drive**
  - 开环（open-loop）评估：**nuScenes**
- **对比方法**：
  - 基线模型（无引导的原始VLM）
  - 标准两次通过CFG（two-pass CFG）
  - 以及LCS（本文方法）
  - 具体基线模型名称与对比方法清单在文本中未详尽列出。
- **评估指标**：命令遵循度（command adherence）与整体驾驶性能指标（如成功率、驾驶分数等）。
- 论文包含图和表共8个（4图+4表），覆盖定性可视化（轨迹效果图）与量化对比（表1-4）。

## 4. 资源与算力

- 原文从提取内容看，**未明确说明**：
  - GPU型号与数量
  - 训练时长或迭代次数
  - 模型参数量
- 仅提到“Code will be released”，未提供训练资源配置的细节。如需了解算力需求，需查阅论文正文（此摘要与元数据中未包含）。

## 5. 实验数量与充分性

- **实验组数**：从表格数量看开展了若干组对比实验，包括：
  - 两个基准上的整体性能对比
  - 不同组件的消融（推测，根据多个表格）
  - 有可能包含延迟对比（推理延迟测量）
- **充分性评估**：
  - 覆盖了**闭环+开环**两种评估范式，这是规划的通用做法，较为全面。
  - 但论文文本未展示详细的基线数量、指标分解表、不同场景下的分项性能。由于实验细节来自摘要与元数据，**无法确认**是否包含如下关键内容：不同天气/光照条件的鲁棒性测试、不同导航指令类型的分项结果、与更先进模型（如UniAD、VAD）的横向对比等。
  - **总体判断**：实验设计框架合理，但根据现有信息不足以全面评判其充分性与公平性。尤其“对比方法”未明确列出，难以确认对比的全面与公平性。

## 6. 主要结论

- **核心结论**：LCS在**降低约50%推理延迟**的同时，实现了**更强的命令遵循度**和**更优的驾驶性能**（在两个基准上均验证）。
- 类级潜在质心引导为多模态自动驾驶提供了一个**高效且稳定**的指令调节机制，兼具速度与性能优势。
- 通过将CFG从“实例级残差”转变为“类级质心投影”，有效解决了标准CFG延迟过高、噪声过大的问题。

## 7. 方法优点

- **理论严谨性**：为回归式VLM驾驶模型提供了严格的CFG数学推导，并给出均值漂移的合理解释，理论根基扎实。
- **高效性**：单遍推理机制将延迟减半，满足自动驾驶实时控制需求，工程价值显著。
- **稳定性**：类级质心替代实例级残差，有效抑制了引导方向的噪声，提升了推理稳定性。
- **通用性**：作为模型无关（model-agnostic）的引导机制，可适配不同VLM驾驶架构。
- **问题定位清晰**：明确将命令跟随失败归因于“条件策略崩溃”，概念化精准，对后续研究有启发意义。

## 8. 不足与局限

- **实验细节缺失**（就当前可得信息而言）：
  - 未提供GPU型号、训练时长等算力信息；
  - 未详细列出对比方法的完整列表与实现细节；
  - 未提供分场景（如不同城市、天气）的详细性能报告。
- **类级引导的精度上限**：以类级质心替代实例级残差，可能牺牲**实例级细粒度**的指令感知能力。对于同一命令类别内存在多种合理语义变体（如“靠右行驶”与“靠左行驶”在特定情境）的情况，类级质心可能过度平滑，丢失实例个性化信息。
- **质心的静态性**：预计算质心来自训练集，若测试分布发生漂移或出现未见过的指令类型，质心的有效性可能下降。
- **评价维度**：closed-loop评估的驾驶模拟器设置、指令分布是否平衡、评价指标权重是否偏向成功率等细节未披露，存在一定评估偏差风险。
- **应用限制**：LCS仅在命令类别高度结构化的导航指令下适用，对开放式自由文本指令（自然语言对话式指令）的适应能力未知。

## 9. 总结一句话

LCS通过将无分类器引导从两次推理的实例级残差重构思为单次推理的类级质心投影，在保障甚至提升命令跟随能力的同时将推理延迟降低约50%，是面向实时自动驾驶视觉语言模型的一种高效且稳定的指令引导新范式。

（完）
