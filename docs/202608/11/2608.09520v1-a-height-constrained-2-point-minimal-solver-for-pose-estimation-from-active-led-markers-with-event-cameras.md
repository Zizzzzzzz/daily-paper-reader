---
title: A Height-Constrained 2-Point Minimal Solver for Pose Estimation from Active LED Markers with Event Cameras
title_zh: 一种用于事件相机主动LED标记姿态估计的高度约束2点最小求解器
authors: "Runze Yuan, Alexander Kappler, Jun Zhang, Kuangyi Chen, Fabio Morbidi, Pascal Vasseur, Cédric Demonceaux, Friedrich Fraundorfer"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09520v1"
tags: ["query:ev-cam"]
score: 9.0
evidence: 基于事件相机与主动LED标记的位姿估计
tldr: 主动LED标记与事件相机组合可实现低延迟定位，但现有PnP类方法依赖多个标记，空间受限场景部署困难。本文利用IMU/高度计提供的倾斜角与高度信息，仅用两个LED标记即可求解相机位姿，给出闭式解与线性最小二乘解，并分析高度信息对旋转估计无贡献的退化条件。在真实事件相机数据上的实验表明，所提解法精度优于现有P2P求解器，与P3P相当，兼顾了稀疏标记部署与高精度需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.09520v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 897, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1836, \"height\": 1103, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1829, \"height\": 1066, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 892, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09520v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 890, \"height\": 371, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.09520v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 492, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09520v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 479, \"label\": \"Table\"}]"
motivation: 现有基于PnP的位姿求解器需要至少多个LED标记，结构布局在空间受限场景中部署困难，而IMU和高度计等自运动信息未被利用。
method: 结合已知相机倾斜角与高度，仅用两个LED标记构建约束，推导闭式解与线性最小二乘解，并分析退化构型。
result: 在合成与真实数据上，所提方法精度优于现有P2P求解器，与P3P性能相当，验证了仅双标记解算的可行性。
conclusion: 该方法有效利用自运动传感器信息，以最少两个标记实现稳健位姿估计，适合空间受限下的实时定位应用。
---

## 摘要
在许多需要实时定位的自主应用中，基于主动标记的系统因其低延迟和易于部署而优于计算密集型的基于特征的方法。事件相机提供高时间分辨率和最小延迟，通常与主动LED标记一起用于鲁棒的实时定位。现有方法通常依赖透视n点（PnP）求解器进行姿态估计。然而，在空间受限的场景中，结构化标记布局可能难以部署，而部分自运动信息（如重力方向和高度）可从机载传感器轻松获得。我们推导了一种鲁棒且精确的最小求解器，通过结合机载传感器（如IMU或高度计）测量的已知倾斜角和相机高度，仅从两个LED标记估计相机姿态。所提出的公式通过闭式解和线性最小二乘解唯一确定相机姿态。我们进一步分析了退化配置，并表征了高度信息不助于旋转估计的条件。为了评估，我们开发了一个基于事件的主动标记系统，以从运动捕捉系统收集带有真实值的实际数据。在合成和真实数据上的实验表明，与最先进的P2P求解器相比，所提出的方法提高了准确性，并且相对于P3P具有竞争力的性能。

## Abstract
In many autonomous applications requiring real-time localization, active marker-based systems are preferred due to their low latency and ease of deployment compared to computationally demanding feature-based methods. Event~\mbox{cameras} offer high temporal resolution and minimal delay and are commonly used with active LED markers for robust real-time localization. Existing methods typically rely on Perspective-n-Point (PnP) solvers for pose estimation. However, structured marker layouts can be challenging to deploy in space-constrained scenarios, while partial self-motion information (e.g., gravity direction and altitude) is readily available from onboard sensors. We derive a robust and accurate minimal solver that estimates camera pose from only two LED markers by incorporating known tilt angle and camera height measured by an onboard sensor, such as an IMU or an altimeter. The proposed formulation uniquely determines the camera pose through both a closed-form and a linear least-squares solution. We further analyze degenerate configurations and characterize the conditions under which height information does not contribute to rotation estimation. For evaluation, we developed an event-based active marker system to collect real-world data with ground truth from a motion capture system. Experiments on both synthetic and real data demonstrate improved accuracy over the state-of-the-art P2P solver and competitive performance relative to P3P.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

**研究动机与背景：**
- 在GNSS受限场景（如室内、地下隧道）中，快速精确的相机位姿估计至关重要。
- 传统PnP求解器依赖大量3D-2D特征匹配，特征检测与匹配计算开销大，影响实时性能。
- 基于主动标记（如AprilTag、ArUco、主动LED标记）的方法避免了预建地图，只需少量标记即可求解位姿。
- 事件相机具有高时间分辨率、低延迟、高动态范围等优势，特别适合追踪主动闪烁的LED标记。
- 现有方法（如P3P）通常需要至少3个点对应，增加硬件成本与部署复杂度；而现有P2P求解器（如Li等人[5]）产生两个可行解，仍需第三点消歧。

**核心问题：**
如何仅利用**两个LED标记**，结合机载传感器（IMU/高度计）提供的**倾斜角与高度先验**，实现唯一、鲁棒且精确的相机位姿估计，从而减少标记数量、降低部署复杂度？

**整体含义：**
本文通过引入高度先验，将P2P问题的自由度进一步约束，推导出无需额外点即可获得唯一解的求解器，在稀疏标记部署条件下实现与P3P相当、优于现有P2P的精度表现。

---

## 2. 论文提出的方法论

### 核心思想
利用已知的**垂直方向（重力方向）** 与**相机高度**作为先验信息，将原本需要6自由度估计的问题简化为仅求解**偏航角**以及**平面平移量**，从而仅用两个标记即可唯一确定相机位姿。

### 关键技术细节

**问题建模：**
- 相机内参已知（标定后的透视模型），图像测量点表示为归一化射线方向向量 `b_i`。
- 旋转矩阵参数化为 `R_bw = Rx · Ry · Rz`（欧拉角），其中 `Rx·Ry`（倾斜分量）由IMU提供的垂直方向闭合求解。
- 已知相机高度h后，平移t′的z分量为常数（`-h + t∆z`），未知数降为 `[θ, t′x, t′y]`，即偏航角与两个水平平移分量。

**约束方程：**
- 基于点-射线共线性约束，推导出线性系统 `Ax = b`（4个独立方程对应2个点）。
- 未知向量 `x = [cos θ, sin θ, t′x, t′y]^T`，结合三角函数恒等式 `cos²θ + sin²θ = 1` 构成5个约束对3个未知数的超定系统。

**两种解法：**
1. **闭式解（Closed-Form Solution）**：
   - 通过消去水平平移项，得到仅含θ的两个独立二次方程（采用 `s = tan(θ/2)` 代换）。
   - 通过判别式选优准则选择数值更稳定的方程求解，剩余方程用于消除二义性。
   - 优点：物理含义清晰，不需迭代，旋转估计独立于平移。
2. **线性最小二乘解（Linear Least-Squares Solution）**：
   - 忽略三角函数约束，直接对超定系统进行SVD求解，将结果投影回SO(3)。
   - 优点：实现简单、数值稳定；退化为特殊场景时分析更直观。

**理论退化分析：**
- **空间配置退化**：两点水平坐标相同（仅高度不同）时矩阵A奇异；两点同高时，高度信息在LS解中变为比例因子、退化为[5]的方法（但闭式解仍保留高度信息的影响）。
- **相机运动退化**：相机与某点同高时，对应约束行归零；极端情况下系统仅有平凡解。

---

## 3. 实验设计：数据集 / 场景 / Benchmark / 对比方法

### 合成数据实验
- **场景1 — 像素噪声敏感性分析**：在半径10m的球面上采样1000个位姿，添加0~5像素零均值高斯噪声，评估中位平移/旋转误差。
- **场景2 — 目标距离影响分析**：固定噪声σ=2px，在1m~30m半径球面上采样1000个位姿，评估不同距离下的误差。
- **退化配置实验**：
  - 两标记同高（均为3m），相机高度从0~10m变化；
  - 两标记不同高（3m与4m），相机高度同范围变化。

### 真实数据实验
- **硬件平台**：Prophesee EVK4 HD事件相机（IMX636ES传感器，1280×720）+ Soyo SFA0820-5M镜头，三颗高功率LED（频率分别为1000/1150/850 Hz），ESP32微控制器驱动，OptiTrack运动捕捉系统提供真值。
- **数据采集**：4个轨迹序列：
  - Seq. 1、Seq. 2：工作空间内任意运动，验证整体可行性；
  - Seq. 3：近距离（1.25~2.75m）运动；
  - Seq. 4：远距离（3.4~4.6m）运动。
- **标记检测流程**：20ms时间窗累积事件 → DBSCAN空间聚类 → 时间域频率估计 → 取平均像素坐标作为观测点。

### 对比方法
- **P3P**：Kneip等人[6]（需要3点，多解时选取真值误差最小解）；
- **P2P (Li)**：Li等人[5]（需要已知旋转轴，2点但产生多解）；
- **P2P (Closed-Form)**：本文闭式解；
- **P2P (LS)**：本文线性最小二乘解。

---

## 4. 资源与算力

**论文未明确提及所使用的GPU型号、数量、训练时长等计算资源信息。** 该工作主要涉及求解器数学推导与数值实验，估计仅需普通CPU即可完成所有实验（合成数据采样、SVD求解、DBSCAN聚类等均非高算力需求任务）。文中也未报告具体运行时间或复杂度基准。

---

## 5. 实验数量与充分性

### 实验数量统计
- **合成实验**：3组主要评估（噪声、距离、退化配置，每组1000个位姿样本），其中退化配置含2个子场景（同高/异高），共约5组合成场景；
- **无消融实验**：论文未对高度先验误差敏感性、不同标记间距等参数进行消融分析；
- **真实实验**：4个轨迹序列，覆盖任意运动、近距离、远距离场景。

### 充分性与客观性评估
- **优点**：实验设计较为系统，覆盖噪声等级、目标距离、退化构型三个关键维度；真实数据由运动捕捉系统提供真值，评估基准可靠；对比了当前最先进的P2P与P3P基线。
- **不足**：
  - 未报告单一方法的计算耗时（如闭式解 vs LS的实时性对比）；
  - 合成实验未对高度误差、倾斜角误差进行敏感性分析；
  - 退化分析实验未包含矩阵A奇异（两点水平坐标重合）这一情形的定量评估；
  - 真实实验中P3P与P2P(Li)均选取“最接近真值的解”，这在真实应用中并不现实（实际无法知道哪个解是最优的），存在一定“作弊”倾向，反而削弱了对比公平性中对基线的偏袒预估。

---

## 6. 主要结论与发现

1. **高度先验有效提升平移估计精度**：在较大像素噪声和远距离条件下，本文方法在平移误差上显著优于P2P[5]和P3P[6]。
2. **旋转估计鲁棒性优于P3P**：P2P类方法在旋转估计上对图像噪声和观测距离均表现出比P3P更强的鲁棒性；但高度先验对旋转精度的提升作用有限，与[5]表现相近。
3. **唯一解优势显著**：与[5]、[6]的多解特性不同，本文方法天然提供唯一解，无需额外点消歧，降低硬件复杂度。
4. **退化配置理论验证成立**：当相机与标记同高时求解器数值不稳定，与理论分析一致；此外发现[5]在特定配置下还存在未在原文中讨论的退化现象。
5. **闭式解 vs LS**：两者平移精度几乎一致，但LS在旋转估计上数值稳定性更好（标准差更低）。

---

## 7. 优点

### 方法论亮点
- **最小标记数（2个）实现唯一解**：通过巧妙引入高度先验，避免了传统P2P的多解问题，显著降低系统硬件复杂度。
- **双解策略（闭式 + LS）**：提供互补方案，兼顾物理可解释性与数值稳定性。
- **详尽的退化分析**：从标记空间配置与相机运动两个视角系统分析退化条件，为实际部署提供了明确的设计指导（如避免同高配置、保持水平间距等）。
- **理论推导严谨**：线性化推导过程清晰，与[5]的等价性关系分析透彻。

### 实验设计亮点
- **合成 + 真实双验证**：合成实验覆盖噪声、距离、退化三大维度；真实实验使用自建的事件主动标记系统，包含OptiTrack真值基准。
- **检测流程针对事件数据特点**：DBSCAN空间聚类 + 频率时间聚类，充分利用事件相机异步特性，无需传统图像处理。

---

## 8. 不足与局限

### 实验覆盖不足
- **未测试高度/倾斜角噪声的影响**：实际IMU/高度计测量存在噪声，论文仅对像素噪声和距离进行了敏感性分析；
- **缺乏多场景泛化**：真实实验只在单一室内环境进行，未验证不同光照条件、更复杂的背景干扰或剧烈运动下的性能；
- **未报告计算开销/实时性指标**：未与P3P、P2P(Li)进行运行时间对比，无法评估实时应用中的计算效率。

### 对比公平性偏差
- **基线多解选择策略过于理想化**：真实实验中对P3P和P2P(Li)选用“真值误差最小的解”进行对比，实际操作中无法预知哪个解最优，可能导致基线性能被高估，对比结果倾向保守（这也侧面说明本文的优越性可能更强）。

### 应用限制
- **依赖稳定的高度测量**：在剧烈机动（如无人机快速爬升/下降）或复杂电磁环境中，高度计/气压计精度受限，可能影响方法稳定性；
- **退化条件在实际场景中可能难以完全避免**：当机器人工作于平面地形且标记安装高度相近时，容易接近退化区域；
- **仅适用于已知垂直方向的场景**：不能直接推广到任意姿态或无IMU的纯视觉系统；
- **标记数量虽降到2，仍需LED驱动电路与频率控制**：整体硬件复杂度虽低于三标记方案，但相比无源标记（如AprilTag）仍需额外电源与电路支持。

---

（完）
