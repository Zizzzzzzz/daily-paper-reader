---
title: Just Noticeable Difference Modeling for Token Compression in Vision-Language-Action Models
title_zh: 视觉-语言-动作模型中令牌压缩的恰可察觉差建模
authors: "Zhuoyuan Li, Rui Zhao, Jin Wang, Hanwei Zhu, Cong Zhang, Giuseppe Valenzise, Weisi Lin, Kin-Man Lam"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.21247v1"
tags: ["query:evlm"]
score: 10.0
evidence: 视觉-语言-动作模型中的令牌压缩
tldr: 视觉-语言-动作模型推理开销大，现有token压缩多基于冗余度或注意力等间接线索，未考虑下游动作容差。Action-JND将JND思想扩展到具身感知，以语言条件动作响应定义可察觉差异，并训练轻量估计器预测每个token的最大可容忍扰动。在LIBERO基准上，该方法作为即插即用压缩准则，与OpenVLA及OpenVLA-OFT配合，一致提升压缩可靠性，尤其在高压缩比下。该工作为具身智能中的token压缩提供了接收者相关的容差准则。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.21247v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1866, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1795, \"height\": 856, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1564, \"height\": 736, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 871, \"height\": 978, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1861, \"height\": 1897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 249, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 277, \"height\": 326, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 251, \"height\": 323, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 249, \"height\": 319, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 256, \"height\": 331, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 277, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 278, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 277, \"height\": 348, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.21247v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1887, \"height\": 1160, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.21247v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1887, \"height\": 1161, \"label\": \"Table\"}]"
motivation: 现有token压缩依赖冗余度或注意力等间接线索，未直接衡量token变化对下游动作可接受偏差的影响。
method: 引入Action-JND，在深度视觉特征空间训练轻量估计器，预测保持VLA策略响应不变的最大可容忍扰动，输出动作容差分数作为压缩准则。
result: 在LIBERO基准上，Action-JND即插即用地用于KV缓存复用和token剪枝，一致提升压缩可靠性，尤其在激进压缩比下。
conclusion: 首次将JND建模引入具身感知，提供接收者依赖的容差度量，为VLA高效部署提供新思路。
---

## 摘要
令牌压缩已成为降低大型基础模型推理成本的关键技术，令牌剪枝和KV缓存重用等方法在视觉-语言模型中被广泛采用，并最近被探索用于具身智能体。在具身智能体中，令牌不仅支持感知和语义理解，还直接影响延迟敏感的闭环机器人动作预测。现有方案通常使用冗余性或重要性线索（如视觉相似性、注意力分数和显著性）来指导压缩。然而，这些线索仅间接衡量安全压缩的关键因素：令牌在导致下游动作产生不可接受偏差之前可以改变多少。这种接收者相关的容差与恰可察觉差（JND）原理密切相关。经典JND表征人类视觉系统中的信号容差，而面向机器的JND将这一概念扩展到下游机器响应。基于这一发展，我们引入了Action-JND，通过定义语言条件化的动作响应在闭环控制中的可察觉性，将JND建模扩展到具身感知。仅当引起的动作偏差保持在允许范围内时，令牌变化才被视为可接受的。为实现这一概念，我们在深度视觉特征空间中开发了一个轻量级的令牌级JND估计器，以在保持策略响应的同时预测最大可容忍扰动。由此产生的动作容差分数作为VLA压缩范式的即插即用标准，包括陈旧KV重用和令牌剪枝，优先压缩动作容忍的令牌。在LIBERO基准上使用OpenVLA和OpenVLA-OFT进行的实验表明，Action-JND持续提高了压缩可靠性，尤其是在激进压缩比下。

## Abstract
Token compression has become a key technique for reducing the inference cost of large foundation models, with approaches such as token pruning and KV-cache reuse widely adopted in vision-language models and recently explored for embodied agents. In embodied agents, tokens not only support perception and semantic understanding but also directly affect latency-sensitive closed-loop robot action prediction. Existing schemes typically guide compression using redundancy or importance cues, such as visual similarity, attention scores, and saliency. However, these cues only indirectly measure the key factor for safe compression: how much a token can change before causing an unacceptable deviation in downstream actions. This receiver-dependent tolerance is closely related to the principle of just noticeable difference (JND). Classical JND characterizes signal tolerance in the human visual system, while machine-oriented JND extends this concept to downstream machine responses. Building on this progression, we introduce Action-JND, which extends JND modeling to embodied perception by defining noticeability through the language-conditioned action response of a vision-language-action (VLA) policy in closed-loop control. A token change is considered admissible only when the induced action deviation remains within a tolerated margin. To realize this concept, we develop a lightweight token-wise JND estimator in deep visual-feature space to predict the maximum tolerable perturbation while preserving policy responses. The resulting action-tolerance score serves as a plug-and-play criterion for VLA compression paradigms, including stale-KV reuse and token pruning, prioritizing action-tolerant tokens for compression. Experiments on the LIBERO benchmark with OpenVLA and OpenVLA-OFT demonstrate that Action-JND consistently improves compression reliability, especially under aggressive compression ratios.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景与动机**：视觉-语言-动作（VLA）模型在机器人操作中承担闭环控制任务，推理延迟直接影响控制频率与响应及时性，因此高效推理至关重要。现有 token 压缩方法（token pruning、KV-cache 重用）被引入 VLA 加速，但这些方法通常依赖**间接线索**（如视觉相似性、注意力分数、语言相关性、视觉冗余度）来判断哪些 token 可以被压缩。
- **核心问题**：间接线索无法回答压缩安全性的根本问题——**一个 token 被剪掉或替换为陈旧表示后，下游机器人动作是否仍能保持在可接受范围内**？尤其在激进压缩比下，视觉上稳定或语义上不显著的 token 可能对动作生成十分关键，现有准则极易误删。
- **提出的解决方向**：借鉴经典 JND（恰可察觉差）理论的"接收者相关容差"思想，将 JND 建模扩展到具身感知领域，提出 **Action-JND**：一个 token 表示的变化是否"可察觉"，取决于它是否导致 VLA 策略预测的机器人动作产生超出容限的偏差，而非仅仅取决于人类或静态机器任务的感知。
- **整体含义**：该工作首次将 JND 从人类视觉感知、静态机器感知延伸到具身交互场景，为 VLA token 压缩提供了直接面向动作保持的接收者相关准则。

## 2. 论文提出的方法论

### 2.1 核心思想

- 在深度视觉特征空间中定义 Action-JND：给定视觉 token 特征 Fₜ 与语言指令 ℓ，冻结的 VLA 策略 πθ 预测动作 aₜ。Action-JND 求解**保持策略动作响应不变的最大 token 扰动** Δₜ⋆，即：

  Δₜ⋆ = arg max M(Δₜ)，约束条件为 D_act(Rθ(Fₜ, ℓ), Rθ(Fₜ + Δₜ, ℓ)) ≤ ϵₐ

- 对离散动作策略（OpenVLA），动作差异 D_act 使用动作 token 分布的 KL 散度；对连续动作策略（OpenVLA-OFT），D_act 使用加权 ℓ1 回归距离（包含平移、旋转、夹爪三项）。
- 优化目标被松弛为无约束形式：max λ_mag·M(Δₜ) − λ_act·D_act(·,·)，同时鼓励扰动幅度大且动作保持。

### 2.2 关键技术细节

- **JND 估计器架构**：轻量级卷积网络。输入为投影后的视觉 token 序列（单视图为 256 个 token，16×16 空间网格；双视图为 512 个 token），经 LayerNorm、reshape、3×3 空间卷积、多个空间残差块与通道混合残差块、1×1 输出投影，最终生成与输入同形状的逐 token 扰动图。
- **双视图处理**：对 OpenVLA-OFT 的主视角与腕部视角分别使用独立参数的估计器，避免混合不同相机坐标系的拓扑结构。
- **训练策略**：VLA 策略完全冻结，仅训练 JND 估计器。损失函数为 L_JND = λ_act·D_act(·,·) − λ_mag·M(Δ̂ₜ)，同时最小化动作偏差、最大化扰动幅度。扰动幅度 M 取所有 token 的 ℓ2 范数平均。
- **JND 分数转换**：预测扰动可转为绝对分数（ℓ2 范数）或相对分数（ℓ2 范数除以 token 特征范数加小常数）。相对分数用于 KV-cache 重用以消除特征尺度影响，绝对分数用于 token 剪枝。
- **压缩集成方式**：Action-JND 不替换现有压缩框架，而是作为**即插即用排名信号**：
  - **KV-cache 重用**（算法 1）：在 VLA-Cache 构建的候选集合内，按 JND 分数从高到低选择重用 token；若候选不足，从守护回退池按同一排名补齐。
  - **Token 剪枝**（算法 2）：基于时间稳定性筛选候选 token，用前一时刻动作到视觉的注意力保护关键 token，然后按 JND 分数从高到低剪除最动作容忍的 token。

## 3. 实验设计

- **基准（Benchmark）**：LIBERO 机器人操作基准，涵盖四个套件——Spatial（空间推理）、Object（物体中心操作）、Goal（目标条件执行）和 Long（长时程任务）。
- **骨干模型**：
  - **OpenVLA**（7B 参数，单 RGB 视图，离散动作 token 预测）。
  - **OpenVLA-OFT**（连续动作分块回归，主视角 + 腕部视角双输入）。
- **KV-cache 重用实验**：
  - 策略：软自适应策略（真实重用比例取决于候选数量）与 strict_guarded 策略（目标重用比例 30%、40%、60%、80%）。
  - 对比基线：VLA-Cache、FastV、SparseVLM、DivPrune 的 KV-cache 兼容改编版。
- **Token 剪枝实验**：
  - 策略：预算固定策略，目标剪枝比例为 25%、50%、75%、87.5%。
  - 对比基线：FastV、SparseVLM、DivPrune、VLA-Cache 剪枝基线。
- **评估指标**：各套件成功率与平均准确率（Acc.）、FLOPs、相对 FLOPs、CUDA 延迟、控制频率等。

## 4. 资源与算力

- **论文未明确说明训练 Action-JND 估计器所使用的 GPU 型号、数量及训练时长**。
- 仅能推断：JND 估计器本身为轻量级卷积网络，参数量小；训练时需冻结 VLA 策略进行前向传播（clean 与 perturbed 两个分支），因此训练开销主要来自 VLA 前向推理，而非估计器本身。论文未提供具体算力明细。

## 5. 实验数量与充分性

- **实验数量**：
  - KV-cache 重用：两个骨干（OpenVLA、OpenVLA-OFT）×（软策略 + 4 种严格比例）= 共 10 组设置。
  - Token 剪枝：OpenVLA ×（4 种剪枝比例）= 共 4 组设置。
  - 每组设置均报告 4 个 LIBERO 套件 + 平均结果，即总共约 140 个成功率数据点。
- **充分性评估**：
  - **优点**：覆盖了从保守到激进（80% 重用、87.5% 剪枝）的完整压缩区间；两种骨干模型涵盖离散与连续动作两种代表性策略形式；与多个主流压缩方法对比。
  - **不足**：论文**未提供消融研究**（如单独验证绝对/相对分数选择、JND 排名与候选构建解耦的影响、λ 超参敏感性等）；没有验证在不同 VLA 骨干（如 RT-2、π0）上的泛化性；缺乏对失败案例的系统分析。

## 6. 论文的主要结论与发现

- **KV-cache 重用**：
  - 在 OpenVLA 上，60% 和 80% 重用比例下 Action-JND 相比 VLA-Cache 分别提升平均准确率 **23.70 和 41.65 个百分点**；
  - 在 OpenVLA-OFT 上，对应提升 **6.20 和 10.10 个百分点**；
  - 在软自适应策略下，两个骨干上平均准确率均为最高。
- **Token 剪枝**：
  - 在 50%、75%、87.5% 剪枝比例下，相比 VLA-Cache 剪枝基线分别提升 **1.45、3.05 和 11.65 个百分点**；
  - 87.5% 剪枝时仍保持 42.00% 平均准确率，远超最强基线（35.45%）；
  - 同时仅需 vanilla 模型 33.06% 的 FLOPs，延迟从 52.88 ms 降至 30.16 ms，控制频率从 18.91 Hz 提升至 33.15 Hz。
- **总体发现**：压缩比例越高，基于间接线索的方法性能下降越剧烈，而 Action-JND 直接估计动作容差，在激进压缩下仍能有效保持任务成功率，提供更可靠的准确率-效率权衡。

## 7. 优点

- **理论创新**：将 JND 从人类视觉感知、静态机器感知扩展至具身感知，提出 Action-JND 这一新的容差建模范式，为"安全的 token 压缩"提供了直接的动作层面度量。
- **问题定义清晰**：明确指出现有压缩方法的缺陷——代理线索不直接反映动作安全边界，论证充分且自然。
- **即插即用设计**：Action-JND 作为排名信号可无缝集成到现有压缩框架中，不改变候选构建逻辑，兼容性好，实用性强。
- **轻量高效**：估计器为轻量卷积网络，推理时仅用于生成分数，不注入扰动，额外开销极小。
- **实验覆盖充分**：在多个压缩等级、两种骨干、离散/连续动作两种参数化方式下进行系统评估，尤其在激进压缩下展现了显著优势。
- **可视化直观**：JND 热力图清晰展示了哪些区域可安全压缩（亮色）与哪些区域必须保留（暗色），结果符合直觉——如夹爪、物体边界附近的 token 被识别为动作敏感区域。

## 8. 不足与局限

- **缺少消融研究**：未分析各组件（JND 分数形式、候选构建策略、回退池选择、λ 权重）的独立贡献，也缺少与"随机排名""反向排名"等简单基线的对比。
- **基准覆盖面有限**：仅在 LIBERO 仿真基准上验证，未涉及真实机器人平台；未验证在其它 VLA 架构（如 RT-2、π0）或其它任务类型（如导航、多平台协作）上的有效性。
- **训练开销未报告**：虽然估计器轻量，但训练需要冻结大模型（7B 级）进行多次前向传播，论文未提供训练 GPU 时长与资源明细，难以评估部署成本。
- **评估指标单一**：任务性能仅用成功率衡量，缺少对动作轨迹质量、操作安全性、失败模式分布等更细粒度的分析。
- **潜在偏差风险**：JND 估计器在特定策略和数据集上训练，可能过拟合于训练分布中的动作偏差度量；对分布外场景的泛化能力未经检验。
- **压缩候选构建仍依赖代理线索**：Action-JND 只做候选池内的排名优化，若候选构建阶段（基于稳定性、注意力）漏掉了应保留的动作关键 token，JND 排名无法挽救，存在继承性偏差。

（完）
