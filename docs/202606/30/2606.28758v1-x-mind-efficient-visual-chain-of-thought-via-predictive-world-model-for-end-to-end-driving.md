---
title: "X-Mind: Efficient Visual Chain-of-Thought via Predictive World Model for End-to-End Driving"
title_zh: X-Mind：通过预测世界模型实现高效视觉思维链的端到端驾驶
authors: "Bohao Zhao, Chengrui Wei, Guangfeng Jiang, Ruixin Liu, Xuejie Lv, Liu Liang, Sutao Deng, Xiuyang Fan, Pengkun Zheng, Jinyun Zhou, Rui Guo, Hanpeng Liu, Yutong Zheng, Yi Guo, Xinlong Zheng, Qingyu Luo, Zhuangzhuang Ding, Yu Zhang, Hang Zhang, Xianming Liu"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28758v1"
tags: ["query:evlm"]
score: 9.0
evidence: 具有预测世界模型的高效VLA端到端驾驶模型
tldr: 当前端到端驾驶模型缺乏对未来状态的预测能力，依赖反应式感知-动作映射，限制了动态环境下的鲁棒性。为此提出X-Mind框架，将预测世界模型内化为视觉链式思维，通过紧凑的BEV草图和抽象驾驶先验表示，将12帧未来展开压缩为96个token。同时采用循环块扩散方案，将去噪步骤融入大模型前向传播，显著加速生成。在大规模真实数据上训练，X-Mind达到有竞争力的驾驶性能，且延迟极低，首次将认知推理高效部署到资源受限的车载平台。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28758v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1158, \"height\": 651, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1407, \"height\": 580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 882, \"height\": 969, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1452, \"height\": 940, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28758v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1215, \"height\": 217, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28758v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1309, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28758v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1214, \"height\": 218, \"label\": \"Table\"}]"
motivation: 现有VLA模型缺乏对未来状态的预测能力，仅进行反应式感知-动作映射，导致在动态环境中鲁棒性不足。
method: 将预测世界模型内化为视觉链式思维，使用紧凑的BEV草图与驾驶先验表示，结合循环块扩散加速生成，实现低延迟推理。
result: 在大规模真实数据上训练后，X-Mind达到有竞争力的端到端驾驶性能，且推理延迟显著低于传统级联方法。
conclusion: X-Mind成功将大规模认知推理部署到资源受限车辆平台，为高效安全驾驶提供了可行方案。
---

## 摘要
预测未来状态对于自主智能体至关重要，然而当前的视觉-语言-动作（VLA）模型从根本上缺乏这一能力，它们依赖反应式的感知-动作映射。虽然集成预测世界模型（PWM）可以解决这一缺陷，但现有方法要么产生难以承受的级联延迟，要么作为浅层终端任务，未能深度嵌入前瞻性推理。为了赋予VLA模型这种推理能力，我们提出了X-Mind。该框架并非将PWM视为外部辅助模块，而是将其内化为视觉思维链（Visual CoT）。通过在动作之前强制执行世界展开（world rollout），模型被迫首先想象未来的演变，从而产生一种稳健地根植于环境动力学、并意识到其动作将产生的未来后果的驾驶策略。这里的挑战在于效率，我们从两个方面着手解决。首先，我们引入了一种视觉思维的紧凑表示：一种抽象草图，它将鸟瞰图（BEV）布局与抽象的驾驶先验（如导航意图和交通规则）融合在一起。模型不在密集的未来帧上展开，而是在这张作为心理画布的草图上进行推理；借助深度压缩自编码器（DC-AE），一个12帧的未来展开被缩减到仅96个token，从而缓解了长上下文计算瓶颈。其次，为了进一步加速生成，我们提出了一种循环块扩散方案，该方案将去噪步骤展开到大型驾驶模型的各层中，将迭代细化折叠到骨干网络的一次前向传递中。在大规模真实世界数据上训练和验证，X-Mind实现了具有竞争力的端到端驾驶性能，使其成为一种高度实用、低延迟的解决方案，成功地将大规模认知推理直接部署到资源受限的车辆平台上。

## Abstract
Predicting future states is essential for autonomous agents, yet current Vision-Language-Action (VLA) models fundamentally lack this capability, relying instead on reactive perception-action mapping. While integrating Predictive World Models (PWMs) addresses this gap, existing approaches either incur prohibitive cascaded latency or act as shallow terminal tasks that fail to deeply embed forward-looking reasoning. To endow VLA models with this reasoning capability, we propose X-Mind. Rather than treating PWMs as an external auxiliary module, this framework internalizes them as the Visual Chain-of-Thought (Visual CoT). By enforcing a world rollout prior to action, the model is constrained to imagine future evolution first, yielding a driving policy that is robustly grounded in environmental dynamics and aware of the future consequences its actions will unfold. The challenge here is efficiency, and we tackle it on two fronts. First, we introduce a compact representation of visual thinking: an abstract sketch that fuses a Bird's-Eye-View (BEV) layout with abstract driving priors (e.g., navigation intents and traffic rules). Rather than rolling out dense future frames, the model reasons over this sketch as a mental canvas; aided by a Deep Compression Autoencoder (DC-AE), a 12-frame future rollout is reduced to merely 96 tokens, alleviating the long-context computational bottleneck. Second, to accelerate generation further, we propose a recurrent block diffusion scheme that unrolls the denoising steps across the layers of the large drive model, folding iterative refinement into the backbone's one forward pass. Trained and validated on large-scale real-world data, X-Mind achieves competitive end-to-end driving performance, which makes it a highly practical, low-latency solution that successfully deploys large-scale cognitive reasoning directly onto resource-constrained vehicle platforms.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有视觉-语言-动作（VLA）模型在端到端自动驾驶中主要依赖反应式的感知-动作映射，缺乏对未来状态（如交通流、障碍物轨迹、场景演化）的显式预测能力。这种“缺乏前瞻性推理”的架构在复杂动态环境中容易陷入快捷学习（shortcut learning），鲁棒性不足。
- **现有方法缺陷**：直接级联预测世界模型（PWM）会导致过高的车载延迟；将PWM作为浅层终端任务（如辅助重建损失）则难以将监督信号有效传播到大语言模型的深层骨干，无法真正内化物理推理。
- **整体含义**：X-Mind旨在将PWM内化为模型的视觉思维链（Visual CoT），让模型在生成动作前先“想象”未来场景的演化，从而在决策中融入对环境动力学的理解，并实现低延迟的车载部署。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：将世界模型嵌入到大模型内部，作为推理的一个强制步骤。模型先预测一个紧凑的抽象草图（aggregated sketch），再基于此未来状态通过逆动力学规划器输出轨迹。
- **关键技术细节**：
  - **抽象草图表示（Visual Thinking Representation）**：
    - 融合鸟瞰图（BEV）布局与抽象驾驶先验（导航意图、交通灯状态、速度合规条）。
    - 通过深度压缩自编码器（DC-AE）将12帧未来展开压缩为仅96个token，解决了长上下文计算瓶颈。
  - **循环块扩散（Recurrent Block Diffusion, RBD）**：
    - 将传统扩散模型的迭代去噪步骤分布到大语言模型的不同层中，通过层流匹配（Layer Flow Matching, LFM）实现单次前向传播生成未来草图。
    - 训练时：在不同层注入不同噪声水平的潜伏表示（线性插值），监督速度场。
    - 推理时：从纯噪声开始，利用各层输出的速度进行欧拉积分逐步去噪。
    - 公式示意：
      - 层流匹配训练：\( z_t = (1-t) \cdot \epsilon + t \cdot z_1 \)
      - 推理：\( z_{k+1} = z_k + (t_{k+1} - t_k) \cdot v_k \)
  - **端到端联合优化**：总损失包含世界模型展开损失（速度场监督 + 重建损失）和运动学规划损失（对纵向加速度、横摆角速度的L1监督）。

### 3. 实验设计

- **数据集**：使用小鹏汽车内部大规模真实驾驶数据集，约28万小时连续数据，划分为3400万个视频片段。7个摄像头（前鱼眼、前窄、左前、右前、左后、右后），共13.8T个视觉token，城市/高速占比约86.8%/13.2%。实验采用该数据集1/8的子集。
- **Benchmark与指标**：
  - 主要指标：横向/纵向平均位移误差（ADE Lat./Lon. @6s），未来预测质量（FID），推理延迟（归一化相对值）。
  - 对比设置：Base模型（标准VLA，无未来预测）作为基线。
- **对比方法**：
  - 场景表示对比：Base + Image（3584额外token）、Base + 3DGS（3072额外token）、Base + Sketch（96额外token）。
  - 扩散架构对比：Base（无预测）、Base + Sketch（单步去噪）、Base + Sketch（RBD）。
  - 预测目标对比：当前帧重建、未来1帧预测、未来12帧预测。

### 4. 资源与算力

- **文中未明确说明训练所用GPU型号、数量或训练时长**，仅提及数据集规模（13.8T token）和实验基于1/8子集。
- 但指出推理时X-Mind仅比基线增加0.1倍延迟（归一化延迟为1.1 vs 1.0），远低于Image（22.0）和3DGS（19.0）方案，说明计算开销极低。

### 5. 实验数量与充分性

- **实验数量**：共进行了多组系统消融和对比实验：
  - 3种场景表示（Tab.1）
  - 3种扩散架构（Tab.2）
  - 3种预测目标（Tab.3）
  - 定性可视化（图4、图6）
  - 结构化草图GT分析（图5）
- **充分性与客观性**：
  - 每个对比均保持骨干网络、训练配置一致，控制变量良好。
  - 指标涵盖规划精度（ADE）、生成质量（FID）、推理效率（延迟），多维评估。
  - 消融实验清晰展示了每个组件的贡献（如RBD vs 单步去噪的FID从67.30降到9.59，ADE也有提升）。
  - 但仅在单一内部数据集上评估，未在公开基准（如nuScenes、Waymo）上验证，外部泛化性有待确认。

### 6. 主要结论与发现

- **核心结论**：将PWM内化为Visual CoT能显著提升端到端驾驶规划的准确性，且通过紧凑的抽象草图和循环块扩散，该认知推理过程可被高效地压缩在单次前向传播中，延迟极低。
- **关键发现**：
  - 抽象草图比密集图像、3DGS更高效，仅需96额外token，ADE最优。
  - RBD相比单步去噪，FID从67.30降至9.59，同时ADE更优，表明多层渐进扩散更有利于生成高质量未来状态。
  - 预测未来12帧虽然FID略高于重建当前帧，但ADE显著更好，说明前瞻性生成（而非重建）是提升规划性能的关键。
  - 定性结果显示，RBD模型能预测GT中缺失或遮挡的动态物体，表明学习了因果物理推理。

### 7. 优点

- **创新性**：首次将预测世界模型作为“视觉思维链”直接内化到大语言模型的深层结构，而非外部模块或浅层任务。
- **高效性**：通过抽象草图+DC-AE压缩至96 token，结合RBD在单次LLM前向中完成未来生成，实现了低延迟推理（仅1.1倍基线）。
- **可解释性**：抽象草图清晰显示道路拓扑、交通灯状态、导航路径、速度合规线，便于诊断和调试。
- **实验设计**：系统消融了表示、架构、预测目标，结论明确且支撑充分。

### 8. 不足与局限

- **未公开数据集与基准**：实验仅基于内部数据，未在nuScenes、Waymo等公开数据集上评估，无法与社区方法直接公平比较。
- **安全指标缺失**：主要使用ADE评估规划精度，缺乏碰撞率、接管率、舒适性等安全相关的度量。
- **标注依赖**：当前抽象草图的GT需要大量人工标注（交通灯、导航路径、速度等），限制了规模化扩展。
- **未来工作提及**：作者指出需要引入自监督学习摆脱标注瓶颈，以及探索联合采样控制动作与草图以进一步提升安全性。
- **计算资源未披露**：未报告训练阶段的具体算力消耗，难以评估可复现性。

（完）
